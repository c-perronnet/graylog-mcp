---
phase: quick-260516-mt4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/dashboards.js
  - src/widget-templates/top-sources-by-volume.js
  - src/widget-templates/field-value-distribution.js
  - src/widget-templates/recent-events-table.js
  - src/widget-templates/error-rate-over-time.js
  - test/snapshots/dashboards.test.js
  - test/snapshots/widget-payload-shapes.test.js
autonomous: true
requirements: [MT4-BUG3, MT4-BUG4, MT4-BUG5, MT4-BUG6, MT4-BUG7]

must_haves:
  truths:
    - "POST /api/views/search emits skip_no_streams_check (snake_case), not skipNoStreamsCheck"
    - "No pivot/series sort entry anywhere carries an `id` key — sort entries use `field`"
    - "The message-list widget config.sort entry has both `type` and `direction` keys"
    - "Every emitted widget position `col` is an integer (no {type:'infinity'} object)"
    - "The emitted view-state payload has no display_mode_settings block"
    - "The messages search_type sort {field, order:'DESC'} is unchanged"
    - "npm test passes with regenerated snapshots"
  artifacts:
    - path: "src/services/dashboards.js"
      provides: "buildSearchDTO without skipNoStreamsCheck; buildViewDTO without display_mode_settings"
      contains: "skip_no_streams_check"
    - path: "test/snapshots/widget-payload-shapes.test.js"
      provides: "Regression tests pinning all 5 corrected shapes"
  key_links:
    - from: "src/widget-templates/*.js builders"
      to: "buildSearchDTO/buildViewDTO in src/services/dashboards.js"
      via: "widget triplet emission consumed into SearchDTO/ViewDTO wire payloads"
      pattern: "buildSearchDTO|buildViewDTO"
---

<objective>
Fix 5 confirmed wire-shape bugs in the Graylog dashboard domain so that POST
/api/views/search then POST /api/views both succeed against Graylog 7.x.

Purpose: create_dashboard, create_app_health_dashboard, and
add_widget_from_template currently emit ViewDTO/SearchDTO payloads Graylog 7.x
rejects. The MCP's core value (configure Graylog from intent, no web UI) is
broken for the dashboard surface until these shapes are corrected.

Output: corrected payload emission in 4 source files, refreshed snapshot
fixtures, and a new regression test file pinning all 5 corrected shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<bug_inventory>
Audit complete. Exact occurrences (line numbers as of planning):

BUG #3 — pivot/series SORT entry keyed `id`, must be `field`:
  - src/widget-templates/top-sources-by-volume.js:21
      `const sortByCountDesc = [{ type: "series", id: "count()", direction: "Descending" }];`
  - src/widget-templates/field-value-distribution.js:28  (identical line)
  `sortByCountDesc` is referenced TWICE in each file — once on the
  search_type `sort` and once on the widget `config.sort` — so fixing the
  single const fixes both call sites.
  NOTE: the SERIES DEFINITION `series: [{ type:"count", id:"count()", field:null }]`
  is a DIFFERENT shape — `id` there is CORRECT. Do NOT touch series definitions.
  Other widget templates use empty `sort: []` — no other BUG #3 occurrences.

BUG #4 — create-search body camelCase key:
  - src/services/dashboards.js:155  `skipNoStreamsCheck: false,` in buildSearchDTO
    → must be `skip_no_streams_check: false,`

BUG #5 — message-list WIDGET config.sort missing type discriminator:
  - src/widget-templates/recent-events-table.js:29
      `const widgetSort = [{ field: "timestamp", order: "DESC" }];`
      → must be `[{ type: "pivot", field: "timestamp", direction: "Descending" }]`
  - recent-events-table.js:30 `searchTypeSort` `{field,order:"DESC"}` is CORRECT
    (the messages SEARCH_TYPE sort) — do NOT change it.

BUG #6 — widget position `col` emitted as `{type:"infinity"}` object:
  - src/widget-templates/error-rate-over-time.js:73  `col: { type: "infinity" }` → `col: 1`
  - src/widget-templates/recent-events-table.js:69    `col: { type: "infinity" }` → `col: 1`
  All other templates already emit integer `col` (1 or 7) — no other occurrences.
  src/tools/dashboards/schemas.js:67 keeps its `z.union([int, {type:"infinity"}])`
  — it ACCEPTS both forms; only the emitted defaults change. Do NOT edit schemas.js.

BUG #7 — view-state `display_mode_settings` block rejected:
  - src/services/dashboards.js:205-209  the `display_mode_settings: { ... }` object
    inside the `state[queryId]` object in buildViewDTO → delete the whole block.
</bug_inventory>

<existing_test_shapes>
test/snapshots/dashboards.test.js encodes old shapes that will need updating:
  - line ~155: a `display_mode_settings` block in an expected ViewDTO fixture
  - lines ~412, ~504: `skipNoStreamsCheck: false` in expected SearchDTO fixtures
These are hand-written expectations (not .snapshot files). They must be
updated by hand to the corrected shapes. The .snapshot files under
test/snapshots/__snapshots__/ are regenerated, not hand-edited.
</existing_test_shapes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all 5 payload-shape fixes to the 4 source files</name>
  <files>src/services/dashboards.js, src/widget-templates/top-sources-by-volume.js, src/widget-templates/field-value-distribution.js, src/widget-templates/recent-events-table.js, src/widget-templates/error-rate-over-time.js</files>
  <action>
Apply exactly these five edits (MT4-BUG3..7). Change ONLY the listed shapes;
all other widget structure, series, visualization, query, streams, position
row/height/width, and widget ids stay byte-identical.

BUG #4 — src/services/dashboards.js buildSearchDTO: rename the key
`skipNoStreamsCheck` to `skip_no_streams_check` (value stays `false`).

BUG #7 — src/services/dashboards.js buildViewDTO: delete the entire
`display_mode_settings: { positions_inferred, show_summary, show_message_row }`
object from the `state[queryId]` object. Remove the trailing comma cleanly so
the object literal stays valid. Also update the buildViewDTO doc comment that
claims `display_mode_settings` is emitted EXPLICITLY — change it to note the
block is intentionally omitted (Graylog 7.x rejects positions_inferred).

BUG #3 — top-sources-by-volume.js and field-value-distribution.js: in the
`sortByCountDesc` const, rename the key `id` to `field` so the entry becomes
`{ type: "series", field: "count()", direction: "Descending" }`. This single
const flows to both the search_type `sort` and widget `config.sort`. Do NOT
touch the `series: [{ type:"count", id:"count()", field:null }]` definitions —
the `id` on a series DEFINITION is correct.

BUG #5 — recent-events-table.js: change `widgetSort` (line 29, the WIDGET
config.sort) from `[{ field: "timestamp", order: "DESC" }]` to
`[{ type: "pivot", field: "timestamp", direction: "Descending" }]`. Leave
`searchTypeSort` (line 30) UNCHANGED — the messages search_type sort
`{field, order:"DESC"}` is correct and verified. Update the inline comment
above these consts to reflect that the widget config.sort uses the
SortConfigDTO discriminated shape while the search_type sort uses {field,order}.

BUG #6 — error-rate-over-time.js (line 73) and recent-events-table.js
(line 69): change the default `position` `col` from `{ type: "infinity" }` to
the integer `1`. All other position keys (row/height/width) unchanged.
  </action>
  <verify>
    <automated>node -e "import('./src/services/dashboards.js').then(m=>{const s=m.buildSearchDTO({queryId:'q',widgets:[],timerange:{}});if('skipNoStreamsCheck' in s)throw new Error('BUG4 not fixed');if(!('skip_no_streams_check' in s))throw new Error('snake key missing');const v=m.buildViewDTO({title:'t',searchId:'x',queryId:'q',widgets:[]});if('display_mode_settings' in v.state.q)throw new Error('BUG7 not fixed');console.log('dashboards.js OK');})" && node -e "import('./src/widget-templates/top-sources-by-volume.js').then(m=>{const t=m.buildTopSourcesByVolume({});if(t.searchType.sort.some(e=>'id' in e))throw new Error('BUG3 sort id');if(!t.searchType.sort.every(e=>'field' in e))throw new Error('BUG3 no field');if(t.widget.config.sort.some(e=>'id' in e))throw new Error('BUG3 config.sort id');console.log('top-sources OK');})" && node -e "import('./src/widget-templates/field-value-distribution.js').then(m=>{const t=m.buildFieldValueDistribution({field:'f'});if(t.searchType.sort.some(e=>'id' in e)||t.widget.config.sort.some(e=>'id' in e))throw new Error('BUG3 fvd');console.log('fvd OK');})" && node -e "import('./src/widget-templates/recent-events-table.js').then(m=>{const t=m.buildRecentEventsTable({});const ws=t.widget.config.sort[0];if(!ws.type||!ws.direction)throw new Error('BUG5 widget sort');const sts=t.searchType.sort[0];if(sts.order!=='DESC'||sts.type)throw new Error('BUG5 search_type sort must stay {field,order}');if(typeof t.position.col!=='number')throw new Error('BUG6 recent col');console.log('recent OK');})" && node -e "import('./src/widget-templates/error-rate-over-time.js').then(m=>{const t=m.buildErrorRateOverTime({});if(typeof t.position.col!=='number')throw new Error('BUG6 error-rate col');console.log('error-rate OK');})"</automated>
  </verify>
  <done>buildSearchDTO emits skip_no_streams_check; buildViewDTO state has no display_mode_settings; top-sources & fvd sort/config.sort entries use `field` not `id`; recent-events widget config.sort has type+direction while search_type sort stays {field,order}; error-rate & recent-events position.col are integers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests pinning the 5 corrected shapes and update stale dashboards.test.js expectations</name>
  <files>test/snapshots/widget-payload-shapes.test.js, test/snapshots/dashboards.test.js</files>
  <behavior>
    - Test (a): for every builder in WIDGET_TEMPLATES that produces a searchType,
      no entry in searchType.sort or widget.config.sort carries an `id` key;
      every present sort entry has a `field` key.
    - Test (b): buildSearchDTO output has own-property `skip_no_streams_check`
      and does NOT have `skipNoStreamsCheck`.
    - Test (c): buildRecentEventsTable widget.config.sort[0] has both `type`
      and `direction`; its searchType.sort[0] still has `field` + `order` and
      no `type` (the messages search_type sort is correct as-is).
    - Test (d): for every builder, the emitted position.col is `Number.isInteger`.
    - Test (e): buildViewDTO output state[queryId] has no own-property
      `display_mode_settings`.
  </behavior>
  <action>
Create test/snapshots/widget-payload-shapes.test.js using node:test
(`import { test } from "node:test"`, `import assert from "node:assert/strict"`)
following the existing test/snapshots/widget-templates.test.js conventions.
Import WIDGET_TEMPLATES from src/widget-templates/index.js and iterate every
builder; field_value_distribution requires `{field:"x"}` options, so pass a
minimal options object covering each builder's required option. Import
buildSearchDTO/buildViewDTO/buildRecentEventsTable from their modules.
Implement the five behavior assertions (a)-(e) above as discrete tests. These
are plain assertion tests — do NOT use t.assert.snapshot here (snapshot
regeneration is handled separately below).

Then update the stale hand-written expectations in
test/snapshots/dashboards.test.js: remove the `display_mode_settings` block
from the expected ViewDTO fixture (~line 155) and change every
`skipNoStreamsCheck: false` to `skip_no_streams_check: false` (~lines 412,
504). Do not weaken any assertion — only correct the expected shape to match
the fixed wire form.

Finally regenerate the snapshot fixtures that encode old shapes:
run `node --test --test-update-snapshots 'test/snapshots/widget-templates.test.js' 'test/snapshots/dashboards.test.js' 'test/snapshots/blueprints.test.js'`
so test/snapshots/__snapshots__/{widget-templates,dashboards,blueprints}.test.js.snapshot
pick up the corrected shapes. Do NOT hand-edit .snapshot files.
  </action>
  <verify>
    <automated>node --test --test-update-snapshots 'test/snapshots/widget-templates.test.js' 'test/snapshots/dashboards.test.js' 'test/snapshots/blueprints.test.js' && grep -rL 'skipNoStreamsCheck' test/snapshots/__snapshots__/dashboards.test.js.snapshot test/snapshots/__snapshots__/blueprints.test.js.snapshot >/dev/null && ! grep -rl 'display_mode_settings\|"type": *"infinity"\|type:"infinity"' test/snapshots/__snapshots__/*.snapshot && npm test</automated>
  </verify>
  <done>widget-payload-shapes.test.js exists with 5 passing regression tests; dashboards.test.js expectations corrected; snapshot fixtures regenerated with no skipNoStreamsCheck / display_mode_settings / infinity-col remnants; full `npm test` passes.</done>
</task>

</tasks>

<verification>
- `npm test` passes (full suite, node --test conventions).
- Regression test file pins all 5 corrected shapes — re-introducing any bug
  fails a discrete test.
- `grep -rn 'skipNoStreamsCheck\|display_mode_settings' src/` returns only doc
  comments (or nothing); no live emission.
- `grep -rn 'type: *"infinity"' src/widget-templates/` returns nothing.
- The messages search_type sort `{field, order:"DESC"}` in recent-events-table.js
  is unchanged.
</verification>

<success_criteria>
- All 5 bugs fixed comprehensively across src/services/dashboards.js and the
  4 affected widget-template builders.
- Backward compat: only the listed payload shapes changed; everything else
  byte-identical.
- Regression tests cover (a)-(e) from fix_requirements §4.
- Snapshot fixtures regenerated via --test-update-snapshots, not hand-weakened.
- Full `npm test` green.
</success_criteria>

<output>
Create `.planning/quick/260516-mt4-fix-graylog-dashboard-widget-payload-sha/260516-mt4-SUMMARY.md` when done.
</output>
