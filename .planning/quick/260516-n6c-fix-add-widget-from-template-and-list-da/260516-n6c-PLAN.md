---
phase: quick-260516-n6c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/dashboards/list-dashboards.js
  - src/widget-templates/error-rate-over-time.js
  - src/tools/dashboards/add-widget-from-template.js
  - test/dashboards.test.js
  - test/widget-templates.test.js
autonomous: true
requirements: [BUG-9, BUG-8a, BUG-8b]

must_haves:
  truths:
    - "list_dashboards returns Graylog dashboards instead of always 0"
    - "Widget templates honor a caller-supplied options.queryString"
    - "add_widget_from_template places a new widget below existing widgets, not overlapping"
  artifacts:
    - path: "src/tools/dashboards/list-dashboards.js"
      provides: "list_dashboards request path without query=type:DASHBOARD"
    - path: "src/widget-templates/error-rate-over-time.js"
      provides: "queryString that AND-combines base filter with options.queryString"
    - path: "src/tools/dashboards/add-widget-from-template.js"
      provides: "auto-placement of new widget below existing widgets"
  key_links:
    - from: "src/tools/dashboards/add-widget-from-template.js"
      to: "triplet.position"
      via: "auto-computed next-row position when options.position absent"
      pattern: "options\\.position"
---

<objective>
Fix three confirmed live bugs in the MCP dashboard surface against Graylog 7.x.

Purpose: list_dashboards currently always returns 0; widget templates silently
drop a caller-supplied query filter; add_widget_from_template overlaps existing
widgets at col:1,row:1.

Output: corrected list-dashboards request path, error-rate-over-time template
that honors options.queryString, add-widget-from-template auto-placement, and
regression tests covering all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

list-dashboards.js fetch() current path (BUG #9, line ~40):
  /api/views?query=<encoded type:DASHBOARD>&page=1&per_page=<limit>&sort=title&order=asc
  Graylog 7.x treats `query` as FREE-TEXT over view title/summary, so
  `query=type:DASHBOARD` matches literal text and returns total:0.
  Wrapper-side filter at line ~50 `items.filter(v => v?.type === "DASHBOARD")`
  is the REAL type filter — keep it.

error-rate-over-time.js (BUG #8a, line ~22):
  `const queryString = "level:>=4";`  — hardcoded, never reads options.queryString.
  Used at searchType.query.query_string (line ~39) AND widget.query.query_string
  (line ~54). All 7 OTHER templates already do `options.queryString ?? ""` —
  error-rate-over-time is the ONLY offender. top-error-clusters uses literal ""
  (text widget, no query) — leave it.

add-widget-from-template.js build() (BUG #8b):
  triplet = builder(args.options); triplet.position is the widget position.
  state.positions is `{ widgetId: { col, row, height, width } }`.
  Builders default position to e.g. {col:1,row:1,height:4,width:6}.
  args.options.position, when present, overrides the builder default INSIDE the
  builder (each builder: `options.position ?? <default>`).

Test runner: `node --test 'test/**/*.test.js'`. Snapshots via node:test
snapshot(); regenerate with `node --test --test-update-snapshots`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix list_dashboards request path (BUG #9)</name>
  <files>src/tools/dashboards/list-dashboards.js, test/dashboards.test.js</files>
  <action>
In src/tools/dashboards/list-dashboards.js fetch(): remove the
`query=${encodeURIComponent("type:DASHBOARD")}&` segment from the path so it
becomes `/api/views?page=1&per_page=${limit}&sort=title&order=asc`. Keep the
wrapper-side `items.filter((v) => v?.type === "DASHBOARD")` at line ~50 — it is
the real type filter. Drop the now-stale `?query=type:DASHBOARD` clause from the
comment block (lines ~3-8) and the `Path shape` comment (line ~22), and replace
the line ~36-39 "Wrapper-fixed `?query=type:DASHBOARD`" comment with an
accurate note that Graylog 7.x `query` is free-text and structured filtering
happens wrapper-side.

In test/dashboards.test.js: Test 3 ("list_dashboards URL carries
?query=type:DASHBOARD") at line ~245 currently asserts
`assert.match(captured.path, /\/api\/views\?query=type%3ADASHBOARD/)`. Invert it
to assert the path has NO `query=` segment — rename the test to "list_dashboards
URL has no query= segment (BUG #9 — Graylog 7.x query is free-text)" and assert
`assert.doesNotMatch(captured.path, /query=/)` plus
`assert.match(captured.path, /\/api\/views\?page=1&per_page=/)`. Update the
file-header comment (line ~5) that references `?query=type:DASHBOARD`.
  </action>
  <verify>
    <automated>node --test test/dashboards.test.js</automated>
  </verify>
  <done>list-dashboards.js path has no `query=` segment; comments accurate; Test 3 inverted and the dashboards suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honor options.queryString in error-rate-over-time template (BUG #8a)</name>
  <files>src/widget-templates/error-rate-over-time.js, test/widget-templates.test.js</files>
  <behavior>
    - buildErrorRateOverTime({}) — no queryString: query_string stays exactly "level:>=4" (no `() AND ()` wrapping).
    - buildErrorRateOverTime({ queryString: "service:api" }) — AND-combines: query_string === "(level:>=4) AND (service:api)" on BOTH widget.query and searchType.query.
    - buildErrorRateOverTime({ queryString: "" }) — empty string is treated as absent: query_string stays "level:>=4".
  </behavior>
  <action>
AUDIT RESULT (already performed during planning): of the 8 templates only
error-rate-over-time.js ignores options.queryString — the other 7 already do
`options.queryString ?? ""` and top-error-clusters is a text widget with literal
"". So this task fixes ONLY error-rate-over-time.js.

In src/widget-templates/error-rate-over-time.js replace the hardcoded
`const queryString = "level:>=4";` (line ~22) with a base + override combine:
read `const userQuery = options.queryString ?? "";`, keep base `"level:>=4"`,
and emit `queryString = userQuery ? \`(level:>=4) AND (${userQuery})\` : "level:>=4"`.
Do NOT wrap when userQuery is empty/absent — the result must be exactly
`level:>=4` (no `() AND ()`). Apply the computed `queryString` to BOTH
`searchType.query.query_string` (line ~39) and `widget.query.query_string`
(line ~54) — they already reference the same `queryString` variable, so a
single variable change covers both. Update the header comment (lines ~1-5) to
note the base filter AND-combines with options.queryString.

In test/widget-templates.test.js add three tests after Test 1
("error_rate_over_time emits aggregation widget..."): (a) no queryString →
both query_string === "level:>=4"; (b) queryString:"service:api" → both
query_string === "(level:>=4) AND (service:api)"; (c) queryString:"" → both
query_string === "level:>=4". Do NOT modify existing Test 1 — it passes no
queryString and must still see "level:>=4".

Do NOT touch the other 7 template files. Do NOT regenerate snapshots: the
error-rate snapshot test (test/snapshots/widget-templates.test.js) calls
buildErrorRateOverTime with no queryString, so its output is byte-identical.
  </action>
  <verify>
    <automated>node --test test/widget-templates.test.js test/snapshots/widget-templates.test.js</automated>
  </verify>
  <done>error-rate-over-time.js AND-combines options.queryString when present and emits exactly `level:>=4` when absent; 3 new tests pass; existing snapshot unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Auto-place new widget below existing widgets (BUG #8b)</name>
  <files>src/tools/dashboards/add-widget-from-template.js, test/dashboards.test.js</files>
  <behavior>
    - add_widget_from_template with options.position ABSENT and a dashboard with one existing widget at {row:1,height:2} → new widget position.row === 3 (max(row+height)=3), col === 1.
    - add_widget_from_template with options.position PRESENT → triplet.position is used verbatim, auto-placement skipped.
    - add_widget_from_template with options.position absent on an EMPTY-positions state → new widget row === 1.
  </behavior>
  <action>
In src/tools/dashboards/add-widget-from-template.js build(): after
`const triplet = builder(args.options);` (line ~129) and after
`const currentPositions = state.positions ?? {};` (line ~133), add
auto-placement. When `args.options?.position` is NOT supplied, compute the next
free row as the max of `(pos.row + pos.height)` across all values of
currentPositions, defaulting to 1 when currentPositions is empty; then OVERRIDE
the position used downstream so the widget lands below all existing widgets at
col 1, preserving the builder's height/width. When `args.options?.position` IS
supplied, leave triplet.position as-is (the builder already honored it).

Implementation note: triplet is Object.freeze'd, so do not mutate
triplet.position. Instead introduce a local `const placedPosition = ...` and use
`placedPosition` in place of `triplet.position` at the newPositions assignment
(line ~139, `[triplet.widget.id]: triplet.position`) — when options.position is
present, placedPosition === triplet.position. Compute next row only from numeric
row/height values; guard against non-numeric entries by coalescing to 0.

Do NOT change create-dashboard.js or create-app-health-dashboard.js — they do
not route through this auto-placement and the template `position` defaults stay
as-is for those create paths (per fix requirement 2).

In test/dashboards.test.js add tests in the DASH-06 section (after line ~1276):
(a) options.position absent against VIEW_FOR_ADD's existing widget → assert the
new widget's entry in `payload.chain[<view-step>].request.body.entity.state[...]
.positions` has row === existing.row + existing.height and col === 1; (b)
options.position supplied → assert the new widget's position deep-equals the
supplied position; (c) options.position absent with an empty `positions` map →
row === 1. Reuse the existing dry-run harness pattern (handleAddWidgetFromTemplate
with dryRun default true, parse payload.chain). Inspect VIEW_FOR_ADD near line
~1315 for the existing-widget position shape to pick concrete expected numbers.
  </action>
  <verify>
    <automated>node --test test/dashboards.test.js</automated>
  </verify>
  <done>add-widget-from-template.js auto-places the widget below existing widgets when options.position is absent and honors options.position when present; 3 new tests pass; create paths untouched.</done>
</task>

</tasks>

<verification>
- `npm test` (`node --test 'test/**/*.test.js'`) passes with no failures.
- list-dashboards.js request path contains no `query=` segment.
- error-rate-over-time.js emits `level:>=4` with no queryString and
  `(level:>=4) AND (<q>)` with one.
- add-widget-from-template.js places a 2nd widget at a non-overlapping row.
- No snapshot fixture weakened — if `node --test` reports a snapshot mismatch
  that reflects a genuine behavior change, regenerate with
  `node --test --test-update-snapshots` and inspect the diff; do not delete or
  loosen assertions.
</verification>

<success_criteria>
- All three bugs fixed per the bug report.
- create_dashboard / create_app_health_dashboard output unchanged.
- No `() AND ()` double-wrap when options.queryString is empty/absent.
- Regression tests cover: (a) list_dashboards path has no `query=`; (b)
  error-rate-over-time AND-combines / passes-through / emits base; (c)
  add_widget_from_template auto-places and honors options.position.
- Full `npm test` green.
</success_criteria>

<output>
Create `.planning/quick/260516-n6c-fix-add-widget-from-template-and-list-da/260516-n6c-SUMMARY.md` when done
</output>
