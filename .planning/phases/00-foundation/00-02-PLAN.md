---
phase: 00-foundation
plan: 02
type: execute
wave: 2
depends_on: [00-01]
files_modified:
  - test/existing/clustering-preprocess.test.js
  - test/existing/clustering-strategy.test.js
  - test/existing/template-store.test.js
  - test/existing/template-mgmt.test.js
  - test/existing/features.test.js
  - test/existing/aggregation-fixes.test.js
  - test/existing/histogram-fixes.test.js
  - test-clustering.js
  - test-features.js
  - test-aggregation-fixes.js
  - test-histogram-fixes.js
autonomous: true
requirements: [FOUND-06]
must_haves:
  truths:
    - "Every assertion in the 4 original test-*.js scripts survives in a node:test test() block under test/existing/"
    - "Running `npm test` invokes all 7 migrated files and they all pass"
    - "The 4 original `test-*.js` scripts at repo root no longer exist (deleted, not moved)"
    - "Each migrated file uses the node:test header (import `test`, `describe`, `beforeEach` from node:test; `assert` from node:assert/strict)"
  artifacts:
    - path: "test/existing/clustering-preprocess.test.js"
      provides: "Preprocessor + tokenize assertions from test-clustering.js"
      contains: "normalizeMessage"
    - path: "test/existing/clustering-strategy.test.js"
      provides: "Registry + Drain3 + serialize roundtrip from test-clustering.js"
      contains: "register"
    - path: "test/existing/template-store.test.js"
      provides: "loadTemplateStore + saveTemplateStore + corrupt-recovery + lock from test-clustering.js"
      contains: "loadTemplateStore"
    - path: "test/existing/template-mgmt.test.js"
      provides: "Formatter + cluster_log_messages handler + template-mgmt ops from test-clustering.js"
      contains: "handleClusterLogMessages"
    - path: "test/existing/features.test.js"
      provides: "Time-range + aggregation payload assertions from test-features.js"
    - path: "test/existing/aggregation-fixes.test.js"
      provides: "Aggregation payload structural assertions from test-aggregation-fixes.js"
    - path: "test/existing/histogram-fixes.test.js"
      provides: "Histogram payload assertions from test-histogram-fixes.js"
  key_links:
    - from: "test/existing/*.test.js"
      to: "src/clustering/, src/timerange.js, src/aggregations.js, src/tools/cluster-errors.js, src/tools/template-mgmt.js"
      via: "import { ... } from '../../src/...'"
      pattern: "from \"\\.\\./\\.\\./src/"
    - from: "npm test"
      to: "test/existing/**/*.test.js"
      via: "node --test glob expansion in scripts.test"
      pattern: "node --test"
---

<objective>
Migrate the four pre-existing repo-root test scripts (`test-clustering.js`, `test-features.js`, `test-aggregation-fixes.js`, `test-histogram-fixes.js`) into `node:test`-compliant files under `test/existing/`, then delete the originals. This finalises D-05 and closes the FOUND-06 deliverable that requires `npm test` to run the "full unified suite (existing + new)".

Purpose:
- D-05 mandates "Migrate all four existing test-*.js scripts to node:test in Phase 0. Result: npm test invokes one runner, runs everything."
- D-02 fix is incomplete without these tests reachable from `npm test`.
- Plans 03-06 will assume `npm test` exercises the whole codebase.

Output: 7 new files under `test/existing/`, 4 original files deleted at repo root, full `npm test` run green.

Migration is mechanical 1:1 per RESEARCH.md §Q8 (Test migration plan): each `console.log("=== X ===")` block becomes a `describe` or top-level `test` group, each `console.log("✓ ...")` line becomes one `test()` callback containing the surrounding `assert.*` calls. State-setup calls like `_clearForTests()` and `_withStorePathOverride()` become `beforeEach` hooks. **No new assertions are added; no assertions are dropped.** The only mechanical changes:
- Import paths shift from `./src/...` to `../../src/...` (two directories deeper)
- `console.log("✓ ...")` lines are dropped; node:test prints its own status
- Underscore test seams (`_setSearchOverride`, `_clearForTests`, `_withStorePathOverride`, `_testConnection`) are preserved verbatim
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-PATTERNS.md
@.planning/phases/00-foundation/00-VALIDATION.md
@test-clustering.js
@test-features.js
@test-aggregation-fixes.js
@test-histogram-fixes.js

<interfaces>
<!-- Existing exports the migrated tests will continue to import. Source-tree paths from src/. -->

From `src/clustering/preprocess.js`:
- `normalizeMessage(line: string): string`
- `tokenize(line: string): string[]`

From `src/clustering/index.js`:
- `register(name: string, strategy: object): void`
- `get(name: string): object` (throws "Unknown clustering algorithm" on miss)
- `list(): string[]`
- `_clearForTests(): void`
- `_register(name, strategy)` — legacy alias if present

From `src/clustering/template-store.js`:
- `loadTemplateStore(connectionName: string): { templates, algorithm_state, version }`
- `saveTemplateStore(connectionName: string, data: object): void`
- `_withStorePathOverride(fn: (conn) => string): void`

From `src/clustering/formatter.js`:
- `formatClusterResponse(clusters, options): object`

From `src/clustering/_test_hooks.js`:
- `_setSearchOverride(fn): void`
- `_getSearchOverride(): function | null`

From `src/tools/cluster-errors.js`:
- `handleClusterLogMessages(request): Promise<MCPResponse>`

From `src/tools/template-mgmt.js`:
- `handleListTemplates(request)`, `handleDeleteTemplate(request)`, `handleRenameTemplate(request)`, `handleExportTemplates(request)`, `handleImportTemplates(request)`

From `src/timerange.js`:
- `buildTimeRange(args)`, `normalizeTimeRangeArgs(args)`, `parseRelativeTime(str)`, `parseAbsoluteTime(from, to)`

From `src/aggregations.js`:
- payload builders — names depend on file contents; the migrated tests already import them, so the new files copy the same import lines with adjusted relative path.

Standard header for every new file in this plan:
```js
import { test, describe, beforeEach, after } from "node:test";
import assert from "node:assert/strict";
import "../snapshot-config.js";
```
(`import` names trimmed to what's actually used per file.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate test-clustering.js into 4 files under test/existing/</name>
  <files>test/existing/clustering-preprocess.test.js, test/existing/clustering-strategy.test.js, test/existing/template-store.test.js, test/existing/template-mgmt.test.js</files>
  <read_first>
    - test-clustering.js (read full file; ~308 lines)
    - src/clustering/index.js, src/clustering/template-store.js, src/clustering/preprocess.js (to confirm export names)
    - src/tools/cluster-errors.js (lines 1-30; confirm `_testConnection` seam usage)
    - .planning/phases/00-foundation/00-RESEARCH.md lines 1111-1117 (split rationale)
    - .planning/phases/00-foundation/00-PATTERNS.md lines 403-437 (migration shape)
  </read_first>
  <action>
Split `test-clustering.js` into 4 new files under `test/existing/`. Map each source section to its target file:

| Source section in test-clustering.js (identified by `console.log("=== X ===")`) | Target file |
|-------|--------|
| `=== Preprocessor ===` and `=== Tokenize ===` (or equivalent) | test/existing/clustering-preprocess.test.js |
| `=== Registry ===` and `=== Drain3 ===` and `=== Serialize roundtrip ===` (Drain3 hydrate/cluster/serialize) | test/existing/clustering-strategy.test.js |
| `=== Template store ===` (load/save/lock/corrupt-recovery) | test/existing/template-store.test.js |
| `=== Formatter ===`, `=== cluster_log_messages handler ===`, `=== Template management ===` (list/delete/rename/export/import) | test/existing/template-mgmt.test.js |

For EACH target file:

1. Start with the header:
```js
import { test, describe, beforeEach } from "node:test";
import assert from "node:assert/strict";
import "../snapshot-config.js";
```
(Drop `describe` or `beforeEach` if a file doesn't need them.)

2. Adjust import paths: `from "./src/..."` becomes `from "../../src/..."`. Add `.js` extensions if any are missing (project convention is explicit `.js`).

3. Wrap each section's body in `describe("section name", () => { ... })` (optional but recommended for readability). Each `console.log("✓ <thing>")` line in the original SCRIPT corresponds to a discrete assertion or assertion group — wrap that group in a `test("<thing>", () => { ... })` block.

4. Drop EVERY `console.log` call. Keep EVERY `assert.*` call. Convert `assert.equal` / `assert.deepEqual` / `assert.throws` to their `node:assert/strict` equivalents (`assert.strictEqual` and `assert.deepStrictEqual` are the strict variants; `assert.equal` / `assert.deepEqual` from `node:assert/strict` already use strict semantics — keep the existing call names, the strict import handles it).

5. Convert any top-level `_clearForTests()` / `_withStorePathOverride(...)` setup calls into `beforeEach(() => { ... })` hooks at the top of the relevant `describe` block (or top-level if used file-wide).

6. The `_setSearchOverride()` / `_getSearchOverride()` seams stay verbatim — they're the project's test-seam idiom.

7. The `_testConnection` magic arg used by `handleClusterLogMessages` (`{ ...args, _testConnection: "fake" }`) stays verbatim.

8. Preserve the original assertion ORDER within each section. Don't reorder.

9. If the original file has any `try { ... } catch (err) { console.error(...) }` blocks that swallowed errors, convert them to `test("<name>", () => { assert.doesNotThrow(() => { ... }) })` — surface the error properly via node:test rather than silently logging.

For template-store.test.js specifically: the file-lock / stale-break tests need filesystem isolation. Use `_withStorePathOverride((conn) => path.join(os.tmpdir(), "graylog-mcp-test", conn + ".json"))` in `beforeEach`, and add an `after` hook that `fs.rmSync(tmpDir, { recursive: true, force: true })`. The original script's cleanup logic copies forward verbatim.

For template-mgmt.test.js: `handleClusterLogMessages` is called via `_testConnection` seam — the test does NOT need a live Graylog. If the original script intercepted `searchGraylog` via `_setSearchOverride`, the new file does the same.

Do NOT add new assertions. Do NOT skip any existing assertion. The migration is 1:1 in coverage; only the wrapping syntax changes.
  </action>
  <verify>
    <automated>node --test test/existing/clustering-preprocess.test.js test/existing/clustering-strategy.test.js test/existing/template-store.test.js test/existing/template-mgmt.test.js 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - All 4 target files exist under `test/existing/`
    - `node --test test/existing/clustering-*.test.js test/existing/template-*.test.js` exits 0
    - Test count per file is ≥ the count of distinct `console.log("✓ ...")` lines in the corresponding source section of test-clustering.js (use `grep -c 'console.log("✓' test-clustering.js` to baseline)
    - `grep -c "import" test/existing/clustering-preprocess.test.js` returns ≥ 3 (node:test, node:assert/strict, plus production imports)
    - No file imports from `./src/` or `src/` (must be `../../src/`)
    - No `console.log` calls remain (`grep -c "console.log" test/existing/clustering-*.test.js test/existing/template-*.test.js` returns 0)
    - `_clearForTests()` appears in `beforeEach` (not at file top) in `clustering-strategy.test.js`
  </acceptance_criteria>
  <done>4 files exist, all assertions migrated, `node --test` against them exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate the 3 remaining scripts and delete originals</name>
  <files>test/existing/features.test.js, test/existing/aggregation-fixes.test.js, test/existing/histogram-fixes.test.js, test-clustering.js, test-features.js, test-aggregation-fixes.js, test-histogram-fixes.js</files>
  <read_first>
    - test-features.js (full file)
    - test-aggregation-fixes.js (full file)
    - test-histogram-fixes.js (full file)
    - .planning/phases/00-foundation/00-RESEARCH.md lines 1118-1124 (per-script migration notes)
  </read_first>
  <action>
Three more 1:1 migrations + the deletion step.

(a) `test/existing/features.test.js` — migrate `test-features.js` (~135 lines, 4 sections: time-range parsing, aggregation payloads, error handling, arg normalization). Per RESEARCH.md Q8, this file uses `try { ... } catch (error) { console.error(...) }` to swallow errors — DO NOT preserve that pattern; let exceptions surface via node:test by removing the try/catch wrapping entirely. Each `console.log("✓ X")` becomes `test("X", () => { ...assertions... })`. Import paths shift from `./src/timerange.js`, `./src/aggregations.js` to `../../src/timerange.js`, `../../src/aggregations.js`.

(b) `test/existing/aggregation-fixes.test.js` — migrate `test-aggregation-fixes.js` (~84 lines, pure structural assertions on `buildTimeHistogram`/`buildFieldAggregation` output shapes). Trivial 1:1. Preserve every `assert.equal(histogram.queries[0].search_types[0].type, "pivot")`-style assertion. No state, no hooks needed.

(c) `test/existing/histogram-fixes.test.js` — migrate `test-histogram-fixes.js` (~74 lines). Same shape as (b). RESEARCH.md notes this file has "print-friendly diffing logic for structure comparison" — replace any pretty-printing logic with direct `assert.deepEqual(actualShape, expectedShape)` calls.

Each file uses the same header:
```js
import { test, describe } from "node:test";
import assert from "node:assert/strict";
import "../snapshot-config.js";
```

(d) After verifying the 7 migrated files run green via `npm test`, **delete the 4 original scripts at repo root**:
```bash
rm test-clustering.js test-features.js test-aggregation-fixes.js test-histogram-fixes.js
```

Do NOT move them to `legacy/`. CONTEXT.md doesn't specify, but research §A5 and the planner's preference is "delete, don't archive" since git history preserves them.

After deletion, run `npm test` once more — must still exit 0.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20 ; test ! -f test-clustering.js && test ! -f test-features.js && test ! -f test-aggregation-fixes.js && test ! -f test-histogram-fixes.js && echo "ALL 4 ORIGINALS DELETED"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0
    - `npm test` output shows tests from all 7 `test/existing/*.test.js` files plus the 11 stubs from Plan 01 — total ≥ 18 test groups passing
    - `ls test-*.js` at repo root returns nothing (the four originals are gone)
    - `git status` shows 4 deletions (test-clustering.js, test-features.js, test-aggregation-fixes.js, test-histogram-fixes.js) and 7 new files under test/existing/
    - `grep -rE 'console\.log\(' test/existing/` returns 0 lines (all logging stripped)
    - `grep -rE 'from "\./src' test/existing/` returns 0 lines (all relative paths shifted to `../../src/`)
  </acceptance_criteria>
  <done>7 migrated files exist, 4 originals deleted, `npm test` passes the full unified suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → filesystem | Migrated template-store tests touch `os.tmpdir()` via `_withStorePathOverride` |
| Test runner → src/clustering | Tests import production code; failures surface as assertion errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02-01 | Information disclosure | Migration of test scripts | accept | Original scripts never embedded secrets; tests use `_testConnection` seam not real API tokens |
| T-00-02-02 | Tampering | Deletion of 4 root-level scripts | mitigate | git history preserves originals; reviewer diffs the rename via `git diff --stat` |
| T-00-02-03 | DoS | Template-store tests writing to os.tmpdir | mitigate | `after` hook removes the temp dir; `_withStorePathOverride` confines I/O to test-owned path |
| T-00-02-04 | Tampering | Swallowed-error try/catch in test-features.js | mitigate | Removed during migration per RESEARCH.md Q8; exceptions now surface to node:test reporter |

</threat_model>

<verification>
- `npm test` exits 0 with ≥18 test groups passing
- All 4 root-level `test-*.js` files deleted
- All 7 `test/existing/*.test.js` files have correct headers and adjusted import paths
- No `console.log` calls remain in migrated tests
- The 11 stub tests from Plan 01 still pass
</verification>

<success_criteria>
1. 7 files under `test/existing/` cover every assertion previously made by the 4 root-level scripts.
2. `npm test` runs the entire unified suite (Plan 01 stubs + 7 migrated files) and exits 0.
3. Original 4 `test-*.js` scripts deleted from repo root.
4. No assertion has been silently dropped (manual cross-check by `grep -c "assert\." test-clustering.js` baseline vs sum across the 4 derived files).
5. Test seams (`_clearForTests`, `_withStorePathOverride`, `_setSearchOverride`, `_testConnection`) survive verbatim with the same semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-02-SUMMARY.md` covering:
- Per-source-file count of migrated tests (e.g., "test-clustering.js: 6 sections → 4 files, 23 total `test()` blocks")
- The `_withStorePathOverride` filesystem-isolation choice
- Confirmation that `npm test` now runs the complete unified suite
</output>
</content>
