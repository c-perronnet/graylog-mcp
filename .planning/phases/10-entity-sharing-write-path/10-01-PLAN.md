---
phase: 10-entity-sharing-write-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/authz-share-entity.test.js
  - src/tools/authz/schemas.js
autonomous: true
requirements: [SHARE-01, SHARE-03, SHARE-04, SHARE-05, SHARE-06, SHARE-07, SHARE-08, AUTHZ-01]
tags: [authz, sharing, zod, schemas, drift-refusal, read-merge-write]

must_haves:
  truths:
    - "The Wave 0 test file test/authz-share-entity.test.js exists, parses, and runs (tests will be RED until Plan 10-02 ships the handler — that is expected)"
    - "The file contains the MANDATORY Pitfall-1 acceptance gate (current=[A,B] + add C → body contains [A,B,C], notDeepStrictEqual to [C])"
    - "The file covers every Phase 10 requirement-to-test row from 10-RESEARCH.md §Validation Architecture (read-merge-write gate, drift refusal, confirmation-token mismatch, capability enum rejection, revoke, last-own guard, 400-with-body validation_result, 403 → not_entity_owner, GRN-type matrix stream/dashboard/search, dryRun default, username→user-GRN resolution)"
    - "ShareEntitySchema validates the entity XOR (entityGrn vs entityType+entityId), the grantee XOR (granteeGrn vs granteeUsername), the revoke↔capability interaction, and pins capability to view/manage/own"
    - "ENTITY_TYPES is exported from src/tools/authz/schemas.js for reuse by ShareEntitySchema (Phase 9's private const is promoted)"
    - "The schema rejects entityType:'report'/'role'/etc., capability:'read'/'edit'/'admin', and the cross-field invariants (revoke:true with capability set; revoke:false without capability)"
  artifacts:
    - path: "test/authz-share-entity.test.js"
      provides: "Wave 0 offline fixture-replay test scaffold for share_entity (RED until Plan 10-02 lands the handler — runs without crash)"
      contains: "PITFALL 1 ACCEPTANCE GATE"
    - path: "src/tools/authz/schemas.js"
      provides: "ShareEntitySchema (mutatingBase + XOR refines + revoke↔capability refine) and exported ENTITY_TYPES"
      contains: "ShareEntitySchema"
  key_links:
    - from: "test/authz-share-entity.test.js"
      to: "src/graylog/client.js"
      via: "_setCaptureRequest seam (offline fixture replay)"
      pattern: "_setCaptureRequest"
    - from: "test/authz-share-entity.test.js"
      to: "test/fixtures/authz/prepare-response-7.0.6.json"
      via: "readFileSync — Phase 8 fixture provides the EntityShareResponse DTO"
      pattern: "prepare-response-7.0.6"
    - from: "src/tools/authz/schemas.js"
      to: "src/tools/_shared/schemas.js"
      via: "import { mutatingBase }"
      pattern: "mutatingBase"
    - from: "src/tools/authz/schemas.js"
      to: "src/tools/authz/grn-helpers.js"
      via: "Capability + ENTITY_TYPES shared with grn-helpers consumers"
      pattern: "ENTITY_TYPES"
---

<objective>
Lay the Wave 0 foundation for Phase 10's `share_entity` tool: (1) the offline fixture-replay test file that ENCODES every Phase 10 requirement and pitfall as an executable acceptance gate BEFORE the handler exists, and (2) `ShareEntitySchema` — the zod validator that pins the input contract (entityGrn XOR (entityType,entityId), granteeGrn XOR granteeUsername, revoke XOR capability, Capability enum locked to view/manage/own).

Purpose: The test file is the failing-RED specification the next plan turns GREEN. The MANDATORY Pitfall-1 acceptance gate (current=[A,B] + add C → body=[A,B,C], `notDeepStrictEqual([C])`) is the single load-bearing test of this phase — if it ever regresses, the tool silently revokes other users' production-log access. The schema pre-locks the input surface so Plan 10-02's handler composes against a frozen contract instead of inventing one.

Output: a Wave 0 test file containing ~15 named tests (the full requirement→test map from 10-RESEARCH.md), `ShareEntitySchema` and `ListGranteesSchema`-unchanged exports in `src/tools/authz/schemas.js`, plus the `ENTITY_TYPES` export Plan 09's private const becomes (the planner-decided lift per 10-PATTERNS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-entity-sharing-write-path/10-RESEARCH.md
@.planning/phases/10-entity-sharing-write-path/10-PATTERNS.md
@.planning/phases/10-entity-sharing-write-path/10-VALIDATION.md
@.planning/phases/08-authz-foundation-grn-helper-live-api-recon/08-TEST-STRATEGY.md
@test/fixtures/authz/prepare-response-7.0.6.json
@test/authz-entity-shares.test.js
@src/tools/authz/schemas.js
@src/tools/authz/grn-helpers.js

<interfaces>
<!-- Frozen contracts the executor composes against. Do NOT explore the codebase — use these. -->

From src/tools/_shared/schemas.js (mutatingBase):
- mutatingBase = z.object({ dryRun: z.boolean().default(true), connectionName: z.string().optional(), idempotencyKey: z.string().optional() })

From src/tools/authz/schemas.js (current — Phase 8/9 baseline):
- Capability = z.enum(["view", "manage", "own"])
- GetEntitySharesSchema (plain z.object — keep untouched)
- ListGranteesSchema (= GetEntitySharesSchema — keep untouched)
- Currently FILE-PRIVATE: const ENTITY_TYPES = z.enum(["stream", "dashboard", "search"])  ← this plan exports it

From src/tools/authz/grn-helpers.js:
- buildGrn(type, id) -> "grn::::<type>:<id>" (lowercased; throws on bad input)
- parseGrn(grn) -> validated 5-token object (throws on malformed)
- resolveEntityGrn(args) -> canonical share-target GRN; rejects grantee-type GRNs via SHAREABLE_TYPES guard
- SHAREABLE_TYPES = Set(["stream","dashboard","search"])

From src/graylog/client.js:
- _setCaptureRequest(fn) / _clearCaptureRequest() — unit-test request-capture seam
- Behavior: a leftover seam silently disables HTTP in LATER test files; `afterEach` MUST clear

From src/graylog/errors.js:
- GraylogValidationError (kind: "validation") — thrown for HTTP 400; carries `body` (the parsed response payload)
- GraylogPermissionError (kind: "permission") — thrown for HTTP 403
- Both inherit isGraylogError: true and accept { status, method, path, body } in the constructor's 2nd arg

From src/tools/_shared/cascade-hash.js:
- computeShareGrantHash({ entityGrn, grants: Array<{grantee, capability}> }) -> 64-char lowercase hex sha-256
- Byte-pinned in test/cascade-hash.test.js — sort by grantee internally, extra keys dropped

From src/tools/_shared/connection.js:
- resolveConnection(args) -> { conn, name, error }  (_testConnection seam supported)

From src/tools/_shared/errors.js:
- errorResponse(text) / formatZodError(err) / wrapGraylogError(err, toolName)

From test/fixtures/authz/prepare-response-7.0.6.json (Phase 8 — verbatim 7.0.6 EntityShareResponse):
- entity: "grn::::stream:6a0899bc670fc246e77ca54e"
- active_shares: []
- available_grantees: [{ id: "grn::::user:69f9...", type: "user", title: "Sidecar System User (built-in)" }, { id: "grn::::user:6a02ebea...", type: "user", title: "User A" }, ...]
- available_capabilities: [{ id:"view", title:"Viewer" }, { id:"manage", title:"Manager" }, { id:"own", title:"Owner" }]
- selected_grantee_capabilities: {}
- missing_permissions_on_dependencies: {}
- synced_entities: []
- validation_result: { errors:{}, error_context:{}, failed:false }

Verbatim pattern source for the Pitfall-1 acceptance gate (DO NOT re-invent — adapt this):
- test/pipelines.test.js:2076-2099 (connect_pipelines_to_stream PITFALL 2 ACCEPTANCE GATE) — current=[a,b] + args=[new] → body.pipeline_ids=[a,b,new], assert.notDeepStrictEqual to [new]

Verbatim pattern source for the Phase-9 test scaffold (afterEach _clearCaptureRequest cleanup; fixture-replay seam; _testConnection magic arg):
- test/authz-entity-shares.test.js — imports, beforeEach/afterEach, fixture loader, handler invocation shape
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 offline test scaffold for share_entity</name>
  <files>test/authz-share-entity.test.js</files>
  <read_first>
    - test/authz-entity-shares.test.js (imports lines 1-50; beforeEach/afterEach; fixture-load pattern; _setCaptureRequest seam usage; handler invocation shape with `_testConnection: "fake"`)
    - test/pipelines.test.js (lines 2076-2099 — the connect_pipelines_to_stream PITFALL 2 ACCEPTANCE GATE; THIS IS THE TEMPLATE TO ADAPT)
    - test/cascade-hash.test.js (lines around computeShareGrantHash byte-pin — confirm the canonical input shape so dry-run token assertions match)
    - test/fixtures/authz/prepare-response-7.0.6.json (the EntityShareResponse DTO every test replays)
    - .planning/phases/10-entity-sharing-write-path/10-RESEARCH.md §"Validation Architecture" §"Phase Requirements → Test Map" (the 16-row requirement → test matrix this file encodes)
    - .planning/phases/10-entity-sharing-write-path/10-PATTERNS.md §"test/authz-share-entity.test.js" (the four test shapes — Pitfall-1 gate, GRN-type matrix, 400-with-body, drift refusal — with verbatim snippets to adapt)
    - src/graylog/errors.js (GraylogValidationError + GraylogPermissionError constructor shape — `new GraylogValidationError(msg, { status, method, path, body })`)
  </read_first>
  <action>
    Create `test/authz-share-entity.test.js` — the Wave 0 offline test file. Fully offline (no network): drive `handleShareEntity` via the `_testConnection` magic arg and replay the Phase 8 prepare fixture through the `_setCaptureRequest` seam. The file MUST run (parse + execute the test runner) under `node --test test/authz-share-entity.test.js` even though the handler does not yet exist — to make this possible, the import line `import { handleShareEntity } from "../src/tools/authz/share-entity.js";` will FAIL at module-load time. That is acceptable for this task IF AND ONLY IF the failure is a clean module-not-found error (no syntax error in the test file itself). The verify step asserts that.

    Imports: `test`, `beforeEach`, `afterEach` from `node:test`; `assert` from `node:assert/strict`; `readFileSync` from `node:fs`; `join`, `dirname` from `node:path`; `fileURLToPath` from `node:url`; `_setCaptureRequest`, `_clearCaptureRequest` from `../src/graylog/client.js`; `_clearConnectionsForTests`, `setActiveConnection` from `../src/config.js`; `GraylogValidationError`, `GraylogPermissionError` from `../src/graylog/errors.js`; `computeShareGrantHash` from `../src/tools/_shared/cascade-hash.js`; `handleShareEntity` from `../src/tools/authz/share-entity.js`. Load `PREPARE_FIXTURE` from `test/fixtures/authz/prepare-response-7.0.6.json`.

    `beforeEach` calls `_clearConnectionsForTests()` and `setActiveConnection(null)`. `afterEach` calls `_clearCaptureRequest()` then `_clearConnectionsForTests()` then `setActiveConnection(null)` (MANDATORY — a leftover seam silently disables HTTP in later test files; same discipline as test/authz-entity-shares.test.js).

    Write these NAMED tests (one `test(...)` block each, names verbatim so grep gates can find them):

    1. `test("share_entity PITFALL 1 ACCEPTANCE GATE: current=[A,B], add C → body contains A,B,C (not just C)", ...)` — THE LOAD-BEARING TEST. Replay a fixture with `active_shares: [{grant:"g1", grantee:"grn::::user:a", capability:"view"}, {grant:"g2", grantee:"grn::::user:b", capability:"manage"}]` and `available_grantees` extended with `{id:"grn::::user:c", type:"user", title:"userC"}`. Drive `handleShareEntity` with `{entityType:"stream", entityId:"s1", granteeUsername:"userC", capability:"view", dryRun:true}`. Parse `res.content[0].text`; pull `payload.preview.body.selected_grantee_capabilities`; assert `Object.keys(...).sort()` deep-equals `["grn::::user:a","grn::::user:b","grn::::user:c"]`; assert `notDeepStrictEqual` of the same to `["grn::::user:c"]`. Mirror test/pipelines.test.js:2076-2099 verbatim except for the merge primitive (Map<grantee, capability> vs Set<id>).

    2. `test("share_entity dry-run default returns confirmationToken matching computeShareGrantHash over merged grant set", ...)` — fixture replay; assert `payload.dryRun === true`, `payload.confirmationToken` is a 64-char lowercase hex string, and equals `computeShareGrantHash({entityGrn:"grn::::stream:s1", grants:[{grantee:"grn::::user:a",capability:"view"},{grantee:"grn::::user:b",capability:"manage"},{grantee:"grn::::user:c",capability:"view"}]})`. AUTHZ-01 evidence.

    3. `test("share_entity refuses apply with confirmation_mismatch when args.confirm is wrong", ...)` — dry-run first to get token1, then dryRun:false with `confirm:"deadbeef"`; assert `res.isError === true` AND `res.reason === "confirmation_mismatch"` AND the text contains `[share_entity] confirmation_mismatch`. AUTHZ-01.

    4. `test("share_entity refuses apply with confirmation_mismatch when active_shares drifted between preview and apply", ...)` — TOCTOU drift test. Use a captureRequest that returns one active_shares list on the first /prepare call and a DIFFERENT active_shares (an extra grantee D appeared) on the second /prepare call (apply-time re-prepare). Echo token1 as `confirm`; recompute over [A,B,C,D] yields token2 ≠ token1 → assert `res.reason === "confirmation_mismatch"`. AUTHZ-01.

    5. For each entityType in `["stream","dashboard","search"]`, `test(\`share_entity encodes a ${type} GRN into the apply path\`, ...)` — capture the request; assert `captured.path === "/api/authz/shares/entities/" + encodeURIComponent("grn::::" + type + ":abc123")` (no `/prepare` suffix on the apply path). SHARE-07 + SHARE-08 + SHARE-01.

    6. `test("share_entity resolves granteeUsername via available_grantees[].title to the user-GRN", ...)` — fixture with `available_grantees: [{id:"grn::::user:6a02e",type:"user",title:"userA"}]`; drive with `granteeUsername:"userA"`; assert the merged map key is `"grn::::user:6a02e"` (not the literal `"userA"`). SHARE-04.

    7. `test("share_entity refuses ambiguous granteeUsername (title collision) with a clear error listing candidates", ...)` — two `available_grantees` entries share `title:"alice"`; assert `res.isError === true` and the rendered text contains both candidate GRNs.

    8. `test("share_entity revoke:true subtracts the grantee from active_shares and the dry-run body contains the remainder", ...)` — active_shares=[A:view,B:manage]; revoke:true with `granteeUsername:"userA"`; assert `Object.keys(body.selected_grantee_capabilities).sort()` deep-equals `["grn::::user:b"]`. SHARE-03.

    9. `test("share_entity revoke:true refuses when revoking the last own grant (client-side last-own guard)", ...)` — active_shares=[A:own,B:view]; revoke:true on userA; assert `res.isError === true` AND `res.reason === "would_leave_entity_ownerless"`. ROADMAP success criterion 5.

    10. `test("share_entity HTTP 400 with body.validation_result.failed surfaces structured share_validation_failed error", ...)` — captureRequest throws `GraylogValidationError` on the 2nd call (the apply) with `body: { validation_result: { failed: true, errors: { owner: ["Removing owners will leave the entity ownerless"] } }, missing_permissions_on_dependencies: {}, active_shares: [] }`. Drive dryRun:true then dryRun:false with `confirm:<correct token>`. Assert `res.isError === true`, `res.reason === "share_validation_failed"`, and `JSON.parse(res.content[0].text).validation_result.errors.owner` deep-equals `["Removing owners will leave the entity ownerless"]`. SHARE-06.

    11. `test("share_entity HTTP 403 on apply surfaces reason=not_entity_owner with ownership hint", ...)` — captureRequest throws `GraylogPermissionError` on apply with status 403; assert wrapped error envelope text contains `not_entity_owner` AND a hint mentioning `own`. Pitfall 6.

    12. `test("share_entity zod rejects capability:'read'/'edit'/'admin' synonyms", ...)` — three sub-assertions; each must return `isError:true` with text mentioning the failed capability value. Capability-enum-rejection.

    13. `test("share_entity zod refuses revoke:true with capability set", ...)` — passing both fails zod-parse → isError envelope. Refine evidence.

    14. `test("share_entity zod refuses revoke:false (default) without capability", ...)` — missing capability + no revoke → isError envelope.

    15. `test("share_entity zod refuses entityGrn type 'user' as a share target (SHAREABLE_TYPES guard)", ...)` — pass `entityGrn:"grn::::user:abc"`; assert `res.isError === true` and text mentions `not a shareable entity`.

    16. `test("share_entity refuses dryRun:false on a writable:false connection with reason=connection_read_only", ...)` — use the `_testConnection` seam with a fake conn whose `writable: false`; assert `res.reason === "connection_read_only"` and apply never fires. Writable-gate evidence.

    Every test that needs a captureRequest seam MUST establish it inside the test body and rely on `afterEach` to clear. Every handler call uses `{ params: { arguments: { _testConnection: "fake", ...rest } } }`.
  </action>
  <verify>
    <automated>node --check test/authz-share-entity.test.js && node --test test/authz-share-entity.test.js 2>&1 | tail -8 || true</automated>
  </verify>
  <acceptance_criteria>
    - File `test/authz-share-entity.test.js` exists and is valid ESM (`node --check test/authz-share-entity.test.js` exits 0).
    - `grep -c '"share_entity PITFALL 1 ACCEPTANCE GATE' test/authz-share-entity.test.js` returns at least 1 (THE mandatory gate).
    - `grep -cE 'notDeepStrictEqual|notDeepEqual' test/authz-share-entity.test.js | head -1` returns at least 1 (the inverse-assert that distinguishes read-merge-write from blind replace).
    - `grep -c '_clearCaptureRequest' test/authz-share-entity.test.js` returns at least 2 (`afterEach` cleanup + at least one import).
    - `grep -cE '^test\(' test/authz-share-entity.test.js` returns at least 15 (the named tests above; tolerated up to 20 with sub-tests via `for`-loop iteration).
    - `grep -c 'confirmation_mismatch\|share_validation_failed\|not_entity_owner\|would_leave_entity_ownerless\|connection_read_only' test/authz-share-entity.test.js` returns at least 5 (all five reason tags asserted).
    - `grep -c 'computeShareGrantHash' test/authz-share-entity.test.js` returns at least 1 (the dry-run token assertion uses it directly).
    - `grep -c "encodeURIComponent" test/authz-share-entity.test.js` returns at least 1 (the GRN-type matrix tests assert the encoded path).
    - Running `node --test test/authz-share-entity.test.js` does NOT crash with a syntax/parse error. It may fail with `ERR_MODULE_NOT_FOUND` for `share-entity.js` (handler does not yet exist) — that is expected and acceptable.
  </acceptance_criteria>
  <done>The Wave 0 test file exists, parses, and encodes every Phase 10 requirement-to-test row plus the load-bearing Pitfall-1 acceptance gate. It is the GREEN target Plan 10-02 must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ShareEntitySchema — mutatingBase + XOR refines + revoke↔capability rule</name>
  <files>src/tools/authz/schemas.js</files>
  <read_first>
    - src/tools/authz/schemas.js (the file being modified — currently exports Capability, GetEntitySharesSchema, ListGranteesSchema; ENTITY_TYPES is a file-private const at line 29)
    - src/tools/_shared/schemas.js (mutatingBase shape — lines 12-16)
    - src/tools/index-sets/schemas.js (DeleteIndexSetSchema — the mutating-tool + confirm field pattern, around lines 265-269)
    - src/tools/pipelines/schemas.js (CreatePipelineRuleSchema — the `Boolean(a) !== Boolean(b)` XOR .refine precedent)
    - .planning/phases/10-entity-sharing-write-path/10-PATTERNS.md §"src/tools/authz/schemas.js" (the proposed ShareEntitySchema shape with three .refine clauses)
    - test/authz-share-entity.test.js (Task 1 — the schema rejection tests 12-15 this schema must satisfy)
  </read_first>
  <behavior>
    - `ShareEntitySchema.parse({ entityType:"stream", entityId:"s1", granteeUsername:"alice", capability:"view" })` succeeds (dryRun defaults true via mutatingBase).
    - `ShareEntitySchema.parse({ entityGrn:"grn::::stream:s1", granteeGrn:"grn::::user:a", capability:"manage" })` succeeds.
    - `ShareEntitySchema.parse({ entityGrn:"grn::::stream:s1", granteeUsername:"alice", revoke:true })` succeeds (no capability when revoke).
    - `ShareEntitySchema.parse({ entityGrn:"x", entityType:"stream", entityId:"y", granteeUsername:"a", capability:"view" })` throws — entity XOR violated (both supplied).
    - `ShareEntitySchema.parse({ entityGrn:"grn::::stream:s1", granteeGrn:"grn::::user:a", granteeUsername:"alice", capability:"view" })` throws — grantee XOR violated.
    - `ShareEntitySchema.parse({ entityGrn:"grn::::stream:s1", granteeUsername:"alice", revoke:true, capability:"view" })` throws — revoke:true forbids capability.
    - `ShareEntitySchema.parse({ entityGrn:"grn::::stream:s1", granteeUsername:"alice", revoke:false })` throws — revoke:false (default) requires capability.
    - `ShareEntitySchema.parse({ entityGrn:"grn::::stream:s1", granteeUsername:"alice", capability:"read" })` throws — Capability enum locked.
    - `ShareEntitySchema.parse({ entityType:"report", entityId:"r1", granteeUsername:"alice", capability:"view" })` throws — ENTITY_TYPES enum locked.
    - Exported `ENTITY_TYPES` from schemas.js is the same z.enum (`stream`/`dashboard`/`search`) that GetEntitySharesSchema already uses — no duplicate definition.
  </behavior>
  <action>
    Edit `src/tools/authz/schemas.js`:

    1. Add `import { mutatingBase } from "../_shared/schemas.js";` at the top (after the existing `import { z } from "zod";`).

    2. Promote `const ENTITY_TYPES = z.enum([...])` at line 29 to an `export const ENTITY_TYPES = z.enum(["stream", "dashboard", "search"]);` so ShareEntitySchema can reuse it without re-declaring. Per 10-PATTERNS.md §"schemas.js" recommendation. Do NOT change the existing `GetEntitySharesSchema` / `ListGranteesSchema` reference to ENTITY_TYPES (they already use the const; the export is purely an addition).

    3. Append a banner comment block: "// Phase 10 Plan 10-01 — entity-shares WRITE schema (SHARE-01,03,04,05,06,07,08 + AUTHZ-01)." then `export const ShareEntitySchema = mutatingBase.extend({ ... }).refine(...).refine(...).refine(...);`. Fields on `.extend({...})`:
       - `entityGrn`: `z.string().optional()`
       - `entityType`: `ENTITY_TYPES.optional()`
       - `entityId`: `z.string().min(1).optional()`
       - `granteeGrn`: `z.string().optional()`
       - `granteeUsername`: `z.string().min(1).optional()`
       - `capability`: `Capability.optional()` (the existing enum at line 17 — reuse, do not redefine)
       - `revoke`: `z.boolean().optional().default(false)`
       - `confirm`: `z.string().optional()` (the apply-time token echo; mirrors DeleteIndexSetSchema's `confirm` field)

    4. Three `.refine` clauses, in this order, each with a clear `message`:
       - Entity XOR: `(a) => Boolean(a.entityGrn) !== Boolean(a.entityType && a.entityId)` — message: "Provide either entityGrn, or both entityType and entityId (not both, not neither)."
       - Grantee XOR: `(a) => Boolean(a.granteeGrn) !== Boolean(a.granteeUsername)` — message: "Provide either granteeGrn or granteeUsername (not both, not neither)."
       - Revoke↔capability: `(a) => (a.revoke === true) ? (a.capability === undefined) : (a.capability !== undefined)` — message: "capability is required when revoke is false (default), and must be absent when revoke is true."

    5. Do NOT introduce a JSON-schema `required` array (the .refine clauses are the source of truth; the schema-parity test already accepts this pattern).

    6. Do NOT remove or modify `Capability`, `GetEntitySharesSchema`, or `ListGranteesSchema` — they ship in production and are referenced by Phase 9 handlers and tests.

    Then run `node --check src/tools/authz/schemas.js` to confirm the module parses.
  </action>
  <verify>
    <automated>node --check src/tools/authz/schemas.js && node --input-type=module -e "import {ShareEntitySchema, Capability, ENTITY_TYPES, GetEntitySharesSchema} from './src/tools/authz/schemas.js'; import assert from 'node:assert/strict'; ShareEntitySchema.parse({entityType:'stream',entityId:'s1',granteeUsername:'alice',capability:'view'}); ShareEntitySchema.parse({entityGrn:'grn::::stream:s1',granteeGrn:'grn::::user:a',capability:'manage'}); ShareEntitySchema.parse({entityGrn:'grn::::stream:s1',granteeUsername:'alice',revoke:true}); assert.throws(()=>ShareEntitySchema.parse({entityGrn:'x',entityType:'stream',entityId:'y',granteeUsername:'a',capability:'view'})); assert.throws(()=>ShareEntitySchema.parse({entityGrn:'grn::::stream:s1',granteeGrn:'grn::::user:a',granteeUsername:'alice',capability:'view'})); assert.throws(()=>ShareEntitySchema.parse({entityGrn:'grn::::stream:s1',granteeUsername:'alice',revoke:true,capability:'view'})); assert.throws(()=>ShareEntitySchema.parse({entityGrn:'grn::::stream:s1',granteeUsername:'alice'})); assert.throws(()=>ShareEntitySchema.parse({entityGrn:'grn::::stream:s1',granteeUsername:'alice',capability:'read'})); assert.throws(()=>ShareEntitySchema.parse({entityType:'report',entityId:'r1',granteeUsername:'alice',capability:'view'})); assert.equal(Capability.options.length, 3); assert.equal(ENTITY_TYPES.options.length, 3); GetEntitySharesSchema.parse({entityGrn:'grn::::stream:a'}); console.log('schemas OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/tools/authz/schemas.js` exits 0.
    - The inline schema-verify command prints `schemas OK` and exits 0 — all 8 valid parses succeed and all 6 invalid parses throw.
    - `grep -c 'export const ShareEntitySchema' src/tools/authz/schemas.js` returns 1.
    - `grep -c 'export const ENTITY_TYPES' src/tools/authz/schemas.js` returns 1.
    - `grep -c 'mutatingBase' src/tools/authz/schemas.js` returns at least 2 (one import, one `.extend()` call).
    - `grep -cE '\.refine\(' src/tools/authz/schemas.js` returns at least 4 (existing GetEntitySharesSchema has 1; ShareEntitySchema adds 3).
    - `grep -c 'GetEntitySharesSchema\|ListGranteesSchema\|Capability' src/tools/authz/schemas.js` returns the SAME counts as before the edit (Phase 9 exports unchanged). To check this, `grep -c 'export const Capability' src/tools/authz/schemas.js` returns exactly 1 AND `grep -c 'export const GetEntitySharesSchema' src/tools/authz/schemas.js` returns exactly 1 AND `grep -c 'export const ListGranteesSchema' src/tools/authz/schemas.js` returns exactly 1.
    - `npm test` does not regress — the existing 1131 tests stay green (Phase 9 read-path tests rely on GetEntitySharesSchema and pass unchanged).
  </acceptance_criteria>
  <done>`ShareEntitySchema` validates the entity-XOR, grantee-XOR, and revoke↔capability invariants; `ENTITY_TYPES` is now an export reusable by Phase 10's handler and the apply-path tests; the Phase 9 schema surface is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP agent → ShareEntitySchema | Agent-supplied entityGrn/entityType/entityId/granteeGrn/granteeUsername/capability/revoke cross into the validator; bad values must be rejected BEFORE the handler sees them. |
| Test runner → src/tools/authz/share-entity.js | The Wave 0 test file imports a not-yet-existing module; the test runner must surface this as a clean module-not-found, not a silent skip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering / Elevation | ShareEntitySchema accepting a synonym like `capability:"read"` and coercing it to "view" | mitigate | `Capability = z.enum(["view","manage","own"])` — the existing pinned enum is reused without redefinition; Task 1 Test 12 grep-asserts the rejection. The enum's three values match the Java source exactly (Capability.java); no admin/read/edit synonyms accepted. |
| T-10-01-02 | Tampering | ShareEntitySchema accepting a non-shareable `entityType` (e.g. `"role"`, `"user"`, `"team"`) | mitigate | `ENTITY_TYPES = z.enum(["stream","dashboard","search"])` — the existing pinned enum is reused, now exported for ShareEntitySchema's reach. SHAREABLE_TYPES guard in grn-helpers.js is the structural backstop on the `entityGrn` path. |
| T-10-01-03 | Tampering | revoke:true paired with capability set — silently picks one over the other, agent intent unclear | mitigate | The 3rd `.refine` rejects `revoke:true && capability !== undefined` AND `revoke:false && capability === undefined`. Task 1 Tests 13-14 grep-assert both halves. |
| T-10-01-04 | Information Disclosure | Wave 0 test file printing real-grantee titles from the live fixture into CI logs | accept | Phase 8 fixture uses real UNESCO production titles (Tubaldo, Pitzalis, etc.) — already committed to git. No new exposure introduced. Tests reference titles only in assertion strings, never log them as side-effect output. |
| T-10-01-SC | Tampering | npm/pip/cargo installs | accept | NO new packages installed in this plan. Zero new dependencies; Phase 10 composes only existing primitives. RESEARCH.md §"Package Legitimacy Audit" — Phase 10 installs no new packages. |

No `high`-severity threats: this plan ships zero handler code — only tests and schemas. The mutating write surface lands in Plan 10-02.
</threat_model>

<verification>
- `node --check test/authz-share-entity.test.js` exits 0 (clean parse).
- `node --check src/tools/authz/schemas.js` exits 0 (clean parse).
- The inline schema verify command for Task 2 prints `schemas OK`.
- `npm test` — 1131 baseline tests stay green (no regression on Phase 9 read path; Plan 10-01 adds no tool, so test/list-admin-tools.test.js count stays at 93).
- `node --test test/authz-share-entity.test.js` runs and surfaces `ERR_MODULE_NOT_FOUND` for `share-entity.js` (expected; the handler ships in Plan 10-02).
- Grep gates: PITFALL 1 ACCEPTANCE GATE present; notDeepStrictEqual present; ≥15 named tests; all 5 reason tags present; computeShareGrantHash referenced.
</verification>

<success_criteria>
- The Wave 0 test file `test/authz-share-entity.test.js` exists, parses, and encodes the full Phase 10 requirement → test map (≥15 named tests; MANDATORY Pitfall-1 acceptance gate; all 5 reason tags asserted; computeShareGrantHash dry-run-token assertion).
- `ShareEntitySchema` validates the entity-XOR, grantee-XOR, revoke↔capability, Capability-enum, and ENTITY_TYPES-enum invariants — verified by 8 valid-parse + 6 reject-parse assertions in the inline verify command.
- `ENTITY_TYPES` is exported from `src/tools/authz/schemas.js`; `Capability`, `GetEntitySharesSchema`, `ListGranteesSchema` exports are unchanged.
- The full offline `npm test` suite stays green at 1131 tests (no Phase 9 regression).
- Tool count remains 93 (Plan 10-01 ships NO new tool; test/list-admin-tools.test.js, test/pipelines.test.js, test/dashboards.test.js all unchanged).
</success_criteria>

<output>
Create `.planning/phases/10-entity-sharing-write-path/10-01-SUMMARY.md` when done.
</output>
