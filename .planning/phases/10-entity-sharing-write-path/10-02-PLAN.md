---
phase: 10-entity-sharing-write-path
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/tools/authz/share-entity.js
  - src/tools/authz/index.js
  - src/tools.js
  - src/tools/meta/list-admin-tools.js
  - test/list-admin-tools.test.js
  - test/pipelines.test.js
  - test/dashboards.test.js
autonomous: true
requirements: [SHARE-01, SHARE-03, SHARE-04, SHARE-05, SHARE-06, SHARE-07, SHARE-08, AUTHZ-01]
tags: [authz, sharing, mutating-handler, drift-refusal, read-merge-write, write-path]

must_haves:
  truths:
    - "share_entity composes defineMutatingHandler + Phase 9 fetchEntitySharePreview + Phase 8 computeShareGrantHash + Phase 8 resolveEntityGrn — NO new safety primitive is invented"
    - "build() reads current active_shares via fetchEntitySharePreview, merges Map<granteeGrn, capability>, computes confirmation token over the merged set, and returns the apply descriptor with POST /api/authz/shares/entities/{encodeURIComponent(grn)} (NO /prepare suffix)"
    - "Adding a grantee NEVER silently revokes other grantees — the Pitfall-1 acceptance gate (current=[A,B], add C → body=[A,B,C]) PASSES"
    - "revoke:true subtracts the grantee from the merged map; revoking the last `own` grant client-side refuses with reason=would_leave_entity_ownerless BEFORE the apply call"
    - "HTTP 400 with body.validation_result.failed=true is parsed inside apply() and returned as a structured isError envelope with reason=share_validation_failed and the validation_result/missing_permissions_on_dependencies/active_shares verbatim"
    - "HTTP 403 on apply is tagged with reason=not_entity_owner so the agent sees an ownership hint instead of a generic permission error"
    - "Drift refusal works because build() runs unconditionally at handler.js step 5 — apply-time re-prepare yields a different merged set if active_shares drifted, the recomputed token differs, and requireConfirm refuses with confirmation_mismatch"
    - "Username→user-GRN resolution matches preview.available_grantees[].title; an ambiguous title (multiple matches) refuses with a clear error listing candidates"
    - "share_entity is reachable via the authz barrel (register call), defined in tools.js, classified `authz` in list-admin-tools DOMAIN_OVERRIDES, and the tool count moves 93 → 94 across three test files"
    - "Every requirement (SHARE-01,03,04,05,06,07,08 + AUTHZ-01) has a passing test in test/authz-share-entity.test.js"
  artifacts:
    - path: "src/tools/authz/share-entity.js"
      provides: "handleShareEntity — defineMutatingHandler with async build(), merge helpers, last-own guard, 400-with-body parser"
      exports: ["handleShareEntity"]
      contains: "defineMutatingHandler"
    - path: "src/tools/authz/index.js"
      provides: "register(\"share_entity\", handleShareEntity) — 3rd register call in the authz barrel"
      contains: "register(\"share_entity\""
    - path: "src/tools.js"
      provides: "share_entity { name, description, inputSchema } catalogue entry — description ≤200 chars"
      contains: "name: \"share_entity\""
    - path: "src/tools/meta/list-admin-tools.js"
      provides: "DOMAIN_OVERRIDES['share_entity'] = 'authz'"
      contains: "share_entity: \"authz\""
  key_links:
    - from: "src/tools/authz/share-entity.js"
      to: "src/tools/_shared/handler.js"
      via: "defineMutatingHandler — wrapper enforces dryRun default, writable gate, idempotency, requireConfirm"
      pattern: "defineMutatingHandler"
    - from: "src/tools/authz/share-entity.js"
      to: "src/tools/authz/prepare-share.js"
      via: "fetchEntitySharePreview — one call provides active_shares + available_grantees + validation_result"
      pattern: "fetchEntitySharePreview"
    - from: "src/tools/authz/share-entity.js"
      to: "src/tools/_shared/cascade-hash.js"
      via: "computeShareGrantHash over the merged grant set — byte-pinned canonical form"
      pattern: "computeShareGrantHash"
    - from: "src/tools/authz/share-entity.js"
      to: "src/tools/authz/grn-helpers.js"
      via: "resolveEntityGrn — SHAREABLE_TYPES guard prevents user-GRNs as share targets"
      pattern: "resolveEntityGrn"
    - from: "src/tools/authz/index.js"
      to: "src/tools/authz/share-entity.js"
      via: "register(\"share_entity\", handleShareEntity)"
      pattern: "register\\(\"share_entity\""
    - from: "src/tools.js"
      to: "src/tools/authz/index.js"
      via: "assertAllToolsRegistered parity — tools.js name matches register() name"
      pattern: "share_entity"
    - from: "src/tools/meta/list-admin-tools.js"
      to: "src/tools/authz/share-entity.js"
      via: "DOMAIN_OVERRIDES classification ensures list_admin_tools groups share_entity under authz"
      pattern: "share_entity: \"authz\""
---

<objective>
Ship `share_entity` — the v3.1.0 headline tool. Compose Phase 8's `computeShareGrantHash` + `resolveEntityGrn`, Phase 9's `fetchEntitySharePreview`, and v3.0.0's `defineMutatingHandler` into a single `src/tools/authz/share-entity.js` that turns the Wave 0 test file (Plan 10-01) GREEN. Wire it into the authz barrel, `tools.js`, and the `list-admin-tools` DOMAIN_OVERRIDES; bump the tool count 93 → 94 in three test files.

Purpose: An agent can grant, change, or revoke a user's access to a stream, dashboard, or saved search through ONE tool with the full v3.0.0 safety stack (dryRun:true default, sha-256 confirmation token, drift refusal, writable-gate short-circuit) and Phase 10's new safety properties (read-merge-write Pitfall-1 acceptance, client-side last-own guard, 400-with-body `validation_result` surfacing, 403→`not_entity_owner` ownership hint). The handler is composition over invention — every primitive already exists; only the merge step, the diff, the last-own guard, the 400-body parser, and the composition file are new.

Output: a new `share-entity.js` (~250 lines including helper functions); one new `register()` in the authz barrel; one new `{name, description, inputSchema}` entry in `tools.js`; one new line in DOMAIN_OVERRIDES; three test-file edits bumping 93 → 94. All 16 tests from Plan 10-01's Wave 0 file pass GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-entity-sharing-write-path/10-RESEARCH.md
@.planning/phases/10-entity-sharing-write-path/10-PATTERNS.md
@.planning/phases/10-entity-sharing-write-path/10-VALIDATION.md
@.planning/phases/10-entity-sharing-write-path/10-01-SUMMARY.md
@test/authz-share-entity.test.js
@src/tools/pipelines/connect-pipelines-to-stream.js
@src/tools/pipelines/disconnect-pipelines-from-stream.js
@src/tools/index-sets/delete-index-set.js
@src/tools/_shared/handler.js
@src/tools/_shared/cascade-hash.js
@src/tools/authz/schemas.js
@src/tools/authz/grn-helpers.js
@src/tools/authz/prepare-share.js
@src/tools/authz/index.js
@src/tools/authz/get-entity-shares.js

<interfaces>
<!-- Frozen contracts the executor composes against. Do NOT re-explore the codebase. -->

From src/tools/_shared/handler.js (defineMutatingHandler — Plan 10-02 wraps it as-is):
- Order of operations (DO NOT reorder):
  1. zod.parse(args) — strip _testConnection BEFORE parse, re-merge after for resolveConnection
  2. resolveConnection({...args, _testConnection?})
  3. conn.writable === false → reason: "connection_read_only" — BEFORE build/apply
  4. idempotencyKey = args.idempotencyKey ?? deriveIdempotencyKey(...)
  5. req = await build({ ...args, _connectionName, _conn })   ← runs UNCONDITIONALLY (dryRun + apply)
  6. if dryRun: emit preview envelope; spreads req._confirmationToken as `confirmationToken`, req.existingMatches as []-default
  6b. if typeof requireConfirm === "function": expectedToken = requireConfirm({args, req}); if expectedToken != null && args.confirm !== expectedToken → reason: "confirmation_mismatch"
  7. apply(client, req); if raw.isError === true → return raw verbatim (the 400-with-body envelope path)
  8. normalize(raw) ?? { id: raw?.id, body: raw }
- Critical for drift refusal (Pattern 2 in 10-RESEARCH): build() at step 5 runs on BOTH dry-run AND apply branches — apply-time re-prepare yields fresh active_shares, fresh merged set, fresh token; if token differs from args.confirm, requireConfirm refuses.

From src/tools/authz/prepare-share.js:
- fetchEntitySharePreview(client, entityGrn) -> EntityShareResponse  (POST /api/authz/shares/entities/{encoded}/prepare with {} body; has endsWith("/prepare") self-guard)

From src/tools/authz/grn-helpers.js:
- resolveEntityGrn(args) -> canonical lowercase GRN; throws on malformed; throws on non-shareable GRN type (SHAREABLE_TYPES guard)

From src/tools/_shared/cascade-hash.js:
- computeShareGrantHash({ entityGrn, grants: [{grantee,capability}, ...] }) -> 64-char lowercase hex
- Canonical: JSON.stringify({entityGrn, grants: [sorted by grantee asc]}); throws on any malformed grant entry

From src/tools/authz/schemas.js (Plan 10-01 product):
- ShareEntitySchema — mutatingBase + entity XOR + grantee XOR + revoke↔capability + Capability + ENTITY_TYPES + confirm
- Capability = z.enum(["view","manage","own"])
- ENTITY_TYPES = z.enum(["stream","dashboard","search"])  ← Plan 10-01 exported it

From src/graylog/client.js:
- makeClient(conn).request(method, path, body) — POST with {} or {...} body sets Content-Type: application/json (line 53); null body sends nothing; validateStatus: () => true (line 66) so 4xx flows through mapGraylogError

From src/graylog/errors.js:
- GraylogValidationError extends GraylogError {kind:"validation"; isGraylogError:true} — thrown on HTTP 400, carries body, status, method, path
- GraylogPermissionError extends GraylogError {kind:"permission"; isGraylogError:true} — thrown on HTTP 403
- Adding `err.reason = "not_entity_owner"` propagates through wrapGraylogError (errors.js line ~70 surfaces a `reason:` tag when present)

From src/tools.js:
- Existing get_entity_shares entry at line 1916 (template); list_grantees at line 1929
- Append share_entity AFTER list_grantees, BEFORE list_admin_tools

From src/tools/meta/list-admin-tools.js:
- DOMAIN_OVERRIDES is the const at line 18; lines 65-66 already classify get_entity_shares + list_grantees as "authz". Append share_entity in the same block.

From src/tools/pipelines/connect-pipelines-to-stream.js:40-92 (THE GET-merge-POST acceptance-gate precedent — adapt this verbatim):
- async build(args): pre-flight GET, Set-based merge, return { method:"POST", path, body, existingMatches, postApplyEstimate }
- Snapshot existingMatches BEFORE mutating the merge set (otherwise every arg looks like a pre-existing match)

From src/tools/pipelines/disconnect-pipelines-from-stream.js:54-65 (the revoke shape — subtract from current):
- for (const id of args.pipelineIds) { if (!currentSet.has(id)) existingMatches.push({id, similarity_reason: "not_currently_connected"}); else currentSet.delete(id); }

From src/tools/index-sets/delete-index-set.js:148-219 (the _confirmationToken + requireConfirm precedent):
- build() returns { ..., _confirmationToken: confirmationToken } — handler.js step 6 spreads it as `confirmationToken` in the dry-run envelope
- requireConfirm: ({req}) => req._confirmationToken ?? null  ← copy verbatim
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/tools/authz/share-entity.js — the handler composition</name>
  <files>src/tools/authz/share-entity.js</files>
  <read_first>
    - src/tools/pipelines/connect-pipelines-to-stream.js (PRIMARY analog — lines 36-92 for the defineMutatingHandler + async build() + GET-merge-POST + existingMatches + postApplyEstimate shape)
    - src/tools/pipelines/disconnect-pipelines-from-stream.js (lines 40-80 for the GET-subtract-POST revoke shape and the 404→empty-record idiom)
    - src/tools/index-sets/delete-index-set.js (lines 146-220 — `_confirmationToken` stashing + `requireConfirm: ({req}) => req._confirmationToken ?? null` precedent verbatim)
    - src/tools/_shared/handler.js (lines 58-258 — wrapper steps; lines 211-225 for the requireConfirm gate; line 236 for the `raw.isError === true` passthrough that the 400-body parser exploits)
    - src/tools/_shared/cascade-hash.js (lines 280-339 — computeShareGrantHash byte-pinned canonical form)
    - src/tools/authz/grn-helpers.js (lines 156-175 — resolveEntityGrn + SHAREABLE_TYPES guard)
    - src/tools/authz/prepare-share.js (the helper signature and the {} body discipline)
    - src/tools/authz/schemas.js (ShareEntitySchema from Plan 10-01)
    - src/tools/authz/get-entity-shares.js (reference shape for the get-entity-shares handler — for how the prepare response is consumed; this Plan extends the same pattern)
    - src/graylog/errors.js (GraylogValidationError / GraylogPermissionError class shape — for the 400-with-body and 403 branches)
    - test/authz-share-entity.test.js (the 16 tests this handler must turn GREEN)
    - .planning/phases/10-entity-sharing-write-path/10-RESEARCH.md §"Architecture Patterns" §"Common Pitfalls" §"Code Examples"
    - .planning/phases/10-entity-sharing-write-path/10-PATTERNS.md §"share-entity.js" (the full handler skeleton + adaptation notes)
  </read_first>
  <behavior>
    - Compose: `handleShareEntity = defineMutatingHandler({name:"share_entity", schema: ShareEntitySchema, build, apply, summarize, requireConfirm})`.
    - build() reads current state via fetchEntitySharePreview, resolves the grantee, merges, guards last-own, computes the token, computes the diff, returns { method:"POST", path:`/api/authz/shares/entities/${encodeURIComponent(entityGrn)}`, body:{selected_grantee_capabilities, selected_collections:[]}, _confirmationToken, existingMatches, postApplyEstimate }.
    - The apply path is `POST /api/authz/shares/entities/{encodeURIComponent(grn)}` WITHOUT the `/prepare` suffix.
    - The dry-run envelope contains `confirmationToken` (a 64-char lowercase hex sha-256 over the merged grant set).
    - When dryRun:false AND args.confirm matches token: apply() POSTs the merged body; on 200 the wrapper emits the standard applied envelope.
    - When dryRun:false AND args.confirm mismatches: handler.js step 6b refuses with reason=confirmation_mismatch (NO code in this file — the wrapper enforces it; this file only declares the token via _confirmationToken + requireConfirm).
    - When dryRun:false AND active_shares drifted: apply-time re-prepare yields a different merged set; recomputed token ≠ args.confirm → reason=confirmation_mismatch.
    - When apply receives HTTP 400 with body.validation_result.failed === true: apply() returns an isError envelope with reason=share_validation_failed and the parsed validation_result/missing_permissions_on_dependencies/active_shares JSON-stringified into content[0].text. Handler.js line 236 passes this envelope through unchanged.
    - When apply receives HTTP 403 (GraylogPermissionError): apply() sets `err.reason = "not_entity_owner"` then re-throws; wrapGraylogError surfaces the reason tag and an ownership hint.
    - granteeUsername resolution: match `available_grantees[].title` (case-sensitive primary; if zero matches, refuse "username not found"; if >1 match, refuse with "ambiguous — candidates: [grn1, grn2]"); pass-through granteeGrn if provided.
    - Last-own guard: if current `active_shares` contains ≥1 `capability === "own"` AND the merged set contains ZERO `own` → throw build-time error with `reason: "would_leave_entity_ownerless"` (wrapped by handler.js into an MCP error envelope).
    - Revoke invariant: when revoke:true AND the named grantee is NOT in active_shares → throw error `not_currently_granted`. When revoke:true AND the merged map size doesn't equal `currentMap.size - 1` → throw error `merge_size_mismatch` (defensive invariant per Pitfall 4).
    - summarize() returns either `Revoke <user> from <entity>` or `Grant <capability> to <user> on <entity>`.
  </behavior>
  <action>
    Create `src/tools/authz/share-entity.js` with the following structure (NO fenced code in this directive — names, signatures, and ordering only):

    **Module header (banner comment):** explain this is SHARE-01/03/04/05/06/07/08 + AUTHZ-01; cite the corrected endpoint (`POST /api/authz/shares/entities/{entityGRN}` — no /prepare); cite the read-merge-POST mandate and the Pitfall-1 test; cite `EntitySharesService.updatePrimaryEntityShares` is full-replace (the reason read-merge is mandatory); and note that drift refusal works because handler.js step 5 runs `build()` unconditionally on both branches.

    **Imports:**
    - `defineMutatingHandler` from `../_shared/handler.js`
    - `ShareEntitySchema` from `./schemas.js`
    - `makeClient` from `../../graylog/client.js`
    - `resolveEntityGrn` from `./grn-helpers.js`
    - `fetchEntitySharePreview` from `./prepare-share.js`
    - `computeShareGrantHash` from `../_shared/cascade-hash.js`

    **Module-private helpers** (all `function`, not exported):

    1. `function resolveGranteeFromTitle(availableGrantees, username)` — iterate availableGrantees, collect entries where `g.title === username`. If `matches.length === 0` → throw Error with message: `username "${username}" not found in available_grantees (the entity's resolvable grantees — call list_grantees to see candidates)`. If `matches.length > 1` → throw Error with message listing the candidate `g.id` GRNs and instructing the agent to pass `granteeGrn` explicitly. Otherwise return `matches[0].id`.

    2. `function buildCurrentGrantMap(activeShares)` — return `Map<string, string>` keyed by `s.grantee` valued by `s.capability` from `activeShares ?? []`. Skip entries missing either field (defensive against fixture drift). Return value is the pre-merge baseline.

    3. `function mergeGrants({ current, granteeGrn, capability, revoke })` — Take a `Map<granteeGrn, capability>` (the current grant set), produce a NEW Map (do not mutate `current`). If `revoke === true`: assert `current.has(granteeGrn)` (else throw with reason marker: `not_currently_granted`); set `merged = new Map(current); merged.delete(granteeGrn);` Assert `merged.size === current.size - 1` (else throw `merge_size_mismatch` — Pitfall 4 defensive invariant). Otherwise (grant/change): `merged = new Map(current); merged.set(granteeGrn, capability);` Return `merged`.

    4. `function assertOwnPreserved(activeShares, mergedMap)` — Count `own` capabilities in `activeShares`. If count >= 1 and `[...mergedMap.values()].every(c => c !== "own")` → throw Error with message `would_leave_entity_ownerless: refusing to apply a grant change that would leave entity with no owner (current owners: <list>; merged owners: 0)`. Phase 10 Pitfall 5. If activeShares has zero owners (typical when the sharing user is excluded by Graylog's getForTargetExcludingGrantee), DO NOT block — trust the server-side guard at apply time (it surfaces via the 400-with-body branch).

    5. `function computeDiff(activeShares, mergedMap, revoke)` — Return `{ added, changed, unchanged, removed }` arrays of `{grantee, capability}` (`changed` entries include `from` and `to`). Compare each entry in `activeShares` to `mergedMap` and vice versa. For revoke:true the only `removed` entry is the named grantee; for grant/change the only `added`-or-`changed` entry is the named grantee; `unchanged` lists the rest. The shape mirrors 10-RESEARCH.md §"Pattern 6 — dry-run output diff".

    **Mark errors thrown from build() with a `reason` tag:** when `mergeGrants` throws `not_currently_granted` or `merge_size_mismatch`, when `assertOwnPreserved` throws `would_leave_entity_ownerless`, when `resolveGranteeFromTitle` throws not-found or ambiguous-match — each error gets `err.reason = "<tag>"; err.isGraylogError = true; err.status = 422;` BEFORE the throw so `wrapGraylogError` surfaces the reason tag. (Setting `isGraylogError` is the existing convention — wrapGraylogError checks that flag to attach the reason; if you prefer NOT to spoof GraylogError, set just `err.reason` and write a thin try/catch inside build() that converts the throw into a structured isError envelope — pick ONE approach; document the choice in the file header. Recommendation: spoof `isGraylogError` + status to leverage the existing wrapGraylogError path; this matches how delete_index_set's stats_unreachable case is implemented at delete-index-set.js:134-144.)

    **The defineMutatingHandler call** (`export const handleShareEntity = defineMutatingHandler({...})`):

    - `name: "share_entity"`
    - `schema: ShareEntitySchema`
    - `async build(args)`:
      1. `const client = makeClient(args._conn);`
      2. `const entityGrn = resolveEntityGrn(args);` (throws on malformed/non-shareable — wrapper catches and surfaces)
      3. `const preview = await fetchEntitySharePreview(client, entityGrn);` — one round-trip provides active_shares + available_grantees + validation_result + missing_permissions_on_dependencies + synced_entities.
      4. `const granteeGrn = args.granteeGrn ?? resolveGranteeFromTitle(preview.available_grantees ?? [], args.granteeUsername);`
      5. `const currentMap = buildCurrentGrantMap(preview.active_shares);`
      6. `const mergedMap = mergeGrants({ current: currentMap, granteeGrn, capability: args.capability, revoke: args.revoke ?? false });`
      7. `assertOwnPreserved(preview.active_shares ?? [], mergedMap);`
      8. `const grantsArray = [...mergedMap.entries()].map(([grantee, capability]) => ({ grantee, capability }));`
      9. `const confirmationToken = computeShareGrantHash({ entityGrn, grants: grantsArray });`
      10. `const diff = computeDiff(preview.active_shares ?? [], mergedMap, args.revoke ?? false);`
      11. Build existingMatches from `diff.unchanged.concat(diff.changed)` — entries get a `similarity_reason` of `"already_granted"` or `"capability_changed"` respectively.
      12. Return descriptor:
          - `method: "POST"`
          - `path: \`/api/authz/shares/entities/${encodeURIComponent(entityGrn)}\`` (NO `/prepare` suffix)
          - `body: { selected_grantee_capabilities: Object.fromEntries(mergedMap), selected_collections: [] }`
          - `_confirmationToken: confirmationToken`
          - `existingMatches`
          - `postApplyEstimate: { id: entityGrn, async: false, synced_entities: preview.synced_entities ?? [] }`

    - `apply: async (client, req)`:
      1. `try { return await client.request(req.method, req.path, req.body); }`
      2. `catch (err) { ` ...
         - If `err?.isGraylogError && err.status === 400 && err.body?.validation_result?.failed === true`: return the structured envelope `{ isError: true, reason: "share_validation_failed", content: [{ type: "text", text: JSON.stringify({ tool: "share_entity", status: 400, validation_result: err.body.validation_result, missing_permissions_on_dependencies: err.body.missing_permissions_on_dependencies ?? {}, active_shares: err.body.active_shares ?? [] }) }] }` — the wrapper passes this through verbatim via handler.js line 236.
         - Else if `err?.isGraylogError && err.status === 403`: `err.reason = "not_entity_owner";` AND set `err.message = err.message + " — sharing requires \`own\` capability on the target entity (not \`manage\`); confirm the connection's API-token user is an owner via get_entity_shares."` (concatenate cautiously — preserve the original message). Then `throw err;` so wrapGraylogError surfaces the reason.
         - Else `throw err;` (5xx, network errors, etc. — wrapGraylogError converts to a generic MCP error envelope).

    - `summarize: (args) => args.revoke ? \`Revoke ${args.granteeUsername ?? args.granteeGrn} from ${args.entityType ?? args.entityGrn}\` : \`Grant ${args.capability} to ${args.granteeUsername ?? args.granteeGrn} on ${args.entityType ?? args.entityGrn}\``

    - `requireConfirm: ({ req }) => req._confirmationToken ?? null`  (verbatim from delete-index-set.js:219)

    DO NOT add an `idempotencyKey` field to the input or build descriptor — the wrapper auto-derives one at handler.js step 4. DO NOT add `synced_entities` to the confirmation hash (Pitfall 3 — out of scope for v3.1.0; the canonical input is the merged primary-entity grant set only).
  </action>
  <verify>
    <automated>node --check src/tools/authz/share-entity.js && node --test test/authz-share-entity.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/tools/authz/share-entity.js` exits 0.
    - `src/tools/authz/share-entity.js` exports `handleShareEntity` (`grep -c 'export const handleShareEntity' src/tools/authz/share-entity.js` returns 1).
    - **MANDATORY Pitfall-1 acceptance gate passes:** the test named `share_entity PITFALL 1 ACCEPTANCE GATE: current=[A,B], add C → body contains A,B,C (not just C)` in `test/authz-share-entity.test.js` is GREEN. Verifies adding a grantee NEVER silently revokes other grantees (the load-bearing safety invariant of this milestone). Direct precedent: `test/pipelines.test.js:2076-2099`.
    - **All 16 tests in test/authz-share-entity.test.js are GREEN** (the requirement→test matrix from 10-RESEARCH.md §"Validation Architecture"). Specifically:
      - SHARE-01: grant view/manage/own on a stream — green
      - SHARE-03: revoke a user's access — green
      - SHARE-04: granteeUsername → user-GRN resolution via available_grantees[].title — green; ambiguous-title refusal — green
      - SHARE-05: Pitfall-1 acceptance gate — green; last-own guard — green
      - SHARE-06: HTTP 400 with body.validation_result.failed → reason=share_validation_failed — green; HTTP 403 → reason=not_entity_owner — green
      - SHARE-07: dashboard GRN-type — green
      - SHARE-08: search GRN-type — green
      - AUTHZ-01: dryRun:true default — green; sha-256 confirmation token returned — green; drift refusal — green; confirmation-token mismatch — green; capability enum rejection — green; SHAREABLE_TYPES guard — green; writable:false short-circuit — green
    - The file's body NEVER calls the `/prepare` endpoint directly — it uses `fetchEntitySharePreview` exclusively. `grep -c '/prepare' src/tools/authz/share-entity.js` returns 0 (the helper builds that path; the handler does not).
    - The apply path is the commit endpoint (no `/prepare`): `grep -c 'encodeURIComponent' src/tools/authz/share-entity.js` returns at least 1 AND the path string contains `/api/authz/shares/entities/` (verified by grep) AND NO occurrence of `'/prepare"' src/tools/authz/share-entity.js`.
    - The file uses `defineMutatingHandler`, not `defineListHandler` or a plain async export: `grep -c 'defineMutatingHandler' src/tools/authz/share-entity.js` returns at least 2 (import + call); `grep -c 'defineListHandler' src/tools/authz/share-entity.js` returns 0.
    - The file declares `requireConfirm: ({ req }) => req._confirmationToken ?? null` verbatim: `grep -F "requireConfirm: ({ req }) => req._confirmationToken ?? null" src/tools/authz/share-entity.js` returns 1 line.
    - The file's body contains `selected_grantee_capabilities` and `selected_collections: []` (the EntityShareRequest fields).
    - The Token assertion test (Plan 10-01 Test 2) passes — `payload.confirmationToken` equals the byte-pinned sha-256 over the sorted merged grant set.
    - The drift-refusal test (Plan 10-01 Test 4) passes — apply-time re-prepare yields a different merged set, recomputed token mismatches args.confirm, wrapper emits `reason: "confirmation_mismatch"`.
  </acceptance_criteria>
  <done>`share-entity.js` exists and turns ALL 16 tests in `test/authz-share-entity.test.js` GREEN. The mandatory Pitfall-1 acceptance gate is the load-bearing assertion; once it passes, the milestone's headline safety property holds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire share_entity — barrel register, tools.js catalogue, DOMAIN_OVERRIDES, tool-count bumps</name>
  <files>src/tools/authz/index.js, src/tools.js, src/tools/meta/list-admin-tools.js, test/list-admin-tools.test.js, test/pipelines.test.js, test/dashboards.test.js</files>
  <read_first>
    - src/tools/authz/index.js (the existing 2-register-call barrel from Phase 9 — append a 3rd)
    - src/tools.js (lines 1916-1940 — the get_entity_shares + list_grantees catalogue entries, the templates for the share_entity entry)
    - src/tools/meta/list-admin-tools.js (lines 18-66 — DOMAIN_OVERRIDES const, with get_entity_shares/list_grantees at lines 65-66)
    - test/list-admin-tools.test.js (lines 48, 55, 143 — the three `93` assertions and the "91 v3.0.0 + get_entity_shares + list_grantees" message string)
    - test/pipelines.test.js (lines 396, 427-428 — the assertAllToolsRegistered test naming and toolDefinitions.length assertion)
    - test/dashboards.test.js (lines 1733, 1737, 1743 — same pattern)
    - scripts/audit-tool-descriptions.js (line 24 — DESCRIPTION_BUDGET = 200; the description string must be ≤200 chars)
    - .planning/phases/10-entity-sharing-write-path/10-PATTERNS.md §"Tool-count assertions" (the 8 grep-verified line locations)
  </read_first>
  <action>
    **2.1 — Edit `src/tools/authz/index.js`** (append the 3rd register call):
    - Add `import { handleShareEntity } from "./share-entity.js";` after the existing 2 imports.
    - Add `register("share_entity", handleShareEntity);` after the existing 2 register calls.
    - Update the file's banner comment to note Phase 10 ships share_entity (the v3.1.0 headline write path).

    **2.2 — Edit `src/tools.js`** (append the share_entity catalogue entry):
    - Add a new `{ name, description, inputSchema }` object AFTER the existing `list_grantees` entry (currently ends around line 1940) and BEFORE the `list_admin_tools` entry.
    - `name: "share_entity"`
    - `description: "Share, change, or revoke a user's access to a stream/dashboard/search (view/manage/own). Defaults dryRun:true; agent must echo the confirmation token to apply. Use revoke:true to remove."` — verify char count ≤200 BEFORE saving (the proposed string is 179 chars per 10-PATTERNS.md). If the audit-tool-descriptions.js fails on length, trim adjectives — never add unrelated synonyms.
    - `inputSchema.type: "object"`. `properties:`
      - `connectionName: { type: "string" }`
      - `dryRun: { type: "boolean", description: "Defaults true. Set false to apply." }`
      - `idempotencyKey: { type: "string" }`
      - `entityGrn: { type: "string", description: "Full GRN. Either this OR entityType+entityId." }`
      - `entityType: { type: "string", enum: ["stream", "dashboard", "search"] }`
      - `entityId: { type: "string" }`
      - `granteeGrn: { type: "string", description: "Full user-GRN. Either this OR granteeUsername." }`
      - `granteeUsername: { type: "string", description: "Resolved via available_grantees[].title from the /prepare response." }`
      - `capability: { type: "string", enum: ["view", "manage", "own"], description: "Required unless revoke:true." }`
      - `revoke: { type: "boolean", description: "Defaults false. When true, removes the grantee's grant." }`
      - `confirm: { type: "string", description: "Echo the dry-run confirmationToken to apply." }`
    - Do NOT add a JSON-schema `required` array — zod `.refine` enforces the XOR/interaction invariants.
    - Keep `properties` byte-aligned with the zod schema (a suite-wide parity test compares them — same caveat as Plan 09-01).

    **2.3 — Edit `src/tools/meta/list-admin-tools.js`** (append to DOMAIN_OVERRIDES):
    - After the `list_grantees: "authz",` line (around line 66), add `share_entity: "authz",`. Keep alphabetical-or-thematic order consistent with the existing block.

    **2.4 — Edit `test/list-admin-tools.test.js`** (bump 93 → 94 in three locations):
    - Line 48 test name: `93 tools` → `94 tools`. Also update the trailing comment/string mentioning `91 v3.0.0 + get_entity_shares + list_grantees` to `91 v3.0.0 + get_entity_shares + list_grantees + share_entity`.
    - Line 55 assertion: `payload.count, 93` → `payload.count, 94`; update the embedded error message similarly.
    - Line 143 assertion: `payload.count, 93` → `payload.count, 94`.

    **2.5 — Edit `test/pipelines.test.js`** (bump 93 → 94 in two locations):
    - Line 396 test name: `count = 93; +authz read path` → `count = 94; +authz write path`.
    - Line 427-428 assertion: `toolDefinitions.length, 93` → `toolDefinitions.length, 94`; update the embedded error message to mention `Phase 10 Plan 02 complete: share_entity shipped`.

    **2.6 — Edit `test/dashboards.test.js`** (bump 93 → 94 in three locations):
    - Line 1733 comment: update mention of `Plan 09-01 end` → `Plan 10-02 end` and add `share_entity` to the list.
    - Line 1737 test name: `count = 93; +authz read path` → `count = 94; +authz write path`.
    - Line 1743 assertion: `toolDefinitions.length, 93` → `94`; update the embedded error message.

    Do NOT edit `src/tools/_register.js` (it already imports the authz barrel since Phase 8). Do NOT add new env vars or config-file entries.

    Run `node scripts/audit-tool-descriptions.js` to confirm the share_entity description passes the 200-char budget AND the discrimination-sentence rule.
  </action>
  <verify>
    <automated>node scripts/audit-tool-descriptions.js && npm test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'register("share_entity"' src/tools/authz/index.js` returns 1.
    - `grep -c 'name: "share_entity"' src/tools.js` returns 1.
    - `grep -c 'share_entity: "authz"' src/tools/meta/list-admin-tools.js` returns 1.
    - `git diff --name-only src/tools/_register.js` shows no change (the barrel import already existed since Phase 8).
    - The share_entity description in src/tools.js is ≤200 chars: `node scripts/audit-tool-descriptions.js` exits 0 and prints OK.
    - Tool-count bumps applied: `grep -c 'payload.count, 94\|toolDefinitions.length, 94\|94 tools' test/list-admin-tools.test.js test/pipelines.test.js test/dashboards.test.js` returns at least 6 total (3 in list-admin-tools.test.js + 2 in pipelines.test.js + 3 in dashboards.test.js, with line counting — use `grep -rc` per file if needed).
    - No occurrence of the previous count remains (the bumps are exhaustive): `grep -nE '93 tools|count, 93|toolDefinitions.length, 93' test/list-admin-tools.test.js test/pipelines.test.js test/dashboards.test.js | grep -v '^[^:]*:[0-9]*: *//'` returns nothing (any remaining `93` outside comments is a missed bump).
    - `npm test` — the full offline suite passes (≥1147 tests = baseline 1131 + Plan 10-01 schema check delta + Plan 10-01 Wave 0 file ~15 tests, all green; tolerance: 1145-1160 per 10-RESEARCH.md).
    - `assertAllToolsRegistered` passes at server startup (every tools.js def has a registered handler — share_entity satisfied via the new register call).
    - `node --test test/authz-share-entity.test.js` — all 16 tests GREEN (carried forward from Task 1's verify; this Task only adds wiring, must not regress).
  </acceptance_criteria>
  <done>share_entity is reachable through the authz barrel, defined in tools.js with a ≤200-char description, classified as authz in DOMAIN_OVERRIDES, and the tool-count assertions across three test files move 93 → 94 consistently. The full offline suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP agent → handleShareEntity input | Agent-supplied entity/grantee identifiers + capability + revoke + confirm cross the parse boundary. The zod schema (Plan 10-01) is the first wall; resolveEntityGrn is the second; mergeGrants + assertOwnPreserved are the third. |
| handleShareEntity → Graylog REST API | The handler constructs `POST /api/authz/shares/entities/{encoded-grn}` with the merged grant body — a privileged write. The connection's API-token user must hold `own` on the entity (Graylog enforces server-side via checkOwnership). |
| Graylog 4xx response → apply()'s catch | A 400-with-body carries a structured `validation_result`; a 403 indicates `checkOwnership` refused. Both must be classified, not swallowed. |
| Agent's stale confirmation token → apply-time recomputed token | If active_shares drifted between dry-run and apply, the recomputed token differs and `requireConfirm` refuses. This is the TOCTOU safety net. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | **Tampering, Repudiation** | share-entity.js build() merge step — silent grant revocation (full-replace semantics: a naive POST {selected_grantee_capabilities:{newUser:view}} silently revokes A and B) | **mitigate (highest priority)** | Mandatory read-merge-write via fetchEntitySharePreview + mergeGrants Map clone (not mutate) + Pitfall-1 acceptance gate test (current=[A,B] add C → body=[A,B,C] notDeepStrictEqual [C]). The merge is the load-bearing safety property of v3.1.0. |
| T-10-02-02 | **Tampering** | TOCTOU drift — admin X mutates active_shares between dry-run and apply | **mitigate** | computeShareGrantHash over the merged set + handler.js step 5 unconditional build() re-runs `fetchEntitySharePreview` on apply → recomputes the merged set → recomputes the token. If active_shares drifted, recomputed token ≠ args.confirm → handler.js step 6b refuses with reason=confirmation_mismatch. Verified by Plan 10-01 Test 4. |
| T-10-02-03 | **Denial of Service** (entity unmanageable) | revoke:true on the last `own` grant — leaves entity ownerless and unmanageable | **mitigate** | Client-side assertOwnPreserved BEFORE the commit; refuses with reason=would_leave_entity_ownerless. Server-side `validation_result.failed=true` on apply is the backstop (Pitfall 5; verified Plan 10-01 Test 9). |
| T-10-02-04 | **Elevation of Privilege** | Confused deputy — non-owner agent tries to share an entity it merely manages | **mitigate** | Graylog's checkOwnership server-side returns 403; apply()'s catch tags `err.reason = "not_entity_owner"` and concatenates an ownership hint to the error message. Verified Plan 10-01 Test 11. |
| T-10-02-05 | **Elevation of Privilege** | Capability synonym attack — `capability:"admin"` coerced to "own" by lax matching | **mitigate (Plan 10-01)** | ShareEntitySchema's Capability enum is locked to view/manage/own; no synonyms accepted. Verified Plan 10-01 Test 12. |
| T-10-02-06 | **Information Disclosure** | Granting to `grn::::builtin-team:everyone` accidentally — opens production logs to all users | **mitigate** | The MCP surface does not auto-resolve "everyone"; agent must explicitly pass the literal builtin-team:everyone GRN. Dry-run preview surfaces the diff (added: builtin-team:everyone) for human review before apply. summarize() includes the grantee identifier prominently. Note: this is a partial mitigation; v2 considers an extra-confirmation flag for builtin-team grants — out of scope for v3.1.0 per 10-RESEARCH Open Question 5. |
| T-10-02-07 | **Tampering / DoS** | Empty `selected_grantee_capabilities: {}` body — Graylog interprets this as "remove all shares" (Pitfall 4) | **mitigate** | mergeGrants throws `merge_size_mismatch` if a revoke produces a merged set whose size doesn't equal current.size - 1 (defensive invariant — the only valid path to an empty map is revoking the sole grantee, in which case mergedMap.size === 0 === currentMap.size - 1 and the operation is legitimate). |
| T-10-02-08 | **Tampering (attempted)** | Apply against a read-only connection (writable: false) | **mitigate (handler.js)** | The wrapper's writable gate at handler.js step 3 short-circuits BEFORE build() runs; reason=connection_read_only. Verified Plan 10-01 Test 16. |
| T-10-02-09 | **Information Disclosure** | active_shares under-reports — sharing user's own grant excluded by Graylog's getForTargetExcludingGrantee | **accept** | This is server-side filter behavior, documented in get_entity_shares' tool description ("active_shares excludes your own grant"). share_entity inherits the documentation; no code defense possible. The client-side last-own guard is a partial-view check (asserts mergedMap preserves own IFF activeShares had ≥1 own — if activeShares had 0 owners, the sharing user themselves is presumed owner, and the apply will succeed or 400 with validation_result). |
| T-10-02-10 | **Tampering** | Token replay across distinct entities — token for stream-X used to authorize an apply on stream-Y | **mitigate (Phase 8)** | computeShareGrantHash includes entityGrn in the canonical input (test/cascade-hash.test.js byte-pin proves entityGrn-sensitivity). A token from stream-X cannot validate against an apply on stream-Y; the recomputed token over stream-Y's merged set differs. |
| T-10-02-11 | **Information Disclosure** | Production-log exposure via mis-targeted apply against the live UNESCO `test` connection during automated `npm test` | **mitigate** | The offline test file uses `_testConnection: "fake"` + `_setCaptureRequest` seam exclusively — no live HTTP call from `npm test`. Live UAT is Plan 10-03 (opt-in `.smoke.js`, gated by human approval, throwaway entity per 08-TEST-STRATEGY.md). |
| T-10-02-12 | **Repudiation** | Apply succeeds but the agent has no audit trail of the exact merged body that was sent | **mitigate** | The dry-run envelope (preview.body) shows the EXACT body the apply will send (handler.js spreads `preview: { method, path, body }` at step 6). The apply-success envelope echoes `result.body` from Graylog's 200 response. Combined, the agent has both the intended state AND the server-confirmed state. |
| T-10-02-SC | **Tampering** | npm/pip/cargo installs | **mitigate (Plan 10-01)** | NO new packages installed in Plan 10-01 or Plan 10-02. Zero new dependencies; the handler composes only existing primitives (defineMutatingHandler, computeShareGrantHash, fetchEntitySharePreview, resolveEntityGrn, makeClient, GraylogValidationError). Per 10-RESEARCH §"Package Legitimacy Audit" — Phase 10 is install-free. |

**High-severity threats present:** T-10-02-01 (silent grant revocation) is the highest-priority threat in the milestone. The Pitfall-1 acceptance gate in Plan 10-01 (Test 1) is its mandatory mitigation — if that test fails, this plan does NOT pass.
</threat_model>

<verification>
- `node --check src/tools/authz/share-entity.js` exits 0.
- `node scripts/audit-tool-descriptions.js` passes (share_entity description ≤200 chars, has a discrimination sentence).
- `node --test test/authz-share-entity.test.js` — all 16 tests GREEN; specifically the `PITFALL 1 ACCEPTANCE GATE` test is GREEN.
- `npm test` — full offline suite green; tool count is now 94 across `test/list-admin-tools.test.js`, `test/pipelines.test.js`, `test/dashboards.test.js`; assertAllToolsRegistered passes.
- `grep` gates: `share-entity.js` contains `defineMutatingHandler`, `fetchEntitySharePreview`, `computeShareGrantHash`, `resolveEntityGrn`, `requireConfirm`; the apply path lacks `/prepare`; the description in tools.js is ≤200 chars.
- No regression in Phase 9 read-path tests (`test/authz-entity-shares.test.js` still GREEN).
- The share_entity tool is listed under the `authz` domain when an agent calls `list_admin_tools` (manual sanity — covered by test/list-admin-tools.test.js asserting the domain mapping).
</verification>

<success_criteria>
- **ROADMAP Phase 10 Success Criterion 1 — SHARE-01, SHARE-07, SHARE-08:** `share_entity` grants a user view/manage/own capability on a stream, dashboard, OR saved search (only the GRN type token changes) — Plan 10-01 Tests 5+12+15 GREEN.
- **ROADMAP Phase 10 Success Criterion 2 — SHARE-05 + the Pitfall-1 gate:** Adding a grantee NEVER revokes existing grantees; the three-grantee acceptance test (current=[A,B] + add C → body=[A,B,C], notDeepStrictEqual [C]) is GREEN.
- **ROADMAP Phase 10 Success Criterion 3 — SHARE-03:** Revoke re-POSTs the merged grant set minus the named grantee; the dry-run envelope explicitly diffs added/changed/unchanged/removed.
- **ROADMAP Phase 10 Success Criterion 4 — AUTHZ-01:** `share_entity` defaults to `dryRun: true`, returns a sha-256 confirmation token, refuses apply on drift (`reason: "confirmation_mismatch"`), and refuses apply on token mismatch (same reason).
- **ROADMAP Phase 10 Success Criterion 5 — SHARE-06:** `validation_result` and `missing_permissions_on_dependencies` are surfaced as structured output (including the HTTP 400-with-body case via `reason: "share_validation_failed"`); a non-owner attempt yields `reason: "not_entity_owner"`; a request that would drop the last `own` grant is refused with `reason: "would_leave_entity_ownerless"`.
- **SHARE-04:** `granteeUsername` resolves to a user-GRN via `available_grantees[].title`; ambiguous titles refuse with candidate list.
- Tool surface is 94. `assertAllToolsRegistered` passes. The full offline suite is green.
</success_criteria>

<output>
Create `.planning/phases/10-entity-sharing-write-path/10-02-SUMMARY.md` when done.
</output>
