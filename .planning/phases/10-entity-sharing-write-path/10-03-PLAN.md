---
phase: 10-entity-sharing-write-path
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - test/authz-share-entity-live.smoke.js
autonomous: false
requirements: [SHARE-01, SHARE-03, SHARE-04, SHARE-05, SHARE-06, SHARE-07, SHARE-08, AUTHZ-01]
tags: [authz, sharing, live-uat, smoke, dry-run-only, human-verify, production-safety]

must_haves:
  truths:
    - "An opt-in dryRun:true live probe exists for share_entity against the production UNESCO `test` connection — excluded from `npm test` via the `.smoke.js` suffix"
    - "The probe self-guards: every authz request path issued by handleShareEntity is asserted to be either /prepare (the build-phase read) OR the commit endpoint with dryRun:true (which means apply NEVER runs because the wrapper short-circuits at handler.js step 6 before apply); the probe ABORTS with a non-zero exit if a non-dryRun commit POST is observed"
    - "The probe verifies the read-merge-write pipeline end-to-end: live /prepare returns active_shares, build() merges, returns a confirmationToken; the agent receives a well-formed dry-run envelope; NO grant is ever written to the live instance"
    - "A human-verify checkpoint pauses for the user to (optionally) perform the throwaway-entity full-apply UAT manually per 08-TEST-STRATEGY.md — Plan 10-03 does NOT auto-execute that step"
    - "All 8 Phase 10 requirements (SHARE-01,03,04,05,06,07,08 + AUTHZ-01) are evidenced by passing offline tests (Plan 10-02) AND the dryRun:true live probe (Plan 10-03); the throwaway-entity full-apply UAT is the human-gated milestone-close evidence"
  artifacts:
    - path: "test/authz-share-entity-live.smoke.js"
      provides: "Opt-in dryRun-only live smoke probe (mirrors Phase 9 authz-entity-shares-live.smoke.js); excluded from npm test"
      contains: "dryRun: true"
  key_links:
    - from: "test/authz-share-entity-live.smoke.js"
      to: "src/tools/authz/share-entity.js"
      via: "import handleShareEntity — drive the handler against the live `test` connection"
      pattern: "handleShareEntity"
    - from: "test/authz-share-entity-live.smoke.js"
      to: "src/graylog/client.js"
      via: "_setCaptureRequest pass-through guard + assertSafeAuthzPath self-guard"
      pattern: "_setCaptureRequest"
    - from: "test/authz-share-entity-live.smoke.js"
      to: "src/config.js"
      via: "getConnections — resolve the `test` connection directly without an MCP envelope"
      pattern: "getConnections"
---

<objective>
Add the opt-in `dryRun:true` live smoke probe for `share_entity` against the production UNESCO Graylog 7.0.6 `test` connection — and gate the throwaway-entity full-apply UAT behind a blocking human-verify checkpoint. This is the highest-blast-radius capability in v3.1.0 (entity sharing changes who can read production logs); the probe stays read-only (never POSTs the commit endpoint with `dryRun:false`), and the human-UAT step happens off-tool per 08-TEST-STRATEGY.md.

Purpose: Phase 9 ships a `*.smoke.js` for the read path (`test/authz-entity-shares-live.smoke.js`). Phase 10 adds the write-path equivalent — but the write path's commit endpoint is a real mutation, so the smoke probe is strictly `dryRun:true`. The dry-run envelope still exercises the full build pipeline (`/prepare` round-trip + grantee resolution + merge + token computation) against live data, so it catches drift between Phase 9's offline fixture and the actual live 7.0.6 surface (Pitfalls 2 + 6 — `title` vs username; ownership 403). The throwaway-entity full-apply UAT is documented but executed by a human, not by Plan 10-03's autonomous run.

Output: one new `test/authz-share-entity-live.smoke.js` file (~150 lines mirroring Phase 9's smoke harness verbatim), and one human-verify checkpoint task that pauses for the user to either skip live UAT (defer to milestone close per 10-RESEARCH Open Question 5) or run the throwaway-entity full-apply UAT manually with their own dedicated test user + disposable stream.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-entity-sharing-write-path/10-RESEARCH.md
@.planning/phases/10-entity-sharing-write-path/10-PATTERNS.md
@.planning/phases/10-entity-sharing-write-path/10-VALIDATION.md
@.planning/phases/10-entity-sharing-write-path/10-01-SUMMARY.md
@.planning/phases/10-entity-sharing-write-path/10-02-SUMMARY.md
@.planning/phases/08-authz-foundation-grn-helper-live-api-recon/08-TEST-STRATEGY.md
@test/authz-entity-shares-live.smoke.js
@src/tools/authz/share-entity.js
@src/tools/authz/grn-helpers.js

<interfaces>
<!-- Frozen contracts the executor composes against. -->

From test/authz-entity-shares-live.smoke.js (THE template to mirror — verbatim shape, only the handler under test changes):
- File header: shebang `#!/usr/bin/env node`; banner explaining `.smoke.js` suffix → excluded from npm test; SAFETY block citing 08-TEST-STRATEGY §2/§6 + Pitfall 6.
- Imports: `fileURLToPath` from "node:url"; `resolve` from "node:path"; `makeClient`, `_setCaptureRequest`, `_clearCaptureRequest` from "../src/graylog/client.js"; `getConnections`, `getActiveConnectionConfig` from "../src/config.js"; `buildGrn` from "../src/tools/authz/grn-helpers.js"; the handlers under test from `../src/tools/authz/<name>.js`.
- Constants: `TARGET_CONNECTION = "test"`; `AUTHZ_PREFIX = ["/api", "authz", "shares", "entities", ""].join("/")`.
- Helpers: `resolveProbeConnection()` (reads registry directly), `parseHandlerResult(res, label)` (asserts non-error envelope), `assertSafeAuthzPath(path)` (the SELF-GUARD — aborts process.exit(2) on a forbidden path), `driveHandlerGuarded(handler, args, conn)` (pass-through interceptor).
- Pattern: pass-through interceptor clears the seam, calls the real makeClient, then reinstates the seam — so live HTTP works while every issued path is guarded.
- `process.exit(2)` is the convention for safety-guard violations; `process.exit(1)` for handler errors.

From src/tools/authz/share-entity.js (Plan 10-02 product):
- handleShareEntity expects `{ params: { arguments: { entityType|entityGrn, granteeGrn|granteeUsername, capability?, revoke?, dryRun, connectionName } } }`.
- With `dryRun: true`, build() runs but apply() does NOT (handler.js step 6 returns the preview envelope before step 7 apply). The build()-time `/prepare` round-trip still hits the live instance; the commit-endpoint POST never does.
- With `dryRun: false`, the wrapper requires args.confirm to match req._confirmationToken; if absent or wrong, refused at step 6b. THE SMOKE PROBE NEVER PASSES dryRun:false.

From 08-TEST-STRATEGY.md (the live-production authz test contract):
- The `test` connection is live UNESCO PRODUCTION Graylog — NOT a sandbox (project memory).
- Throwaway-entity rule: full-apply UAT MUST use a freshly created throwaway stream + a dedicated test user; NEVER `builtin-team:everyone`; NEVER an existing production entity.
- Sequence: create throwaway stream → share-to-test-user (apply) → observe → revoke (apply) → delete stream.
- Plan 10-03 does NOT execute the full-apply UAT — it stages the human-verify checkpoint for the user to optionally run.

From .planning/STATE.md:
- The UNESCO `test` connection IS a live production instance. Every live call is treated with the same care as a hand-typed `psql` against prod.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: test/authz-share-entity-live.smoke.js — opt-in dryRun-only live probe</name>
  <files>test/authz-share-entity-live.smoke.js</files>
  <read_first>
    - test/authz-entity-shares-live.smoke.js (FULL FILE — the verbatim template; mirror header, imports, helpers, pass-through interceptor, guard discipline)
    - .planning/phases/08-authz-foundation-grn-helper-live-api-recon/08-TEST-STRATEGY.md (the throwaway-entity contract + the dryRun:true / `@NoAuditEvent` discipline)
    - src/tools/authz/share-entity.js (Plan 10-02 — the handler under test; confirm the apply path is `POST /api/authz/shares/entities/{encoded-grn}` WITHOUT /prepare, so the SELF-GUARD must allow this path ONLY when dryRun:true means apply NEVER runs)
    - src/tools/authz/grn-helpers.js (buildGrn for assembling the share-target GRN inside the probe)
    - .planning/phases/10-entity-sharing-write-path/10-PATTERNS.md §"test/authz-share-entity-live.smoke.js" (the structural template citation)
    - package.json (confirm the `test` script glob is `test/**/*.test.js` — `.smoke.js` is excluded; do not change package.json)
  </read_first>
  <action>
    Create `test/authz-share-entity-live.smoke.js` — a NEW file that mirrors the structure of `test/authz-entity-shares-live.smoke.js` verbatim. Diverge ONLY where the new tool under test requires it.

    **Header (banner comment block):**
    - Shebang `#!/usr/bin/env node`.
    - Title: "Phase 10 Plan 10-03 — LIVE, DRY-RUN-ONLY smoke check for share_entity against the real UNESCO-production Graylog 7.0.6 `test` instance."
    - `.smoke.js` discipline: explicitly note this file is EXCLUDED from `npm test` (the suite glob in package.json is `test/**/*.test.js`); the file is hand-run: `node test/authz-share-entity-live.smoke.js`.
    - SAFETY block (cite 08-TEST-STRATEGY §2/§6, 10-RESEARCH Pitfall 6, project memory `graylog-test-connection-is-live-unesco`):
      - `dryRun: true` ONLY. handler.js step 6 returns the preview envelope before step 7 apply runs, so the commit endpoint POST is NEVER issued.
      - No grant is ever written. The probe verifies the read-merge-token pipeline against an entity the connection's API-token user already has at least `view` on (recommendation: use an entity the user already owns, e.g. their own personal default dashboard — read-only on it is safe; the probe never applies).
      - The pass-through interceptor's SELF-GUARD aborts process.exit(2) on a forbidden path. For Phase 10 the forbidden path is "a commit-endpoint POST issued AT ALL". (Since the handler with dryRun:true never reaches apply, the commit endpoint should never be observed; if it IS observed, that's a wrapper regression and we abort.)
    - HTTP discipline: all calls go through `makeClient(conn).request(...)`; no raw axios import.

    **Imports:**
    - `fileURLToPath` from `"node:url"`, `resolve` from `"node:path"`
    - `makeClient`, `_setCaptureRequest`, `_clearCaptureRequest` from `"../src/graylog/client.js"`
    - `getConnections`, `getActiveConnectionConfig` from `"../src/config.js"`
    - `buildGrn` from `"../src/tools/authz/grn-helpers.js"`
    - `handleShareEntity` from `"../src/tools/authz/share-entity.js"`

    **Constants:**
    - `TARGET_CONNECTION = "test"` — same name as the Phase 8 capture probe and the Phase 9 smoke.
    - `AUTHZ_PREFIX = ["/api", "authz", "shares", "entities", ""].join("/")` — assembled from segments (same idiom as Phase 9 smoke; the prefix constant is intentionally NOT a hand-built path).
    - `COMMIT_SUFFIX_DISALLOWED = true` — a documentation constant noting that ANY path under AUTHZ_PREFIX that does NOT end with `/prepare` is forbidden by this probe (because the probe is dryRun-only and apply never runs).

    **Helpers (verbatim from Phase 9 smoke; adapt names where needed):**
    - `resolveProbeConnection()` — reads `getConnections()[TARGET_CONNECTION] ?? getActiveConnectionConfig()`; throws if missing; returns `{ name: TARGET_CONNECTION, conn }`. Verbatim from Phase 9.
    - `parseHandlerResult(res, label)` — asserts `res.isError` falsy; returns `JSON.parse(res.content[0].text)`. Verbatim from Phase 9.
    - `assertSafeAuthzPath(path)` — the SELF-GUARD. If path does not start with AUTHZ_PREFIX, return (not an authz path). If `path.endsWith("/prepare")`, return (the build-phase read is allowed). OTHERWISE — the path is under AUTHZ_PREFIX without `/prepare` (i.e. the commit endpoint) — `console.error("[smoke] FATAL: authz request path \"" + path + "\" is the MUTATING commit endpoint. This probe is dryRun-only — apply must never run. Refusing."); process.exit(2);`. Also assert the GRN segment between AUTHZ_PREFIX and the trailing `/prepare` does not contain a raw `:` (must be `%3A`-encoded); abort process.exit(2) on violation.
    - `driveHandlerGuarded(handler, args, conn)` — pass-through interceptor mirroring Phase 9 smoke's lines 140-166. The interceptor captures every request, calls `assertSafeAuthzPath(req.path)` BEFORE issuing the real call, clears the seam, awaits `passthroughClient.request(req.method, req.path, req.body)`, then reinstates the seam in `finally`. The outer try/finally clears the seam after the handler returns.

    **The probe main routine — an async `main()` function:**
    1. `const { name: connName, conn } = resolveProbeConnection();`
    2. `console.log("[smoke] Probing share_entity against connection \"" + connName + "\" — dryRun:true ONLY, no commit endpoint will fire.")`
    3. For each entity type in `["stream", "dashboard", "search"]`:
       - **Choose a SAFE target entity** the API-token user already has at least view on. Recommendation: fetch the first entity of that type via the existing list tools' endpoints AND use that one as a READ-only target (the probe is dryRun-only — no grant is written). If no live list-helper exists in the smoke harness budget, accept the constraint that the operator picks an entity GRN before running and exposes it via an env var or stdin prompt; for the autonomous-friendly default, fall back to `process.env.SHARE_SMOKE_<TYPE>_GRN` (per type) OR skip that type with a warning. Document the env-var convention in the file header.
       - Drive `handleShareEntity` via `driveHandlerGuarded(handleShareEntity, args, conn)` with:
         - `_connectionName: connName` (so resolveConnection picks it up — Phase 9 smoke uses `connectionName` in args; same approach)
         - `entityGrn: <safe-target-grn>` (or the assembled (entityType, entityId) pair)
         - `granteeGrn: <a known-existing user-GRN, e.g. the calling token's own user grn>` OR `granteeUsername: <a known title from the live available_grantees response — discoverable by running list_grantees first; the probe can hard-code the title "Sidecar System User (built-in)" which is present in the Phase 8 fixture and likely also present live>`
         - `capability: "view"` (least-privilege; this is the dry-run, no grant is written)
         - `dryRun: true` (MANDATORY — the SAFETY contract; never set false in this file)
       - Parse the dry-run envelope. Assert: `payload.dryRun === true`, `payload.tool === "share_entity"`, `payload.confirmationToken` is a 64-char lowercase hex string, `payload.preview.method === "POST"`, `payload.preview.path` starts with AUTHZ_PREFIX, `payload.preview.path` does NOT end with `/prepare` (the apply path is the commit endpoint, even though apply does not actually fire under dryRun:true), and `payload.preview.body.selected_grantee_capabilities` is an object.
       - Print a one-line success summary per entity type.
    4. **Also drive one revoke:true dryRun** against the same target — assert the dry-run envelope's diff shows the revoke shape (or that the merge size shrinks by 1; pick one structural assertion that does NOT mutate state).
    5. **Also drive one failure case**: pass `granteeUsername: "nonexistent-username-no-such-user"` and assert the handler returns an isError envelope (the resolveGranteeFromTitle branch — username not found in available_grantees). This is a cheap correctness check.
    6. On all-green: `console.log("[smoke] OK — share_entity dryRun:true probe passed for all entity types.")` and `process.exit(0)`. On any assertion failure: print the failing assertion and `process.exit(1)`.

    Do NOT modify `package.json` — the `.smoke.js` suffix is already excluded from the npm test glob. Do NOT add automated invocation of this file from any other test. Do NOT include code that constructs a request without `dryRun: true`.

    Make the file executable: chmod +x is optional — `node <file>` works either way; the shebang is for clarity.
  </action>
  <verify>
    <automated>node --check test/authz-share-entity-live.smoke.js && grep -c "dryRun: true" test/authz-share-entity-live.smoke.js && ! grep -E "dryRun *: *false" test/authz-share-entity-live.smoke.js</automated>
  </verify>
  <acceptance_criteria>
    - File `test/authz-share-entity-live.smoke.js` exists and `node --check test/authz-share-entity-live.smoke.js` exits 0.
    - The file is NAMED `*.smoke.js` (not `*.test.js`); `npm test` does NOT pick it up: confirm by running `npm test 2>&1 | grep -c authz-share-entity-live.smoke` returns 0.
    - The file imports `handleShareEntity` from `../src/tools/authz/share-entity.js` (so the Plan 10-02 handler is the unit under live exercise).
    - The file contains `dryRun: true` at least 3 times (per entity type) and contains ZERO occurrences of `dryRun: false`: `grep -c 'dryRun: true' test/authz-share-entity-live.smoke.js` returns ≥3; `grep -cE 'dryRun *: *false' test/authz-share-entity-live.smoke.js` returns 0.
    - The file contains the SELF-GUARD: `grep -c 'assertSafeAuthzPath\|process.exit(2)' test/authz-share-entity-live.smoke.js` returns ≥2.
    - The file references `getConnections` / `getActiveConnectionConfig`: `grep -cE 'getConnections|getActiveConnectionConfig' test/authz-share-entity-live.smoke.js` returns ≥2 (the resolveProbeConnection helper uses both).
    - The file is NOT executed by automated tests: `grep -l 'authz-share-entity-live.smoke' test/*.test.js` returns nothing (no .test.js file imports or invokes it).
    - `npm test` — the full offline suite is still green (the new `.smoke.js` is invisible to the suite).
    - Manual sanity (informational, NOT enforced — execute step ONLY if the operator opts in): `node test/authz-share-entity-live.smoke.js` exits 0 against a configured `test` connection AND the operator has a known SHARE_SMOKE_STREAM_GRN env var (or accepts the per-type skip-with-warning fallback).
  </acceptance_criteria>
  <done>The opt-in dryRun-only live smoke probe exists, is excluded from `npm test`, references `handleShareEntity`, never sets `dryRun:false`, and self-guards every commit-endpoint path observation by aborting with `process.exit(2)`. The probe is a milestone-close manual verification tool — NOT part of the automated CI loop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify checkpoint — throwaway-entity full-apply UAT (gated; OPTIONAL)</name>
  <files>(no files modified — operator-mediated UAT against a throwaway entity per 08-TEST-STRATEGY.md; optionally captures test/fixtures/authz/commit-response-7.0.6.json if Option A is run)</files>
  <action>Pause for the operator to choose Option A (run the throwaway-entity full-apply UAT manually), Option B (defer to milestone close), or Option C (skip live-apply UAT and rely on offline + dryRun:true live evidence). See <how-to-verify> for the step-by-step procedure each option entails. The executor must NOT auto-execute Option A — full-apply UAT is human-supervised by contract.</action>
  <what-built>
    - Plan 10-01 shipped the Wave 0 offline test scaffold and `ShareEntitySchema` (16-test requirement matrix, MANDATORY Pitfall-1 acceptance gate, schema-validated XOR/refines).
    - Plan 10-02 shipped `src/tools/authz/share-entity.js` (`defineMutatingHandler` composition; read-merge-write via `fetchEntitySharePreview`; `computeShareGrantHash` confirmation token; client-side last-own guard; 400-with-body `share_validation_failed` parser; 403 → `not_entity_owner` tagging); wired it into `src/tools/authz/index.js`, `src/tools.js`, and `src/tools/meta/list-admin-tools.js`; bumped tool count 93 → 94 across `test/list-admin-tools.test.js`, `test/pipelines.test.js`, `test/dashboards.test.js`. All 16 offline tests are GREEN.
    - Plan 10-03 Task 1 shipped `test/authz-share-entity-live.smoke.js` — opt-in dryRun-only live probe; the live `/prepare` round-trip is exercised end-to-end without ever issuing a commit-endpoint POST.

    The remaining evidence Phase 10 OWES the milestone is **the throwaway-entity full-apply UAT** documented in `.planning/phases/08-authz-foundation-grn-helper-live-api-recon/08-TEST-STRATEGY.md`. This task does NOT auto-execute that UAT — it pauses for the operator to either run it now (with a freshly-created throwaway stream + dedicated test user) or defer it to milestone close per 10-RESEARCH Open Question 5 (researcher recommendation: option (a) `scripts/share-entity-live-uat.js` for the human-supervised throwaway run; option (c) "skip live-apply UAT entirely and rely on offline fixture-replay" is also valid for v3.1.0).
  </what-built>
  <how-to-verify>
    1. **Pre-flight — verify the offline + dry-run-live evidence is in place** (these should already be GREEN from Plan 10-02 and Plan 10-03 Task 1):
       - `node --test test/authz-share-entity.test.js 2>&1 | tail -5` — all 16 tests pass; the named `PITFALL 1 ACCEPTANCE GATE` test is GREEN.
       - `npm test 2>&1 | tail -10` — full offline suite green; tool count is 94.
       - `grep -c 'register("share_entity"' src/tools/authz/index.js` returns 1.

    2. **Optionally — run the dryRun-only live smoke probe** against the production UNESCO `test` connection (NO grant will be written; the commit endpoint is structurally unreachable):
       ```
       node test/authz-share-entity-live.smoke.js
       ```
       Expected: prints "[smoke] OK — share_entity dryRun:true probe passed for all entity types." and exits 0. If it exits with code 2, a path-guard violation occurred — investigate immediately (this would indicate a wrapper regression where dryRun:true is not short-circuiting apply).

    3. **Decide — full-apply UAT now, defer, or skip:**
       - **Option A (RECOMMENDED for milestone confidence):** Run the throwaway-entity full-apply UAT manually. Per 08-TEST-STRATEGY.md:
         a. Use `create_stream` (Phase 3) to create a freshly-disposable stream named e.g. `_phase10-uat-throwaway-<timestamp>`.
         b. Identify a dedicated test user (NEVER `builtin-team:everyone`, NEVER a real production user with operational dependencies). If your `test` connection doesn't have one, abort this option.
         c. Call `share_entity` with the throwaway stream's GRN + the test user's GRN + `capability:"view"` + `dryRun:true` to capture the confirmationToken from the preview.
         d. Re-call with `dryRun:false` AND `confirm:<token>` — observe the apply envelope, the diff, and the 200 response.
         e. Call `get_entity_shares` on the throwaway stream — verify the grant appears in active_shares.
         f. Call `share_entity` again with `revoke:true` + a new confirmationToken to remove the grant. Apply.
         g. Call `delete_stream` (Phase 3) to remove the throwaway stream.
         h. Capture the commit-response body as `test/fixtures/authz/commit-response-7.0.6.json` (optional — for future offline tests that pin the apply-response shape).
         i. Document the run outcome and any unexpected behavior in `10-VERIFICATION.md`.
       - **Option B (DEFER):** Defer the full-apply UAT to milestone close. The offline tests cover all 8 requirements structurally; the deferred UAT is the live confirmation layer. Acceptable per 10-RESEARCH Open Question 5 — document the deferral in the resume signal so STATE.md captures it.
       - **Option C (SKIP):** Skip live-apply UAT entirely; rely on offline fixture-replay. Acceptable per 10-RESEARCH Open Question 5 option (c). Document the rationale (e.g. "no dedicated test user available on the production `test` instance; defer to a future development instance").

    4. **If Option A was run:** verify the commit-response body looked structurally identical to the prepare-response fixture (both are `EntityShareResponse` DTOs). Note any 7.0.6-vs-7.2 source-divergence observations.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "apply-uat-complete: <brief summary>" — Option A executed; provide a 1-sentence outcome summary
    - "apply-uat-deferred-to-milestone-close" — Option B; deferral logged for milestone-close UAT
    - "apply-uat-skipped: <reason>" — Option C; rationale captured
    - "apply-uat-failed: <issue>" — UAT ran but observed unexpected behavior; describe the issue so a follow-up plan can be created
  </resume-signal>
  <verify>Operator reports back via the resume-signal options below. No automated verify command — this is a human-gated checkpoint by design.</verify>
  <done>The operator has selected one of the four resume-signal options (apply-uat-complete | apply-uat-deferred-to-milestone-close | apply-uat-skipped | apply-uat-failed); the choice is recorded in 10-VERIFICATION.md and (for Option A) in STATE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live `test` connection (production UNESCO Graylog 7.0.6) | The operator hand-runs the smoke probe AND (optionally) the throwaway-entity UAT. Both touch production infrastructure. |
| Smoke probe → handler under test | The probe issues `dryRun:true` requests; the handler's wrapper short-circuits apply at handler.js step 6. The pass-through interceptor self-guards every authz path. |
| Throwaway-entity UAT (manual) → production Graylog | A real grant write against a freshly-created throwaway stream + dedicated test user. The operator is responsible for cleanup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | **Information Disclosure** (highest severity) | Smoke probe mis-targeting a real production entity + a real production user — mistakenly grants access | **mitigate** | Probe is `dryRun:true` ONLY; the wrapper's handler.js step 6 returns the preview BEFORE step 7 apply runs, so the commit endpoint POST is never issued. The pass-through interceptor's `assertSafeAuthzPath` aborts process.exit(2) if any authz path lacking `/prepare` is ever observed (defense in depth — if the wrapper regresses to firing apply, the probe halts before the network call). Verified by Task 1 grep gates (`dryRun: false` count = 0). |
| T-10-03-02 | **Information Disclosure** | Throwaway-entity UAT (Option A) leaks a grant to a real user by mistake | **mitigate (operator-mediated)** | The 08-TEST-STRATEGY.md throwaway-entity rule is the structural control: freshly-created stream + dedicated test user + revoke + delete in sequence. The human-verify checkpoint surfaces the rule verbatim before the operator decides whether to run. Plan 10-03 does NOT auto-execute Option A — the operator chooses. |
| T-10-03-03 | **Tampering** | Smoke probe self-guard regression — `assertSafeAuthzPath` silently disabled or its `process.exit(2)` removed | **mitigate** | Task 1's acceptance gates grep for `assertSafeAuthzPath` AND `process.exit(2)` — both must be present. The pass-through interceptor pattern verbatim mirrors Phase 9's smoke; any divergence is caught in code review. |
| T-10-03-04 | **Denial of Service** (CI flakiness) | Smoke probe accidentally included in `npm test` and failing CI when the `test` connection is unreachable | **mitigate** | The `.smoke.js` suffix excludes the file from `npm test` (the suite glob is `test/**/*.test.js`). Task 1 acceptance gate `grep -c 'authz-share-entity-live.smoke' test/*.test.js` returns 0 — no .test.js file imports the smoke probe. |
| T-10-03-05 | **Repudiation** | The throwaway-entity UAT (Option A) leaves no record of what was changed | **mitigate** | If executed, the operator captures the dry-run preview body + the apply response body in `10-VERIFICATION.md` AND (optionally) writes `test/fixtures/authz/commit-response-7.0.6.json` for future offline regression. The resume-signal captures a 1-sentence outcome summary so STATE.md records the run. |
| T-10-03-06 | **Information Disclosure** | Smoke probe logs include sensitive grant details (e.g. real user GRNs from available_grantees) | **mitigate** | Probe logs only one-line success summaries + the file's pass-through interceptor adds NO logging of request bodies. The dry-run envelopes contain GRNs by necessity (they are the agent-facing output), but the probe does not pipe them to a log file by default — operator can `> /dev/null` if running in a recording shell. |
| T-10-03-SC | **Tampering** | npm/pip/cargo installs | **mitigate** | NO new packages installed in Plan 10-03. The smoke probe imports only existing modules. |

**High-severity threats present:** T-10-03-01 (live-production grant mis-target) is the dominant risk. The mitigation stack (`dryRun:true` literal + wrapper short-circuit + pass-through self-guard with `process.exit(2)` + `.smoke.js` suffix excluding from automated runs) is defense-in-depth: each layer alone is sufficient; together they make a regression detectable.
</threat_model>

<verification>
- `node --check test/authz-share-entity-live.smoke.js` exits 0.
- `grep -c 'dryRun: true' test/authz-share-entity-live.smoke.js` returns ≥3 AND `grep -cE 'dryRun *: *false' test/authz-share-entity-live.smoke.js` returns 0.
- `grep -c 'assertSafeAuthzPath\|process.exit(2)' test/authz-share-entity-live.smoke.js` returns ≥2.
- `npm test` is unchanged in scope: `npm test 2>&1 | grep -c authz-share-entity-live.smoke` returns 0 (the smoke file is excluded).
- The full offline suite stays green at 94 tools, all Phase 10 + earlier-phase tests passing.
- Task 2 checkpoint resolves with one of the four resume signals; the chosen path is documented in 10-VERIFICATION.md (or the resume signal itself for deferral/skip cases).
- The Phase 10 SUMMARY.md notes the disposition of the throwaway-entity UAT (executed | deferred | skipped) so the milestone close has a complete audit trail.
</verification>

<success_criteria>
- An opt-in `dryRun:true` live smoke probe exists at `test/authz-share-entity-live.smoke.js`, mirrors the Phase 9 smoke harness verbatim, is excluded from `npm test`, and structurally CANNOT issue a commit-endpoint POST (the wrapper short-circuits apply under dryRun:true; the pass-through interceptor's `assertSafeAuthzPath` aborts process.exit(2) on any commit-endpoint path observation).
- The human-verify checkpoint (Task 2) pauses for the operator to run, defer, or skip the throwaway-entity full-apply UAT; the resume signal captures the outcome so STATE.md and the Phase 10 SUMMARY document the decision.
- All 8 Phase 10 requirements (SHARE-01,03,04,05,06,07,08 + AUTHZ-01) have offline test evidence (Plan 10-01 + Plan 10-02) PLUS dryRun-live evidence (Plan 10-03 Task 1). The throwaway-entity full-apply UAT (Plan 10-03 Task 2) is the optional human-supervised live confirmation layer; its disposition is operator-chosen.
- ROADMAP Phase 10 Success Criterion 4 — "defaults to dryRun:true, returns a sha-256 confirmation token, refuses apply with grants_changed_since_preview when the live grant set drifted" — has live evidence via the dry-run probe; the drift-refusal branch is verified offline via Plan 10-01 Test 4 (a fixture-replay TOCTOU simulation that captures the same behavior the live drift would surface).
- Zero new dependencies. Existing v2.3 tool contracts unchanged. The `test` connection's live state is unmutated by Plan 10-03 Task 1 (the only optional mutation is the operator-mediated UAT in Task 2 against a throwaway entity).
</success_criteria>

<output>
Create `.planning/phases/10-entity-sharing-write-path/10-03-SUMMARY.md` when done — record the smoke-probe outcome and the operator's UAT disposition (executed / deferred / skipped).
</output>
