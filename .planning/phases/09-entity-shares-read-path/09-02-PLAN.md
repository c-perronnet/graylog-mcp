---
phase: 09-entity-shares-read-path
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - test/authz-entity-shares-live.smoke.js
autonomous: false
requirements: [SHARE-02, SHARE-09]

must_haves:
  truths:
    - "get_entity_shares is verified against the live UNESCO-production test instance with a non-mutating /prepare-only call"
    - "list_grantees is verified against the live test instance and returns a well-formed available_grantees table"
    - "The live smoke check exercises a stream and, if discoverable, a dashboard and a saved search — confirming GRN-type coverage live"
    - "The live smoke check never touches the commit endpoint and never sends a non-empty body"
  artifacts:
    - path: "test/authz-entity-shares-live.smoke.js"
      provides: "Non-mutating live smoke check against the test connection — separate from npm test"
      contains: "prepare"
  key_links:
    - from: "test/authz-entity-shares-live.smoke.js"
      to: "src/tools/authz/get-entity-shares.js"
      via: "imports and calls handleGetEntityShares against the live test connection"
      pattern: "handleGetEntityShares"
---

<objective>
Add a live, non-mutating smoke check that verifies `get_entity_shares` and `list_grantees` work against the real UNESCO-production Graylog 7.0.6 `test` instance — closing ROADMAP Phase 9 success criterion 4 ("both tools are smoke-tested non-mutatingly against the live `test` instance").

Purpose: The offline fixture (Plan 09-01) proves DTO parsing; this plan proves the live endpoint path, GRN URL-encoding, and HTTP-Basic auth actually work against the running 7.0.6 server — and confirms GRN-type coverage (stream / dashboard / search) live, since the Phase 8 fixture only captured a stream.

Output: `test/authz-entity-shares-live.smoke.js` — a `/prepare`-only probe that is NOT part of `npm test` and is run as a deliberate, human-supervised network check before `/gsd:verify-work`.

This plan has a checkpoint: the smoke check requires a network call to live production infrastructure, so a human verifies the run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-entity-shares-read-path/09-RESEARCH.md
@.planning/phases/08-authz-foundation-grn-helper-live-api-recon/08-TEST-STRATEGY.md
@scripts/capture-authz-prepare-fixture.js
@src/tools/authz/get-entity-shares.js
@src/tools/authz/list-grantees.js

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. -->

From src/tools/authz/get-entity-shares.js (Plan 09-01):
- handleGetEntityShares(request) -> MCP response; request shape { params: { arguments: { connectionName, entityType, entityId | entityGrn } } }

From src/tools/authz/list-grantees.js (Plan 09-01):
- handleListGrantees(request) -> MCP response

From scripts/capture-authz-prepare-fixture.js (Phase 8 — verified live precedent):
- Uses the named `test` connection from ~/.graylog-mcp/config.json
- Discovers a real entity id, builds the GRN, POSTs encodeURIComponent(grn) + /prepare with {} body
- Self-guards that the request path ends with /prepare and exits non-zero otherwise
</interfaces>

<live_instance_warning>
The `test` connection is live UNESCO PRODUCTION Graylog — NOT a sandbox (project memory).
This smoke check is safe ONLY because it calls `POST .../prepare` with an empty `{}` body —
that endpoint is `@NoAuditEvent` and mutates nothing. The smoke check MUST NEVER:
  - call the commit endpoint `POST .../entities/{grn}` (no /prepare suffix);
  - send a non-empty body / any `selected_grantee_capabilities` content;
  - run as part of `npm test`.
See 08-TEST-STRATEGY.md §2, §6 and 09-RESEARCH.md Pitfall 6.
</live_instance_warning>
</context>

<tasks>

<task type="auto">
  <name>Task 1: live non-mutating smoke check for get_entity_shares + list_grantees</name>
  <read_first>
    - scripts/capture-authz-prepare-fixture.js (the verified live-call pattern — connection use, entity discovery, /prepare-only-suffix self-guard)
    - src/tools/authz/get-entity-shares.js (the handler under test, from Plan 09-01)
    - src/tools/authz/list-grantees.js (the handler under test, from Plan 09-01)
    - 08-TEST-STRATEGY.md (§2 the only safe live call; §6 not part of npm test)
    - 09-RESEARCH.md (Open Question 2 — discover a dashboard/search via GET /api/views; Pitfall 6 live-test safety)
  </read_first>
  <action>
    Create `test/authz-entity-shares-live.smoke.js` — a standalone live smoke check. It is NOT part of `npm test` (the `.smoke.js` suffix keeps it outside the suite glob; confirm the `package.json` test glob does not match `*.smoke.js` and adjust the filename if it does).

    The smoke check, using the named `test` connection (same connection-resolution path as `capture-authz-prepare-fixture.js`):
    1. Discover a real stream id: `GET /api/streams` and take the first stream's `id`. Build the GRN via `buildGrn("stream", id)`.
    2. Drive `handleGetEntityShares` with `{ params: { arguments: { connectionName: "test", entityType: "stream", entityId: <id> } } }`. Assert the result is NOT an error envelope, parse `content[0].text`, and assert `entity_shares` carries `available_grantees`, `available_capabilities`, `active_shares`, `validation_result` (the live `EntityShareResponse` shape).
    3. Drive `handleListGrantees` with the same arguments; assert `grantees` is an array and, if non-empty, each entry has `id` / `type` / `title`.
    4. GRN-type coverage: attempt to discover one dashboard and one saved search via `GET /api/views` (dashboards + searches share the views API — filter on the `type` field: `DASHBOARD` vs `SEARCH`). For each discovered entity, run `handleGetEntityShares` with the matching `entityType` (`dashboard` / `search`) and assert a well-formed result. If none is discoverable on the live instance, log a clear SKIP line for that type — the stream probe plus Plan 09-01's offline GRN-type-parameterized tests are the floor (do NOT fail the smoke check on a missing dashboard/search).
    5. Self-guard: every request path the smoke check (or the handlers it drives) issues under `/api/authz/shares/entities/` MUST end with `/prepare`. Assert this — mirror `capture-authz-prepare-fixture.js`'s endsWith guard — and exit non-zero if violated. The smoke check sends NO `selected_grantee_capabilities` and NEVER calls the commit endpoint.
    6. Print a clear PASS / SKIP / FAIL summary line per probed entity type and exit 0 on overall pass, non-zero on any failure.

    The file is run directly: `node test/authz-entity-shares-live.smoke.js` (or `node --test test/authz-entity-shares-live.smoke.js` if written as a node:test file — either is acceptable as long as it stays out of the `npm test` glob).
  </action>
  <verify>
    <human-check>Run `node test/authz-entity-shares-live.smoke.js` against the live `test` connection; confirm it prints PASS for the stream probe and PASS-or-SKIP for dashboard/search, exits 0, and that no request path lacks the `/prepare` suffix.</human-check>
  </verify>
  <acceptance_criteria>
    - File `test/authz-entity-shares-live.smoke.js` exists and `node --check` passes.
    - `grep -c "selected_grantee_capabilities" test/authz-entity-shares-live.smoke.js` returns 0 (no commit-body content).
    - `grep -nE '/api/authz/shares/entities/' test/authz-entity-shares-live.smoke.js | grep -v '/prepare' | grep -v '^[^:]*: *//'` returns nothing, OR the file builds no path itself (paths come from the handlers / prepare-share.js).
    - The file is excluded from `npm test` — `npm test` output does not list `authz-entity-shares-live.smoke.js`.
    - `npm test` still passes fully offline (the smoke file's existence does not break the suite).
  </acceptance_criteria>
  <done>The live smoke-check file exists, is excluded from the offline suite, calls only the `/prepare` read endpoint, and is ready for the human-run verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>A non-mutating live smoke check (`test/authz-entity-shares-live.smoke.js`) that drives `get_entity_shares` and `list_grantees` against the live UNESCO-production Graylog 7.0.6 `test` instance, calling only the `@NoAuditEvent` `POST .../prepare` endpoint with an empty `{}` body. It mutates nothing.</what-built>
  <how-to-verify>
    1. Confirm the `test` connection is configured in `~/.graylog-mcp/config.json` and reachable.
    2. Run: `node test/authz-entity-shares-live.smoke.js`
    3. Expect: a PASS line for the stream probe (`get_entity_shares` + `list_grantees` both return a well-formed `EntityShareResponse`-derived result); PASS or SKIP lines for the dashboard and saved-search probes; overall exit code 0.
    4. Confirm the output shows NO request path under `/api/authz/shares/entities/` missing a `/prepare` suffix, and the run reports zero mutations.
    5. Confirm `npm test` still passes and does NOT include the `.smoke.js` file.
  </how-to-verify>
  <resume-signal>Type "approved" if the live smoke check passed (stream PASS, dashboard/search PASS-or-SKIP, exit 0, /prepare-only); otherwise describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke check → live production Graylog | The smoke check makes a real network call to live UNESCO-production infrastructure; the only safety margin is the non-mutating `/prepare` endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering / Elevation | smoke check accidentally hitting the mutating commit endpoint or sending a non-empty body | mitigate | The smoke check drives only `handleGetEntityShares` / `handleListGrantees` — handlers that exclusively build `/prepare` paths (Plan 09-01); a `path.endsWith("/prepare")` self-guard and a `grep` acceptance gate for absence of `selected_grantee_capabilities` enforce read-only. The commit endpoint is never referenced. |
| T-09-07 | Information Disclosure | dumping live production grant maps / GRNs to logs or CI output | mitigate | The smoke check prints PASS/SKIP/FAIL summary lines and shape assertions only — it does not print full grant maps. It is human-run, not CI-run, and excluded from `npm test`. |
| T-09-08 | Denial of Service | repeated smoke runs hammering live production | accept | The check is a single deliberate human-supervised run before `/gsd:verify-work`, not automated. Read-only `/prepare` calls are low-cost; one run probes at most three entities. Low risk, accepted. |

No `high`-severity threats: the smoke check is read-only against a `@NoAuditEvent` endpoint. The blocking human checkpoint is the control for the residual "live production" risk.
</threat_model>

<verification>
- `node test/authz-entity-shares-live.smoke.js` exits 0 with a PASS line for the stream probe and PASS-or-SKIP for dashboard/search.
- No request path under `/api/authz/shares/entities/` lacks the `/prepare` suffix; the smoke check sends no `selected_grantee_capabilities`.
- `npm test` remains green and does not include the `.smoke.js` file.
</verification>

<success_criteria>
- Both `get_entity_shares` and `list_grantees` are smoke-tested non-mutatingly against the live `test` instance (ROADMAP success criterion 4).
- GRN-type coverage (stream / dashboard / search) is confirmed live where the live instance has discoverable entities of each type; the stream probe is guaranteed, dashboard/search are best-effort with a clear SKIP.
- No mutation reaches the live production instance — `/prepare`-only, empty `{}` body, verified by the blocking human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/09-entity-shares-read-path/09-02-SUMMARY.md` when done.
</output>
