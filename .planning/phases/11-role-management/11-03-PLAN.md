---
phase: 11-role-management
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - scripts/capture-roles-fixtures.js
  - test/authz-roles-live.smoke.js
  - test/fixtures/authz/roles/list-roles-7.0.6.json
  - test/fixtures/authz/roles/get-role-admin-7.0.6.json
  - test/fixtures/authz/roles/get-role-members-reader-7.0.6.json
  - test/fixtures/authz/roles/user-roles-admin-7.0.6.json
  - test/fixtures/authz/roles/permissions-catalogue-7.0.6.json
autonomous: false
requirements: [ROLE-01, ROLE-02, ROLE-03, ROLE-04, ROLE-05, ROLE-06, ROLE-07]
tags: [authz, roles, live-uat, smoke, dry-run-only, human-verify, production-safety, fixture-capture]

must_haves:
  truths:
    - "scripts/capture-roles-fixtures.js exists, is one-shot read-only (every call asserted as GET; any non-GET aborts process.exit(2)), and successfully captures the 5 live fixtures into test/fixtures/authz/roles/ on the live UNESCO `test` Graylog 7.0.6 connection — overwriting the Plan 11-01 placeholders with verbatim live responses + _provenance blocks (D-22 — one-shot, gated, read-only capture script ships the 4 mandated fixture filenames + permissions-catalogue; fixture-replay via the `_setCaptureRequest` seam from Phase 10 is what Plan 11-01's tests consume)"
    - "test/authz-roles-live.smoke.js exists as a *.smoke.js file (excluded from npm test), exercises all 7 role tools against the live `test` connection in dryRun:true mode ONLY, and self-guards every mutation endpoint via assertSafeAuthzPath (mirroring authz-share-entity-live.smoke.js)"
    - "The dryRun smoke probe verifies the read pipeline end-to-end: live GETs return expected DTOs; build() merges current state; tokens are 64-hex; preview envelopes are well-formed; NO mutation is ever written to the live instance"
    - "A human-verify checkpoint pauses for the operator to choose: (A) run the throwaway-role full-lifecycle UAT manually now, (B) defer to v3.1.0 milestone close (per CONTEXT D-24 bundling with Phase 10's deferred share_entity UAT), or (C) skip live-UAT and rely on offline + dryRun-live evidence"
    - "All 7 Phase 11 requirements (ROLE-01..07) have offline test evidence (Plan 11-02) AND dryRun-live evidence (Plan 11-03 Task 2); the throwaway-role full-lifecycle UAT is the optional human-supervised live confirmation layer; its disposition is operator-chosen and recorded in STATE.md + 11-03-SUMMARY.md"
    - "Offline npm test suite remains green after the fixture overwrites — the captured-from-live fixtures structurally match the seed shapes Plan 11-01 expected (16 roles, role-with-members projection, user-roles list, permissions-catalogue map)"
    - "BUILT_IN_ROLES static set in src/tools/authz/schemas.js is verified to match the captured live list (16 entries, all read_only:true) — if drift is observed, the operator updates the Set in a follow-up commit and re-runs npm test"
  artifacts:
    - path: "scripts/capture-roles-fixtures.js"
      provides: "One-shot read-only live recon probe; captures 5 fixtures into test/fixtures/authz/roles/ with _provenance blocks; aborts on any non-GET method observation"
      contains: "_provenance"
    - path: "test/authz-roles-live.smoke.js"
      provides: "Opt-in dryRun-only live probe for all 7 role tools; excluded from npm test via *.smoke.js suffix; self-guards every authz mutation endpoint observation"
      contains: "dryRun: true"
    - path: "test/fixtures/authz/roles/list-roles-7.0.6.json"
      provides: "Verbatim live GET /api/roles response (16 built-in roles + N custom roles) — replaces Plan 11-01 placeholder"
      contains: "_provenance"
    - path: "test/fixtures/authz/roles/get-role-admin-7.0.6.json"
      provides: "Verbatim live GET /api/roles/Admin response — replaces placeholder"
      contains: "_provenance"
    - path: "test/fixtures/authz/roles/get-role-members-reader-7.0.6.json"
      provides: "Verbatim live GET /api/roles/Reader/members response — replaces placeholder"
      contains: "_provenance"
    - path: "test/fixtures/authz/roles/user-roles-admin-7.0.6.json"
      provides: "Verbatim live GET /api/authz/roles/user/admin response — replaces placeholder"
      contains: "_provenance"
    - path: "test/fixtures/authz/roles/permissions-catalogue-7.0.6.json"
      provides: "Verbatim live GET /api/system/permissions response — replaces placeholder; used by Phase 11 catalogue-validation tests"
      contains: "_provenance"
  key_links:
    - from: "scripts/capture-roles-fixtures.js"
      to: "src/graylog/client.js"
      via: "makeClient(conn).request — every call MUST be GET; any non-GET aborts process.exit(2)"
      pattern: "makeClient"
    - from: "scripts/capture-roles-fixtures.js"
      to: "test/fixtures/authz/roles/"
      via: "writeFileSync of {_provenance, ...response} JSON; atomic per fixture; 5 fixtures total"
      pattern: "writeFixture"
    - from: "test/authz-roles-live.smoke.js"
      to: "src/tools/authz/index.js"
      via: "import the 7 role handlers; drive each via {params:{arguments:{_testConnection: omitted, ...real conn}}}; verify dryRun:true preview shape"
      pattern: "handleListRoles"
    - from: "test/authz-roles-live.smoke.js"
      to: "src/graylog/client.js"
      via: "_setCaptureRequest pass-through guard + assertSafeAuthzPath (no non-dryRun mutation paths permitted)"
      pattern: "_setCaptureRequest"
---

<objective>
Complete Phase 11's verification gate per CONTEXT D-24: (1) capture the 5 live 7.0.6 fixtures via a one-shot read-only script, (2) ship an opt-in dryRun-only live smoke probe exercising all 7 role tools against the production UNESCO `test` connection, and (3) pause for the operator to choose whether to run the throwaway-role full-lifecycle UAT now, defer it to milestone close (bundled with Phase 10's deferred share_entity UAT per D-24), or skip.

**Encodes (Plan 11-03 traceability):** D-22 (live fixtures captured by `scripts/capture-roles-fixtures.js` — one-shot, gated, read-only; the 5 named fixture files in `test/fixtures/authz/roles/` are the canonical replay inputs Plan 11-01's offline tests consume via the `_setCaptureRequest` seam). The rest of Phase 11's decisions (D-01..D-21, D-23) are enforced by code shipped in Plans 11-01 and 11-02; this plan's role is to capture authoritative live data + verify the wire-level contract.

Purpose: Mirrors Plan 10-03's three-task structure exactly. Role management is the second-highest-blast-radius surface in v3.1.0 (changes what users CAN DO across the entire Graylog instance — second only to entity sharing which changes who can SEE production logs). The dryRun smoke probe stays read-only (never mutates the live instance); the throwaway-role UAT is human-supervised and defers to milestone close per D-24 (alongside the deferred Phase 10 share_entity UAT). The fixture capture replaces Plan 11-01's seed placeholders with the verbatim live data — finishing the Wave 0 → live-fixture migration that began with Plan 11-01.

Output: 1 new capture script (`scripts/capture-roles-fixtures.js` mirrors `scripts/capture-authz-prepare-fixture.js`); 1 new smoke probe (`test/authz-roles-live.smoke.js` mirrors `test/authz-share-entity-live.smoke.js`); 5 fixture files overwritten with live captures (committed to git after operator review); 1 human-verify checkpoint for the throwaway-role UAT disposition. Plan 11-03 finalizes the v3.1.0 AuthZ & Sharing milestone close gate — both Phase 10 (share_entity) and Phase 11 (role management) ship their offline + dryRun-live evidence here; the bundled HUMAN-UAT is the optional final layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-role-management/11-CONTEXT.md
@.planning/phases/11-role-management/11-RESEARCH.md
@.planning/phases/11-role-management/11-PATTERNS.md
@.planning/phases/11-role-management/11-VALIDATION.md
@.planning/phases/11-role-management/11-01-SUMMARY.md
@.planning/phases/11-role-management/11-02-SUMMARY.md
@.planning/phases/10-entity-sharing-write-path/10-03-PLAN.md
@.planning/phases/10-entity-sharing-write-path/10-03-SUMMARY.md
@.planning/phases/08-authz-foundation-grn-helper-live-api-recon/08-TEST-STRATEGY.md
@scripts/capture-authz-prepare-fixture.js
@test/authz-share-entity-live.smoke.js
@src/tools/authz/index.js
@src/tools/authz/role-helpers.js
@src/tools/authz/schemas.js
@src/graylog/client.js

<interfaces>
<!-- Frozen contracts. The executor composes against these. -->

From scripts/capture-authz-prepare-fixture.js (THE template for capture-roles-fixtures.js — verbatim shape):
- Shebang `#!/usr/bin/env node`.
- Banner: title + SAFETY block citing `graylog-test-connection-is-live-unesco` (user memory).
- Imports: `makeClient` from `../src/graylog/client.js`; `getConnections` from `../src/config.js`; `writeFileSync, mkdirSync` from `node:fs`; `join` from `node:path`.
- Constants: `TARGET_CONNECTION = "test"`.
- Helpers: `provenance(endpoint)` builds `{captured_by, instance, connection, graylog_version, endpoint, captured_at, note}`; `writeFixture(name, content)` mkdirSync + writeFileSync to test/fixtures/authz/roles/.
- Top-level await pattern (file is `.js`, the script body runs at module load).
- SELF-GUARD: every request method is asserted as GET; any non-GET aborts `process.exit(2)`.
- Captures the system version via `GET /api/system` first (so the `_provenance.graylog_version` field is populated).
- Exits 0 on success, 1 on caught errors, 2 on safety-guard violations.

From test/authz-share-entity-live.smoke.js (THE template for authz-roles-live.smoke.js — verbatim shape):
- File header: shebang + title + `.smoke.js` discipline (excluded from npm test) + SAFETY block.
- Imports: `fileURLToPath` from `node:url`; `resolve` from `node:path`; `makeClient, _setCaptureRequest, _clearCaptureRequest` from `../src/graylog/client.js`; `getConnections, getActiveConnectionConfig` from `../src/config.js`; the handlers from `../src/tools/authz/`.
- Constants: `TARGET_CONNECTION = "test"`; an `AUTHZ_PREFIX` or `ROLES_PREFIX` constant per the domain.
- Helpers: `resolveProbeConnection()`, `parseHandlerResult(res, label)`, `assertSafeRolesPath(path, allowedMethods)` (the Phase 11 SELF-GUARD adapted for roles endpoints), `driveHandlerGuarded(handler, args, conn)` (pass-through interceptor that clears+reinstates the seam around the live call).
- `process.exit(2)` on safety-guard violation; `process.exit(1)` on handler error; `process.exit(0)` on all-green.

From test/authz-share-entity-live.smoke.js (lines around 949-972 — the writable:false inline-object pattern is the negative-case test; Plan 11-03 reuses this for the smoke probe negative test).

From src/tools/authz/index.js (Plan 11-02 product):
- 10 registered handlers total. The 7 role handlers (handleListRoles, handleGetRole, handleCreateRole, handleUpdateRole, handleDeleteRole, handleAssignRole, handleUnassignRole) are exported via the barrel; the smoke probe imports them.

From src/config.js:
- getConnections() returns the connection-registry map; `getConnections()["test"]` is the live UNESCO Graylog 7.0.6 connection.
- getActiveConnectionConfig() returns the active connection (set via `use_connection` tool); fallback if the registry lookup misses.

From .planning/STATE.md §"Deferred Items":
- "share_entity throwaway-entity full-apply UAT" — deferred to v3.1.0 milestone close (Plan 10-03 Option B).
- "Saved-search-view dryRun smoke" — same deferral.
- Plan 11-03's UAT (Option A run-now path) creates a third bundled UAT for milestone close (per CONTEXT D-24) IF the operator picks Option B.

From CONTEXT.md D-24 (the phase verification gate):
- 1. Offline test suite green (`npm test`) — Plan 11-02 ships this.
- 2. `scripts/capture-roles-fixtures.js` runs successfully against the live `test` connection — Plan 11-03 Task 1.
- 3. A dryRun-only smoke probe exercises each of the seven tools against the live `test` connection — Plan 11-03 Task 2.
- 4. share_entity throwaway-entity full-apply UAT AND throwaway-role full-lifecycle UAT bundled into single HUMAN-UAT checkpoint at v3.1.0 milestone close — NOT a Phase 11 gate. Plan 11-03 Task 3 captures the operator's disposition.

From 08-TEST-STRATEGY.md (the throwaway-entity test contract — adapted for roles):
- Throwaway-role rule: full-lifecycle UAT MUST create a freshly-named throwaway role (e.g. `_phase11-uat-throwaway-<timestamp>`) + use a DEDICATED test user (never an existing production user with operational dependencies); never `Admin` (forbidden by D-18 anyway).
- Sequence: create throwaway role → update permissions → assign dedicated test user → unassign → delete role → verify cleanup.
- Plan 11-03 does NOT auto-execute the UAT — it stages the human-verify checkpoint for the operator.

ENDPOINT SET captured (D-22; verified by RESEARCH §"Live API Recon"):
1. GET /api/system (for graylog_version in _provenance)
2. GET /api/roles (full role list)
3. GET /api/roles/Admin (single role)
4. GET /api/roles/Reader/members (multi-user members)
5. GET /api/authz/roles/user/admin (user's roles)
6. GET /api/system/permissions (catalogue for D-01 validation tests)

USER MEMORY CONSTRAINT (graylog-test-connection-is-live-unesco): the `test` connection is REAL UNESCO production infrastructure. Every probe defaults to GETs only. Plan 11-03 Task 1 + Task 2 are READ-ONLY against the live instance; the only write path is the optional operator-mediated Task 3 UAT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/capture-roles-fixtures.js — one-shot read-only live fixture capture (overwrites Plan 11-01 placeholders)</name>
  <files>scripts/capture-roles-fixtures.js, test/fixtures/authz/roles/list-roles-7.0.6.json, test/fixtures/authz/roles/get-role-admin-7.0.6.json, test/fixtures/authz/roles/get-role-members-reader-7.0.6.json, test/fixtures/authz/roles/user-roles-admin-7.0.6.json, test/fixtures/authz/roles/permissions-catalogue-7.0.6.json</files>
  <read_first>
    - scripts/capture-authz-prepare-fixture.js (FULL FILE — the verbatim template; mirror imports, _provenance block, writeFixture helper, SELF-GUARD process.exit(2) discipline)
    - .planning/phases/11-role-management/11-CONTEXT.md §D-22 (the 5 fixture names + endpoints captured)
    - .planning/phases/11-role-management/11-RESEARCH.md §"Live API Recon" lines 117-300 (the 6 endpoints + their response shapes; verifies the capture script reproduces real-world responses)
    - .planning/phases/11-role-management/11-PATTERNS.md §"scripts/capture-roles-fixtures.js" lines 511-543 (the structural template citation)
    - test/fixtures/authz/roles/ (Plan 11-01 product — 5 placeholder fixtures; this task overwrites them)
    - src/graylog/client.js (the makeClient + request shape; the capture script uses the same client v3.0.0 code uses for all live calls)
    - src/config.js (getConnections — for resolving the `test` connection)
  </read_first>
  <action>
    **Encodes:** D-22 (the script is the one-shot, gated, read-only capture mechanism that produces the 5 canonical fixture filenames Plan 11-01 declared as the test-replay sources. Fixture-replay via the `_setCaptureRequest` seam from Phase 10 is what Plan 11-01's offline tests consume — this script is the SOLE producer of the live data those replay tests assert against).

    Create `scripts/capture-roles-fixtures.js`. Mirror `scripts/capture-authz-prepare-fixture.js` structure verbatim. The script is ONE-SHOT (the operator runs `node scripts/capture-roles-fixtures.js` manually; it is NOT invoked by any test or CI). The 5 captured fixtures OVERWRITE the Plan 11-01 placeholders in a single atomic run.

    **Module shebang + banner**:
    ```
    #!/usr/bin/env node
    // Phase 11 Plan 11-03 — one-shot read-only live recon probe for the
    // role-management surface against the production UNESCO Graylog 7.0.6
    // `test` connection.
    //
    // SAFETY: Every request is a GET. Never PUT/POST/DELETE. The live `test`
    // connection is REAL UNESCO production infrastructure (user memory:
    // "graylog-test-connection-is-live-unesco"). The script self-guards by
    // asserting every issued method === "GET"; any non-GET aborts
    // process.exit(2).
    //
    // Captures 5 fixtures, OVERWRITING the Plan 11-01 placeholders with the
    // verbatim live responses + _provenance blocks. Run ONCE per Graylog
    // upgrade or built-in-role drift; the resulting fixture files are
    // committed to git and replayed offline by test/authz-roles.test.js.
    //
    // Endpoints captured (D-22):
    //   1. GET /api/system                                  (graylog_version → _provenance)
    //   2. GET /api/roles                                   → list-roles-7.0.6.json
    //   3. GET /api/roles/Admin                             → get-role-admin-7.0.6.json
    //   4. GET /api/roles/Reader/members                    → get-role-members-reader-7.0.6.json
    //   5. GET /api/authz/roles/user/admin                  → user-roles-admin-7.0.6.json
    //   6. GET /api/system/permissions                      → permissions-catalogue-7.0.6.json
    //
    // Pattern source: scripts/capture-authz-prepare-fixture.js (Phase 8 plan
    // 08-03 — same _provenance + writeFixture + SELF-GUARD discipline).
    ```

    **Imports**:
    - `import { makeClient } from "../src/graylog/client.js";`
    - `import { getConnections, getActiveConnectionConfig } from "../src/config.js";`
    - `import { writeFileSync, mkdirSync } from "node:fs";`
    - `import { join } from "node:path";`

    **Constants**:
    - `const TARGET_CONNECTION = "test";`
    - `const FIXTURE_DIR = join("test", "fixtures", "authz", "roles");`

    **Helpers** (mirror capture-authz-prepare-fixture.js verbatim):

    1. `function resolveProbeConnection()`:
       - Try `getConnections()[TARGET_CONNECTION]` first.
       - Fallback to `getActiveConnectionConfig()` if active connection name matches.
       - Throw with a clear message if neither resolves.
       - Return `{ name: TARGET_CONNECTION, conn }`.

    2. `function buildProvenance(endpoint, graylogVersion, conn)`:
       - Return `{ captured_by: "scripts/capture-roles-fixtures.js", instance: conn.baseUrl, connection: TARGET_CONNECTION, graylog_version: graylogVersion, endpoint, captured_at: new Date().toISOString(), note: "Verbatim live 7.0.6 response. Phase 11 Plan 11-03 capture overwrote Plan 11-01 placeholder." }`.

    3. `function writeFixture(name, content)`:
       - `mkdirSync(FIXTURE_DIR, { recursive: true });`
       - `writeFileSync(join(FIXTURE_DIR, name), JSON.stringify(content, null, 2) + "\n", "utf8");`
       - `console.error("[capture-roles] wrote " + join(FIXTURE_DIR, name));` (use console.error for tool diagnostics so stdout stays clean for piping).

    4. `function assertGetOnly(method, path)`:
       - If `method !== "GET"`: `console.error("[capture-roles] FATAL: non-GET method '" + method + "' to path '" + path + "' — refusing.")`; `process.exit(2);`.

    **Main routine** (top-level await; file is ESM):
    ```
    let conn, name;
    try {
        ({ name, conn } = resolveProbeConnection());
    } catch (err) {
        console.error("[capture-roles] FATAL:", err.message);
        process.exit(1);
    }

    const client = makeClient(conn);
    console.error("[capture-roles] Probing connection \"" + name + "\" at " + conn.baseUrl + " — GET-only");

    // 0. System version (populates _provenance.graylog_version)
    assertGetOnly("GET", "/api/system");
    const sys = await client.request("GET", "/api/system", null);
    const graylogVersion = sys.version ?? "unknown";

    const provenance = (endpoint) => buildProvenance(endpoint, graylogVersion, conn);

    // 1. List roles
    assertGetOnly("GET", "/api/roles");
    const listRoles = await client.request("GET", "/api/roles", null);
    writeFixture("list-roles-7.0.6.json", { _provenance: provenance("GET /api/roles"), ...listRoles });

    // 2. Single Admin role
    assertGetOnly("GET", "/api/roles/Admin");
    const admin = await client.request("GET", "/api/roles/Admin", null);
    writeFixture("get-role-admin-7.0.6.json", { _provenance: provenance("GET /api/roles/Admin"), ...admin });

    // 3. Reader members (multi-user)
    assertGetOnly("GET", "/api/roles/Reader/members");
    const readerMembers = await client.request("GET", "/api/roles/Reader/members", null);
    writeFixture("get-role-members-reader-7.0.6.json", { _provenance: provenance("GET /api/roles/Reader/members"), ...readerMembers });

    // 4. Admin user's roles
    assertGetOnly("GET", "/api/authz/roles/user/admin");
    const adminUserRoles = await client.request("GET", "/api/authz/roles/user/admin", null);
    writeFixture("user-roles-admin-7.0.6.json", { _provenance: provenance("GET /api/authz/roles/user/admin"), ...adminUserRoles });

    // 5. Permissions catalogue (for D-01 catalogue validation tests)
    assertGetOnly("GET", "/api/system/permissions");
    const catalogue = await client.request("GET", "/api/system/permissions", null);
    writeFixture("permissions-catalogue-7.0.6.json", { _provenance: provenance("GET /api/system/permissions"), ...catalogue });

    console.error("[capture-roles] OK — 5 fixtures written to " + FIXTURE_DIR);
    process.exit(0);
    ```

    Wrap the top-level await calls in a single try/catch that converts thrown Graylog errors into `process.exit(1)` with a clear diagnostic. The SELF-GUARD `assertGetOnly` is the "structural" guard (every call hand-asserted); a future refactor could intercept via _setCaptureRequest pass-through (like the smoke probe in Task 2) but for the capture script the hand-assertion is sufficient.

    **POST-CAPTURE VERIFICATION** (the operator runs this after the script succeeds):
    - The 5 fixture files in test/fixtures/authz/roles/ are now LIVE captures, not placeholders.
    - `npm test` must remain green — the captured fixtures should structurally match what Plan 11-01's tests expect (16 built-in roles; role-with-members projection; etc.).
    - If `npm test` REDs after the capture, the operator investigates: either (a) the captured fixture diverges structurally from the seed (e.g. live UNESCO has a 17th built-in role from a new enterprise plugin → update BUILT_IN_ROLES in src/tools/authz/schemas.js + bump test count expectations), or (b) the seed was over-specific to a synthetic value (e.g. seed username "alice" → live username "<user-a>" → update the test assertions to use the real captured username OR keep the synthetic fixture as a separate `*-synthetic.json` for the parameterized tests).

    The operator commits the captured fixture changes in a SEPARATE commit from Task 1's script-only commit so reviewers can audit the live data independently from the script logic.
  </action>
  <verify>
    <automated>node --check scripts/capture-roles-fixtures.js && grep -c '"GET"' scripts/capture-roles-fixtures.js && ! grep -E '"(POST|PUT|DELETE|PATCH)"' scripts/capture-roles-fixtures.js</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/capture-roles-fixtures.js` exists and `node --check scripts/capture-roles-fixtures.js` exits 0.
    - The script contains AT LEAST 6 `"GET"` strings (one per endpoint captured + the `assertGetOnly` call): `grep -c '"GET"' scripts/capture-roles-fixtures.js` returns at least 6.
    - The script contains ZERO non-GET HTTP methods: `grep -cE '"(POST|PUT|DELETE|PATCH)"' scripts/capture-roles-fixtures.js` returns 0 (encodes D-22: read-only by structural enforcement).
    - The script contains the SELF-GUARD: `grep -c 'process\.exit(2)' scripts/capture-roles-fixtures.js` returns at least 1; `grep -c 'assertGetOnly' scripts/capture-roles-fixtures.js` returns at least 6 (one assertion per endpoint — encodes D-22 gated read-only contract).
    - The script references all 5 target fixture filenames: `grep -cE 'list-roles-7\.0\.6\.json|get-role-admin-7\.0\.6\.json|get-role-members-reader-7\.0\.6\.json|user-roles-admin-7\.0\.6\.json|permissions-catalogue-7\.0\.6\.json' scripts/capture-roles-fixtures.js` returns at least 5 (encodes D-22: the exact fixture set the offline tests replay via `_setCaptureRequest`).
    - The script references `_provenance` for each fixture write: `grep -c '_provenance' scripts/capture-roles-fixtures.js` returns at least 6 (one per fixture + the helper definition).
    - The script is NOT executed by automated tests: `grep -l 'capture-roles-fixtures' test/*.test.js 2>/dev/null` returns nothing (encodes D-22 one-shot semantics — operator-run, never CI).
    - **OPERATOR-RUN VERIFICATION** (informational; NOT enforced by this plan's automated verify — pre-flight before Task 3 checkpoint):
      - The operator runs `node scripts/capture-roles-fixtures.js` once against a `test` connection that resolves.
      - Expected: exit 0; 5 fixture files in test/fixtures/authz/roles/ now contain live data + `_provenance` blocks with `captured_by: "scripts/capture-roles-fixtures.js"`, `connection: "test"`, `graylog_version: "7.0.6+<hash>"`, and a recent `captured_at` timestamp.
      - After the capture, the operator runs `npm test` — if any test REDs due to fixture-shape drift (e.g. BUILT_IN_ROLES count differs from 16; usernames in the seed differ from live), the operator fixes the BUILT_IN_ROLES Set in src/tools/authz/schemas.js and/or the affected test assertions in a follow-up commit. THIS plan's acceptance does NOT require `npm test` green after the capture (the operator may legitimately observe live drift that needs schemas/test adjustment in a follow-up).
  </acceptance_criteria>
  <done>scripts/capture-roles-fixtures.js exists, is one-shot read-only (6 GET assertions, 0 non-GET methods, SELF-GUARD via process.exit(2)), and operator-runnable against the live `test` connection. The fixture overwrites are a follow-up operator-mediated step — not auto-applied by this plan's autonomous run.</done>
</task>

<task type="auto">
  <name>Task 2: test/authz-roles-live.smoke.js — opt-in dryRun-only live probe for the 7 role tools</name>
  <files>test/authz-roles-live.smoke.js</files>
  <read_first>
    - test/authz-share-entity-live.smoke.js (FULL FILE — the verbatim template; mirror header, imports, helpers, pass-through interceptor, guard discipline)
    - .planning/phases/10-entity-sharing-write-path/10-03-PLAN.md (Plan 10-03 Task 1 — the share_entity smoke probe structure this Plan 11-03 Task 2 mirrors)
    - .planning/phases/08-authz-foundation-grn-helper-live-api-recon/08-TEST-STRATEGY.md (the dryRun:true + `@NoAuditEvent` discipline)
    - src/tools/authz/index.js (Plan 11-02 product — the 10 handlers exported via the barrel; the smoke imports the 7 role handlers from individual paths NOT the barrel, mirroring share-entity-live.smoke.js convention)
    - src/tools/authz/role-helpers.js (Plan 11-02 product — referenced if the smoke needs to compute hashes for assertion shape, though typically the smoke asserts `/^[0-9a-f]{64}$/` shape only)
    - .planning/phases/11-role-management/11-CONTEXT.md §D-24 (the verification gate — Task 2 satisfies item 3 of D-24)
    - .planning/phases/11-role-management/11-VALIDATION.md §"Per-Task Verification Map" row 11-03-02 (the smoke test's contractual scope)
    - package.json (confirm `npm test` glob is `test/**/*.test.js` — *.smoke.js is structurally excluded; do not modify package.json)
  </read_first>
  <action>
    Create `test/authz-roles-live.smoke.js` — a NEW file mirroring the structure of `test/authz-share-entity-live.smoke.js` verbatim. Diverge ONLY where the role tools differ from share_entity.

    **Header (banner comment block)**:
    ```
    #!/usr/bin/env node
    // Phase 11 Plan 11-03 — LIVE, DRY-RUN-ONLY smoke check for the 7 role
    // management tools against the production UNESCO Graylog 7.0.6 `test`
    // instance.
    //
    // `.smoke.js` discipline: this file is EXCLUDED from `npm test` (the
    // suite glob in package.json is `test/**/*.test.js`); the file is
    // hand-run: `node test/authz-roles-live.smoke.js`.
    //
    // SAFETY (cite 08-TEST-STRATEGY §2/§6, 11-RESEARCH Pitfall 6, project
    // memory `graylog-test-connection-is-live-unesco`):
    //   - dryRun: true ONLY for the 5 mutating tools (create/update/delete/
    //     assign/unassign_role). handler.js step 6 returns the preview
    //     envelope BEFORE step 7 apply runs — the mutating endpoints
    //     (POST /api/roles, PUT /api/roles/{n}, DELETE /api/roles/{n},
    //     PUT/DELETE /api/roles/{n}/members/{u}) are NEVER issued.
    //   - The 2 read tools (list_roles, get_role) are non-mutating; they
    //     issue GETs that hit live data without modifying it.
    //   - SELF-GUARD: assertSafeRolesPath(path, method) aborts process.exit(2)
    //     on any observed POST/PUT/DELETE to /api/roles* — this fires only
    //     on a wrapper regression where dryRun:true fails to short-circuit
    //     apply. Belt+braces.
    //
    // Pattern source: test/authz-share-entity-live.smoke.js (Plan 10-03 —
    // same dryRun + SELF-GUARD discipline).
    ```

    **Imports**:
    - `import { fileURLToPath } from "node:url";`
    - `import { resolve } from "node:path";`
    - `import { makeClient, _setCaptureRequest, _clearCaptureRequest } from "../src/graylog/client.js";`
    - `import { getConnections, getActiveConnectionConfig } from "../src/config.js";`
    - 7 handler imports from individual files: `handleListRoles` from `"../src/tools/authz/list-roles.js"`, etc.

    **Constants**:
    - `const TARGET_CONNECTION = "test";`
    - `const ROLES_PREFIX = "/api/roles";`
    - `const AUTHZ_ROLES_PREFIX = "/api/authz/roles";`
    - `const SAFE_READ_METHODS = new Set(["GET"]);`

    **Helpers (verbatim from share-entity-live.smoke.js where possible — adapt names)**:

    1. `function resolveProbeConnection()` — verbatim from share-entity-live.smoke.js (reads getConnections + falls back to getActiveConnectionConfig; throws if both miss; returns `{ name: TARGET_CONNECTION, conn }`).

    2. `function parseHandlerResult(res, label)` — verbatim from share-entity-live.smoke.js: asserts `res.isError` is falsy with a clear message; returns `JSON.parse(res.content[0].text)`.

    3. `function assertSafeRolesPath(path, method)` — Phase 11 SELF-GUARD adapted from share-entity-live.smoke.js's `assertSafeAuthzPath`:
       - If path does NOT start with `/api/roles` AND does NOT start with `/api/authz/roles` AND does NOT start with `/api/system/permissions` AND does NOT start with `/api/system` (the version endpoint) — return (not a Phase 11 path; allowed).
       - If `SAFE_READ_METHODS.has(method)` — return (GET is always allowed).
       - Otherwise — the call is a MUTATION on a Phase-11-covered path. ABORT:
         `console.error("[smoke] FATAL: " + method + " to '" + path + "' is a role-tool MUTATION endpoint. This probe is dryRun-only — apply must never run. Refusing."); process.exit(2);`

    4. `function driveHandlerGuarded(handler, args, conn)` — pass-through interceptor (mirror share-entity-live.smoke.js lines 140-166 verbatim):
       - On every request the seam intercepts, call `assertSafeRolesPath(req.path, req.method)` BEFORE issuing the live call.
       - Clear the seam, await `passthroughClient.request(req.method, req.path, req.body)`, then reinstate the seam in `finally`.
       - Outer try/finally clears the seam after the handler returns.

    **The probe `main()` async routine**:

    ```
    async function main() {
        const { name: connName, conn } = resolveProbeConnection();
        console.log("[smoke] Probing 7 role tools against connection \"" + connName + "\" — dryRun:true for mutators, GET-only for reads.");

        // === Read probes (GETs against live data) ===

        // 1. list_roles
        const listRes = await driveHandlerGuarded(handleListRoles, { _connectionName: connName }, conn);
        const listPayload = parseHandlerResult(listRes, "list_roles");
        if (!Array.isArray(listPayload.roles) || listPayload.roles.length < 1) {
            console.error("[smoke] list_roles returned no roles — investigate"); process.exit(1);
        }
        console.log("[smoke] list_roles OK — " + listPayload.roles.length + " roles");

        // 2. get_role on a known built-in (Admin always exists)
        const getRes = await driveHandlerGuarded(handleGetRole, { _connectionName: connName, roleName: "Admin" }, conn);
        const getPayload = parseHandlerResult(getRes, "get_role Admin");
        if (getPayload.role?.name !== "Admin") { console.error("[smoke] get_role Admin returned wrong role"); process.exit(1); }
        console.log("[smoke] get_role Admin OK — " + (getPayload.members?.length ?? 0) + " members");

        // === Mutating-tool probes (dryRun:true ONLY) ===

        // 3. create_role dryRun — proposes creating a hypothetical role; never applies
        const createRes = await driveHandlerGuarded(handleCreateRole, {
            _connectionName: connName,
            name: "_smoke_probe_role_" + Date.now(), // unique throwaway name (never applied)
            description: "Phase 11 smoke probe — should never be created",
            permissions: ["streams:read"],
            dryRun: true,
        }, conn);
        const createPayload = parseHandlerResult(createRes, "create_role dryRun");
        if (createPayload.dryRun !== true) { console.error("[smoke] create_role dryRun should be true"); process.exit(1); }
        if (!/^[0-9a-f]{64}$/.test(createPayload.confirmationToken ?? "")) { console.error("[smoke] create_role missing confirmationToken"); process.exit(1); }
        console.log("[smoke] create_role dryRun OK — token: " + createPayload.confirmationToken.slice(0, 12) + "…");

        // 4. update_role dryRun — propose updating Admin (will refuse — built-in)
        const updateRefusedRes = await driveHandlerGuarded(handleUpdateRole, {
            _connectionName: connName,
            roleName: "Admin", // built-in — assertRoleIsMutable refuses BEFORE the pre-flight
            permissions: ["streams:read"],
            dryRun: true,
        }, conn);
        if (updateRefusedRes.isError !== true || updateRefusedRes.reason !== "builtin_role_immutable") {
            console.error("[smoke] update_role Admin should refuse with builtin_role_immutable — got:", updateRefusedRes); process.exit(1);
        }
        console.log("[smoke] update_role Admin builtin-refusal OK");

        // 5. delete_role dryRun — propose deleting a likely-nonexistent custom role
        //    Two acceptable outcomes: (a) the role doesn't exist → role_not_found, (b) it does exist and the cascade preview is well-formed
        const deleteRes = await driveHandlerGuarded(handleDeleteRole, {
            _connectionName: connName,
            roleName: "_smoke_probe_nonexistent_" + Date.now(),
            dryRun: true,
        }, conn);
        if (deleteRes.isError === true && deleteRes.reason === "role_not_found") {
            console.log("[smoke] delete_role nonexistent role correctly refused with role_not_found");
        } else if (!deleteRes.isError) {
            const deletePayload = JSON.parse(deleteRes.content[0].text);
            if (deletePayload.dryRun !== true) { console.error("[smoke] delete_role should default dryRun:true"); process.exit(1); }
            console.log("[smoke] delete_role dryRun OK — cascades.count=" + (deletePayload.cascades?.count ?? 0));
        } else {
            console.error("[smoke] delete_role unexpected error:", deleteRes); process.exit(1);
        }

        // 6. assign_role dryRun — propose assigning the connection's API-token user to Reader (idempotent if already member)
        //    The connection's user is the natural test subject — assigning them to a role they likely already hold tests the idempotent path
        const tokenUserResp = await makeClient(conn).request("GET", "/api/users/me", null).catch(() => null);
        const probeUsername = tokenUserResp?.username ?? "admin"; // fallback if /me unavailable

        const assignRes = await driveHandlerGuarded(handleAssignRole, {
            _connectionName: connName,
            roleName: "Reader",
            username: probeUsername,
            dryRun: true,
        }, conn);
        const assignPayload = parseHandlerResult(assignRes, "assign_role Reader/" + probeUsername);
        if (assignPayload.dryRun !== true) { console.error("[smoke] assign_role should default dryRun:true"); process.exit(1); }
        if (assignPayload.preview?.body && JSON.stringify(assignPayload.preview.body) !== "{}") {
            console.error("[smoke] assign_role body MUST be {} (Pitfall 3 / AP1) — got:", JSON.stringify(assignPayload.preview.body)); process.exit(1);
        }
        console.log("[smoke] assign_role Reader/" + probeUsername + " dryRun OK — body=={}");

        // 7. unassign_role dryRun — propose unassigning the same user from Reader (likely refused as not_currently_assigned if not member, or accepted with drift-safe preview)
        const unassignRes = await driveHandlerGuarded(handleUnassignRole, {
            _connectionName: connName,
            roleName: "Reader",
            username: probeUsername,
            dryRun: true,
        }, conn);
        if (unassignRes.isError === true && unassignRes.reason === "not_currently_assigned") {
            console.log("[smoke] unassign_role correctly refused with not_currently_assigned (user not a Reader)");
        } else if (!unassignRes.isError) {
            const unassignPayload = JSON.parse(unassignRes.content[0].text);
            if (unassignPayload.dryRun !== true) { console.error("[smoke] unassign_role should default dryRun:true"); process.exit(1); }
            console.log("[smoke] unassign_role dryRun OK — current_roles=" + (unassignPayload.cascades?.current_roles?.length ?? 0));
        } else {
            console.error("[smoke] unassign_role unexpected error:", unassignRes); process.exit(1);
        }

        // === Negative case — built-in refusal sanity ===
        // delete_role Admin must refuse client-side BEFORE any HTTP call
        let preCallCount = 0;
        _setCaptureRequest((req) => { preCallCount++; throw new Error("delete_role Admin attempted HTTP call — assertRoleIsMutable should have refused"); });
        const adminDelRes = await handleDeleteRole({ params: { arguments: { _connectionName: connName, roleName: "Admin", dryRun: true } } });
        _clearCaptureRequest();
        if (adminDelRes.isError !== true || adminDelRes.reason !== "builtin_role_immutable") {
            console.error("[smoke] delete_role Admin should refuse client-side with builtin_role_immutable — got:", adminDelRes); process.exit(1);
        }
        if (preCallCount !== 0) {
            console.error("[smoke] delete_role Admin made " + preCallCount + " HTTP calls — should be 0 (client-side refusal)"); process.exit(1);
        }
        console.log("[smoke] delete_role Admin client-side refusal OK (0 HTTP calls)");

        console.log("\n[smoke] === ALL 7 ROLE TOOLS PASSED dryRun:true LIVE PROBE ===");
        process.exit(0);
    }

    main().catch(err => {
        console.error("[smoke] FATAL:", err);
        process.exit(1);
    });
    ```

    DO NOT modify package.json — the *.smoke.js suffix excludes this file from `npm test` (the suite glob is `test/**/*.test.js`).
    DO NOT include any `dryRun: false` literal anywhere in this file.
    DO NOT include code that constructs a request to POST/PUT/DELETE `/api/roles*` outside the SELF-GUARD's refusal-throw path.
  </action>
  <verify>
    <automated>node --check test/authz-roles-live.smoke.js && grep -c "dryRun: true" test/authz-roles-live.smoke.js && ! grep -E "dryRun *: *false" test/authz-roles-live.smoke.js && grep -c "assertSafeRolesPath\|process.exit(2)" test/authz-roles-live.smoke.js && npm test 2>&1 | grep -c authz-roles-live.smoke</automated>
  </verify>
  <acceptance_criteria>
    - File `test/authz-roles-live.smoke.js` exists and `node --check test/authz-roles-live.smoke.js` exits 0.
    - The file is NAMED `*.smoke.js` (not `*.test.js`); `npm test` does NOT pick it up: `npm test 2>&1 | grep -c authz-roles-live.smoke` returns 0.
    - The file imports the 7 role handlers from individual file paths under `../src/tools/authz/`: `grep -cE 'from "../src/tools/authz/(list|get|create|update|delete|assign|unassign)-role.js"' test/authz-roles-live.smoke.js` returns 7.
    - The file contains `dryRun: true` AT LEAST 5 TIMES (one per mutating tool: create, update, delete, assign, unassign): `grep -c 'dryRun: true' test/authz-roles-live.smoke.js` returns at least 5.
    - The file contains ZERO occurrences of `dryRun: false`: `grep -cE 'dryRun *: *false' test/authz-roles-live.smoke.js` returns 0.
    - The file contains the SELF-GUARD: `grep -c 'assertSafeRolesPath\|process.exit(2)' test/authz-roles-live.smoke.js` returns at least 2.
    - The file references `getConnections` / `getActiveConnectionConfig`: `grep -cE 'getConnections|getActiveConnectionConfig' test/authz-roles-live.smoke.js` returns at least 2.
    - The file is NOT executed by automated tests: `grep -l 'authz-roles-live.smoke' test/*.test.js 2>/dev/null` returns nothing.
    - The file's body=={} assertion for assign_role is present: `grep -F '"{}"' test/authz-roles-live.smoke.js` returns at least 1 (the JSON.stringify comparison).
    - The file's built-in refusal sanity check is present: `grep -F 'builtin_role_immutable' test/authz-roles-live.smoke.js` returns at least 2 (one for update_role + one for delete_role; the count may be higher if helpers reuse the string).
    - `npm test` — the full offline suite is STILL GREEN (this Plan adds the *.smoke.js file but does not modify anything in the npm-test glob).
    - **OPERATOR-RUN VERIFICATION** (informational; NOT enforced by autonomous verify): The operator runs `node test/authz-roles-live.smoke.js` against the configured `test` connection. Expected: prints "[smoke] === ALL 7 ROLE TOOLS PASSED dryRun:true LIVE PROBE ===" and exits 0. If it exits 2, investigate immediately — a wrapper regression where dryRun:true fails to short-circuit apply. If exits 1, investigate the specific handler error.
  </acceptance_criteria>
  <done>The opt-in dryRun-only live smoke probe for the 7 role tools exists, is excluded from `npm test`, references all 7 role handlers, has ZERO `dryRun: false` occurrences, and SELF-GUARDS every observed mutation path with process.exit(2). The probe satisfies CONTEXT D-24 item 3 (dryRun-only live exercise of each tool).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify checkpoint — throwaway-role full-lifecycle UAT (OPTIONAL; deferral default per D-24)</name>
  <files>(no files modified by this task — operator-mediated UAT against a throwaway role per 08-TEST-STRATEGY.md adapted for roles; optionally appends an outcome line to 11-03-SUMMARY.md after Plan 11-03 completes; optionally bumps STATE.md Deferred Items table)</files>
  <action>Pause for the operator to choose Option A (run the throwaway-role full-lifecycle UAT manually now), Option B (DEFAULT per D-24 — defer to v3.1.0 milestone close, bundled with Phase 10's deferred share_entity UAT), or Option C (skip live-UAT and rely on offline + dryRun-live evidence). See <how-to-verify> for the step-by-step procedure each option entails. The executor MUST NOT auto-execute Option A — full-mutation UAT is human-supervised by contract (08-TEST-STRATEGY.md + CONTEXT D-24 + project memory `graylog-test-connection-is-live-unesco`).</action>
  <what-built>
    - Plan 11-01 shipped the Wave 0 offline test scaffold (test/authz-roles.test.js — ~26 named tests), 5 placeholder fixtures, 7 zod schemas + BUILT_IN_ROLES Set, 7 tools.js entries, 7 DOMAIN_OVERRIDES entries, tool-count bumps (94→101), and 7 cascade-hash byte-pin tests (4 with PLACEHOLDER hashes).
    - Plan 11-02 shipped computeRoleCascadeHash + role-helpers.js + 7 role handlers + barrel registration; replaced the 4 PLACEHOLDER cascade-hash digests with real values; turned the entire Wave 0 RED specification GREEN including the 4 MANDATORY safety acceptance gates (update_role PITFALL 1, delete_role cascade preview, assign_role body=={}, unassign_role last-admin guard).
    - Plan 11-03 Task 1 shipped `scripts/capture-roles-fixtures.js` — the one-shot read-only live capture script (5 endpoints, GET-only, SELF-GUARDED).
    - Plan 11-03 Task 2 shipped `test/authz-roles-live.smoke.js` — the opt-in dryRun-only live probe exercising all 7 role tools end-to-end without ever issuing a mutation endpoint POST/PUT/DELETE.

    The remaining evidence Phase 11 OWES the milestone is **the throwaway-role full-lifecycle UAT** documented in `08-TEST-STRATEGY.md` (adapted for roles per CONTEXT D-24). This task does NOT auto-execute that UAT — it pauses for the operator to either run it now (with a freshly-created throwaway role + dedicated test user) or defer it to milestone close (DEFAULT per D-24) where it bundles with Phase 10's deferred share_entity UAT into a single HUMAN-UAT checkpoint at v3.1.0 close.
  </what-built>
  <how-to-verify>
    1. **Pre-flight — verify the offline + dry-run-live evidence is in place**:
       - `npm test 2>&1 | tail -10` — full offline suite green; tool count is 101.
       - `node --test test/authz-roles.test.js 2>&1 | tail -5` — all Plan 11-01 tests GREEN; specifically the 4 MANDATORY safety acceptance gates pass.
       - `node --test test/cascade-hash.test.js 2>&1 | tail -5` — 7 computeRoleCascadeHash tests GREEN (4 byte-pinned + 1 order-independence + 1 cross-tool replay + 1 malformed).
       - `grep -c 'register("list_roles\|register("get_role\|register("create_role\|register("update_role\|register("delete_role\|register("assign_role\|register("unassign_role' src/tools/authz/index.js` returns 7.

    2. **Capture live fixtures** (Plan 11-03 Task 1 product — operator-run):
       ```
       node scripts/capture-roles-fixtures.js
       ```
       Expected: exits 0; 5 fixture files in `test/fixtures/authz/roles/` now contain live data with `_provenance.captured_by: "scripts/capture-roles-fixtures.js"`. If exits 2, a wrapper regression observed a non-GET — investigate immediately. After the capture, re-run `npm test` to verify no fixture-shape drift caused regressions; if a drift surfaces, fix the BUILT_IN_ROLES Set in src/tools/authz/schemas.js (or the affected test assertion) in a follow-up commit.

    3. **Run the dryRun-only live smoke probe** (Plan 11-03 Task 2 product — operator-run):
       ```
       node test/authz-roles-live.smoke.js
       ```
       Expected: prints "[smoke] === ALL 7 ROLE TOOLS PASSED dryRun:true LIVE PROBE ===" and exits 0. If exits 2, a wrapper regression — investigate. If exits 1, a handler error — investigate (likely a fixture drift or a Graylog response shape change).

    4. **Decide — full-lifecycle UAT now, defer (DEFAULT), or skip**:

       - **Option A (RECOMMENDED for milestone confidence — equivalent to a full v3.1.0 acceptance signal):** Run the throwaway-role full-lifecycle UAT manually. Per CONTEXT D-24 + 08-TEST-STRATEGY.md adapted for roles:
         a. Identify a DEDICATED test user (NEVER an existing production user with operational dependencies; NEVER the admin account; ideally a `_phase11-uat-testuser` account created specifically for this UAT). If your `test` connection doesn't have one, abort this option.
         b. Run `create_role` with a freshly-disposable name (e.g. `_phase11_uat_<timestamp>`), `permissions:["streams:read"]`, `description:"Phase 11 UAT throwaway"`, `dryRun:true` — capture the confirmationToken from the preview envelope.
         c. Re-call `create_role` with `dryRun:false` AND `confirm:<token>` — observe the apply envelope, the 200 response, the new role.
         d. Run `get_role` for the new role — verify the role appears with the expected permissions.
         e. Run `update_role` with the throwaway role's name, `permissions:["streams:read","dashboards:read"]`, `dryRun:true` → grab token → apply with `dryRun:false,confirm:<token>` — verify the diff shows `added:["dashboards:read"]`.
         f. Run `assign_role` for the throwaway role + dedicated test user, `dryRun:true` → apply → verify the user gains the role via `get_role` showing the user in members.
         g. Run `unassign_role` for the same pairing, `dryRun:true` → apply → verify the user no longer appears in `get_role` members.
         h. Run `delete_role` for the throwaway role, `dryRun:true` (verify `cascades.users_dissociated.count === 0` since the only user was already unassigned), then apply — verify `list_roles` no longer includes the role.
         i. Document the run outcome and any unexpected behavior in `11-03-SUMMARY.md` AND in `.planning/STATE.md` §"Accumulated Decisions" / §"Performance Metrics" (record Phase 11 throwaway-role UAT executed + outcome summary).

       - **Option B (DEFAULT per CONTEXT D-24):** Defer the throwaway-role full-lifecycle UAT to v3.1.0 milestone close. It bundles with Phase 10's deferred share_entity throwaway-entity UAT into a single HUMAN-UAT checkpoint. The offline tests + dryRun-live smoke cover all 7 requirements structurally + dryRun-live; the deferred UAT is the live-mutation confirmation layer. Per CONTEXT D-24: "share_entity throwaway-entity full-apply UAT (Phase 10 deferred) AND a Phase 11 throwaway-role full-lifecycle UAT … are bundled into a single HUMAN-UAT checkpoint at v3.1.0 milestone close. NOT a Phase 11 gate." Document the deferral in the resume signal so STATE.md §"Deferred Items" gets a new row.

       - **Option C (SKIP):** Skip live-UAT entirely; rely on offline fixture-replay + dryRun-live evidence. Acceptable per CONTEXT D-24's "the deferred UAT is the optional live-mutation confirmation layer" framing. Document the rationale (e.g. "no dedicated test user available on the production `test` instance; defer to a future development instance"). STATE.md gets a row noting the skip + rationale.

    5. **If Option A was run:** verify the live-cluster state is CLEAN — no `_phase11_uat_<timestamp>` role remains; the dedicated test user's roles list does not include the throwaway role. Log the outcome in `11-03-SUMMARY.md` per the resume signal options.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "uat-complete: <brief summary>" — Option A executed; provide a 1-sentence outcome summary (e.g. "throwaway-role lifecycle 7-step PASS; live cluster clean; no unexpected drift").
    - "uat-deferred-to-milestone-close" — Option B (DEFAULT per D-24); deferral logged for milestone-close UAT alongside Phase 10's deferred share_entity UAT.
    - "uat-skipped: <reason>" — Option C; rationale captured for STATE.md.
    - "uat-failed: <issue>" — UAT ran but observed unexpected behavior; describe the issue so a follow-up plan can be created (e.g. "create_role apply 400 with unexpected RequestError path 'permissions[0]'; needs investigation").
  </resume-signal>
  <verify>Operator reports back via the resume-signal options below. No automated verify command — this is a human-gated checkpoint by contract (08-TEST-STRATEGY.md throwaway-entity rule extended to roles per CONTEXT D-24).</verify>
  <acceptance_criteria>
    - The operator has selected one of the four resume-signal options.
    - The chosen path is recorded in `11-03-SUMMARY.md` (autonomous tasks 1+2 create the summary; this checkpoint task appends the outcome line).
    - For Option A: the live cluster state is clean (no `_phase11_uat_*` role remains; `list_roles` against the live `test` connection shows the throwaway role is gone).
    - For Option B: STATE.md §"Deferred Items" gets a new row: `| Live-UAT (v3.1.0) | Phase 11 throwaway-role full-lifecycle UAT — create→update→assign test user→unassign→delete per 08-TEST-STRATEGY adapted for roles | Deferred | Plan 11-03 (Option B / DEFAULT per D-24) | (operator) |`.
    - For Option C: STATE.md §"Deferred Items" or §"Accumulated Decisions" captures the skip rationale.
    - For Option D (uat-failed): a follow-up plan is created via `/gsd:plan-phase --gaps` or an explicit issue is logged in STATE.md §"Blockers/Concerns".
  </acceptance_criteria>
  <done>The operator has selected one of the four resume-signal options (uat-complete | uat-deferred-to-milestone-close | uat-skipped | uat-failed). The choice is recorded in 11-03-SUMMARY.md and (for Options B/C/D) in STATE.md. Phase 11 verification gate per CONTEXT D-24 is satisfied: offline npm test green (Plan 11-02) + scripts/capture-roles-fixtures.js operator-runnable (Task 1) + dryRun smoke probe operator-runnable (Task 2) + throwaway-role UAT disposition recorded (Task 3). Phase 11 is COMPLETE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live `test` connection (production UNESCO Graylog 7.0.6) | The operator hand-runs the capture script + smoke probe + (optionally) the throwaway-role UAT. All three touch production infrastructure; the first two are read-only by structural enforcement. |
| Capture script → Graylog REST API | All 6 calls are GETs; assertGetOnly() refuses non-GET methods with process.exit(2). |
| Smoke probe → Graylog REST API | Mutating handlers run with dryRun:true; the wrapper's handler.js step 6 short-circuits apply BEFORE the commit endpoint POST/PUT/DELETE is issued; the pass-through SELF-GUARD aborts process.exit(2) on any observed Phase-11 mutation path. |
| Throwaway-role UAT (manual; Option A) → production Graylog | Real mutations against a freshly-created throwaway role + dedicated test user. The operator is responsible for cleanup (steps i-step 5 of how-to-verify). |
| Captured fixtures → git repository | The captured fixtures may contain real UNESCO usernames + permission strings. Phase 8 already committed similar data in prepare-response-7.0.6.json — no NEW exposure beyond what's in the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | **Information Disclosure / Elevation of Privilege** (highest severity) | Smoke probe mis-targeting a real production user + a real production role — mistakenly assigns/unassigns a role, changing what that user can do | **mitigate** | All 5 mutating-tool probes use `dryRun:true` ONLY; the wrapper's handler.js step 6 returns the preview BEFORE step 7 apply runs, so the mutation endpoints are NEVER issued. The pass-through SELF-GUARD `assertSafeRolesPath` aborts process.exit(2) if any non-GET path under `/api/roles*` is observed (defense in depth — if the wrapper regresses to firing apply, the probe halts before the network call). Verified by Task 2 grep gates (`dryRun: false` count = 0; `assertSafeRolesPath` + `process.exit(2)` count ≥ 2). |
| T-11-03-02 | **Denial of Service (instance lockout)** | Throwaway-role UAT (Option A) attempts a `would_leave_no_admin` scenario by mistake (e.g. operator picks the bootstrap admin user as the test subject) | **mitigate (handler-level + operator discipline)** | The unassign_role last-admin guard (Plan 11-02 / D-17) refuses with reason=would_leave_no_admin client-side. The operator's how-to-verify step (4a) explicitly forbids using the admin account as the test subject. Two-layer defense. |
| T-11-03-03 | **Tampering** | Throwaway-role UAT (Option A) leaks a custom role + assignment to a real user by mistake | **mitigate (operator-mediated)** | The 08-TEST-STRATEGY.md throwaway-role rule (adapted from throwaway-entity for roles) is the structural control: freshly-named role + DEDICATED test user (not a production user). The human-verify checkpoint surfaces the rule verbatim in step 4a before the operator decides whether to run. Plan 11-03 does NOT auto-execute Option A. |
| T-11-03-04 | **Tampering** | Capture script's SELF-GUARD regression — assertGetOnly silently removed or process.exit(2) downgraded | **mitigate** | Task 1's acceptance gates grep for `process.exit(2)` (≥1) AND `assertGetOnly` (≥6) AND `grep -cE '"(POST|PUT|DELETE|PATCH)"' = 0`. Any regression triggers an acceptance-gate failure. |
| T-11-03-05 | **Tampering** | Smoke probe's SELF-GUARD regression — assertSafeRolesPath silently disabled or process.exit(2) removed | **mitigate** | Task 2's acceptance gates grep for `assertSafeRolesPath` + `process.exit(2)` (≥2) AND `dryRun: false` (=0). The pass-through interceptor pattern verbatim mirrors Plan 10-03's share-entity smoke — any divergence is caught in code review. |
| T-11-03-06 | **Denial of Service (CI flakiness)** | Smoke probe accidentally included in `npm test` and failing CI when the `test` connection is unreachable | **mitigate** | The `.smoke.js` suffix excludes the file from `npm test` (the suite glob is `test/**/*.test.js`). Task 2 acceptance gate `grep -l 'authz-roles-live.smoke' test/*.test.js` returns nothing — no .test.js file imports the smoke probe. Verified by `npm test 2>&1 | grep -c authz-roles-live.smoke` returning 0. |
| T-11-03-07 | **Information Disclosure** | Captured fixtures contain real UNESCO usernames (e.g. <user-b>, <user-c>, <user-a>) — committed to public git | **accept** | Phase 8 already committed identical names in test/fixtures/authz/prepare-response-7.0.6.json. The git repo is the operator's local development repo; if pushed to a public host, the exposure is identical to what's already there. No new disclosure introduced. |
| T-11-03-08 | **Repudiation** | Throwaway-role UAT (Option A) leaves no record of what was changed; cluster state diverges from the documented "clean" expectation | **mitigate** | The operator's how-to-verify step (4i + step 5) explicitly requires verifying the cluster state is CLEAN after the UAT AND logging the outcome in 11-03-SUMMARY.md + STATE.md. The resume-signal options force one of four explicit outcomes (uat-complete / uat-deferred / uat-skipped / uat-failed) so STATE.md always has a documented disposition. |
| T-11-03-09 | **Tampering (BUILT_IN_ROLES drift)** | Live capture shows 17 built-in roles (e.g. a new enterprise plugin shipped a role) but BUILT_IN_ROLES Set in src/tools/authz/schemas.js still has 16 — agents could attempt to mutate the new built-in and only the server-side 400 backstop refuses | **mitigate (defense in depth)** | The static Set is a FAST-PATH (D-19); the server-side `read_only:true` field from the pre-flight GET response is the AUTHORITATIVE refusal. The 17th built-in is still refused — just via the slower path. Task 1's POST-CAPTURE VERIFICATION step instructs the operator to fix the Set in a follow-up commit if drift is observed. |
| T-11-03-10 | **Information Disclosure** | Smoke probe logs include real Graylog user metadata from `GET /api/users/me` (`probeUsername` derivation) | **mitigate** | The probe logs only the username (e.g. `[smoke] assign_role Reader/admin dryRun OK`), not the full UserSummary. The username is the bare identifier the agent needs for the API call; logging it is unavoidable. No emails or permission sets are logged. |
| T-11-03-SC | **Tampering** | npm/pip/cargo installs | **mitigate** | NO new packages installed in Plan 11-03. The capture script + smoke probe import only existing modules (makeClient, getConnections, the 7 handlers from Plan 11-02). RESEARCH §"Package Legitimacy Audit" confirms Phase 11 is install-free. |

**High-severity threats present:** T-11-03-01 (live-production role mis-mutation via probe) is the dominant risk. The mitigation stack (`dryRun:true` literal + wrapper short-circuit + pass-through SELF-GUARD with `process.exit(2)` + `.smoke.js` suffix excluding from automated runs) is defense-in-depth: each layer alone is sufficient; together they make a regression detectable before any live-cluster impact.
</threat_model>

<verification>
- `node --check scripts/capture-roles-fixtures.js` exits 0; `grep -c '"GET"' scripts/capture-roles-fixtures.js` ≥ 6; `grep -cE '"(POST|PUT|DELETE|PATCH)"' scripts/capture-roles-fixtures.js` = 0; `grep -c 'process\.exit(2)' scripts/capture-roles-fixtures.js` ≥ 1.
- `node --check test/authz-roles-live.smoke.js` exits 0; `grep -c 'dryRun: true' test/authz-roles-live.smoke.js` ≥ 5; `grep -cE 'dryRun *: *false' test/authz-roles-live.smoke.js` = 0; `grep -c 'assertSafeRolesPath\|process.exit(2)' test/authz-roles-live.smoke.js` ≥ 2.
- `npm test` is unchanged in scope: `npm test 2>&1 | grep -c authz-roles-live.smoke` returns 0 (the smoke file is excluded).
- The full offline suite stays green at 101 tools, all Phase 11 + earlier-phase tests passing.
- Task 3 checkpoint resolves with one of the four resume signals; the chosen path is documented in 11-03-SUMMARY.md (and for Options B/C, in STATE.md §"Deferred Items").
- The Phase 11 SUMMARY.md notes the disposition of the throwaway-role UAT (executed | deferred | skipped | failed) so the milestone close has a complete audit trail.
- **Operator-mediated steps** (informational; NOT enforced by autonomous verify):
  - `node scripts/capture-roles-fixtures.js` exits 0 against the live `test` connection; 5 fixture files overwritten with verbatim live data + _provenance blocks.
  - `node test/authz-roles-live.smoke.js` exits 0; prints "[smoke] === ALL 7 ROLE TOOLS PASSED dryRun:true LIVE PROBE ===".
</verification>

<success_criteria>
- **CONTEXT D-24 item 2 satisfied:** `scripts/capture-roles-fixtures.js` exists, is one-shot read-only (GET-only with SELF-GUARD), and operator-runnable against the live `test` connection. The 5 fixture files in test/fixtures/authz/roles/ are operator-overwritable with verbatim live data.
- **CONTEXT D-24 item 3 satisfied:** `test/authz-roles-live.smoke.js` exists as a *.smoke.js file (excluded from npm test), exercises all 7 role tools against the live `test` connection in dryRun:true mode ONLY, and structurally CANNOT issue a mutation endpoint POST/PUT/DELETE (the wrapper short-circuits apply under dryRun:true; the pass-through SELF-GUARD aborts process.exit(2) on any observed Phase-11 mutation path).
- **CONTEXT D-24 item 4 dispatched:** the human-verify checkpoint pauses for the operator to choose Option A/B/C/D for the throwaway-role full-lifecycle UAT; the resume signal captures the outcome so STATE.md and the Phase 11 SUMMARY document the decision. Option B (defer to milestone close, bundling with Phase 10's deferred share_entity UAT) is the DEFAULT per D-24.
- All 7 Phase 11 requirements (ROLE-01..07) have offline test evidence (Plan 11-02) PLUS dryRun-live evidence (Plan 11-03 Task 2). The throwaway-role full-lifecycle UAT (Plan 11-03 Task 3) is the optional human-supervised live confirmation layer; its disposition is operator-chosen and recorded.
- **ROADMAP Phase 11 success criteria 1-5** (list_roles + get_role; create_role + update_role + delete_role custom-role CRUD; assign_role + unassign_role; built-in refusal; dryRun:true + token + drift refusal) — all have BOTH offline test evidence AND dryRun-live evidence.
- Zero new dependencies. Existing v2.3 tool contracts unchanged. The `test` connection's live state is unmutated by Plan 11-03 Tasks 1-2 (the only optional mutation is the operator-mediated UAT in Task 3 against a throwaway role + dedicated test user).
- Phase 11 is COMPLETE. v3.1.0 AuthZ & Sharing milestone has all 4 phases shipped; the bundled HUMAN-UAT at milestone close (Phase 10 share_entity + Phase 11 throwaway-role) is the only remaining live-mutation verification gate for the milestone.
</success_criteria>

<output>
Create `.planning/phases/11-role-management/11-03-SUMMARY.md` when done — record the smoke-probe + capture-script outcomes and the operator's UAT disposition (executed / deferred / skipped / failed).
</output>
