---
phase: 02-index-sets-retention
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/tools/index-sets/schemas.js
  - src/tools/index-sets/set-default-index-set.js
  - src/tools/index-sets/cycle-deflector.js
  - src/tools/index-sets/index.js
  - src/tools.js
  - test/index-sets.test.js
autonomous: true
requirements: [INDEX-06, INDEX-07]
tags: [index-sets, set-default, cycle-deflector, m2-invariant, nd3, d-13, d-14-updated, cycle-sync]

must_haves:
  truths:
    - "set_default_index_set pre-flights GET /system/indices/index_sets/{id} and reads can_be_default (D-13). When can_be_default is false (events-style or system index set), dry-run returns reason: non_regular_index_set with a helpful message naming can_be_default:false — surfacing the would-be 409 BEFORE any PUT"
    - "set_default_index_set apply path issues PUT /api/system/indices/index_sets/{id}/default with an empty body; postApplyEstimate.id matches the indexSetId and isDefault is true"
    - "cycle_deflector pre-flights GET /system/indices/index_sets/{id} and refuses if current.writable === false: reason non_writable_index_set (ND3) — fires BEFORE any POST"
    - "cycle_deflector apply path issues POST /api/system/deflector/{indexSetId}/cycle with an empty body. The response is treated as SYNCHRONOUS per the updated D-14 (RESEARCH.md §Cycle Deflector Behavior verified DeflectorResource.cycle is void / synchronous on the JVM thread)"
    - "cycle_deflector returns { rotated: true, message: 'Cycled index set <id>; closed previous active index', side_effects: { observable_at: '/system/jobs', describes: 'closed-index range rebuild' } } — NOT the D-15 async envelope (D-14 UPDATED)"
    - "cycle_deflector dry-run preview makes the side_effects visible so the agent can choose to await_system_job on the range-rebuild side job if it cares about the secondary work completing"
  artifacts:
    - path: "src/tools/index-sets/schemas.js"
      provides: "SetDefaultIndexSetSchema + CycleDeflectorSchema appended"
      exports: ["SetDefaultIndexSetSchema", "CycleDeflectorSchema"]
    - path: "src/tools/index-sets/set-default-index-set.js"
      provides: "INDEX-06 — defineMutatingHandler with D-13 + m2 pre-flight"
      exports: ["handleSetDefaultIndexSet"]
    - path: "src/tools/index-sets/cycle-deflector.js"
      provides: "INDEX-07 — defineMutatingHandler with ND3 pre-flight + UPDATED D-14 synchronous semantics + side_effects.observable_at"
      exports: ["handleCycleDeflector"]
  key_links:
    - from: "src/tools/index-sets/set-default-index-set.js"
      to: "src/tools/_shared/handler.js"
      via: "defineMutatingHandler with async build() pre-flighting can_be_default"
      pattern: "can_be_default"
    - from: "src/tools/index-sets/cycle-deflector.js"
      to: "src/tools/_shared/handler.js"
      via: "defineMutatingHandler with async build() pre-flighting current.writable + side_effects envelope on apply"
      pattern: "side_effects.*observable_at"
---

<objective>
Ship set_default_index_set (INDEX-06) — the m2 invariant pre-flight that surfaces the "not eligible as default" 409 in dry-run BEFORE any PUT; and cycle_deflector (INDEX-07) — the manual rotation primitive with the ND3 non-writable refusal and the UPDATED D-14 SYNCHRONOUS response shape (verified against Graylog 7.0.6 source: DeflectorResource.cycle is void / synchronous; D-14 was UPDATED in CONTEXT.md to surface side_effects.observable_at for the closed-index range rebuild instead of the original async envelope).

Purpose: ROADMAP success criterion 3 ("set_default_index_set enforces the regular: true invariant — calling it against an events-style index set surfaces a clear 409-style error in the dry-run output before any apply is attempted") is Plan 04's contract. The cycle_deflector D-14 update is the second load-bearing decision from research that lands here.

Output: 2 new mutating tools registered; ~10 new tests; tool count 41 -> 43.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-index-sets-retention/02-CONTEXT.md
@.planning/phases/02-index-sets-retention/02-RESEARCH.md
@.planning/phases/02-index-sets-retention/02-01-SUMMARY.md
@.planning/phases/02-index-sets-retention/02-U1-SMOKE.md

@src/tools/_shared/handler.js
@src/tools/_shared/schemas.js
@src/tools/index-sets/schemas.js
@src/tools/index-sets/index.js
@src/tools.js
@test/index-sets.test.js

<interfaces>
RESEARCH.md §Set Default Pre-flight Algorithm (D-13 + m2):
- GET /api/system/indices/index_sets/{id} returns IndexSetResponse with field can_be_default: boolean
- can_be_default === false means the index set is events-style OR system-template OR otherwise not eligible. Graylog server-side throws 409 if you try to PUT /default on such a set.
- Pre-flight reads current.can_be_default; if false, throw a typed error (or return synthetic isError envelope) with reason non_regular_index_set, message naming current.title + current.id + can_be_default:false.

RESEARCH.md §Cycle Deflector Behavior (UPDATED D-14):
- POST /api/system/deflector/{indexSetId}/cycle returns 204 (void on the Java side). The cycle is SYNCHRONOUS — DeflectorResource.cycle calls indexSet.cycle() directly, NOT via systemJobManager.submit. No system_job_id returned.
- Side effect: closed-index range rebuild kicks off as a separate system job — observable via GET /system/jobs but not strictly necessary for routing new messages (only for searching the just-closed index).
- D-14 UPDATED envelope (CONTEXT.md 2026-05-15 revision): { rotated: true, message: "Cycled index set <id>; closed previous active index", side_effects: { observable_at: "/system/jobs", describes: "Graylog spawns an IndexRangesUpdateJob to rebuild the closed index's ranges; the rotation itself is complete on response." } }
- This is NOT the D-15 async envelope.

RESEARCH.md §Pitfall ND3:
- DeflectorResource.checkCycle throws 400 if !indexSet.getConfig().isWritable(). Pre-flight inside cycle_deflector.build() — GET current, check current.writable === false, surface structured pre-flight error if so.

defineMutatingHandler from handler.js (Plan 01 amended): { name, schema, build, apply, summarize, requireConfirm? } where build may be async.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship set_default_index_set (INDEX-06) — D-13 + m2 pre-flight with can_be_default invariant</name>
  <read_first>
    - .planning/phases/02-index-sets-retention/02-RESEARCH.md sections "Set Default Pre-flight Algorithm", "Pitfall m2"
    - src/tools/_shared/handler.js
    - src/tools/inputs/delete-input.js (analog for pre-flight pattern)
    - src/tools/index-sets/schemas.js
  </read_first>
  <behavior>
    - Test 1: SetDefaultIndexSetSchema parses { indexSetId: "x" } -> success; requires indexSetId.
    - Test 2: set_default_index_set against a regular index set — _setCaptureRequest scripts GET /system/indices/index_sets/<id> -> { id, title: "Default index set", can_be_default: true, regular: true, writable: true }. Dry-run preview has method:"PUT", path:"/api/system/indices/index_sets/<id>/default", body: undefined, postApplyEstimate.id matches indexSetId AND postApplyEstimate.isDefault is true.
    - Test 3: set_default_index_set against an events-style index set (D-13 + m2) — _setCaptureRequest scripts GET -> { id, title: "Events Index", can_be_default: false }. Dry-run returns isError:true with text containing "non_regular_index_set" AND "can_be_default" AND the index set title; PUT was NEVER fired (capture count for PUT method === 0).
    - Test 4: set_default_index_set apply path — script GET (regular) + PUT (returns full IndexSetResponse). Apply returns result.id matching indexSetId, result.body matching the PUT response shape.
    - Test 5: set_default_index_set propagates 404 from the pre-flight GET — script GET throws GraylogError status 404. isError surfaced via wrapGraylogError.
  </behavior>
  <action>
1. Append SetDefaultIndexSetSchema to `src/tools/index-sets/schemas.js`:
   ```
   SetDefaultIndexSetSchema = mutatingBase.extend({
       indexSetId: z.string().min(1),
   });
   ```

2. Create `src/tools/index-sets/set-default-index-set.js`:
   - import defineMutatingHandler, SetDefaultIndexSetSchema, makeClient.
   - handleSetDefaultIndexSet = defineMutatingHandler({ name: "set_default_index_set", schema, build, apply, summarize }).
   - build(args) async:
     - client = makeClient(args._conn).
     - current = await client.request("GET", "/api/system/indices/index_sets/" + args.indexSetId, null).
     - if (current.can_be_default === false): throw a typed error (extending GraylogError if possible; status 409, method "PUT", path "/api/system/indices/index_sets/" + args.indexSetId + "/default") with message: "Index set \"" + current.title + "\" (" + args.indexSetId + ") is not eligible as the default index set (can_be_default: false). This typically means it's an events-style or system index set. Default index sets must be writable + regular + use the standard message template." AND ensure the typed error carries a `reason: "non_regular_index_set"` property so wrapGraylogError preserves it in the rendered envelope.
     - else: return descriptor { method: "PUT", path: "/api/system/indices/index_sets/" + args.indexSetId + "/default", body: undefined, postApplyEstimate: { id: args.indexSetId, isDefault: true } }.
   - apply: (client, req) => client.request(req.method, req.path, req.body).
   - summarize: (args) => "Set default index set to " + args.indexSetId.

   Type-error decision: Either (a) extend GraylogError with a new subclass GraylogValidationError (in src/graylog/errors.js) carrying status:409 and an isGraylogError:true marker so wrapGraylogError renders it cleanly; OR (b) return a synthetic isError envelope directly (don't throw). Path (a) is preferred for uniformity with the existing wrapGraylogError contract — verify whether `src/graylog/errors.js` already has a suitable class; if not, add `GraylogValidationError extends GraylogError` with constructor accepting `{ status, method, path, reason }` and re-export. Use this same class in delete_index_set's ND1 path from Plan 03 (cross-task class reuse — if Plan 03 chose path (b), refactor at this point OR leave both paths; document the choice).

3. Register in `src/tools/index-sets/index.js`: + handleSetDefaultIndexSet + register("set_default_index_set", handleSetDefaultIndexSet).

4. Append toolDefinitions entry to `src/tools.js`. Description names the can_be_default invariant: "Designate an index set as the default. The wrapper pre-flights `can_be_default` on the target; if false (events-style or system index set), the dry-run returns a structured error BEFORE any PUT is attempted — the would-be 409 surfaces in dry-run, not apply (D-13 / pitfall m2)." required: ["indexSetId"].

5. Append Tests 1-5 to `test/index-sets.test.js`. Use `_setCaptureRequest` with path-keyed routing so the GET pre-flight response is independent from the PUT apply response.
  </action>
  <verify>
    <automated>node --test test/index-sets.test.js -t 'set_default_index_set' && grep -q "handleSetDefaultIndexSet" src/tools/index-sets/set-default-index-set.js && grep -q "register(\"set_default_index_set\"" src/tools/index-sets/index.js && grep -q "set_default_index_set" src/tools.js && grep -q "can_be_default" src/tools/index-sets/set-default-index-set.js && grep -q "non_regular_index_set" src/tools/index-sets/set-default-index-set.js</automated>
  </verify>
  <done>set_default_index_set registered; 5 new tests cover D-13 + m2 + apply path + propagation; tool count grows from 41 to 42; ROADMAP success criterion 3 provably met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship cycle_deflector (INDEX-07) — ND3 pre-flight + UPDATED D-14 synchronous semantics + side_effects.observable_at</name>
  <read_first>
    - .planning/phases/02-index-sets-retention/02-CONTEXT.md D-14 (UPDATED) — note the rewritten envelope
    - .planning/phases/02-index-sets-retention/02-RESEARCH.md sections "Cycle Deflector Behavior", "Pitfall ND3", "Pitfall m3"
    - .planning/phases/02-index-sets-retention/02-U1-SMOKE.md (cycle decision: SYNC_OPTION_A is the default unless live-smoke proved otherwise)
    - src/tools/_shared/handler.js
    - src/tools/index-sets/schemas.js
  </read_first>
  <behavior>
    - Test 1: CycleDeflectorSchema parses { indexSetId: "x" } -> success; requires indexSetId.
    - Test 2: cycle_deflector against a writable index set — _setCaptureRequest scripts GET -> { id, title: "App errors", writable: true, ... }. Dry-run preview has method:"POST", path:"/api/system/deflector/<id>/cycle", body: undefined, postApplyEstimate.id matches, postApplyEstimate.async === false (per UPDATED D-14 — synchronous), postApplyEstimate.message mentions "Cycled index set" OR "closed previous active index", postApplyEstimate.side_effects.observable_at === "/system/jobs", postApplyEstimate.side_effects.describes mentions "range rebuild" or "IndexRangesUpdateJob".
    - Test 3: cycle_deflector against a non-writable index set (ND3) — script GET -> { writable: false, title: "Archive Read Only", ... }. Dry-run returns isError:true with text containing "non_writable_index_set" AND the index set title; POST was NEVER fired (capture count for POST === 0).
    - Test 4: cycle_deflector apply path — script GET (writable:true) + POST (204 no body). Apply returns { rotated: true, message: <one-line summary>, side_effects: { observable_at: "/system/jobs", describes: <closed-index range rebuild text> } }. Result body has those keys.
    - Test 5: cycle_deflector propagates 404 from pre-flight GET — script GET throws GraylogError status 404. isError surfaced.
  </behavior>
  <action>
1. Append CycleDeflectorSchema to `src/tools/index-sets/schemas.js`:
   ```
   CycleDeflectorSchema = mutatingBase.extend({
       indexSetId: z.string().min(1),
   });
   ```

2. Create `src/tools/index-sets/cycle-deflector.js`:
   - import defineMutatingHandler, CycleDeflectorSchema, makeClient.
   - handleCycleDeflector = defineMutatingHandler({ name: "cycle_deflector", schema, build, apply, summarize }).
   - build(args) async:
     - client = makeClient(args._conn).
     - current = await client.request("GET", "/api/system/indices/index_sets/" + args.indexSetId, null).
     - if (current.writable === false): throw GraylogValidationError (or equivalent typed) with status:400, method:"POST", path:"/api/system/deflector/" + args.indexSetId + "/cycle", reason: "non_writable_index_set", message: "Index set \"" + current.title + "\" (" + args.indexSetId + ") is not writable; cannot cycle. Set writable: true via update_index_set first."
     - else: return descriptor {
         method: "POST",
         path: "/api/system/deflector/" + args.indexSetId + "/cycle",
         body: undefined,
         postApplyEstimate: {
             id: args.indexSetId,
             async: false,  // UPDATED D-14 — cycle is synchronous
             rotated: true,
             message: "Cycled index set " + args.indexSetId + "; closed previous active index",
             side_effects: {
                 observable_at: "/system/jobs",
                 describes: "closed-index range rebuild (IndexRangesUpdateJob fires asynchronously; the rotation itself is complete on response)",
             },
         },
       }.
   - apply: async (client, req) => { await client.request(req.method, req.path, req.body); return { rotated: true, message: "Cycled index set " + req.path.split("/")[3] + "; closed previous active index. Deflector cycle complete on Graylog side.", side_effects: { observable_at: "/system/jobs", describes: "closed-index range rebuild" } }; }.
   - summarize: (args) => "Cycle deflector for index set " + args.indexSetId.

3. Register in `src/tools/index-sets/index.js`: + handleCycleDeflector.

4. Append toolDefinitions entry to `src/tools.js`. Description MUST warn (m3 visibility): "Cycle the deflector — close the current active write index and open the next one. The cycle is SYNCHRONOUS in Graylog 7.0.6 — when the apply response returns, the rotation is complete. In-flight writes may briefly buffer until the new index is ready (m3). The closed index's message ranges are rebuilt asynchronously as a separate system job observable at /system/jobs; call await_system_job on that job ID if you need to wait for the rebuild before searching the just-closed index by time range. Refuses non-writable index sets (ND3) AND refuses writable: false connections (D-07/D-16)." required: ["indexSetId"].

5. Append Tests 1-5 to `test/index-sets.test.js`.

If the U1-SMOKE.md Cycle decision is ASYNC_OPTION_B (live-smoke proved cycle returns a job_id), revise this task: the apply response handling would extract a job_id from a POST response body (currently expected to be 204 void) AND the postApplyEstimate envelope would carry async:true. This branch is unlikely (RESEARCH.md confirms sync on 7.0.6 source); document the divergence in the task SUMMARY if it occurs.
  </action>
  <verify>
    <automated>node --test test/index-sets.test.js -t 'cycle_deflector' && grep -q "handleCycleDeflector" src/tools/index-sets/cycle-deflector.js && grep -q "register(\"cycle_deflector\"" src/tools/index-sets/index.js && grep -q "cycle_deflector" src/tools.js && grep -q "non_writable_index_set" src/tools/index-sets/cycle-deflector.js && grep -q "side_effects" src/tools/index-sets/cycle-deflector.js && grep -q "observable_at" src/tools/index-sets/cycle-deflector.js && ! grep -E "async:\s*true" src/tools/index-sets/cycle-deflector.js</automated>
  </verify>
  <done>cycle_deflector registered; 5 new tests cover synchronous response + ND3 + apply path + 404 propagation; postApplyEstimate.async is false (UPDATED D-14); side_effects.observable_at points at /system/jobs for the closed-index range rebuild; tool count grows from 42 to 43; full npm test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> set_default_index_set | can_be_default pre-flight ensures the agent cannot designate a system / events index set as the default; the 409 surfaces in dry-run not apply |
| Agent -> cycle_deflector | writable pre-flight ensures the agent cannot cycle a non-writable (e.g. archived) index set; the 400 surfaces in dry-run not apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | T (tampering) | set_default invariant bypass | mitigate | Two-layer defense: wrapper-side can_be_default check (D-13) surfaces in dry-run; Graylog server-side `isRegularIndex` check throws 409 on apply. Even if the wrapper check is bypassed, the server refuses. |
| T-02-04-02 | E (elevation) | cycle_deflector against non-writable | mitigate | Wrapper-side `current.writable === false` refusal (ND3) AND Graylog server-side `checkCycle` 400. Two-layer defense. |
| T-02-04-03 | I (information disclosure) | Pre-flight GET on index set | accept | The GET returns public index-set metadata (no creds). Existing Phase 1 cascade-preflight contract. |
| T-02-04-04 | D (denial of service) | cycle_deflector cluster-wide impact | accept | Cycling causes a brief write-buffer gap (m3) and triggers an async range rebuild. These are inherent to the Graylog rotation mechanism — the wrapper documents them via tool description and side_effects.describes; the agent decides whether to cycle. |
| T-02-04-05 | R (repudiation) | Set default audit trail | accept | Graylog @AuditEvent emits a server-side log entry on the PUT /default call. Same model as Phase 1. |
</threat_model>

<verification>
1. `node --test test/index-sets.test.js -t 'set_default_index_set'` — 5 set-default tests pass.
2. `node --test test/index-sets.test.js -t 'cycle_deflector'` — 5 cycle tests pass.
3. `grep -q "can_be_default" src/tools/index-sets/set-default-index-set.js` — D-13 invariant present.
4. `grep -q "non_writable_index_set" src/tools/index-sets/cycle-deflector.js` — ND3 pre-flight present.
5. `! grep -E "async:\s*true" src/tools/index-sets/cycle-deflector.js` — UPDATED D-14 sync semantics enforced; no leftover async envelope.
6. `node --test test/dispatch.test.js` — assertAllToolsRegistered OK (41 -> 43 tools).
7. Full `npm test` shows zero regression on the prior baseline.
</verification>

<success_criteria>
- set_default_index_set ships with the D-13 can_be_default pre-flight; non-regular index sets surface a structured dry-run error BEFORE any PUT.
- cycle_deflector ships with the ND3 writable pre-flight AND the UPDATED D-14 synchronous response shape (postApplyEstimate.async === false, side_effects.observable_at: /system/jobs naming the range rebuild).
- Both tool descriptions warn about the m2 (events-style refusal) and m3 (cycle brief gap) operational consequences.
- Tool count grows from 41 to 43; all 10 new tests pass; ROADMAP success criterion 3 provably met.
</success_criteria>

<output>
After completion, create `.planning/phases/02-index-sets-retention/02-04-SUMMARY.md` with:
- Frontmatter (phase, plan, subsystem, requires/provides/affects, key-files, key-decisions, requirements-completed: [INDEX-06, INDEX-07])
- The D-13 can_be_default pre-flight contract (where it fires; what reason it surfaces; the m2 mitigation path)
- The UPDATED D-14 synchronous envelope shape on cycle_deflector + the side_effects.observable_at semantics
- The ND3 non-writable pre-flight contract
- The GraylogValidationError class (if added) and how it interfaces with wrapGraylogError
- Test growth delta (10 new tests)
</output>
