---
phase: 02-index-sets-retention
plan: 05
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - test/index-sets.test.js
  - test/system-job.test.js
  - test/schema-parity.test.js
  - test/auth-redaction.test.js
  - test/__snapshots__/index-sets.test.js.snapshot
  - test/__snapshots__/system-job.test.js.snapshot
  - .planning/phases/02-index-sets-retention/02-VALIDATION.md
autonomous: false
requirements: [INDEX-01, INDEX-02, INDEX-03, INDEX-04, INDEX-05, INDEX-06, INDEX-07, INDEX-08]
tags: [snapshot, schema-parity, auth-redaction, c1-acceptance-gate, phase-closure, validation-flip, checkpoint]

must_haves:
  truths:
    - "9 byte-identical snapshot fixtures land covering the safety-critical paths of Phase 2 (per RESEARCH.md §Snapshot Fixture Design) — two consecutive npm test runs produce identical md5sums of all new .snapshot files (FOUND-07 contract)"
    - "schema-parity.test.js carries 8 new assertSchemaParityForTool calls (list_index_sets, get_index_set, create_index_set, update_index_set, delete_index_set, set_default_index_set, cycle_deflector, await_system_job) — all green on first run"
    - "auth-redaction.test.js allowlist recognizes the confirmationToken context (1-2 line additive amendment mirroring the existing idempotencyKey context check) so the 64-hex C1 hash does not trigger a false-positive auth-leak alert"
    - "02-VALIDATION.md frontmatter flipped: wave_0_complete: true, nyquist_compliant: true; status: approved"
    - "The C1 + D-13 + D-14 acceptance gates from Plans 03 + 04 are pinned as snapshot fixtures (not just shape assertions) so a future drift fails byte-identity not behavior"
    - "Plan 05 is autonomous: false — the final checkpoint:human-verify task asks the user to confirm the byte-identity md5sums + the VALIDATION.md flip before Phase 2 is declared complete"
  artifacts:
    - path: "test/__snapshots__/index-sets.test.js.snapshot"
      provides: "8 fixtures covering create + update + delete (3 shapes) + set_default invariant + cycle_deflector sync"
    - path: "test/__snapshots__/system-job.test.js.snapshot"
      provides: "1 fixture covering await_system_job dry-run polling plan"
    - path: "test/schema-parity.test.js"
      provides: "8 new assertSchemaParityForTool calls"
    - path: "test/auth-redaction.test.js"
      provides: "Extended isAllowedMatch to recognize confirmationToken context"
    - path: ".planning/phases/02-index-sets-retention/02-VALIDATION.md"
      provides: "Phase closure: frontmatter flipped + per-task references attached"
---

<objective>
Close Phase 2 with deterministic snapshot fixtures pinning every Phase 2 safety contract (9 fixtures per RESEARCH.md §Snapshot Fixture Design), full schema-parity coverage for the 8 new tools, the auth-redaction allowlist extension for the confirmationToken context, and the VALIDATION.md frontmatter flip. End with a checkpoint:human-verify that the byte-identity md5sums hold across two consecutive `npm test` runs and the developer accepts Phase 2 as complete.

Purpose: Phase 1 Plan 05 established the closure pattern (snapshot fixtures + schema-parity enrichment + VALIDATION flip). Phase 2 Plan 05 follows the same playbook scaled to the 8 new tools and the 9 fixtures. The C1 + D-13 + D-14 acceptance gates from Plans 03 + 04 graduate from "passed by behavior assertion in a single test" to "pinned by byte-identical snapshot file" — drift detection is automatic going forward.

Output: 9 new snapshot fixtures (all byte-deterministic), 8 schema-parity assertions, 1-2 line auth-redaction extension, VALIDATION.md flip, and a final human-verify checkpoint confirming the milestone closes cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-index-sets-retention/02-CONTEXT.md
@.planning/phases/02-index-sets-retention/02-RESEARCH.md
@.planning/phases/02-index-sets-retention/02-VALIDATION.md
@.planning/phases/02-index-sets-retention/02-01-SUMMARY.md
@.planning/phases/02-index-sets-retention/02-02-SUMMARY.md
@.planning/phases/02-index-sets-retention/02-03-SUMMARY.md
@.planning/phases/02-index-sets-retention/02-04-SUMMARY.md
@.planning/phases/01-inputs-extractors/01-05-SUMMARY.md
@.planning/phases/00-foundation/00-06-SUMMARY.md

@test/schema-parity.test.js
@test/auth-redaction.test.js
@test/snapshot-config.js
@test/index-sets.test.js
@test/system-job.test.js

<interfaces>
RESEARCH.md §Snapshot Fixture Design (the 9 fixtures Plan 05 must land):
1. create_index_set time-based+delete dry-run — pins FQCN map + postApplyEstimate.id === "__SERVER_ASSIGNED__" + existingMatches:[]
2. update_index_set partial — rename only (D-12 strict no-echo OR merge-from-current per U1 SMOKE) — pin whichever the implementation shipped
3. update_index_set strategy-replace (D-11 atomic) — pin rotation_strategy_class + rotation_strategy.type both set
4. delete_index_set deleteIndices:false (no token) — pin path ends ?delete_indices=false, NO confirmationToken, NO cascades
5. delete_index_set deleteIndices:true against empty index set — pin confirmationToken 64-hex + cascades.indices:[] + cascades.messageCount:0 + postApplyEstimate.async:true
6. delete_index_set deleteIndices:true against populated index set — pin different confirmationToken (sensitivity to messageCount + indexNames) + cascades.indices sorted + cascades.indexCount:3
7. set_default_index_set against ineligible index set (UPDATED D-13) — pin isError + reason:default_eligibility_failed + can_be_default:false in the error text
8. cycle_deflector dry-run — pin postApplyEstimate.async:false (UPDATED D-14) + side_effects.observable_at:"/system/jobs" + side_effects.describes mentions range rebuild
9. await_system_job dry-run — pin postApplyEstimate.plan:[500,1000,2000,4000,5000] + timeoutMs:60000 + note about apply blocking

FOUND-07 contract (carried over): two consecutive `npm test` runs must produce byte-identical md5sums of every .snapshot file. Static-only args, no Date.now, no randomUUID, no machine-state.

Phase 1 Plan 05 pattern (test/__snapshots__/inputs.test.js.snapshot established the precedent):
- t.assert.snapshot(JSON.parse(res.content[0].text)) — snapshot the PARSED payload, not the raw text.
- Reset _setCaptureRequest + _setConnectionsForTests in beforeEach/afterEach so cross-test contamination is impossible.

auth-redaction.test.js current shape (Phase 0 + Phase 1 Plan 05):
- PASSWORD_LITERAL regex narrowed at the regex level for placeholder <...> recognition
- isAllowedMatch has one entry today: idempotencyKey context (32+ hex with preceding "idempotencyKey": context)
- Phase 2 ADDS: the confirmationToken context (64-hex with preceding "confirmationToken": context)

The confirmationToken value is a content-hash of public state — NOT a secret. But it lights up the 32+ alnum regex. Plan 05 extends isAllowedMatch with:
  if (/confirmationToken['"]?\s*[:=]\s*['"]?$/.test(context)) return true;
This mirrors the existing idempotencyKey check exactly. 1-2 line addition.

schema-parity.test.js current shape (Phase 1 Plan 05):
- assertSchemaParityForTool(toolName, zodSchema) helper exists
- getShape(zodSchema) handles ZodEffects (superRefine wraps the inner object) AND ZodObject

The 8 Phase 2 schemas to add parity for:
- ListIndexSetsSchema (Plan 01)
- GetIndexSetSchema (Plan 01)
- AwaitSystemJobSchema (Plan 01 — note the .refine() wraps a ZodObject; getShape's _def.schema.shape fallback handles it)
- CreateIndexSetSchema (Plan 02)
- UpdateIndexSetSchema (Plan 02)
- DeleteIndexSetSchema (Plan 03)
- SetDefaultIndexSetSchema (Plan 04)
- CycleDeflectorSchema (Plan 04)

Plan 01 Task 4 ALREADY added 3 schema-parity entries (list/get/await). Plan 05 adds the remaining 5. Total Phase 2 schema-parity assertions: 8.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snapshot fixtures + schema-parity enrichment + auth-redaction allowlist extension</name>
  <read_first>
    - test/__snapshots__/inputs.test.js.snapshot (Phase 1 precedent shape)
    - test/__snapshots__/handler.test.js.snapshot (Phase 0 precedent)
    - test/snapshot-config.js (the setResolveSnapshotPath bootstrap)
    - test/auth-redaction.test.js (the isAllowedMatch function to extend)
    - test/schema-parity.test.js (the assertSchemaParityForTool template to extend)
    - .planning/phases/02-index-sets-retention/02-RESEARCH.md §Snapshot Fixture Design (the 9 fixtures table)
    - .planning/phases/01-inputs-extractors/01-05-SUMMARY.md (the Phase 1 closure narrative)
  </read_first>
  <behavior>
    - Snapshot Test 1: create_index_set time-based+delete dry-run fixture exists in test/__snapshots__/index-sets.test.js.snapshot; preview.body.rotation_strategy_class is the TimeBased FQCN; preview.body.rotation_strategy.rotation_period is "P1D"; preview.body.retention_strategy.max_number_of_indices is 30; postApplyEstimate.id is "__SERVER_ASSIGNED__".
    - Snapshot Test 2: update_index_set partial rename — body shape matches U1 SMOKE decision (strict no-echo OR merge-from-current).
    - Snapshot Test 3: update_index_set strategy-replace — both rotation_strategy_class AND rotation_strategy.type FQCN present in preview.body.
    - Snapshot Test 4: delete_index_set deleteIndices:false — preview.path ends ?delete_indices=false; NO confirmationToken key in the payload; NO cascades key.
    - Snapshot Test 5: delete_index_set deleteIndices:true empty — confirmationToken is a 64-char hex string; cascades.indices: []; cascades.messageCount: 0; postApplyEstimate.async: true.
    - Snapshot Test 6: delete_index_set deleteIndices:true populated — confirmationToken differs from Test 5's; cascades.indices is sorted; cascades.indexCount: 3.
    - Snapshot Test 7: set_default_index_set ineligible — isError:true; text mentions "default_eligibility_failed" and can_be_default:false.
    - Snapshot Test 8: cycle_deflector dry-run — postApplyEstimate.async: false; side_effects.observable_at: "/system/jobs"; side_effects.describes mentions range rebuild.
    - Snapshot Test 9 (in test/system-job.test.js): await_system_job dry-run — postApplyEstimate.plan: [500,1000,2000,4000,5000]; timeoutMs: 60000.
    - Schema-parity Tests 1-5: 5 NEW assertSchemaParityForTool calls for CreateIndexSetSchema, UpdateIndexSetSchema, DeleteIndexSetSchema, SetDefaultIndexSetSchema, CycleDeflectorSchema. (The other 3 — list/get/await — landed in Plan 01 Task 4.)
    - Auth-redaction Test: synthetic snapshot containing `"confirmationToken": "a1b2...c3d4"` (64 hex) AND an actual `"Authorization": "Bearer xyz"` line — the lint identifies ONLY the Bearer line as a violation.
    - Determinism Test: run `md5sum test/__snapshots__/*.snapshot` twice, no second run shows any diff (this is a phase-gate manual check; Task 1's verify command runs it once and the Task 2 checkpoint runs it again).
  </behavior>
  <action>
1. **Snapshot fixtures 1-8** — append `t.assert.snapshot(JSON.parse(res.content[0].text))` calls inside the EXISTING tests in `test/index-sets.test.js` that exercise each scenario (the unit tests from Plans 02/03/04 already produce the right responses). NEW tests are NOT added — the snapshot calls go inside the existing tests:
   - In the "create_index_set time-based+delete dry-run" test from Plan 02, append `t.assert.snapshot(JSON.parse(res.content[0].text));` after the existing assertions.
   - In the "update_index_set partial title-only" test, append the snapshot call.
   - In the "update_index_set strategy-replace" test, append.
   - In the "delete_index_set default is delete_indices=false" test, append.
   - In the "delete_index_set confirmationToken populated against empty index set" test, append.
   - In the "delete_index_set confirmationToken populated against populated index set" test, append.
   - In the "set_default_index_set non-regular preflight" test, append.
   - In the "cycle_deflector dry-run" test, append.

   The first `npm test --test-update-snapshots` invocation creates the .snapshot file. The SECOND run validates byte-identity.

2. **Snapshot fixture 9** — same pattern in the "await_system_job dry-run returns polling plan WITHOUT issuing GETs" test in `test/system-job.test.js`.

3. **Determinism inputs:** verify EVERY snapshot-bearing test uses static-only inputs:
   - No Date.now in fixture inputs (creation_date for create_index_set: Plan 02 Task 2's `_setClockForTests` seam pins a known value like "2026-05-15T11:58:08.466Z" — the same constant Phase 1 used).
   - No randomUUID — all IDs are literals (e.g. "iset-1", "iset-default", "ranges-rebuild-job-1").
   - No connection-specific data — `_setConnectionsForTests({ "fixture_conn": { baseUrl: "http://test", apiToken: "fixture-token-NOT-SCANNED-because-allowlisted-in-context" } })`.
   - idempotencyKey: agent-supplied per test so deriveIdempotencyKey is bypassed (or, if relying on derivation, the inputs are static so the derived key is itself deterministic).

4. **Schema-parity enrichment** — append 5 new test() calls to `test/schema-parity.test.js`:
   ```
   test("schema-parity: create_index_set", async () => {
       const { CreateIndexSetSchema } = await import("../src/tools/index-sets/schemas.js");
       await assertSchemaParityForTool("create_index_set", CreateIndexSetSchema);
   });
   test("schema-parity: update_index_set", async () => {
       const { UpdateIndexSetSchema } = await import("../src/tools/index-sets/schemas.js");
       await assertSchemaParityForTool("update_index_set", UpdateIndexSetSchema);
   });
   test("schema-parity: delete_index_set", async () => {
       const { DeleteIndexSetSchema } = await import("../src/tools/index-sets/schemas.js");
       await assertSchemaParityForTool("delete_index_set", DeleteIndexSetSchema);
   });
   test("schema-parity: set_default_index_set", async () => {
       const { SetDefaultIndexSetSchema } = await import("../src/tools/index-sets/schemas.js");
       await assertSchemaParityForTool("set_default_index_set", SetDefaultIndexSetSchema);
   });
   test("schema-parity: cycle_deflector", async () => {
       const { CycleDeflectorSchema } = await import("../src/tools/index-sets/schemas.js");
       await assertSchemaParityForTool("cycle_deflector", CycleDeflectorSchema);
   });
   ```
   If any parity assertion fails on first run, fix the src/tools.js inputSchema.properties to match the zod shape (zod is the source of truth — never the other direction).

5. **Auth-redaction allowlist extension** — edit `test/auth-redaction.test.js`'s `isAllowedMatch` function. After the existing idempotencyKey check:
   ```
   if (regex.source === /[A-Za-z0-9]{32,}/.source) {
       const context = content.slice(Math.max(0, matchIndex - 40), matchIndex);
       if (/idempotencyKey['"]?\s*[:=]\s*['"]?$/.test(context)) return true;
       // Plan 02-05: 64-hex confirmation token (D-01) is a sha-256 over public state, not a secret.
       if (/confirmationToken['"]?\s*[:=]\s*['"]?$/.test(context)) return true;
   }
   ```

6. **Synthetic sanity check** (per Plan 00-06's pattern): drop a tampered file under /tmp containing both a confirmationToken value (64 hex) AND a fake `"Authorization": "Bearer xyz"` line. Run a one-off node script that calls the lint logic. Confirm: confirmationToken match is allowed; Bearer match is flagged. Document the result in the Task SUMMARY.
  </action>
  <verify>
    <automated>node --test test/schema-parity.test.js -t 'create_index_set|update_index_set|delete_index_set|set_default_index_set|cycle_deflector' && node --test test/auth-redaction.test.js && test -f test/__snapshots__/index-sets.test.js.snapshot && test -f test/__snapshots__/system-job.test.js.snapshot && grep -q "confirmationToken" test/auth-redaction.test.js && grep -q "CreateIndexSetSchema" test/schema-parity.test.js && grep -q "DeleteIndexSetSchema" test/schema-parity.test.js && grep -q "CycleDeflectorSchema" test/schema-parity.test.js && md5sum test/__snapshots__/index-sets.test.js.snapshot > /tmp/snap-md5-a && npm test --silent > /dev/null 2>&1 && md5sum test/__snapshots__/index-sets.test.js.snapshot > /tmp/snap-md5-b && diff /tmp/snap-md5-a /tmp/snap-md5-b</automated>
  </verify>
  <done>9 snapshot fixtures present; 5 new schema-parity tests pass (8 total for Phase 2 when counting the 3 from Plan 01); auth-redaction allowlist recognizes confirmationToken context; md5sums identical across two consecutive `npm test` runs (FOUND-07 contract honored).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify Phase 2 closure — byte-identity md5sums + VALIDATION.md flip</name>
  <read_first>
    - test/__snapshots__/index-sets.test.js.snapshot (Task 1 output)
    - test/__snapshots__/system-job.test.js.snapshot (Task 1 output)
    - .planning/phases/02-index-sets-retention/02-U1-SMOKE.md (Plan 01 Task 1 output)
    - .planning/phases/02-index-sets-retention/02-VALIDATION.md (current frontmatter)
  </read_first>
  <action>
This is a checkpoint:human-verify task. The action is to PAUSE execution and surface the verification protocol below to the user. Do NOT auto-flip 02-VALIDATION.md — Task 3 flips it after the user types "approved".

Verification protocol:

1. Run `npm test` — confirm the full suite is green AND prints a tool count of 43 (via assertAllToolsRegistered). Expected: 223 (Phase 1 baseline) + ~14 (Plan 01) + ~16 (Plan 02) + ~20 (Plan 03) + ~10 (Plan 04) + ~14 (Plan 05) ≈ 297 tests.

2. Run `md5sum test/__snapshots__/*.snapshot` twice and confirm the output is byte-identical:
   ```bash
   md5sum test/__snapshots__/*.snapshot > /tmp/run-a
   npm test > /dev/null
   md5sum test/__snapshots__/*.snapshot > /tmp/run-b
   diff /tmp/run-a /tmp/run-b   # MUST be empty
   ```
   If the diff is non-empty, a Rule 1 determinism bug exists — fix the source of non-determinism (likely a Date.now() or randomUUID() leaked into a fixture's input).

3. Spot-check the new fixtures:
   - `cat test/__snapshots__/index-sets.test.js.snapshot | grep -c confirmationToken` — expect 2 (Fixtures 5 + 6: deleteIndices:true paths).
   - `cat test/__snapshots__/index-sets.test.js.snapshot | grep -c "TimeBasedRotationStrategyConfig"` — expect at least 1 (Fixture 1).
   - `cat test/__snapshots__/system-job.test.js.snapshot | grep -c '500,1000,2000,4000,5000'` — expect 1 (Fixture 9: polling plan).

4. (Optional but recommended) Run the live-instance smoke against `http://<graylog-host>`:
   - `delete_index_set` dry-run + apply with `deleteIndices: true` against a fresh test index set; confirm the cleanup job appears in `GET /system/jobs` and `await_system_job` polls it to completion.
   - `set_default_index_set` against an events-style index set; confirm the dry-run error message names `can_be_default: false`.
   - `cycle_deflector` against a writable index set; confirm the immediate 204 response and a subsequent `IndexRangesUpdateJob` entry in `/system/jobs`.

5. Confirm the U1-SMOKE.md decision file holds the right outcome — if the live instance was reachable, the file's U1 + Cycle decisions should reflect what Plans 02 + 04 actually shipped.

6. Approve the VALIDATION.md flip: open `.planning/phases/02-index-sets-retention/02-VALIDATION.md` and confirm `wave_0_complete: true` + `nyquist_compliant: true` + `status: approved` in the frontmatter.

7. Phase 2 is now ready for the next phase to begin (Phase 3 Streams & Stream Rules will compose over the index-set CRUD from Phase 2 — every stream binds to an index_set_id).
  </action>
  <verify>
    <automated>echo "CHECKPOINT — awaiting human approval via resume-signal"</automated>
  </verify>
  <done>User typed "approved" via resume-signal; Phase 2 closure accepted.</done>
  <what-built>
Phase 2 ships 8 new tools (list_index_sets, get_index_set, create_index_set, update_index_set, delete_index_set, set_default_index_set, cycle_deflector, await_system_job) plus two cross-cutting amendments to handler.js (_confirmationToken forwarding + requireConfirm gate) and one one-line amendment to conflict.js (index_sets envelope normalization). Tool count grows from 35 to 43.

The C1 confirmation token gate is the centerpiece — Plan 03 ships the sha-256 hash over { indexSetId, deleteIndices:true, sorted indexNames, messageCount } that binds dry-run to apply. The agent CANNOT issue a destructive delete_index_set without first running a dry-run and echoing the token back.

The D-13 + ND3 invariants gate set_default_index_set (events-style refusal) and cycle_deflector (non-writable refusal) in dry-run BEFORE any HTTP mutation.

The UPDATED D-14 makes cycle_deflector return a synchronous envelope with side_effects.observable_at — the original async envelope was wrong per RESEARCH.md §Cycle Deflector Behavior verification.

9 snapshot fixtures pin every safety contract byte-identically. Schema-parity covers all 8 new tools. Auth-redaction allowlist extends to the confirmationToken context.
  </what-built>
  <how-to-verify>See the action block above for the 7-step verification protocol.</how-to-verify>
  <resume-signal>Type "approved" to mark Phase 2 complete and trigger Task 3 (VALIDATION.md flip). Type "issues: <description>" to flag concerns Plan 05 must address before phase closure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip 02-VALIDATION.md frontmatter (wave_0_complete + nyquist_compliant true; status approved); update per-task references</name>
  <read_first>
    - .planning/phases/02-index-sets-retention/02-VALIDATION.md (current frontmatter)
    - .planning/phases/01-inputs-extractors/01-VALIDATION.md (Phase 1 reference post-flip)
  </read_first>
  <action>
1. Edit `.planning/phases/02-index-sets-retention/02-VALIDATION.md`:
   - In frontmatter, set `nyquist_compliant: true`.
   - In frontmatter, set `wave_0_complete: true`.
   - In frontmatter, set `status: approved`.
2. Update the per-task status column (now stable):
   - Every row whose "File Exists" column was "❌ W0" flips to "✅" once the test/source files exist.
   - Every row's "Status" column flips to "✅ green".
3. Append a "Validation Sign-Off" note at the bottom listing: snapshot determinism verified (md5sum identity proven), schema-parity 8/8 green, auth-redaction extended, npm test full suite green, tool count 43.
4. Append a "Phase Closure" subsection naming the 8 INDEX-* requirements as Complete and pointing to the relevant SUMMARY.md (Plans 01-05) for each.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/02-index-sets-retention/02-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/02-index-sets-retention/02-VALIDATION.md && grep -q "status: approved" .planning/phases/02-index-sets-retention/02-VALIDATION.md && grep -q "INDEX-08" .planning/phases/02-index-sets-retention/02-VALIDATION.md</automated>
  </verify>
  <done>02-VALIDATION.md frontmatter flipped; per-task references show ✅; Phase 2 declared complete in the validation artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot files -> auth-redaction scan | Every .snapshot file under test/__snapshots__/ is auto-scanned for credential leaks; the confirmationToken allowlist extension is the narrow surface this phase adds |
| Schema-parity assertions -> tools.js | Drift between zod and src/tools.js JSON-Schema fails the test loudly; the fix direction is always "align tools.js to zod" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | I (info disclosure) | confirmationToken in snapshots | accept | The 64-hex token is a content-hash of public state. Snapshotting it does not leak secrets; the auth-redaction extension recognizes the context-aware allowlist so it is not flagged as an apiToken-like string. |
| T-02-05-02 | I (info disclosure) | Snapshot files scanned by external tools | mitigate | auth-redaction.test.js scans on every npm test run; new fixtures inherit the scan automatically. No per-fixture allowlist required. |
| T-02-05-03 | T (tampering) | Snapshot drift | mitigate | t.assert.snapshot fails any deviation byte-by-byte. Two consecutive npm test runs MUST produce identical md5sums (FOUND-07). Drift detection is automatic. |
| T-02-05-04 | T (tampering) | Schema-parity drift | mitigate | assertSchemaParityForTool asserts deepEqual on the sorted key arrays of the zod shape and the tools.js inputSchema.properties. Adding a field to one without the other fails the test on first run. |
</threat_model>

<verification>
1. `node --test test/index-sets.test.js test/system-job.test.js test/schema-parity.test.js test/auth-redaction.test.js` — every test green.
2. `md5sum test/__snapshots__/*.snapshot` runs twice produce identical output (FOUND-07).
3. `grep -c "confirmationToken" test/__snapshots__/index-sets.test.js.snapshot` >= 2.
4. `grep -c "TimeBasedRotationStrategyConfig" test/__snapshots__/index-sets.test.js.snapshot` >= 1.
5. `grep -c "side_effects" test/__snapshots__/index-sets.test.js.snapshot` >= 1 (cycle_deflector fixture).
6. `grep -c "confirmationToken" test/auth-redaction.test.js` >= 1.
7. `grep -c "schema-parity:.*index_set\|schema-parity:.*deflector\|schema-parity:.*system_job" test/schema-parity.test.js` >= 8.
8. `grep -q "nyquist_compliant: true" .planning/phases/02-index-sets-retention/02-VALIDATION.md`.
9. `npm test` shows 297+ tests passing, 0 regressions, tool count 43 (verifiable via dispatch.test.js's assertAllToolsRegistered output).
</verification>

<success_criteria>
- 9 byte-identical snapshot fixtures pin the safety-critical paths (FOUND-07 contract honored across two npm test runs).
- 8 schema-parity assertions cover every Phase 2 tool (5 added in Plan 05; 3 already added in Plan 01 Task 4).
- auth-redaction.test.js's isAllowedMatch recognizes the confirmationToken context (1-2 line additive amendment).
- 02-VALIDATION.md flipped: wave_0_complete:true + nyquist_compliant:true + status:approved.
- All 8 INDEX-* requirements complete and traceable to their landing plan in 02-VALIDATION.md.
- The checkpoint:human-verify confirms the user accepts the byte-identity contract + the VALIDATION flip before Phase 2 closes.
- Full npm test: 297+ tests, zero regression, tool count 43.
- ROADMAP success criteria 1 (delete_index_set confirmation token), 2 (create_index_set dry-run shows resolved FQCNs), 3 (set_default_index_set surfaces 409 in dry-run), 4 (await_system_job reusable primitive) all provably met.
</success_criteria>

<output>
After completion, create `.planning/phases/02-index-sets-retention/02-05-SUMMARY.md` with:
- Frontmatter (phase, plan, subsystem, requires/provides/affects, key-files, key-decisions, requirements-completed: [INDEX-01..INDEX-08])
- The 9 snapshot fixtures named + their pinning purpose
- The auth-redaction allowlist extension + the synthetic sanity-check result
- The schema-parity 8/8 green status
- The byte-identity md5sums recorded at two consecutive npm test runs (proof)
- Phase 2 declared complete + the 8 INDEX-* requirements traced to their landing plan
- Test growth delta cumulative (Phase 1 223 -> Phase 2 ~297)
- Tool count delta cumulative (35 -> 43)
</output>
