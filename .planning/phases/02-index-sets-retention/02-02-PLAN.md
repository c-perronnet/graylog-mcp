---
phase: 02-index-sets-retention
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/tools/index-sets/schemas.js
  - src/tools/index-sets/strategies.js
  - src/tools/index-sets/create-index-set.js
  - src/tools/index-sets/update-index-set.js
  - src/tools/index-sets/index.js
  - src/tools.js
  - test/index-sets.test.js
autonomous: true
requirements: [INDEX-03, INDEX-04]
tags: [index-sets, create, update, strategies, fqcn, partial-update, d-10, d-11, d-12, u1-resolution]

must_haves:
  truths:
    - "create_index_set REQUIRES rotation_strategy + rotation_strategy_config AND retention_strategy + retention_strategy_config (D-10 — no defaults on destruction policies)"
    - "create_index_set translates friendly aliases (time-based/size-based/message-count + delete/close) to FQCNs at wire-build time per D-08; rejects 'archive' with structured reason archive_not_supported"
    - "create_index_set superRefine narrows the strategy_config shape per alias (e.g. size-based requires max_size:number, time-based requires rotation_period ISO-8601 string)"
    - "create_index_set dry-run preview emits rotation_strategy_class: <FQCN>, rotation_strategy: { type: <ConfigFQCN>, ...config }, mirror for retention; postApplyEstimate.id === '__SERVER_ASSIGNED__' per D-17"
    - "create_index_set fires findExistingMatches against /api/system/indices/index_sets with title-based matchFn (M5 idempotency); the index_sets envelope unwrap from Plan 01 is in scope"
    - "update_index_set ships per U1 SMOKE outcome — either D-12 STRICT NO-ECHO (preview body carries ONLY the agent-supplied changes plus the immutable envelope) OR merge-from-current (Phase 1 update_extractor pattern; index-set configs carry no encrypted fields so C3 is not reachable)"
    - "update_index_set D-11 atomic strategy-replace: superRefine rejects changes.rotation_strategy without changes.rotation_strategy_config (and vice versa); same rule for retention"
    - "update_index_set ND2 pre-flight: when current.default === true AND args.changes.writable === false, dry-run returns structured reason default_index_set_must_be_writable BEFORE any PUT"
    - "update_index_set immutable fields (index_prefix, creation_date) are NEVER on the wire even when the agent passes them in changes"
  artifacts:
    - path: "src/tools/index-sets/strategies.js"
      provides: "ALIAS_TO_FQCN map (3 rotation + 2 retention) + buildRotationBlock + buildRetentionBlock translators + aliasToConfigOrError (archive rejection)"
      exports: ["ROTATION_FQCN", "RETENTION_FQCN", "buildRotationBlock", "buildRetentionBlock", "aliasToConfigOrError"]
    - path: "src/tools/index-sets/schemas.js"
      provides: "CreateIndexSetSchema + UpdateIndexSetSchema with D-10/D-11 superRefine + 6 strict strategy-config schemas + ISO_DURATION regex"
      exports: ["CreateIndexSetSchema", "UpdateIndexSetSchema", "ROTATION_CONFIG_BY_ALIAS", "RETENTION_CONFIG_BY_ALIAS"]
    - path: "src/tools/index-sets/create-index-set.js"
      provides: "INDEX-03 — defineMutatingHandler composing strategies.js translators + findExistingMatches"
      exports: ["handleCreateIndexSet"]
    - path: "src/tools/index-sets/update-index-set.js"
      provides: "INDEX-04 — defineMutatingHandler implementing the U1-resolved partial-update + D-11 atomic strategy-replace + ND2 pre-flight"
      exports: ["handleUpdateIndexSet"]
  key_links:
    - from: "src/tools/index-sets/create-index-set.js"
      to: "src/tools/index-sets/strategies.js"
      via: "buildRotationBlock + buildRetentionBlock + aliasToConfigOrError"
      pattern: "buildRotationBlock"
    - from: "src/tools/index-sets/update-index-set.js"
      to: "src/tools/_shared/handler.js"
      via: "defineMutatingHandler + async build() pre-fetching current IndexSetResponse"
      pattern: "GET /api/system/indices/index_sets/"
    - from: "src/tools/index-sets/create-index-set.js"
      to: "src/tools/_shared/conflict.js"
      via: "findExistingMatches with listPath /api/system/indices/index_sets and matchFn on title"
      pattern: "findExistingMatches"
    - from: "src/tools/index-sets/schemas.js"
      to: "wire body shape"
      via: "superRefine variant dispatch on rotation_strategy / retention_strategy aliases (mirrors CreateInputSchema variantMap pattern)"
      pattern: "superRefine"
---

<objective>
Ship the create + update halves of the index-set CRUD surface: create_index_set (INDEX-03) with D-10 required-strategies + D-08 friendly aliases + D-09 6 strict configs + M5 list-before-create idempotency, and update_index_set (INDEX-04) with D-11 atomic strategy-replace + the U1-resolved partial-update wire-build + ND2 default-must-be-writable pre-flight.

Purpose: Plans 03 + 04 depend on the strategies.js alias-to-FQCN map. The U1 smoke outcome from Plan 01 selects ONE of the two update implementations — there is no behavioural ambiguity at execution time.

Output: 2 new mutating tools registered; strategies module; 6 strict strategy-config schemas; ~16 new tests covering D-10/D-11/D-08 alias mapping, ND2 pre-flight, M5 existingMatches, superRefine variant-config narrowing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-index-sets-retention/02-CONTEXT.md
@.planning/phases/02-index-sets-retention/02-RESEARCH.md
@.planning/phases/02-index-sets-retention/02-01-SUMMARY.md
@.planning/phases/02-index-sets-retention/02-U1-SMOKE.md
@.planning/phases/01-inputs-extractors/01-02-SUMMARY.md
@.planning/phases/01-inputs-extractors/01-04-SUMMARY.md

@src/tools/_shared/handler.js
@src/tools/_shared/conflict.js
@src/tools/_shared/schemas.js
@src/tools/index-sets/schemas.js
@src/tools/index-sets/index.js
@src/tools/inputs/schemas.js
@src/tools/inputs/create-input.js
@src/tools/inputs/update-input.js
@src/tools/inputs/update-extractor.js
@src/graylog/normalize.js
@src/tools.js
@test/index-sets.test.js
@test/schema-parity.test.js

<interfaces>
Strategy FQCN Map (RESEARCH.md D-08):
- "time-based"    cls: org.graylog2.indexer.rotation.strategies.TimeBasedRotationStrategy
                  configType: org.graylog2.indexer.rotation.strategies.TimeBasedRotationStrategyConfig
                  config: { rotation_period: ISO-8601 string, max_rotation_period?: ISO-8601, rotate_empty_index_set?: boolean }
- "size-based"    cls: org.graylog2.indexer.rotation.strategies.SizeBasedRotationStrategy
                  configType: org.graylog2.indexer.rotation.strategies.SizeBasedRotationStrategyConfig
                  config: { max_size: number(bytes >= 1) }
- "message-count" cls: org.graylog2.indexer.rotation.strategies.MessageCountRotationStrategy
                  configType: org.graylog2.indexer.rotation.strategies.MessageCountRotationStrategyConfig
                  config: { max_docs_per_index: number(>= 1) }
- "delete"        cls: org.graylog2.indexer.retention.strategies.DeletionRetentionStrategy
                  configType: org.graylog2.indexer.retention.strategies.DeletionRetentionStrategyConfig
                  config: { max_number_of_indices: number(>= 1) }
- "close"         cls: org.graylog2.indexer.retention.strategies.ClosingRetentionStrategy
                  configType: org.graylog2.indexer.retention.strategies.ClosingRetentionStrategyConfig
                  config: { max_number_of_indices: number(>= 1) }
- "archive"       REJECT with reason archive_not_supported (Enterprise plugin)

ISO_DURATION regex (RESEARCH.md): /^P(?!$)(\d+Y)?(\d+M)?(\d+W)?(\d+D)?(T(\d+H)?(\d+M)?(\d+(\.\d+)?S)?)?$/

Wire shape for POST /api/system/indices/index_sets (IndexSetCreationRequest):
- title, description, index_prefix, shards, replicas
- rotation_strategy_class (FQCN string), rotation_strategy { type: ConfigFQCN, ...config }
- retention_strategy_class (FQCN string), retention_strategy { type: ConfigFQCN, ...config }
- index_analyzer:"standard", index_optimization_max_num_segments:1, index_optimization_disabled:false
- field_type_refresh_interval:5000, writable:true, use_legacy_rotation:true
- creation_date: ISO-8601 (wrapper-filled via new Date().toISOString())

defineMutatingHandler (Plan 01 amended): spec destructures { name, schema, build, apply, summarize, requireConfirm? }; build(args) may be async; args._connectionName + args._conn threaded.
findExistingMatches (Plan 01 amended): unwraps index_sets envelope.
toIdBody from src/graylog/normalize.js: extracts id from full DTO response.

CreateInputSchema variantMap precedent (src/tools/inputs/schemas.js): top-level uses z.record(z.unknown()) for the polymorphic field, superRefine looks up the strict variant and re-paths issues. Phase 2 mirrors this pattern keyed on alias.

update_input precedent (D-03 strict no-echo): wire body built ONLY from args.changes; immutable envelope (type) sourced from current via pre-flight GET. agentConfig sentinel: null = key absent on wire; {} = empty object on wire; {...} = literal.
update_extractor precedent (merge-from-current): merged = { ...current, ...args.changes, extractor_type: current.extractor_type }; full body on wire.
</interfaces>

U1 DECISION INPUT: Read `.planning/phases/02-index-sets-retention/02-U1-SMOKE.md` (produced by Plan 01 Task 1). The U1 decision line drives this plan's update_index_set implementation:
- STRICT_NO_ECHO -> Implement D-12 strict-no-echo wire-build (Phase 1 update_input pattern). Wire body carries ONLY the agent-supplied changes keys plus the immutable envelope.
- MERGE_FROM_CURRENT -> Implement merge-from-current (Phase 1 update_extractor pattern). Pre-flight GET, apply changes over current, emit full merged body. C3 not reachable.
- UNREACHABLE_DEFAULT_MERGE -> Same as MERGE_FROM_CURRENT (safest fallback per RESEARCH.md §Pitfall U1).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strategies module + 6 strict zod configs + CreateIndexSetSchema with D-10 + D-08 + superRefine variant dispatch</name>
  <read_first>
    - .planning/phases/02-index-sets-retention/02-RESEARCH.md sections "Strategy FQCN Map", "Strategy Config Schemas", "IndexSetCreationRequest Shape"
    - src/tools/inputs/schemas.js (CreateInputSchema variantMap superRefine pattern)
    - src/tools/index-sets/schemas.js (current — only ListIndexSetsSchema + GetIndexSetSchema from Plan 01)
    - test/inputs.test.js (analog test patterns for superRefine variant rejection)
  </read_first>
  <behavior>
    - Test 1: CreateIndexSetSchema rejects missing rotation_strategy.
    - Test 2: CreateIndexSetSchema rejects missing retention_strategy_config.
    - Test 3: CreateIndexSetSchema rejects "archive" retention with reason archive_not_supported (at aliasToConfigOrError time).
    - Test 4: CreateIndexSetSchema rejects unknown rotation_strategy alias (closed z.enum).
    - Test 5: CreateIndexSetSchema rejects time-based config without rotation_period (superRefine narrows; issue path rotation_strategy_config.rotation_period).
    - Test 6: CreateIndexSetSchema rejects time-based config with rotation_period not ISO-8601.
    - Test 7: CreateIndexSetSchema rejects size-based config without max_size (issue path rotation_strategy_config.max_size).
    - Test 8: CreateIndexSetSchema rejects size-based config with max_size:0 (z.number().int().positive()).
    - Test 9: CreateIndexSetSchema accepts message-count rotation + close retention with valid configs.
    - Test 10: strategies.js buildRotationBlock("time-based", { rotation_period: "P1D" }) returns the exact 2-key wire block with both FQCNs.
    - Test 11: strategies.js buildRetentionBlock mirror for delete + close.
    - Test 12: strategies.js buildRotationBlock throws for unknown alias.
  </behavior>
  <action>
1. Create `src/tools/index-sets/strategies.js` exporting ROTATION_FQCN, RETENTION_FQCN, buildRotationBlock, buildRetentionBlock, aliasToConfigOrError. The exact alias-to-FQCN map is in `<interfaces>` above (5 entries; archive is the 6th absent-by-design entry).
   - buildRotationBlock(alias, agentConfig) returns `{ rotation_strategy_class: map.cls, rotation_strategy: { type: map.configType, ...agentConfig } }` and throws on unknown alias.
   - buildRetentionBlock symmetric for retention.
   - aliasToConfigOrError(kind, alias, agentConfig) is the safe wrapper: returns `{ ok: true, block }` on success OR `{ isError: true, reason: "archive_not_supported"|"unknown_strategy_alias", message }` on failure. kind is "rotation" or "retention".

2. Extend `src/tools/index-sets/schemas.js` (do NOT replace the existing ListIndexSetsSchema + GetIndexSetSchema from Plan 01). Add:
   - ISO_DURATION regex constant (exact regex in `<interfaces>` above).
   - 5 strict configs: TimeBasedConfig (rotation_period string regex ISO_DURATION required, max_rotation_period optional, rotate_empty_index_set optional default false), SizeBasedConfig ({ max_size: z.number().int().positive() }), MessageCountConfig ({ max_docs_per_index: z.number().int().positive() }), DeleteRetentionConfig ({ max_number_of_indices: z.number().int().positive() }), CloseRetentionConfig (same shape).
   - Export ROTATION_CONFIG_BY_ALIAS = { "time-based": TimeBasedConfig, "size-based": SizeBasedConfig, "message-count": MessageCountConfig }.
   - Export RETENTION_CONFIG_BY_ALIAS = { "delete": DeleteRetentionConfig, "close": CloseRetentionConfig }.
   - RotationAliasEnum = z.enum(["time-based", "size-based", "message-count"]).
   - RetentionAliasEnum = z.enum(["delete", "close", "archive"]) — "archive" intentionally in the enum so schema-level error wording can flag it; aliasToConfigOrError emits the structured reason at build time.
   - Export CreateIndexSetSchema = mutatingBase.extend({ ...required + optional fields per <interfaces> wire shape ... }).superRefine((args, ctx) => { lookup rotSchema = ROTATION_CONFIG_BY_ALIAS[args.rotation_strategy]; if exists, parse args.rotation_strategy_config and re-path issues under ['rotation_strategy_config', ...issue.path]; mirror for retention }).
   - Schema-required keys: title, description (optional default ""), index_prefix (z.string().min(1).regex(/^[a-z0-9_-]+$/)), shards (optional default 4), replicas (optional default 0), rotation_strategy (RotationAliasEnum), rotation_strategy_config (z.record(z.unknown())), retention_strategy (RetentionAliasEnum), retention_strategy_config (z.record(z.unknown())), index_analyzer (optional default "standard"), index_optimization_max_num_segments (optional default 1), index_optimization_disabled (optional default false), field_type_refresh_interval (optional default 5000), writable (optional default true), use_legacy_rotation (optional default true).

3. Append Tests 1-12 to `test/index-sets.test.js`. Tests 1-9 parse via `CreateIndexSetSchema.safeParse(payload)` and assert success boolean + issue path. Tests 10-12 call buildRotationBlock / buildRetentionBlock / aliasToConfigOrError directly with assert.deepEqual.

Test name convention: prefix with `create_index_set` so they group lexically. Example: `test("create_index_set rejects missing rotation_strategy (D-10)", ...)`.
  </action>
  <verify>
    <automated>node --test test/index-sets.test.js -t 'create_index_set' && grep -q "ROTATION_FQCN" src/tools/index-sets/strategies.js && grep -q "TimeBasedRotationStrategyConfig" src/tools/index-sets/strategies.js && grep -q "DeletionRetentionStrategyConfig" src/tools/index-sets/strategies.js && grep -q "archive_not_supported" src/tools/index-sets/strategies.js && grep -q "ISO_DURATION" src/tools/index-sets/schemas.js && grep -q "ROTATION_CONFIG_BY_ALIAS" src/tools/index-sets/schemas.js && grep -q "superRefine" src/tools/index-sets/schemas.js</automated>
  </verify>
  <done>strategies.js + schemas.js extended; 12 new tests pass; full npm test shows zero regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship create_index_set (INDEX-03) — defineMutatingHandler composing strategies + findExistingMatches + M5 idempotency</name>
  <read_first>
    - src/tools/inputs/create-input.js (Phase 1 analog — strict zod + findExistingMatches + dry-run preview shape)
    - src/tools/_shared/handler.js (after Plan 01 amendments)
    - src/tools/_shared/conflict.js (after Plan 01 amendment for index_sets envelope)
    - src/tools/index-sets/strategies.js (Task 1)
    - src/tools/index-sets/schemas.js (Task 1)
    - src/graylog/normalize.js (toIdBody helper)
    - .planning/phases/02-index-sets-retention/02-RESEARCH.md sections "IndexSetCreationRequest Shape", "Endpoint Catalogue" row 3, "Pitfall M2"
  </read_first>
  <behavior>
    - Test 1: create_index_set time-based+delete dry-run emits the exact wire shape — preview.body has `rotation_strategy_class === "org.graylog2.indexer.rotation.strategies.TimeBasedRotationStrategy"`, `rotation_strategy.type === "org.graylog2.indexer.rotation.strategies.TimeBasedRotationStrategyConfig"`, `rotation_strategy.rotation_period === "P1D"`, `retention_strategy_class === "org.graylog2.indexer.retention.strategies.DeletionRetentionStrategy"`, `retention_strategy.max_number_of_indices === 30`, and `postApplyEstimate.id === "__SERVER_ASSIGNED__"`.
    - Test 2: create_index_set size-based+close dry-run mirror — different FQCNs surface.
    - Test 3: create_index_set archive retention is rejected with isError + reason archive_not_supported in build (NOT at apply time).
    - Test 4: create_index_set existingMatches surfaces when a same-title index set exists — _setCaptureRequest returns `{ total: 1, index_sets: [{ id: "abc", title: "App errors" }] }` for the list call; dry-run payload has `existingMatches: [{ id: "abc", title: "App errors", similarity_reason: "exact" }]`.
    - Test 5: create_index_set apply path returns server-assigned id — _setCaptureRequest returns the full IndexSetResponse DTO; result.id matches raw.id, result.body matches raw.
    - Test 6: create_index_set defaults fill correctly — payload omits shards/replicas/index_analyzer; preview.body has shards:4, replicas:0, index_analyzer:"standard".
  </behavior>
  <action>
1. Create `src/tools/index-sets/create-index-set.js`. Skeleton (verify exact pattern against `src/tools/inputs/create-input.js`):
   - import defineMutatingHandler, CreateIndexSetSchema, ROTATION_FQCN/RETENTION_FQCN/aliasToConfigOrError, findExistingMatches, makeClient, toIdBody.
   - handleCreateIndexSet = defineMutatingHandler({ name: "create_index_set", schema: CreateIndexSetSchema, build, apply, summarize }).
   - build(args) is async:
     - Resolve `args._conn` to a client via makeClient.
     - Translate aliases via aliasToConfigOrError("rotation", args.rotation_strategy, args.rotation_strategy_config) and ("retention", args.retention_strategy, args.retention_strategy_config). If EITHER returns isError, throw a `GraylogValidationError` (or directly return a synthetic error envelope as done in delete_index_set — see RESEARCH.md Pattern 2) so the wrapper renders it as MCP error. ALTERNATIVELY, raise a typed error class.
     - Compose wireBody from required + defaulted fields, merging rotationBlock + retentionBlock. creation_date = new Date().toISOString() (deterministic via test seam — accept that the snapshot fixture in Plan 05 will need to inject a fixed clock OR mask creation_date as a known sentinel).
     - existingMatches = await findExistingMatches(client, { listPath: "/api/system/indices/index_sets", matchFn: (it) => it.title === args.title, similarityReason: "exact" }).
     - Return descriptor `{ method: "POST", path: "/api/system/indices/index_sets", body: wireBody, postApplyEstimate: { id: "__SERVER_ASSIGNED__" }, existingMatches, normalize: (raw) => toIdBody(raw, { idFields: ["id"] }) }`.
   - apply: `(client, req) => client.request(req.method, req.path, req.body)`.
   - summarize: `(args) => "Create index set " + args.title + " (" + args.rotation_strategy + " rotation, " + args.retention_strategy + " retention)"`.

2. Register in `src/tools/index-sets/index.js`: `import { handleCreateIndexSet } from "./create-index-set.js";` + `register("create_index_set", handleCreateIndexSet);`.

3. Append toolDefinitions entry to `src/tools.js` with description that documents the alias options (time-based / size-based / message-count + delete / close) and warns "archive is reserved for a future milestone — passing it returns a structured error". required: ["title", "index_prefix", "rotation_strategy", "rotation_strategy_config", "retention_strategy", "retention_strategy_config"]. inputSchema.properties enumerates EVERY zod field (including defaulted ones) so schema-parity holds.

4. Append Tests 1-6 to `test/index-sets.test.js`. Use `_setCaptureRequest` to script the list-call return value separately from the POST-call. Tests 1 + 2 + 6 set the list-call to return `{ index_sets: [] }` so existingMatches is empty. Test 4 sets list-call to return `{ index_sets: [{ id: "abc", title: "App errors" }] }`.

5. creation_date determinism: provide a `_setClockForTests(fn)` exported seam in create-index-set.js OR (preferred) inject the wrapper via `new Date().toISOString()` directly inline AND in the snapshot test, replace the creation_date value with a known sentinel via JSON patch BEFORE the snapshot is taken. Plan 05 will pin which approach. For Task 2's tests, use `_setClockForTests` and reset in afterEach.
  </action>
  <verify>
    <automated>node --test test/index-sets.test.js -t 'create_index_set' && grep -q "handleCreateIndexSet" src/tools/index-sets/create-index-set.js && grep -q "handleCreateIndexSet" src/tools/index-sets/index.js && grep -q "register(\"create_index_set\"" src/tools/index-sets/index.js && grep -q "create_index_set" src/tools.js && node --test test/schema-parity.test.js -t 'create_index_set' || (echo "Plan 05 will add schema-parity; OK if it fails today" && true)</automated>
  </verify>
  <done>create_index_set registered; ~6 new tests cover D-10/D-08/D-09/D-17/M5 + archive rejection + defaults + apply path; full npm test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship update_index_set (INDEX-04) — U1-resolved partial-update + D-11 atomic strategy-replace + ND2 default-must-be-writable pre-flight</name>
  <read_first>
    - .planning/phases/02-index-sets-retention/02-U1-SMOKE.md (U1 decision drives THIS task's implementation)
    - src/tools/inputs/update-input.js (STRICT_NO_ECHO precedent)
    - src/tools/inputs/update-extractor.js (MERGE_FROM_CURRENT precedent)
    - .planning/phases/02-index-sets-retention/02-RESEARCH.md sections "IndexSetUpdateRequest Shape", "Pattern 3 Strict no-echo", "Pitfall ND2"
    - src/tools/index-sets/strategies.js (Task 1)
    - src/tools/index-sets/schemas.js (Task 1 — append UpdateIndexSetSchema)
  </read_first>
  <behavior>
    - Test 1: UpdateIndexSetSchema rejects rotation_strategy without rotation_strategy_config (D-11; ctx.addIssue path changes.rotation_strategy_config).
    - Test 2: UpdateIndexSetSchema rejects rotation_strategy_config without rotation_strategy (D-11).
    - Test 3: UpdateIndexSetSchema rejects retention_strategy without retention_strategy_config (D-11).
    - Test 4: UpdateIndexSetSchema rejects empty changes object.
    - Test 5: update_index_set title-only change — preview body matches the U1 resolution (STRICT_NO_ECHO: only title + immutable envelope; MERGE_FROM_CURRENT: full merged DTO). Use a separate test that conditionally reads .planning/phases/02-index-sets-retention/02-U1-SMOKE.md OR (simpler) hard-code the test to expect the U1 outcome that the implementation actually ships.
    - Test 6: update_index_set strategy-replace (D-11 atomic) — `changes: { rotation_strategy: "size-based", rotation_strategy_config: { max_size: 1073741824 } }` → preview body has the new rotation_strategy_class FQCN + rotation_strategy.type FQCN + max_size.
    - Test 7: update_index_set ND2 pre-flight — current.default === true AND changes.writable === false → isError with reason default_index_set_must_be_writable; PUT never fired (_setCaptureRequest count for PUT method === 0).
    - Test 8: update_index_set immutable fields stripped — payload `{ changes: { title: "X", index_prefix: "new", creation_date: "2000-01-01T00:00:00Z" } }` → preview.body has NO index_prefix override (current value preserved) and NO creation_date override.
    - Test 9: update_index_set apply path returns full DTO + id — _setCaptureRequest returns full IndexSetResponse on PUT; result.id matches.
  </behavior>
  <action>
1. Append UpdateIndexSetSchema to `src/tools/index-sets/schemas.js`:
   - UpdateIndexSetSchema = mutatingBase.extend({ indexSetId: z.string().min(1), changes: z.object({ title?, description?, shards?, replicas?, writable?, rotation_strategy?, rotation_strategy_config?, retention_strategy?, retention_strategy_config?, index_optimization_max_num_segments?, index_optimization_disabled?, field_type_refresh_interval?, index_analyzer? }).refine non-empty.superRefine(D-11 pair-check + per-alias strategy_config narrowing — mirror of CreateIndexSetSchema's superRefine but scoped to changes.*) })
   - D-11 pair-check: if changes.rotation_strategy and not changes.rotation_strategy_config: ctx.addIssue path ["changes","rotation_strategy_config"]. Mirror inverse. Mirror for retention.
   - Per-alias narrowing: if changes.rotation_strategy is set AND its config is a record, look up ROTATION_CONFIG_BY_ALIAS[alias] and parse + re-path issues under ["changes","rotation_strategy_config",...issue.path].

2. Create `src/tools/index-sets/update-index-set.js`:
   - Branch on the U1 decision from 02-U1-SMOKE.md. The task's author reads that file FIRST and chooses ONE of the two implementation paths below. The other path is NOT shipped.

   **PATH A — STRICT_NO_ECHO (D-12 strict path):**
   - Pre-flight: `const current = await client.request("GET", "/api/system/indices/index_sets/" + args.indexSetId, null);` to source the immutable `index_prefix` + `creation_date` (re-emitted on every PUT per the Graylog AutoValue Builder shape).
   - ND2 pre-flight: if `current.default === true && args.changes.writable === false`, throw a typed validation error with reason default_index_set_must_be_writable.
   - Translate strategy blocks if changes includes either strategy (D-11 already validated the pair). Otherwise omit the strategy keys entirely.
   - Build wireBody from { agent-supplied changes only, plus { creation_date: current.creation_date, index_prefix: current.index_prefix } }. Strip changes.creation_date and changes.index_prefix BEFORE merging (immutable; D-12 says they never go on the wire as agent-supplied).
   - Return descriptor `{ method: "PUT", path: "/api/system/indices/index_sets/" + args.indexSetId, body: wireBody, postApplyEstimate: { id: args.indexSetId }, normalize: (raw) => toIdBody(raw, { idFields: ["id"] }) }`.

   **PATH B — MERGE_FROM_CURRENT (Phase 1 update_extractor pattern; the U1 fallback):**
   - Pre-flight current.
   - ND2 pre-flight (same as Path A).
   - Translate strategy blocks if changes includes either strategy.
   - Build mergedBody = { ...current, ...args.changes (excluding immutable index_prefix, creation_date), ...rotationBlock?, ...retentionBlock?, creation_date: current.creation_date, index_prefix: current.index_prefix }.
   - Same descriptor shape as Path A.

   - summarize: `(args) => "Update index set " + args.indexSetId + " (" + Object.keys(args.changes).length + " change(s))"`.

3. Register in `src/tools/index-sets/index.js`: + handleUpdateIndexSet.
4. Append toolDefinitions entry to `src/tools.js`. Description documents the U1-resolved behavior + D-11 atomic strategy-replace + the ND2 pre-flight. required: ["indexSetId", "changes"].
5. Append Tests 1-9 to `test/index-sets.test.js`. Tests 5 + 8 are the U1-shape-dependent ones; the task author writes them against the implemented path. Test 7 sets `_setCaptureRequest` to return `{ default: true, writable: true, ... }` on the GET and asserts the PUT was NEVER fired (count tracked via the capture function's call log).
  </action>
  <verify>
    <automated>node --test test/index-sets.test.js -t 'update_index_set' && grep -q "handleUpdateIndexSet" src/tools/index-sets/update-index-set.js && grep -q "register(\"update_index_set\"" src/tools/index-sets/index.js && grep -q "update_index_set" src/tools.js && grep -q "UpdateIndexSetSchema" src/tools/index-sets/schemas.js && grep -q "default_index_set_must_be_writable" src/tools/index-sets/update-index-set.js && grep -q "rotation_strategy_config is required when rotation_strategy is provided\|rotation_strategy is required when rotation_strategy_config is provided" src/tools/index-sets/schemas.js</automated>
  </verify>
  <done>update_index_set registered; UpdateIndexSetSchema exports; 9 new tests pass; ND2 pre-flight + D-11 atomic strategy-replace + immutable field strip + U1-resolved partial body all proven; full npm test green; tool count grows from 38 to 40.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> CreateIndexSetSchema | zod is the structural gate; alias enum is closed; per-alias config superRefine ensures shape correctness BEFORE FQCN translation runs |
| Build() pre-flight GET (update) -> Graylog | Pre-flight failures (404 / 403) bubble through wrapGraylogError (Phase 1 A4 async-build amendment) — the agent sees a structured error, not an unhandled rejection |
| strategies.js translator -> wire body | FQCN strings are static; agent CANNOT inject an FQCN directly (aliases are the only input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T (tampering) | Agent-supplied rotation_strategy | mitigate | Closed z.enum + alias-to-FQCN translator. Agent cannot inject an arbitrary FQCN like "org.attacker.MaliciousStrategy" because the alias enum rejects unknown values. |
| T-02-02-02 | E (elevation) | update_index_set ND2 pre-flight | mitigate | Default index set cannot be made non-writable. Pre-flight surfaces the would-be 409 before any PUT; the wrapper's apply() never runs. Defense in depth with Graylog server-side check. |
| T-02-02-03 | I (info disclosure) | findExistingMatches title scan | accept | List response carries only public index-set metadata; no creds. Phase 1 contract preserved. |
| T-02-02-04 | T (tampering) | Immutable index_prefix on update | mitigate | The wrapper strips agent-supplied index_prefix from the wire body and reasserts current.index_prefix — even if Graylog 7.0.6 ever accepted a partial body that omits it, an attacker cannot rename a victim's index prefix via update_index_set. |
| T-02-02-05 | D (denial of service) | superRefine variant dispatch | accept | superRefine runs in O(1) — one safeParse per strategy block. No agent-controllable loop bound. |
| T-02-02-06 | R (repudiation) | create_index_set audit trail | accept | Graylog server-side `@AuditEvent` emits an audit log entry on every successful create; tool description warns the agent. Out of MCP's hands; existing Phase 1 contract. |
</threat_model>

<verification>
1. `node --test test/index-sets.test.js -t 'create_index_set'` — ~6 create tests + 12 schema/strategy tests from Task 1 pass.
2. `node --test test/index-sets.test.js -t 'update_index_set'` — 9 update tests pass.
3. `grep -q "handleCreateIndexSet\|handleUpdateIndexSet" src/tools/index-sets/index.js` — both tools registered.
4. `grep -q "create_index_set\|update_index_set" src/tools.js` — both tool definitions present.
5. `node --test test/dispatch.test.js` — assertAllToolsRegistered OK (38 → 40 tools).
6. Full `npm test` shows no regression on the Phase 1 + Plan 02-01 baseline.
</verification>

<success_criteria>
- `src/tools/index-sets/strategies.js` exports the alias-to-FQCN map for 3 rotation + 2 retention strategies plus aliasToConfigOrError with archive_not_supported.
- `src/tools/index-sets/schemas.js` carries CreateIndexSetSchema + UpdateIndexSetSchema with D-10 + D-11 + per-alias superRefine narrowing.
- create_index_set fires findExistingMatches with the listPath /api/system/indices/index_sets and unwraps via the Plan 01 envelope amendment.
- update_index_set implements ONE of the two U1-resolved partial-update paths (STRICT_NO_ECHO or MERGE_FROM_CURRENT) per the 02-U1-SMOKE.md decision.
- update_index_set ND2 pre-flight surfaces the default-must-be-writable invariant in dry-run.
- Tool count grows from 38 to 40 (create_index_set + update_index_set).
- All new tests pass; npm test full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-index-sets-retention/02-02-SUMMARY.md` with:
- Frontmatter (phase, plan, subsystem, requires/provides/affects, key-files, key-decisions, requirements-completed: [INDEX-03, INDEX-04])
- The U1 decision that landed (STRICT_NO_ECHO vs MERGE_FROM_CURRENT) + the implementation path taken
- The strategies.js alias-to-FQCN map + the archive-not-supported decision
- The D-11 atomic strategy-replace contract surfaced via superRefine
- Test growth delta
</output>
