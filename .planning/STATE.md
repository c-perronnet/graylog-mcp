---
gsd_state_version: 1.0
milestone: v2.3
milestone_name: milestone
status: Ready to execute
last_updated: "2026-05-16T04:10:55.326Z"
progress:
  total_phases: 8
  completed_phases: 7
  total_plans: 41
  completed_plans: 39
  percent: 95
---

# Project Memory: Graylog MCP — Full Admin Surface

**Last updated:** 2026-05-16 (Plan 07-01 complete — HARD-01 tool-description audit + wholesale-fix pass: `scripts/audit-tool-descriptions.js` static auditor + `test/tool-description-audit.test.js` regression gate + `npm run audit:tool-descriptions` one-liner PR check; 79 descriptions in `src/tools.js` rewritten to ≤200 chars with discrimination sentence; safety-critical primitives preserved (dry-run defaults, confirm tokens, encrypted-field protection, schedule:false/deleteIndices:false inversions); internal mitigation IDs stripped; 1 .gitignore Rule-3 blocker auto-fixed; tool count unchanged at 90; tests 1045 → 1055; Phase 7 Plan 2 (HARD-02 list_admin_tools meta-tool) up next)

## Project Reference

- **Core value**: An AI agent can configure Graylog from intent alone, safely, without touching the web UI.
- **Source of truth**: `.planning/PROJECT.md`
- **Requirements**: `.planning/REQUIREMENTS.md` (71 v1 requirements across 9 categories)
- **Roadmap**: `.planning/ROADMAP.md` (8 phases, coarse granularity, sequential execution, phase-branching)
- **Research**: `.planning/research/SUMMARY.md` (synthesis), `STACK.md`, `FEATURES.md`, `ARCHITECTURE.md`, `PITFALLS.md`
- **Codebase context**: `.planning/codebase/ARCHITECTURE.md`, `STRUCTURE.md`, `CONVENTIONS.md`, `CONCERNS.md`

## Current Position

Phase: 07 (final-hardening) — EXECUTING
Plan: 2 of 3

- **Phase**: 6 — Dashboards, Widget Templates & Blueprints (foundation landed Plan 06-01; Dashboard CRUD landed Plan 06-02 — DASH-01..05 + DASH-07 ship the C7 ACCEPTANCE GATE; widget-template library + DASH-06 add_widget_from_template landed Plan 06-03 — M7 ACCEPTANCE GATE + DASH-08 finalized + Q3 TEXT_WIDGET_PLACEHOLDER for top_error_clusters; Blueprints A landed Plan 06-04 — BLUE-04/05/06 ship setup_pipeline_for_stream (variable-length N+2 chain) + setup_long_term_archival_index (1-step chain) + setup_debug_log_dropping (3-step chain with syslog inversion); Blueprints B landed Plan 06-05 — BLUE-01/02/03 close the wire-tool surface for Phase 6: BLUE-01 setup_app_monitoring_stack HEADLINE 6-step multi-domain chain with composite step 5 preserving C7 mitigation + custom apply walker for stream_id-key resolution + BLUE-02 setup_error_alerting 1-step error-rate event def + BLUE-03 create_app_health_dashboard thin wrapper over create_dashboard with 4 default widgets pre-wired; Plan 06-06 (snapshot freeze) is the final phase plan)
- **Plan**: 2 of 6 complete (Plan 06-02 ships the 6 dashboard CRUD tools — DASH-01 list_dashboards (narrow projection [id, title, summary, description] + Q1 WRAPPER_SIDE_TYPE_FILTER drops saved-searches even if upstream ?query=type:DASHBOARD leaks + response.views envelope unwrap per Pitfall 1), DASH-02 get_dashboard (full ViewDTO via plain async handler; 404 via wrapGraylogError; load-bearing for remove_widget pre-flight + update_dashboard GET-current overlay), DASH-03 create_dashboard (THE C7 ACCEPTANCE GATE — internal Search+View 2-step chain via executeChain; agent NEVER sees the intermediate Search ID; D-01 chain transcript surfaced via handler.js req.chain spread amendment; D-02 .strict() schema rejects agent-supplied searchId at zod parse; D-03 widget-position validator runs BEFORE the FOUND-11 existingMatches probe — orphan widget/position refuses with widget_position_integrity_violation and NO HTTP fires; wrapper-generated UUID widget IDs via randomUUID() + leading-underscore _setUUIDGeneratorForTests test seam; FOUND-11 existingMatches probe filters on view.type==="DASHBOARD" so saved-search title collisions don't count as duplicates), DASH-04 update_dashboard (STRICT_NO_ECHO partial-update with GET-current pre-flight + overlay covering title/description/summary only; UpdateDashboardSchema.changes is .strict() so searchId is rejected at parse — search-ID immutable post-creation; Pitfall 8 body.id matches URL; CreateEntityRequest envelope mirrors createDashboard), DASH-05 delete_dashboard (LEAF DELETE Phase 5 D-08 informational-cascade pattern — NO cascade-hash, NO confirmationToken, NO requireConfirm; informational cascades.widgets.count via best-effort GET pre-flight; 404/403/network failure falls through to count:0 and DELETE surfaces real error on apply), DASH-07 remove_widget (symmetric Search+View atomic two-step PUT chain — PUT /api/views/search/{searchId} FIRST stripping widget's search_types from queries[stateKey].search_types so View never references a removed search_type, then PUT /api/views/{id} stripping widget+position+widget_mapping entry; D-03 validator runs on prospective post-remove sets BEFORE wire emission; widget_not_found refusal + dashboard_missing_search_binding refusal before any PUT; Pitfall 9 acceptance — no transactional rollback; transcript surfaces failed_at_step). handler.js gained TWO amendments in Plan 06-02: req.chain spread onto dry-run preview JSON (reused by every BLUE-XX blueprint in Plans 06-04/05); _testConnection stripped from rawArgs BEFORE schema.parse so .strict() schemas don't reject the test seam (Rule 3 unblocker — landed inside Task 2 GREEN atomically). errors.js wrapGraylogError plain-Error reason surface (Rule 2 cross-cutting — surfaces [reason: <tag>] suffix + response.reason field for client-side plain Errors with structured reasons; widget_not_found, widget_position_integrity_violation, dashboard_missing_search_binding all get agent-programmatic identification parity with GraylogError instances). 42 net-new tests in test/dashboards.test.js covering all 6 tools including all C7/D-02/D-03 acceptance gates pinned by named tests. Closes DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-07 (6 of 8 phase 6 dashboard requirements; DASH-06 add_widget_from_template lands in Plan 06-03; DASH-08 closed by Plan 06-01's widget-template skeleton).
- **Status**: 1006 tests / 18 suites green (+26 net-new over Plan 06-04 baseline of 980 — all in test/blueprints.test.js: 12 BLUE-01 + 5 BLUE-02 + 5 BLUE-03 + 1 D-09 grep extended to all 3 + 3 schema-parity). All Phase 0/1/2/3/4/5 + Plan 06-01/02/03/04/05 contracts preserved. Tool count 87 → 90 (Plan 06-05 end; +setup_app_monitoring_stack +setup_error_alerting +create_app_health_dashboard). src/graylog/errors.js NOT modified in any Plan 06-05 commit. No new npm deps. BLUE-01 (HEADLINE) ships the 6-step multi-domain chain (~7 HTTP round-trips with composite step 5): createStream → createPipelineRule → createPipeline → connectToStream (ARRAY dependsOn) → createDashboard (composite body.chain with internal Search+View 2-step — C7 mitigation preserved inside the blueprint) → createEventDefinition (schedule=false). Custom apply walker handles two structural specialties beyond what executeChain provides: (a) step 1's `stream_id` response key (Phase 3 wire shape, NOT `id`) — resolveDottedPath walks `stepN.response.stream_id` directly against the transcript; (b) step 5's composite body.chain inline walking — creates Search FIRST, substitutes its id into View body's search_id via __SERVER_ASSIGNED__step5a, then creates View. T-06-05-01 mitigation: app_name regex-clamped to alphanumeric + underscore/hyphen at zod parse. Partial-failure transcript surfaces succeeded_steps + failed_at_step (pinned by test 10 with [1,2,3]+failed_at_step:4). BLUE-02 wraps events.createEventDefinition with agent-supplied notification_id, aggregation-v1 query 'level:>=4', schedule=false carry-forward. BLUE-03 is a thin convenience wrapper over create_dashboard's internal Search+View chain (Plan 06-02 DASH-03), pre-wiring 4 default widgets (error_rate_over_time, top_sources_by_volume, level_distribution, recent_events_table) bound to args.streamId. D-09 services-layer compose contract grep-pinned across all 6 BLUE-XX (BLUE-01..06): zero imports from src/tools/<domain>/<handler>.js; architectural-boundary `void serviceFn` markers preserve import as audit signal. 3 atomic per-task commits (1c24d26 BLUE-01, d8fab4b BLUE-02, c70f6f2 BLUE-03) — each lands a fully-green suite with tool count assertions bumping at commit boundaries (87→88→89→90). 14 Phase 6 net-new tools registered (8 DASH + 6 BLUE) — assertAllToolsRegistered passes against 90 tools.
- **Progress bar**: `[██████████] 97%` (37 of 38 milestone plans complete after 06-05)

## Performance Metrics

| Metric | Value |
|--------|-------|
| v1 requirements | 71 mapped / 71 total |
| Phases | 0 complete / 8 total |
| Plans complete | 37 |
| Net-new tools target | ~64 (58 CRUD primitives + 6 blueprints) |
| Total MCP surface at milestone end | ~91 tools |
| Phase 00-foundation P01 | 2min | 2 tasks | 13 files |
| Phase 00-foundation P02 | ~15 min | 2 tasks | 12 files |
| Phase 00-foundation P03 | ~3 min | 2 tasks | 6 files |
| Phase 00-foundation P04 | ~7min | 2 tasks | 13 files |
| Phase 00-foundation P05 | ~26 min | 3 tasks | 13 files |
| Phase 00-foundation P06 | ~10 min | 3 tasks | 11 files |
| Phase 01-inputs-extractors P01 | ~6min | 2 tasks | 16 files |
| Phase 01 P02 | ~8min | 2 tasks | 8 files |
| Phase 01 P03 | ~2 min | 1 tasks | 6 files |
| Phase 01-inputs-extractors P04 | ~6 min | 2 tasks | 8 files |
| Phase 02-index-sets-retention P01 | 10min | 4 tasks | 15 files |
| Phase 02 P02 | 10min | 3 tasks | 8 files |
| Phase 02-index-sets-retention P03 | ~10 min | 2 tasks | 9 files |
| Phase 02-index-sets-retention P04 | ~6 min | 2 tasks | 6 files |
| Phase 03 P01 | ~14 min | 3 tasks | 13 files |
| Phase 03-streams-stream-rules P02 | 8min | 3 tasks | 7 files |
| Phase 03-streams-stream-rules P03 | ~5 min | 1 tasks | 5 files |
| Phase 03-streams-stream-rules P04 | ~11 min | 2 tasks | 9 files |
| Phase 04-pipelines-pipeline-rules-connections P01 | ~25min | 1 tasks | 9 files |
| Phase 04 P02 | ~10 min | 2 tasks | 11 files |
| Phase 04-pipelines-pipeline-rules-connections P03 | 11min | 2 tasks | 8 files |
| Phase 04 P05 | ~6 min | 1 tasks | 7 files |
| Phase 04-pipelines-pipeline-rules-connections P04 | 11 min | 2 tasks | 5 files |
| Phase 05-events-notifications P01 | 11min | 3 tasks | 9 files |
| Phase 05-events-notifications P02 | 10min | 3 tasks | 9 files |
| Phase 05-events-notifications P03 | 7min | 2 tasks | 8 files |
| Phase 05-events-notifications P04 | 17min | 3 tasks | 10 files |
| Phase 05-events-notifications P05 | 4min | 4 tasks | 3 files |
| Phase 06 P01 | 12min | 4 tasks | 15 files |
| Phase 06 P02 | ~12min | 4 tasks | 14 files |
| Phase 06 P03 | ~8 min | 2 tasks | 16 files |
| Phase 06-dashboards-widgets-blueprints P04 | 15min | 3 tasks | 10 files |
| Phase 06-dashboards-widgets-blueprints P05 | 14min | 3 tasks | 10 files |
| Phase 06 P06 | 10min | 4 tasks | 8 files |
| Phase 07-final-hardening P01 | ~19min | 2 tasks | 5 files |

## Accumulated Context

### Decisions Locked

Drawn from `PROJECT.md` Key Decisions table — restated here for quick reference:

- **Single Graylog version target**: 7.2.0-SNAPSHOT (no multi-version branching)
- **Auth model**: Reuse existing connection registry + API token; insufficient permissions surface as Graylog 403
- **Dry-run safety**: Per-call `dryRun: true` default on every mutating tool — applying without explicit `dryRun: false` is a bug
- **Two tool layers**: CRUD primitives + blueprints
- **Pipeline-rule DSL**: Agent emits `when … then …` source; helpers validate client-side before round-trip
- **Dashboard widgets**: Curated templates only (no arbitrary widget construction this milestone)
- **Validation**: Adopt existing `zod ^3.25.76` (do not upgrade to v4)
- **Code organization**: Per-domain extraction under `src/tools/<domain>/`
- **Source of REST shape**: Java REST resource classes in `source-code/graylog2-server/` (not Swagger)
- **Read tools**: Verify-against-v7 only; no refactors

### Decisions Made During Execution

- **Plan 00-01 (test harness bootstrap)**:
  - `setResolveSnapshotPath` redirects snapshots to `test/__snapshots__/` instead of node:test's default sibling-file location (honors D-06; keeps test tree readable).
  - `scripts.test` glob is single-quoted (`node --test 'test/**/*.test.js'`) so Node — not bash — performs the expansion. Without quoting bash matches only one path and breaks the runner. Treated as a Rule 1 bug fix on top of Task 1's value.
  - `@types/node` bumped to `^22.0.0` so devDep types align with the new engine floor (`>= 22.3.0`).

- **Plan 00-02 (test-existing migration)**:
  - Per-suite `mkdtempSync(join(tmpdir(), <prefix>))` + after-hook cleanup (not a single shared temp dir) for every describe block that calls `_withStorePathOverride` — node:test runs suites concurrently and the override is process-global, so sharing the path would race.
  - Swallowed `try { ... } catch (error) { console.error(...) }` in `test-features.js` removed during migration — let node:test reporter fail red on broken assertions rather than log-and-zero-exit (RESEARCH.md Q8).
  - Synced `package-lock.json` to Plan 01's `package.json` bumps (Rule 3 blocking fix). Plan 01 bumped `@types/node ^22` + `engines.node >= 22.3.0` without running `npm install`; axios was missing from `node_modules` and the migrated tests couldn't import production code. Committed as a separate `chore(00-02): sync package-lock.json` so the lockfile churn didn't muddy the test-migration commits.
  - Deleted (not archived) the 4 root-level scripts — git history is the archive.

- **Plan 00-03 (graylog-client extraction)**:
  - Status-code → typed-error mapping is tested via direct `mapGraylogError()` calls, NOT via `mock.module()`. Node 22's `mock.module()` requires `--experimental-test-module-mocks` and is brittle across 22.x patch versions. The `_setCaptureRequest` seam tests prove `makeClient.request` reaches the classifier; the direct `mapGraylogError` tests prove the classifier produces the right typed subclass. Plan `<action>` explicitly authorised this fallback.
  - `src/query.js` is NOT modified. `searchGraylog` and `fetchStreams` stay as-is; the new `makeClient` is purely additive. Migration of existing read tools to `makeClient` is deferred to per-domain phases (Phase 3+). Phase 0's job is to land the primitive, not retrofit.
  - D-07 / Pitfall 4 client-layer defense-in-depth: when `conn.writable === false`, every non-GET request is refused BEFORE axios is reached, throwing `GraylogError(status: 0)` with a "read-only" message. Complements (does not replace) the wrapper-layer check in Plan 04.
  - `_setCaptureRequest` / `_clearCaptureRequest` are exported (not module-internal toggles) so tests can import them cleanly; the `_` prefix plus an explicit test-only comment block in `client.js` flags production-misuse risk. `afterEach(() => _clearCaptureRequest())` is the canonical reset pattern for any test file mutating the seam.
  - `GraylogError` constructor accepts a default empty options object (`({ status, method, path, body } = {})`) so callers that throw the base class without remembering to pass ctx still get a well-formed instance rather than a destructuring TypeError.

- **Plan 00-04 (handler primitives + defineMutatingHandler / defineListHandler)**:
  - `_testConnection` seam re-merged from pre-zod `rawArgs` inside both factory wrappers — `mutatingBase` / `listBase` deliberately omit `_testConnection` so zod's default `strip` mode drops it from agent payloads (threat-model T-00-04-05 — agent cannot bypass connection lookup at runtime). Inside the wrapper we read it from `request.params.arguments` before validation and merge it onto the parsed args before `resolveConnection` consumes them. Net effect: production safe (no agent path puts `_testConnection` on the wire), tests work (seam still reachable from `node:test`). Discovered during Task 2 GREEN as a 14-test cascade failure; fixed in the same GREEN commit.
  - Added `_setConnectionsForTests` + `_clearConnectionsForTests` to `src/config.js` as an underscore-seam (test-only, project convention). `getConnections()` and `getActiveConnectionConfig()` branch on the override ONLY when set, preserving existing behaviour. Chose this over Node 22 `mock.module()` for the same reason Plan 03 did (experimental flag + flakiness).
  - D-07 two-layer defense confirmed: `handler.js` short-circuits `writable === false` BEFORE `build`/`apply` with `reason: "connection_read_only"`; `client.js` (Plan 03) refuses non-GET BEFORE axios with `GraylogError(status: 0)`. Either layer alone catches; both layers together cover bypass paths (e.g. a future service-layer call going directly through `makeClient` without `defineMutatingHandler`).
  - Tool-name-agnostic error wording in `resolveConnection` ("Use the active-connection setter first ..." rather than hardcoded `use_connection` / `set_active_connection`) — survives Plan 05's rename without churn.
  - `wrapGraylogError` truncates response body to 200 chars in the error message — Graylog 4xx bodies (full validation trees) would otherwise blow out the MCP response and the agent's context window.

- **Plan 00-05 (dispatch Map refactor + tool rename)**:
  - Extracted 17 v2.3 read-tool handlers from inline `src/index.js` bodies into `src/handlers.js` (Rule 3 blocker fix — top-level `await server.connect(transport)` in `src/index.js` hangs node:test imports). `src/index.js` re-imports handlers from there, both for the if-chain (Task 1) and via `src/tools/_register.js` (Task 2). Plan-anticipated via the circular-import contingency note; trigger turned out to be top-level await rather than TDZ.
  - `dispatch()` declared `async` so `Tool not found: <name>` throws become rejecting promises. Contract is `Promise<MCPResponse>`; sync throws don't satisfy `await dispatch(...)` at the SDK call site or `assert.rejects(() => dispatch(...))` in tests. Caught by 3 failing dispatch unit tests on first pass; one-character fix (`function` → `async function`).
  - Internal handler function names retain OLD camelCase per RESEARCH.md Q9 closing rationale. `useConnectionHandler` backs `set_active_connection`; `fetchGraylogMessagesHandler` backs `search_messages_graylog`. The dispatch Map decouples external names from internal symbols. Internal-name churn is a follow-up cleanup; Phase 0 keeps blast radius small.
  - Hard rename, no aliases per D-03. CHANGELOG.md is the single migration-pointer document; v3.0.0 marker is staged (CHANGELOG-only); `package.json.version` stays at `2.3.0` until milestone end per D-04. `dispatch({ name: "fetch_graylog_messages" })` rejects with `Tool not found: fetch_graylog_messages` — exact pattern match with the legacy if-chain's error.
  - Error-message text `Use 'use_connection' first` → `Use 'set_active_connection' first` (Rule 1 fix in `src/handlers.js`, `src/tools/cluster-errors.js`, `src/tools/template-mgmt.js`, plus 4 description texts in `src/tools.js`). The old text would mislead agents (Tool not found if followed). Single-line snapshot update captures the new text; every other regression-snapshot entry is byte-identical — plan-anticipated as "only the embedded tool-name strings ... differ from the baseline".
  - Snapshot fixtures live under `test/regression/__snapshots__/` (not `test/__snapshots__/` as the plan predicted) because Plan 01's `setResolveSnapshotPath` uses `dirname(testFilePath)`. Co-located with the test file; functionally identical to the plan's target location.
  - `src/index.js` shrunk from 904 lines (903-line dispatcher) to 32 lines (transport wiring + module-init `assertAllToolsRegistered`). Module-init assertion fails loudly if any tool in `tools.js` lacks a registered handler (Discretion-06).

- **Plan 00-06 (snapshot fixtures + auth-redaction + schema-parity scaffold)**:
  - Snapshot fixtures store the JSON-parsed payload (`t.assert.snapshot(JSON.parse(res.content[0].text))`), not the raw text. The parse-then-snapshot pattern is robust to future refactors of `JSON.stringify` key-order in the wrapper; the resulting `.snapshot` file is also more human-readable because node:test's default serializer normalises indentation and key order.
  - 10 fixtures across 4 test files (handler 4, list 3, normalize 1 combined, dispatch 2) all use static-only args — no `Date.now()`, no `randomUUID()`, no machine-state. Idempotency key `c2563630c56b18bf5dfdb3bda76bea97` is the sha-256-truncated hash of canonicalised `{ connectionName: "fixture_conn", toolName: "create_stream", args: { title: "Snapshot fixture stream" } }`, deterministic across developer machines.
  - Two consecutive `npm test` runs produce byte-identical md5sums of all 4 snapshot files (`06914d2eec3ab4a9f095d6e0f60efe55  list`, `6be9b517c8d9393e2ec637e6afa71aa4  handler`, `e05deb67dad4b96957e4f3d8edd5946e  normalize`, `e45b695d78a41608861711b4db15227d  dispatch`). FOUND-07 determinism contract provably met.
  - `test/auth-redaction.test.js` uses a context-aware allowlist (`isAllowedMatch`) to distinguish idempotency-key field values (32-hex sha-256 truncations) from genuine apiToken leaks. Both are 32+ alphanumeric chars and match the same regex; the allowlist inspects the ~40 chars preceding each match and whitelists only matches whose context is `idempotencyKey":` (or escape-variant). Self-maintaining — new snapshots inherit the same guard automatically.
  - `test/schema-parity.test.js` Phase 0 baseline asserts only `mutatingBase.shape` keys = `["connectionName", "dryRun", "idempotencyKey"]` and `listBase.shape` keys = `["connectionName", "fields", "limit"]`. Commented enrichment template documents the `assertSchemaParityForTool(toolName, zodSchema)` pattern that Phase 1+ must extend when shipping per-domain mutating tools.
  - Synthetic-fixture sanity check: dropped `"Authorization": "Bearer xyz"` plus a 40-char alphanumeric into a tampered snapshot file under `/tmp/test_auth_check/__snapshots__/` and ran the auth-redaction test; confirmed it correctly fires with 2 violations listed. The allowlist does not over-allow.
  - Zero deviations from plan. RED→GREEN sequence clean: Task 1 RED at `422f942` (ERR_INVALID_STATE on missing snapshots); GREEN at `ab6c628` after `--test-update-snapshots`; Task 2 at `2f643e0` (stub-replace, not strict RED/GREEN — both stubs already trivially passed); Task 3 at `e6bfb72` (VALIDATION.md frontmatter flip).

- **Plan 01-01 (foundation amendments + read-only input tools)**:
  - **A4 amendment (await build)**: `defineMutatingHandler` now wraps `const req = await build(args)` in a try/catch routed through `wrapGraylogError(err, name)`. The plan contracted only the `await` widening, but the existing wrapper had no protection against a build rejection — once `build()` can do async pre-flight reads (Plan 02's update_input GETs current input + cached type catalogue inside build), a 404 from the preflight would crash the test runner with an unhandled rejection. The minimal extension surfaces it as an MCP error envelope instead. Test 2 ("rejected promise") asserts `res.isError === true`; the try/catch is what makes that test green.
  - **A2 amendment (real findExistingMatches)**: Phase 0 stub replaced. Accepts `{ listPath, matchFn, similarityReason? }`; fires `client.request("GET", listPath, null)`; normalizes envelope shapes (`inputs ?? streams ?? extractors ?? items ?? bare array`); projects `{ id, title, similarity_reason }`. `similarityReason` is either a literal string (defaults to `"exact"`) or a per-item function. Back-compat preserved: empty opts OR missing matchFn return `[]` without firing the GET.
  - **BLOCKER #3 fix (defaultFields override)**: `defineListHandler` accepts optional `spec.defaultFields`. When provided AND `args.fields` is absent, the override wins over the framework `DEFAULT_FIELDS`. Existing list tools (Phase 0) that don't pass `defaultFields` see no change — Test 20 specifically pins the back-compat. `list_inputs` uses it to narrow to `[id, title, type, global]` (D-M6 rationale: description is rarely set on inputs; type and global are the most filterable fields for an agent).
  - **D-06 type-catalogue cache**: `src/tools/inputs/type-catalogue.js` exports `getCachedTypeCatalogue(connectionName, conn)` backed by a module-level `Map<connectionName, { fetchedAt, catalogue }>`. One GET per connection per process. `_clearTypeCatalogueForTests()` resets for test isolation. `getEncryptedFieldNamesForType(catalogue, typeFQCN)` returns a Set of fields with `is_encrypted: true` — ready for Plan 02's C3 mitigation. Threat-model T-01-01-03 honored: cache keyed by connectionName, so connection A's catalogue can never reach connection B.
  - **`_connectionName` + `_conn` pass-through in defineListHandler fetch**: Plan suggested calling `resolveConnection(seamArgs)` a second time inside `list-input-types.js`'s fetch to feed the cache. Cleaner: thread the already-resolved `connectionName` + `conn` through fetch's args under leading-underscore framework-internal keys. Matches the `_testConnection` seam convention (underscore = framework internal, never agent input).
  - **Per-domain module layout proven end-to-end**: `src/tools/inputs/` now contains `schemas.js` + 3 handlers + the cache + `index.js` registration barrel. `_register.js` imports the barrel via a single side-effect line (`import "./inputs/index.js"`). Plans 02-04 (create / update / delete + extractors) extend `schemas.js` and add new handlers under the same module.
  - **3 read-only tools** (INPUT-01 list_input_types, INPUT-02 list_inputs, INPUT-03 get_input) registered. `assertAllToolsRegistered(toolDefinitions)` returns OK. `list_input_types` is the first list tool to consume the per-tool cache pattern; `list_inputs` is the first to use `defaultFields`; `get_input` is a plain async handler (single DTO, not a list — wouldn't survive the framework's narrow projection).
  - **Test growth**: 153 → 177 (+24). Focused run = 49/49 pass; full `npm test` = 177/177 pass; zero regressions on the Phase 0 baseline. Commits: `c05817d` (Task 1 RED, 9 failures across 5 files), `1b9504e` (Task 2 GREEN, all 24 new tests pass + 153 baseline preserved).
  - **Plan 02 hand-off**: `update_input` can now compose `getCachedTypeCatalogue` + `getEncryptedFieldNamesForType` inside its `build()` for C3 mitigation. `create_input` can wire `findExistingMatches({ listPath: "/api/system/inputs", matchFn })` for list-before-create idempotency. The `cascades-forwarding amendment` on `delete_input` and the `assertSchemaParityForTool` enrichment of `test/schema-parity.test.js` are owed by Plan 02 (no mutating-tool schemas yet exist to enrich; this plan was read-only).

- **Plan 01-02 (input CRUD + C3 mitigation centerpiece)**:
  - **C3 mitigation + D-03 STRICT NO-ECHO landed**: `update_input` wire `configuration` is built ONLY from `args.changes.configuration` — no copy-from-current loop. The prior plan iteration's "iterate current.configuration and copy non-encrypted entries" approach is gone. Graylog's PUT deserializer preserves unspecified fields server-side via `EncryptedInputConfigs.merge` so no data is lost. Encrypted fields the agent did not pass are NEVER on the wire (automatic consequence). Verified via Test 27: `update_input` no-op against TLS GELF TCP with `changes:{configuration:{port:12202}}` → wire `configuration` has `config_key_count===1`, no `tls_key_password`, no `bind_address`.
  - **agentConfig sentinel**: `null` (agent did not touch config) → configuration key omitted entirely from wire body. `{}` (agent passed empty) → `configuration:{}` emitted. `{...}` (agent passed specific) → emitted as-is. Three shapes pinned by Tests 27/28/29; satisfies ROADMAP success criterion 2 ("only the changed field").
  - **D-04 redaction landed**: `create_input` + `update_input` dry-run previews show encrypted fields as `<redacted>`. Apply body wraps explicit string values as `{ set_value: "<plaintext>" }` via `encodeEncryptedForWire`. `_applyBody` sibling on the RequestDescriptor — handler.js stays body-agnostic; per-tool internals carry the preview/apply asymmetry. Test 20 verifies the literal `"supersecret"` never appears in `JSON.stringify(payload)`.
  - **D-05 cascade enumeration landed**: `delete_input.build()` pre-flights `GET /api/system/inputs/{id}/extractors`; `cascades.extractors[{id,title,extractor_type}]` surfaced in dry-run preview. Best-effort: 404/403 on the pre-flight yields empty cascade list, not failure. handler.js owns the one-line spread `...(req.cascades ? { cascades: req.cascades } : {})` in the preview emitter — opt-in, no behavior change for tools that don't set the field.
  - **BLOCKER #1 fix landed**: handler.js cascades forwarding declared in this plan's `files_modified`. Single-line additive amendment between `existingMatches` and `applyHint` in the dry-run JSON.
  - **WARNING #9 fix landed**: `GelfHttpConfig` ships strict — `NettyBase + TlsConfigExt + HTTP-codec fields` (idle_writer_timeout, max_chunk_size, enable_cors, additional_headers, decompress_size_limit). Wired into `variantMap` so GELF HTTP no longer falls through to generic `z.record(z.unknown())`. D-01's GELF-family commitment now end-to-end with UDP/TCP/HTTP all strict.
  - **8 strict input variants total**: GELF UDP/TCP/HTTP, Beats2, Syslog UDP/TCP, Raw UDP/TCP. Generic fallback for AWS / CEF / Kafka / OpenTelemetry / etc. preserved at the top level.
  - **superRefine variant dispatch**: `CreateInputSchema` accepts generic `z.record(z.unknown())` for `configuration`; `superRefine` looks up the per-FQCN strict variant in `variantMap` and re-paths its issues under `['configuration', ...issue.path]`. Adding a new strict variant is a one-line registration in `variantMap`. Pattern reusable for `create_extractor` (Plan 04 — keyed on `extractor_type`).
  - **`_connectionName` + `_conn` pass-through in `defineMutatingHandler.build()`**: Generalizes Plan 01-01's equivalent pattern in `defineListHandler.fetch()`. handler.js calls `build({ ...args, _connectionName, _conn })`. Build callbacks read those keys directly — no duplicate `resolveConnection` call inside each handler. **Found during Task 2 GREEN**: original plan said `build()` would call `resolveConnection(seamArgs)` itself, but zod's `strip` mode removes `_testConnection` from `args` so the seam was unreachable inside build. The handler.js pass-through is the minimal additive fix; existing handlers that don't read `_connectionName` / `_conn` see no change.
  - **`REDACTION_PLACEHOLDER = "<redacted>"`**: Discretion-03 resolved. ASCII-safe (no Unicode normalization in snapshots or grep); self-documenting in JSON. Single constant in `redact.js`.
  - **Test growth**: 177 → 195 (+18 net-new). Focused run = 24/24 pass; full `npm test` = 195/195 pass; zero regressions. Commits: `d1ba1f1` (Task 1 RED, ERR_MODULE_NOT_FOUND), `1e7520b` (Task 2 GREEN, all 18 pass + 177 baseline preserved).
  - **Plan 03-04-05 hand-off**: `_applyBody` sibling pattern + strict-no-echo wire-build available for `update_extractor` (D-09 reuses this contract). `findExistingMatches` per-input scoped is wired for `create_extractor` (Plan 04). The C3 + D-03 + D-04 + D-05 acceptance gates are pinned as ad-hoc tests now; Plan 05 lands them as byte-identical snapshot fixtures + `assertSchemaParityForTool(create_input/update_input/delete_input)` enrichment of `test/schema-parity.test.js`.

- **Plan 01-03 (start_input + stop_input lifecycle — D-08)**:
  - **Lifecycle-as-mutation contract end-to-end**: `start_input` and `stop_input` compose through `defineMutatingHandler` exactly like CRUD tools. dryRun: true defaults + writable-flag gate (wrapper + client defense-in-depth) + idempotency-key auto-derivation inherit from `mutatingBase`. No special-cased "runtime-only" path; D-08 in action.
  - **Verb-mapping contract verified**: `start_input` → PUT /api/system/inputstates/{inputId}; `stop_input` → DELETE on the same path. Both verified by `_setCaptureRequest` seam tests (Tests 38 + 40). The counter-intuitive verb mapping is documented in tools.js descriptions ("Maps to PUT/DELETE …") so the agent never has to know the underlying REST quirk.
  - **Eventually-consistent semantics surfaced in tool descriptions**: both descriptions warn "This sets the DESIRED state; actual state may briefly remain STARTING/STOPPING until Graylog's input registry converges. Poll get_input if you need to wait for RUNNING/STOPPED." The agent gets explicit permission slip to add a poll loop after start/stop calls (RESEARCH.md §Pitfall Start/Stop Are Eventually Consistent).
  - **`postApplyEstimate.id = args.inputId` (NOT __SERVER_ASSIGNED__)**: lifecycle tools are not create-shaped; the ID already exists. Setting the estimate to the known ID lets a blueprint author chain start_input → get_input(inputId) without re-reading args.inputId.
  - **Schemas `StartInputSchema` + `StopInputSchema`**: both = `mutatingBase.extend({ inputId })`. ONLY inputId required (no extraneous lifecycle state args). Discretion-04 (whether to add a `state` arg) resolved cleanly — D-08 says no.
  - **Per-tool-per-file convention reinforced**: start-input.js + stop-input.js join create-input.js / update-input.js / delete-input.js as the established pattern. The two handlers differ only in the HTTP verb string and the summarize callback's verb word, but separating them mirrors the per-tool-per-file convention used by the CRUD tools. Future maintainers see a one-to-one mapping between tool name and file.
  - **`body: undefined`**: build returns `body: undefined` for both lifecycle tools (empty-body verbs). `JSON.stringify({ body: undefined })` produces `"{}"` (key omitted), so the dry-run preview JSON omits the `preview.body` key — observed in tests via `payload.preview.body === undefined`. The apply callback passes `req.body` (undefined) to `client.request`; axios treats undefined as "no body". Per-tool-no-special-casing pattern established for empty-body verbs.
  - **Test growth**: 195 → 203 (+8 net-new). Focused run = 32/32 pass; full `npm test` = 203/203 pass; zero regressions. Commits: `968cdc4` (Task 1 RED, ERR_MODULE_NOT_FOUND), `4e40a2e` (Task 1 GREEN, all 8 pass + 195 baseline preserved).
  - **Plan 04 hand-off (extractors)**: per-tool-per-file convention proven across 5 inputs files (create / update / delete / start / stop); Plan 04 lands 4 more (list / create / update / delete extractor) under the same convention. Lifecycle-as-mutation pattern is reusable for any future runtime-flag tool (e.g. `enable_pipeline`, `pause_event_definition` in later phases) — defineMutatingHandler composition + `postApplyEstimate.id = args.<resourceId>` + `body: undefined` for empty-body verbs.

- **Plan 01-04 (extractor CRUD + D-07 reconfirmation centerpiece)**:
  - **D-07 reconfirmation centerpiece landed**: all 8 Graylog 7.0.6 primitive extractor types ship under STRICT zod schemas — `grok, regex, regex_replace, split_and_index, substring, copy_input, json, lookup_table`. The original 01-CONTEXT.md draft named 6 types and listed "key-value" as a separate primitive; RESEARCH.md A1 surfaced the 6-vs-8 ambiguity (Graylog's actual Extractor.Type enum has 8 entries, no key_value); the user reconfirmed D-07 with the 8-type enumeration during plan-phase 1. `ExtractorTypeEnum = z.enum([...8])` is a closed set — zod's strip mode rejects "key_value", "bogus_type", and any non-Graylog value at parse time before build() runs (T-01-04-01 mitigation). Test "create_extractor zod rejects unknown extractor_type — 'key_value' (D-07 reconfirmation: NOT a real Graylog primitive)" pins the contract.
  - **"key-value → json" mapping documented in 3 places (BLOCKER #4 acceptance)**: (1) `src/tools.js` create_extractor description with the explicit "NO 'key_value' primitive — use extractor_type='json' with kv_separator + key_separator + flatten:true" note; (2) a multi-line comment above `ExtractorConfigJson` in `src/tools/inputs/schemas.js` carrying the same mapping; (3) the 01-04-SUMMARY.md §D-07 Reconfirmation Narrative as the executive-level reference. Test "create_extractor json with kv_separator (the 'key-value flattening' path is the json extractor — D-07 mapping)" exercises the kv config end-to-end.
  - **superRefine variant dispatch keyed on extractor_type**: same pattern as Plan 02's CreateInputSchema (keyed on type FQCN). `CreateExtractorSchema` accepts a generic `z.record(z.unknown())` for `extractor_config`, then superRefine looks up the strict per-type variant in `EXTRACTOR_TYPE_TO_CONFIG` and re-paths its issues under `['extractor_config', ...issue.path]`. Adding a new strict variant (if Graylog 7.3 ships a 9th primitive) is a one-line registration in the map. Test "create_extractor zod rejects missing extractor_config for grok (grok_pattern required)" pins the per-type drilldown.
  - **D-09 partial-update reuse for update_extractor**: same shape as `update_input` (D-03 partial-update). Pre-flight GET on the current extractor; merge envelope built from `args.changes` overriding `current.*`. `extractor_type` is immutable — enforced at BOTH the schema layer (`UpdateExtractorSchema.changes` does not contain extractor_type) AND the build layer (`extractor_type: current.extractor_type` unconditionally). Defense in depth — structural enforcement, not just runtime rejection. Note: merge-from-current (NOT strict no-echo) is acceptable for extractors because RESEARCH.md verified that NONE of the 8 primitive extractor_config types contain encrypted fields — the C3 pitfall is not reachable here.
  - **D-09 leaf-delete for delete_extractor**: build returns a descriptor WITHOUT a `cascades` key. handler.js's preview emitter spreads cascades only when `req.cascades` is truthy (Plan 02 BLOCKER #1 amendment), so the no-cascade contract is provable by ABSENCE. Test "delete_extractor issues DELETE single-target with NO cascade enumeration (D-09)" asserts `payload.cascades === undefined`. Establishes the leaf-delete pattern reusable for any future delete tool whose target has no child resources (pipeline-rule delete, individual widget delete in Phase 6).
  - **toIdBody hint ['extractor_id', 'id']**: Graylog's `POST /api/system/inputs/{inputId}/extractors` returns `{extractor_id: '...'}`, NOT `{id: '...'}` like POST /api/system/inputs. The explicit hint with extractor_id FIRST and id as a back-up handles either shape. RESEARCH.md §"Notes on response shape inconsistencies" row 10. Test "create_extractor apply path returns server-assigned id from extractor_id response field (toIdBody)" pins the contract.
  - **Per-type tests unrolled from for-loop into 8 explicit declarations**: the original plan suggested a for-loop iterating EXTRACTOR_CASES. The loop variant landed 8 runtime tests but only 1 lexical `test(` declaration — below the plan's ≥18 grep gate. Unrolled into 8 explicit top-level `test()` calls, each calling a shared `assertCreateExtractorPreviewShape(type, config)` driver. Net lexical count: 22 `test(` matches. Failure messages now reference the type by name (`create_extractor regex_replace — preview shape FAIL`) instead of iteration index N. Tracked as Rule 1 deviation in the SUMMARY (lexical-grep tightening).
  - **Test growth**: 203 → 223 (+20 net-new). Focused run = 20/20 pass; full `npm test` = 223/223 pass; zero regressions. Commits: `193e218` (Task 1 RED, ERR_MODULE_NOT_FOUND), `25a3b16` (Task 2 GREEN, all 20 pass + 203 baseline preserved).
  - **Tool count milestone reached**: 35 tools registered (23 v2.3 + 12 Phase 1 = 3 read in Plan 01 + 3 mutating in Plan 02 + 2 lifecycle in Plan 03 + 4 extractor in Plan 04). Phase 1's full scope (INPUT-01..11 → 12 tools) is now complete behavior-wise. Module-init `assertAllToolsRegistered(toolDefinitions)` returns "OK".
  - **Plan 05 hand-off**: 8 mutating-tool schemas exist and are exported from `src/tools/inputs/schemas.js` for the `assertSchemaParityForTool(toolName, zodSchema)` enrichment in `test/schema-parity.test.js`. The C3 + D-03 + D-04 + D-05 + D-07 + D-09 + lifecycle verb-mapping acceptance gates are all currently pinned by ad-hoc tests; Plan 05 lands them as byte-identical snapshot fixtures + flips `01-VALIDATION.md` to complete + closes Phase 1.

- **Plan 02-01 (foundation amendments + await_system_job + read tools)**:
  - **U1 smoke unreachable** — no Graylog API token in the executor environment (`~/.graylog-mcp/config.json` does not exist). The instance at `http://<graylog-host>:9000` is reachable (HTTP 401 from unauthenticated probe — confirms live Graylog) but smoke calls require auth. Per `<u1_smoke_protocol>`'s "no matching connection exists" branch, the artifact records `UNREACHABLE_DEFAULT_MERGE` for Plan 02-02's update_index_set (merge-from-current — safe because index-set configs carry NO encrypted fields; C3 not reachable) and `SYNC_OPTION_A` for Plan 02-04's cycle_deflector (synchronous envelope with `side_effects.observable_at` for the IndexRangesUpdateJob — source-verified in RESEARCH.md against `DeflectorResource.java`).
  - **`_confirmationToken` forwarding** — additive amendment in handler.js: build() may return `_confirmationToken: <hex>` on the request descriptor; dry-run preview JSON emits `confirmationToken: <hex>` ONLY when present. Back-compat preserved for every Phase 0/1 tool that does not set it. The leading-underscore key marks it as framework-internal (same convention as `_testConnection`, `_connectionName`).
  - **`requireConfirm` apply-time gate** — second additive amendment in handler.js: `spec.requireConfirm({ args, req }) → token|null` is checked AFTER the writable gate (D-16) but BEFORE apply(). Mismatched OR missing `args.confirm` → isError with `reason: 'confirmation_mismatch'`; apply() never runs. Returning null is a no-op (e.g. delete_index_set with `deleteIndices:false` issues no token). Plan 02-03's delete_index_set will compute the hash in build() and route it through both hooks: forward via `_confirmationToken` + check via `requireConfirm: ({req}) => req._confirmationToken ?? null`.
  - **`findExistingMatches` `index_sets` envelope** — one additive line in conflict.js: `?? response?.index_sets` inserted before `?? response?.items` in the envelope-unwrap chain. Plan 02-02's create_index_set consumes this for M5 list-before-create idempotency. Back-compat: every Phase 1 envelope shape (`inputs`, `streams`, `extractors`, `items`) still works.
  - **`await_system_job` primitive** — INDEX-08 shipped under `src/tools/_shared/system-job.js` (cross-domain reuse; Phase 3+ async tools will import it). Composes through defineMutatingHandler so dryRun + writable-flag inheritance is uniform (D-07). Exponential backoff `[500, 1000, 2000, 4000, 5000]` ms capped at 5s; default 60s timeout, hard zod-bounded 600s max (T-02-01-04). Accepts EXACTLY ONE of `jobId` / `jobIdOrEnvelope` / `info_substring` via zod `.refine()`. `extractJobId` helper handles forgiving input (bare string, `{job_id}`, `{jobId}`, `{id}`). 404 on poll endpoint → `completed:true` with `synthetic:true` finalStatus.
  - **UPDATED D-15 `info_substring` discovery** — delete_index_set's apply envelope deliberately omits `job_id` because Graylog DELETE returns 204 with no body. The agent passes `info_substring: <indexSetId>`; `await_system_job` apply() calls `resolveJobIdFromInfo(client, substring)` which GETs `/api/system/jobs` and matches via `String.prototype.includes` (literal substring — T-02-01-10 mitigation against catastrophic-backtracking DoS). 0 matches → `isError` reason `job_not_found`; 1 match → poll that job's id with the standard schedule; 2+ matches → `isError` reason `ambiguous_info_substring` with the list of candidate ids so the agent can disambiguate. Dry-run (dryRun:true) returns the resolution plan WITHOUT firing the list call.
  - **handler.js apply() isError pass-through (Rule 3 blocking deviation)** — Tests 12/13 (info_substring 0/2+ match) required apply() to return `{isError:true, reason, content:[...]}` envelopes, but the existing wrapper unconditionally normalized via `req.normalize?.(raw) ?? { id: raw?.id, body: raw }`. Added a 4-line additive guard BEFORE the normalize step: `if (raw && raw.isError === true) return raw;`. Pass-through is opt-in via the explicit `isError:true` tag on apply()'s return; no existing tool's behavior changes (existing apply() callbacks return plain DTOs). Pattern is now reusable for any future tool whose apply() needs to surface a structured-error reason without throwing a typed GraylogError.
  - **`list_index_sets` (INDEX-01)** — defineListHandler with `defaultFields: [id, title, description, default, writable, can_be_default, index_prefix]`. Unwraps the `{ total, index_sets: [...], stats: {} }` envelope and queries with `?stats=false` to keep the list response cheap. Stats are intentionally OUT — fetching them would require N stats calls per list (too expensive); the agent uses `get_index_set` or `delete_index_set`'s dry-run preview when stats matter.
  - **`get_index_set` (INDEX-02)** — plain async handler returning the full IndexSetResponse DTO (id, title, description, default, writable, can_be_default, index_prefix, shards, replicas, rotation_strategy_class, rotation_strategy, retention_strategy_class, retention_strategy, creation_date, index_analyzer, ...). Errors map through `wrapGraylogError` so 404 surfaces as a clean MCP error envelope.
  - **Schema-parity coverage** — 3 new `assertSchemaParityForTool` calls (list_index_sets, get_index_set, await_system_job). The await_system_job entry pins `info_substring` (UPDATED D-15) as a public-surface field. ZodEffects (from the `.refine()` exactly-one-of rule) handled via `getShape`'s `_def.schema.shape` fallback — no changes needed to the helper.
  - **Test growth**: 235 → 271 (+36 net-new — 6 handler + 2 conflict + 18 system-job + 7 index-sets + 3 schema-parity). Full `npm test` = 271/271 pass; zero regressions on the Phase 0 + Phase 1 baselines. Tool count 35 → 38.
  - **Commits**: `bf5a274` (Task 1 docs — U1 smoke artifact), `df887b2` (Task 2 RED), `9b982ea` (Task 2 GREEN — handler + conflict), `b6cfd8a` (Task 3 RED), `403fdfb` (Task 3 GREEN — system-job + handler isError pass-through), `7a95858` (Task 4 RED), `a322a67` (Task 4 GREEN — index-sets + register + tools.js).
  - **Plan 02-02 / 02-03 / 02-04 hand-off**: All cross-cutting amendments shipped. (a) Plan 02-02's create_index_set can wire `findExistingMatches({listPath:"/api/system/indices/index_sets", matchFn})` for M5 list-before-create. (b) Plan 02-02's update_index_set ships merge-from-current per the U1 decision artifact. (c) Plan 02-03's delete_index_set ships C1 confirmation hash via `_confirmationToken` (in build()) + `requireConfirm` (gate); apply envelope omits `job_id` per UPDATED D-15 and the tool description tells the agent to call `await_system_job` with `info_substring:<indexSetId>`. (d) Plan 02-04's cycle_deflector ships SYNC_OPTION_A envelope per the artifact.

- **Plan 02-02 (create_index_set + update_index_set — INDEX-03 + INDEX-04)**:
  - **U1 → MERGE_FROM_CURRENT honored**: update_index_set ships the merge-from-current wire-build pattern per 02-U1-SMOKE.md (UNREACHABLE_DEFAULT_MERGE). Pre-flight GET sources the immutable fields (index_prefix, creation_date) + the strategy blocks the agent didn't touch + current.default for ND2; build() merges agent changes over current using an explicit-field-allowlist body builder (drops server-only id/default/can_be_default that the PUT deserializer rejects); full DTO emitted on the wire. Safe because index-set configs carry no encrypted fields (verified against IndexSetResponse — no is_encrypted: true field), so the C3 zero-out pitfall is not reachable.
  - **strategies.js alias→FQCN translator + aliasToConfigOrError safe wrapper**: ROTATION_FQCN (3 aliases) + RETENTION_FQCN (2 aliases — archive intentionally absent from the map). The alias is the agent's only input surface; the wrapper translates to the FQCN pair Graylog needs on the wire (T-02-02-01 mitigation — agent can never inject `org.attacker.MaliciousStrategy`). archive retention rejected with structured reason `archive_not_supported` routed through GraylogValidationError → wrapGraylogError, not a generic zod invalid_enum_value.
  - **D-09 6 strict strategy-config zod schemas**: ISO_DURATION regex + TimeBasedConfig (rotation_period required ISO-8601) + SizeBasedConfig (max_size bytes, int().positive()) + MessageCountConfig (max_docs_per_index) + DeleteRetentionConfig + CloseRetentionConfig (both max_number_of_indices). ROTATION_CONFIG_BY_ALIAS + RETENTION_CONFIG_BY_ALIAS lookup maps. CreateIndexSetSchema + UpdateIndexSetSchema use these via superRefine variant dispatch — same pattern as Phase 1 CreateInputSchema (per-FQCN configuration narrow) and CreateExtractorSchema (per-type extractor_config narrow).
  - **D-10 required strategies on create**: CreateIndexSetSchema declares all four of (rotation_strategy, rotation_strategy_config, retention_strategy, retention_strategy_config) as required fields with NO defaults. Destruction policies must never be defaulted — the agent makes an informed choice every time.
  - **D-11 atomic strategy-replace on update**: UpdateIndexSetSchema.superRefine enforces strategy + strategy_config as a pair. If changes.rotation_strategy is set, changes.rotation_strategy_config MUST also be set (and vice versa). Same rule for retention. Tested explicitly (4 zod-level rejection tests).
  - **ND2 pre-flight (default-must-be-writable)**: when current.default === true AND args.changes.writable === false, update-index-set.js build() throws GraylogValidationError with reason `default_index_set_must_be_writable` BEFORE the PUT fires. Tested: PUT call count tracked, asserted 0. Mirrors the structural-error pattern Plan 02-04's set_default_index_set will use for can_be_default:false invariant.
  - **Immutable-field defense-in-depth (T-02-02-04)**: UpdateChangesShape omits index_prefix + creation_date from the allowed changes set (zod strip drops them at the schema layer), AND the wire body re-asserts current.index_prefix + current.creation_date. Two independent layers — defense-in-depth even if a future schema regression let these through.
  - **Determinism seam (_setClockForTests)**: leading-underscore framework-internal export on create-index-set.js so snapshot fixtures (Plan 02-05) can pin creation_date without baking real timestamps into committed snapshots. Production must NEVER call _setClockForTests.
  - **MERGE_FROM_CURRENT explicit-allowlist body builder**: rather than `{...current, ...changes}`, the merge spells out every field on the wire. Drops server-only fields like id/default/can_be_default that IndexSetUpdateRequest.java rejects; makes the wire shape auditable in a single source-of-truth location. Pattern reusable for Phase 3+ updates against AutoValue-shaped Java DTOs.
  - **Tool count milestone**: 38 → 40 (create_index_set + update_index_set). 5 of 8 Phase 2 net-new tools shipped. Tests added: 271 → 302 (+31: 14 schema/strategy + 6 create + 9 update + 2 schema-parity). Zero deviations from plan, zero regressions, all U1-decision-pinned acceptance gates land green.
  - **Commits**: `39d7f42` (Task 1 RED), `e4e62e6` (Task 1 GREEN), `aee031d` (Task 2 RED), `138dac0` (Task 2 GREEN), `88affee` (Task 3 RED), `22ab2d2` (Task 3 GREEN).
  - **Plan 02-03 hand-off**: delete_index_set will reuse Plan 02-01's `_confirmationToken` forwarding + `requireConfirm` apply-time gate for the C1 confirmation hash. ND1 pre-flight (default index set undeletable) mirrors the ND2 pre-flight pattern shipped here — Plan 03 can copy the GraylogValidationError + reason structural-error shape verbatim. Stats-unreachable hard-block (D-05) is new work; the alias-to-FQCN translation is not load-bearing for delete.

- **Plan 02-03 (delete_index_set — INDEX-05; the C1 mitigation centerpiece)**:
  - **C1 confirmation token end-to-end**: `src/tools/index-sets/c1-hash.js` ships `computeC1Hash({ indexSetId, deleteIndices, indexNames, messageCount }) → 64-hex sha-256` over canonical JSON. Locked literal `deleteIndices: true` (D-02 replay protection — a hash issued for any other value can never validate against a true-mode apply). Sorts `indexNames` internally so input order is irrelevant. `collectIndexNames(allIndices)` walks `closed.indices` Set + `reopened.indices` Set + `all.indices` Map keys with Set-dedupe; tolerant of empty/missing sub-collections (returns `[]`). Three frozen-fixture hashes pinned in tests to detect canonicalization drift across CI runs.
  - **D-04 inverted default**: `DeleteIndexSetSchema.deleteIndices` defaults to `false` in the MCP wrapper, inverting Graylog's server-side `@DefaultValue(true)`. Dry-run preview emits `postApplyEstimate.deletedIndices: false` even when defaulted — the safety choice is in writing.
  - **ND1 pre-flight fires FIRST**: GET `/api/system/indices/index_sets/<id>`; refuse if `current.default === true` with `GraylogValidationError(reason: "default_index_set_undeletable")`. Fires regardless of `deleteIndices` value — Graylog's BadRequestException "Default index set cannot be deleted!" fires on the default check before the delete_indices param is inspected, so the wrapper-side check is also default-first. Tests 8 + 12 prove the consistency.
  - **D-05 stats_unreachable HARD-BLOCKS dry-run**: GET `/index_sets/<id>/stats` failure → `GraylogValidationError(reason: "stats_unreachable")`. NO confirmation token issued. Index-list pre-flight is BEST-EFFORT (404/403 falls back to empty AllIndices shape; the hash is still computable because messageCount comes from /stats independently). Distinct disposition because losing the index list doesn't lose blast-radius visibility (messageCount sourced separately).
  - **UPDATED D-15 no-job_id envelope**: apply returns `{ async: true, job_id_observable_at: "/system/jobs", message: "Submitted cleanup for index set <id>; await via /system/jobs (call await_system_job with info_substring: \"<id>\")" }`. `job_id` deliberately ABSENT — Graylog DELETE returns 204 with no body; there is no server-supplied id to forward. The message field carries the indexSetId substring so the agent's `await_system_job(info_substring:<id>)` path (Plan 02-01) resolves to the IndexSetCleanupJob. Test 10 proves `body.job_id === undefined`.
  - **D-16 writable-before-confirm**: handler.js (Plan 02-01) order — zod.parse → resolveConnection → writable gate (step 3) → idempotency → build (step 5) → dry-run-or-confirm (step 6/6b) → apply. Test 11 proves a `writable: false` connection refuses with `reason: connection_read_only` and ZERO pre-flight GETs fire (defense in depth: no side effects against a connection that can never apply).
  - **Rule 2 deviation — wrapGraylogError surfaces err.reason**: `src/tools/_shared/errors.js` `wrapGraylogError` extended to (a) append ` [reason: <name>]` to the rendered text and (b) propagate `err.reason` as `out.reason` on the MCP envelope. Plan 02-02 set `err.reason` on `update_index_set`'s ND2 refusal but the field was invisible to callers (the rendered text used only `err.message`). The structured-reason surface is what the agent uses to programmatically identify the error type; the field was being silently dropped at the rendering boundary. Additive — no existing thrower depends on absence; Plan 02-01's `await_system_job` info_substring path constructs `out.reason` directly so the property name is already in use on the envelope shape.
  - **Tool count milestone**: 40 → 41 (delete_index_set). 6 of 8 Phase 2 net-new tools shipped. Tests added: 302 → 323 (+21: 8 c1-hash/collectIndexNames + 12 delete_index_set handler + 1 schema-parity). Zero regressions; ROADMAP success criterion 1 provably met.
  - **Commits**: `21d2b7d` (Task 1 RED — c1-hash + collectIndexNames tests), `37ce96e` (Task 1 GREEN — c1-hash.js helpers), `a4e5fdb` (Task 2 RED — delete_index_set handler + schema-parity tests), `6c57864` (Task 2 GREEN — delete_index_set.js + DeleteIndexSetSchema + register + tools.js entry + Rule 2 wrapGraylogError reason surface).
  - **Plan 02-04 hand-off**: `set_default_index_set` (INDEX-06) can reuse the ND-pre-flight + structural-error pattern shipped here (`current.can_be_default === false` → `default_eligibility_failed` mirrors `default_index_set_undeletable`). `cycle_deflector` (INDEX-07) per the U1 smoke artifact ships SYNC_OPTION_A — no system-job linkage needed for the primary action; ND3 pre-flight (writable: false on the index set itself, distinct from the connection-level writable gate) can use the same GraylogValidationError + reason pattern. The Rule 2 `wrapGraylogError.reason` surface is now generally useful: any handler can throw `new GraylogValidationError(msg, ctx); err.reason = "<kebab>"` and the agent sees both human-readable text and the structured reason field.

- **Plan 02-04 (set_default_index_set + cycle_deflector — INDEX-06/07)**:
  - **UPDATED D-13 + m2 pre-flight (set_default_index_set)**: `build()` async reads `current.can_be_default` from `GET /api/system/indices/index_sets/{id}`. When false (events-style or system index set, OR any future server-rejected case), throws `GraylogValidationError` with `err.reason = "default_eligibility_failed"`. The would-be 409 surfaces in dry-run, NOT apply. Wrapper deliberately reads `can_be_default` rather than the underlying `regular` boolean — the derived flag is durable across future server-side eligibility-rule additions. PUT call count on refusal: 0 (asserted by Task 1 Test 3). ROADMAP success criterion 3 provably met.
  - **UPDATED D-14 SYNCHRONOUS apply envelope (cycle_deflector)**: honors 02-U1-SMOKE.md SYNC_OPTION_A exactly. Apply returns `{ rotated: true, message: "Cycled index set <id>; closed previous active index. Deflector cycle complete on Graylog side.", side_effects: { observable_at: "/system/jobs", describes: "closed-index range rebuild (IndexRangesUpdateJob fires asynchronously; the rotation itself is complete on response)" } }`. Critical: NO `async: true` wrapping; NO `job_id` field. The rotation completes on response; only the closed-index range rebuild is async, surfaced via `side_effects.observable_at` so an agent can call `await_system_job(info_substring:<id>)` if it cares about the rebuild before searching the just-closed index. This is NOT the D-15 async envelope.
  - **ND3 pre-flight (cycle_deflector)**: `build()` async reads `current.writable` and refuses with `GraylogValidationError(reason: "non_writable_index_set")` BEFORE the POST when false. Mirrors Graylog's server-side `DeflectorResource.checkCycle` 400 refusal. Two-layer defense. POST call count on refusal: 0 (asserted by Task 2 Test 3).
  - **side_effects envelope pattern**: UPDATED D-14 introduces `side_effects: { observable_at, describes }` for synchronous primary operations with asynchronous side-effects. Phase 3+ rotation-style operations can copy this template; it's distinct from D-15's async envelope shape and gives the agent both completion clarity AND a structured hook for waiting on secondary work.
  - **GraylogValidationError reused from Phase 0**: both new tools import from `src/graylog/errors.js` (line 18 — Phase 0). `src/graylog/errors.js` NOT modified — verified per plan frontmatter. Plan 02-03's `wrapGraylogError` reason surface (renders `[reason: <name>]` suffix + propagates as `out.reason`) is consumed automatically; no additional plumbing needed.
  - **Multi-invariant ND-pre-flight pattern complete**: 4 ND-style structural errors now ship across Phase 2 — ND1 (default_index_set_undeletable, Plan 02-03), ND2 (default_index_set_must_be_writable, Plan 02-02), ND3 (non_writable_index_set, this plan), m2 (default_eligibility_failed, this plan). All four throw `GraylogValidationError` + `err.reason` in `build()` async BEFORE the destructive call. Phase 3+ blueprint tools can copy verbatim.
  - **Tool count milestone**: 41 → 43 (set_default_index_set + cycle_deflector). All 7 Phase-2 net-new mutating tools shipped. Tests added: 323 → 335 (+12: 5 set_default + 5 cycle + 2 schema-parity).
  - **Commits**: `9b6764f` (Task 1 RED — set_default_index_set tests), `9c601bc` (Task 1 GREEN — set_default_index_set.js + SetDefaultIndexSetSchema + CycleDeflectorSchema + register + tools.js entry), `51ad5f2` (Task 2 RED — cycle_deflector tests), `552dfa1` (Task 2 GREEN — cycle_deflector.js + register + tools.js entry).
  - **Plan 02-05 hand-off**: Schema-parity now covers all 8 Phase-2 mutating tools. Snapshot fixtures for the new dry-run shapes (set_default ineligible refusal, cycle non-writable refusal, both apply envelopes) are pending Plan 02-05. The minimal schema shapes (`{ indexSetId }`) and deterministic envelopes make them trivial to snapshot.

- **Plan 03-01 (Phase 3 read tools + cascade-hash promotion + D-14 U1 smoke)**:
  - **D-14 UNREACHABLE_STRICT_NO_ECHO**: no API token in `~/.graylog-mcp/config.json` — partial-PUT smoke against `<graylog-host>:9000` skipped per `<u1_smoke_protocol>` "no matching connection" branch (same precedent as `02-U1-SMOKE.md` UNREACHABLE_DEFAULT_MERGE). Locks STRICT_NO_ECHO for Plan 02 `update_stream` and Plan 04 `update_stream_rule`. Safe-default rationale: no encrypted fields on streams or stream rules (C3 not reachable); smaller wire bytes; consistent with Phase 1 D-12 update_input pattern.
  - **CreateStreamRuleRequest.type non-nullable Java int caveat (Plan 04 hand-off)**: documented in `03-U1-SMOKE.md`. `update_stream_rule`'s strict-no-echo wire-build MUST emit `type: current.type` from the pre-flight GET unconditionally, even when `args.changes.type` is absent. The remaining 4 of 5 fields (value, field, inverted, description) stay strictly no-echo. Plan 04's build() pattern:
    ```js
    const current = await client.request("GET", `/api/streams/${args.streamId}/rules/${args.ruleId}`, null);
    const wireBody = { type: current.type, ...args.changes };
    ```

  - **Cascade-hash helper promoted with full Phase 2 back-compat**: `src/tools/_shared/cascade-hash.js` ships `computeC1Hash` + `collectIndexNames` (Phase 2 — verbatim lift; SAME function objects via re-export) AND `computeCascadeHash({streamId, ruleIds, pipelineConnIds, eventDefIds})` (Phase 3 NEW D-02 keyed-buckets canonicalization). `src/tools/index-sets/c1-hash.js` becomes a 14-line back-compat re-export — every Phase 2 importer (`delete-index-set.js` + `test/index-sets.test.js`) sees zero churn. Phase 2 frozen-fixture hashes continue to validate end-to-end. Function-identity invariant pinned via Test 6: `assert.equal(computeC1HashLegacy, computeC1Hash)`.
  - **D-02 keyed-buckets canonicalization**: `{ streamId, cascades: { rules:[sorted], pipeline_connections:[sorted], event_definitions:[sorted] } }`. Each bucket sorts its own IDs internally; the agent does not pre-sort. Keyed buckets disambiguate type-collision — the same ID string "xyz" placed in three different buckets produces three distinct hashes (Test 2 pins). A flat-sort canonicalization would have collapsed them; D-02 was tightened during planning to lock the keyed shape.
  - **Frozen-fixture hashes pinned** in `test/cascade-hash.test.js`:
    - Populated (streamId=5f9d3b1c7e8a4d2b1c3e5f9d, 2 rules + 1 pipeline + 2 event-defs) → `888cfe478f5ef2d421d1cd4e9a00b7e439e07d5d0b03094891542bea8cbaf991`
    - Empty cascade → `541be7deb65006714cbde5270556b20d80e32e64197c4f5bd9493139f2cacbf6`
  - These literals detect canonicalization drift across CI runs AND across the inevitable Plan 03-03 delete_stream landing.
  - **Pitfall S2 mutable projection**: `list_streams` wire `is_editable` -> agent `mutable: boolean` via the fetch callback's destructuring map step (`const {is_editable, ...rest} = s; return {...rest, mutable: is_editable === true}`). `is_editable` is STRIPPED from the projected item — two-name foot-gun avoided. `get_stream` is the rich-shape read and preserves the wire field name verbatim; the projection-to-`mutable` is a `list_streams`-specific ergonomic. Test 4 + Test 12 in `test/streams.test.js` pin the contract. ROADMAP SC2 ("list_streams returns each stream's `mutable: boolean`") provably met.
  - **Pitfall S5 v2.3 displacement**: `src/tools/_register.js` no longer imports or registers the v2.3 `listStreamsHandler`. The function in `src/handlers.js` STAYS exported (HARD-03 Phase-7 audit reference) but is unregistered from dispatch. The new Phase 3 `list_streams` (registered via `src/tools/streams/index.js`) claims the dispatch name without a rename. Strict-superset back-compat: every old field (id, title, description) is preserved; new fields (mutable, disabled, index_set_id) are additive. Test 12 explicitly pins the strict-superset invariant — agents reading only the v2.3 fields continue to work unchanged.
  - **Discretion-02 resolution**: `get_stream` AND `list_stream_rules` both exist. `get_stream` returns the FULL StreamResponse DTO with embedded rules (rich read); `list_stream_rules` returns a narrow per-rule projection (id, type, field, value, inverted) for token efficiency on streams with many rules. Either tool can satisfy STREAM-07 alone, but having both lets the agent pick the right shape for the task — narrow for filter/routing, rich for full inspection.
  - **Tool count**: 43 → 45 (delta: -1 v2.3 list_streams displaced + 3 net-new = +2). Plan frontmatter declared "tool count = 46" via `43 - 1 + 3 + 1 = 46`, double-counting the "reclaimed" list_streams (which is already one of the 3 net-new). Correct math: `43 - 1 + 3 = 45`. `assertAllToolsRegistered(toolDefinitions)` passes for all 45.
  - **Rule 1 deviations (2)**: both local to `test/streams.test.js` design — no production-code drift from the plan.
    1. `assertAllToolsRegistered` API mismatch — the plan's Test 11 originally asserted `assert.equal(assertAllToolsRegistered(toolDefinitions), "OK")` but the actual contract is "returns undefined on success, throws on missing". Reshaped Test 11 to call without an equality check (function throwing on failure IS the test contract).
    2. Dispatch test cache-bust ES-module-cache interaction — Tests 10 + 11 originally tried `_clearForTests()` + cache-bust import of `_register.js`. The nested side-effect imports inside `_register.js` (inputs/index.js, index-sets/index.js, streams/index.js) are NOT cache-busted by the outer cache-bust, so their `register()` calls do NOT re-fire against the cleared Map. Dropped the clear+cache-bust pattern; tests just `import "../src/tools/_register.js"` once (module-level cache ensures one registration per process).
  - **Test growth**: 335 → 358 (+23 net-new — 8 cascade-hash + 12 streams + 3 schema-parity). Full `npm test` 358/358 pass; zero regressions on Phase 0 + Phase 1 + Phase 2 baselines. Two consecutive `npm test` runs produce byte-identical `.snapshot` md5sums (DETERMINISTIC verified).
  - **Commits**: `8b4b0e1` (Task 1 docs — U1 smoke artifact), `11f4524` (Task 2 RED — cascade-hash tests), `818adf6` (Task 2 GREEN — _shared/cascade-hash.js + c1-hash.js thin re-export), `326edc8` (Task 3 RED — streams + schema-parity tests), `bf6ac23` (Task 3 GREEN — 5 streams files + _register.js displacement + tools.js entries).
  - **Plan 03-02 / 03-03 / 03-04 / 03-05 hand-off**: (a) Plan 03-02's `update_stream` ships STRICT_NO_ECHO; `create_stream` consumes `findExistingMatches` streams envelope (already shipped at conflict.js:31). (b) Plan 03-03's `delete_stream` consumes `computeCascadeHash` with the keyed-buckets signature + `_confirmationToken`/`requireConfirm` machinery (Phase 2 Plan 02-01) — same shape as `delete_index_set`. (c) Plan 03-04's `update_stream_rule` ships STRICT_NO_ECHO with the documented `type: current.type` echo caveat; `STREAM_RULE_TYPE_TO_NUMERIC` map lands in streams/schemas.js. (d) Plan 03-05 (Phase 3 polish) — schema-parity already covers the 3 read tools; remaining 9 mutating tools added by Plans 02/03/04 require schema-parity + snapshot fixtures.

- **Plan 03-02 (create_stream + update_stream + start_stream + pause_stream)**:
  - **CreateEntityRequest envelope locked in create-stream.js build()**: wire body wraps the agent's flat schema in `{ entity: { title, description, rules, content_pack:null, matching_type, remove_matches_from_default_stream, index_set_id }, share_request:null }` (verified at StreamResource.java:229-230). A flat body gets HTTP 400 "missing entity" per Pitfall S3. share_request:null is a wire-shape CONSTANT, not derived from agent input. Plan 05 will snapshot the literal preview JSON for byte-pinning.
  - **`classifySimilarity(proposed, existing)` strictest-wins**: 3-step short-circuit closes the D-05/D-06 contract. byte-equal → "exact"; lowercased-equal → "case_insensitive"; either-prefix-of-the-other (lowercased) → "prefix"; else null. findExistingMatches similarityReason callback drops null matches via matchFn predicate. Closed-set `SimilarityReasonEnum` enforces the bucket-name invariant at the schema layer — any future fuzzy/Levenshtein addition would have to extend the enum first.
  - **`translateInlineRule(r)` Pitfall S10 fix**: empty-string defaults for irrelevant fields. `always_match` emits `value:""` + `field:""`; `present` + `match_input` emit `value` or `field` as `""` as appropriate. `String(value ?? "")` coerces numeric `greater`/`less` values to strings (CreateStreamRuleRequest.value is non-nullable String on the wire). `STREAM_RULE_TYPE_TO_NUMERIC[r.type]` consumes the Object.frozen map for the type-int translation.
  - **STRICT_NO_ECHO for update_stream per 03-U1-SMOKE.md UNREACHABLE_STRICT_NO_ECHO**: wire body emits ONLY fields present in args.changes via conditional-spread. No echoed current.* fields. Per Pattern 6 + StreamResponse.java: streams carry no `is_encrypted: true` annotation → C3 zero-out structurally unreachable on this surface. Reversible if live smoke later proves MERGE required (strict-no-echo body is a strict subset of merge body; back-compat preserved).
  - **D-09 mutable defense-in-depth pre-flight repeated in 3 places (update/start/pause)**: GET /api/streams/{id} reads canonical wire `is_editable` (NOT the projected `mutable` from list_streams — defense in depth means each layer checks against the canonical source). Refusal carries `err.reason = "stream_immutable"` so dispatcher can pattern-match without parsing the message. Per-tool error-message variance ("refusing start" vs "refusing pause" vs unqualified for update) argued against premature abstraction. Plan 03-03 will likely lift to a shared helper when `delete_stream` adds the 4th caller — at the CLAUDE.md 3-callsite lift threshold but per-message-variance held the line.
  - **Lifecycle-as-mutation through defineMutatingHandler (D-12)**: start_stream + pause_stream are NOT special-cased runtime tools. Same factory; same dryRun + writable + idempotency inheritance. body: undefined; verbatim POST. Plan-anticipated and matches Phase 1's start_input/stop_input D-08 precedent.
  - **Test infrastructure addition**: `streamsMultiCapture(routes)` helper (mirrors test/inputs.test.js `multiCapture`) handles the GET-then-PUT/POST mocking pattern for D-09 pre-flight handlers. Reused across all 6 update/start/pause tests.
  - **Tool count**: 45 → 49 (delta: +4 net-new; plan frontmatter target of 50 inherited the +1 accounting error from Plan 01 frontmatter documented in 03-01-SUMMARY.md). `assertAllToolsRegistered(toolDefinitions)` passes against 49.
  - **Test growth**: 358 → 402 (+44 net-new — 21 Task 1 schema + 19 Task 2/3 handler + 4 schema-parity). Full `npm test` 402/402 pass; zero regressions. No new npm dependencies. `src/graylog/errors.js` NOT modified (only imported via `GraylogValidationError`).
  - **Commits**: `3358488` (Task 1 RED — 21 schema tests fail on missing exports), `37b1af5` (Task 1 GREEN — schemas.js extended with 8-variant rule union + 4 mutating schemas), `79ce0b3` (Task 2 RED — 12 handler tests + 2 schema-parity tests), `84e3433` (Task 2 GREEN — create_stream + update_stream shipped), `9d30fbb` (Task 3 RED — 7 handler tests + 2 schema-parity tests), `1795e5d` (Task 3 GREEN — start_stream + pause_stream shipped).
  - **2 Rule-1 deviations**: both inherited accounting-doc bugs from Plan 01 frontmatter math errors carried forward. (a) Plan task-3 test count off-by-one (7 vs 8 handler tests — Task 3's behavior list said 8 but Test 8 was schema-parity; my 7 + 2 schema-parity totals 9 under the name-pattern filter, matching plan intent). (b) Tool-count expected target (50) off by one (correct outcome 49; baseline was 45, not 46). Zero production-code drift from the plan. Both documented in 03-02-SUMMARY.md so the Plan 03-03 baseline carries forward unambiguously (Plan 03-03 baseline: 49 tools, 402 tests).
  - **ROADMAP SC4 met**: `create_stream` dry-run output includes `existingMatches: [{ id, title, similarity_reason }]` across 3 buckets (exact | case_insensitive | prefix). Verified via 3 dedicated tests in test/streams.test.js (one per bucket).
  - **Plan 03-03 hand-off**: D-09 pre-flight pattern + streamsMultiCapture helper + test/streams.test.js mocking structure all ready. `computeCascadeHash` keyed-buckets signature already shipped with frozen-fixture hashes (Plan 03-01). `_confirmationToken` + `requireConfirm` apply-gate machinery already shipped in handler.js (Phase 2 Plan 02-01). `delete_stream` will compose the 3 pre-flight GETs (rules + pipeline connections + event definitions), feed the assembled buckets through `computeCascadeHash`, and surface the hash as `confirmationToken` + `cascades` in the dry-run preview — same shape as `delete_index_set`.

- **Plan 03-03 (delete_stream — C2 mitigation centerpiece)**:
  - **C2 mitigation contract shipped end-to-end**: D-09 mutable pre-flight (FIRST — saves 3 cascade round-trips on built-in/system streams) + D-01 cascade pre-flight (3 endpoints: rules → pipelines bare-array per Pitfall A3 → event-defs paginated client-filtered per Pitfall S6) + D-02 keyed-buckets sha-256 `confirmationToken` via `computeCascadeHash` (shipped Plan 03-01) + D-03 apply-time re-fetch + recompute + refusal on drift (`cascade_changed_since_preview`) + D-04 hard-block on any cascade-endpoint failure (`cascade_preflight_failed`; no token issued). Pattern is the direct structural analog of Phase 2 Plan 02-03's `delete_index_set` C1 mitigation; reuses Plan 02-01's `_confirmationToken` + `requireConfirm` machinery verbatim — no new framework code.
  - **Pitfall S11 sync envelope LOCKED**: apply returns `{deleted:true, streamId}` — NO `async:true`, NO `job_id_observable_at`, NO `await_system_job` involvement. `streamService.destroy` is a direct call server-side (StreamResource.java:418-432). Test 13 explicitly verifies the flattened JSON contains none of those keys.
  - **Pitfall S6 paginated client-filter LOCKED**: `fetchEventDefinitionsForStream(client, streamId)` walks `/api/events/definitions/paginated?page=N&per_page=50` and filters in-process on `def.config.streams.includes(streamId)`. Safety cap at 1000 pages (= 50000 event-defs max) defends against API-shape drift. Early-exit on partial page. Test 9 verifies multi-page traversal + early-exit behavior.
  - **D-04 hard-block disposition matches Phase 2 D-05** (stats_unreachable stance): ANY cascade endpoint failure → `GraylogValidationError(reason:cascade_preflight_failed)` naming the failing endpoint. No confirmation token issued at dry-run; apply-time re-fetch failure propagates through the wrapper's outer try/catch as the same structured envelope. Distinct from Phase 1's best-effort cascade for `delete_input` (extractors are server-side cascade-deleted anyway).
  - **isError envelope return from apply() for business-logic refusals** (vs HTTP errors): `apply()` returns `{ isError:true, reason:"cascade_changed_since_preview", content:[...] }` directly when drift is detected. handler.js:200-204 (Plan 02-01 amendment) passes the envelope through verbatim, distinguishing wrapper-side policy refusals from upstream HTTP failures.
  - **D-09 mutable pre-flight NOT YET extracted into shared helper** at 4 callsites (update_stream, start_stream, pause_stream, delete_stream); per-tool error-text variance still narrowly favours inline duplication. Recommendation for Plan 03-04: at 7 callsites (after stream-rule CRUD adds 3 more) the abstraction case becomes overwhelming — lift into `src/tools/streams/_internal/mutable-preflight.js` with a `verb` parameter for the refusal message.
  - **`buildCascade` kept private to delete-stream.js** (not promoted to `_shared/`); the 3-endpoint cascade taxonomy is streams-specific. Phase 4 `delete_pipeline_rule` will have its own cascade shape. The shared abstraction is `computeCascadeHash` (already in `_shared/`), not the orchestrator.
  - **Worked-example hashes pinned in 03-03-SUMMARY.md** for Plan 05 snapshot fixtures: non-trivial cascade (streamId=`s_a1`, 2 rules + 1 pipeline + 2 event-defs) → `583009711ccc9ce33c47586590ece17638712725a8d75dfda21dcf2fe90b5769`; empty cascade (same streamId) → `bcee2ac955dd0daa454711db37cabbf484555f9197527e85004ef51db8243c68`. Plan 05's snapshot writer can pin these literals as drift sentinels.
  - **Tool count**: 49 → 50 (delta: +1; plan acceptance criteria expected 50→51 inheriting the +1 accounting drift from Plan 01 frontmatter; the delta is correct, the absolute numbers are off by one — documented in 03-03-SUMMARY.md so Plan 03-04 baseline is unambiguously 50). `assertAllToolsRegistered(toolDefinitions)` passes against 50.
  - **Test growth**: 402 → 417 (+15 net-new — 14 handler tests + 1 schema-parity). Full `npm test` 417/417 pass; zero regressions. No new npm dependencies. `src/graylog/errors.js` NOT modified (only imports `GraylogValidationError`).
  - **Commits**: `e80a7a7` (RED — 14 handler tests + 1 schema-parity fail with `ERR_MODULE_NOT_FOUND` for `src/tools/streams/delete-stream.js` + "Tool delete_stream missing from tools.js"); `2fd83d5` (GREEN — DeleteStreamSchema appended, delete-stream.js shipped with full C2 contract, streams/index.js register count 7→8, tools.js delete_stream entry).
  - **1 Rule-1 deviation**: Plan acceptance-criteria expected tool count 50→51 (inherited +1 accounting drift from Plan 01 → Plan 02 frontmatter writes); actual outcome 49→50 (delta correct; absolute numbers off by one). Zero production-code drift; documented in 03-03-SUMMARY.md so Plan 03-04 baseline carries forward unambiguously.
  - **ROADMAP SC1 provably met**: "An agent can preview a stream-delete and see all cascading rules + pipeline connections + event definitions before applying; if the cascade list grows between dry-run and apply, the apply step refuses with a 'world changed since preview' error." Verified via Test 4 (cascade preview happy path), Test 11 (`cascade_changed_since_preview` drift refusal), Test 10 (`confirmation_mismatch`), Tests 6/7/8 (`cascade_preflight_failed` hard-block per endpoint), Test 13 (Pitfall S11 sync envelope shape).
  - **Plan 03-04 hand-off**: D-09 mutable pre-flight pattern at 4 callsites → 7 callsites incoming (create_stream_rule, update_stream_rule, delete_stream_rule each pre-flight parent-stream mutable). Recommend lifting into a `src/tools/streams/_internal/mutable-preflight.js` helper at that point. `STREAM_RULE_TYPE_TO_NUMERIC` map already in streams/schemas.js (Plan 03-02); consumed by `create_stream_rule.build()` + `update_stream_rule.build()`. STRICT_NO_ECHO for `update_stream_rule` locked per 03-U1-SMOKE.md, with the `type: current.type` non-nullable-int caveat documented there. Test infrastructure (streamsMultiCapture helper, GraylogNotFoundError import, _setConnectionsForTests pattern) all reusable.
  - **Plan 03-05 hand-off**: 5 snapshot fixtures pending for delete_stream — (1) dry-run non-trivial cascade with pinned 64-hex token, (2) dry-run empty cascade, (3) apply-time `cascade_changed_since_preview` refusal envelope, (4) `stream_immutable` refusal envelope, (5) `cascade_preflight_failed` refusal envelope. All five will be byte-stable across CI runs (keyed-buckets hash is deterministic; no time-stamping in the apply path).

- **Plan 03-04 (stream-rule CRUD + test_stream_match — STREAM-08/09/10/11)**:
  - **Phase 3 12-tool surface COMPLETE**: all 4 remaining net-new tools landed (create_stream_rule, update_stream_rule, delete_stream_rule, test_stream_match). `streams/index.js` now has 12 register lines. `assertAllToolsRegistered(toolDefinitions)` passes against 54 tools. All 4 ROADMAP SCs for Phase 3 provably met (SC1 Plan 03 cascade preview + drift refusal; SC2 Plan 01 list_streams mutable projection; SC3 Plan 04 test_stream_match server-side; SC4 Plan 02 create_stream existingMatches buckets).
  - **`create_stream_rule` (STREAM-08) shipped with z.intersection composition**: `CreateStreamRuleBase.and(StreamRuleSchema)` — the `.and()` (z.intersection) call preserves the 8-variant discriminated union's per-variant narrowing whereas `.merge()` would flatten it. Accepts all 8 D-11 variants (exact, regex, greater, less, present, contains, always_match, match_input) — D-11's 8th variant `match_input` empirically verified at the schema layer; runtime usability (Assumption A5) against a live Graylog instance remains a Plan-05 fixture concern. D-09 parent-mutable pre-flight (FIRST). Pitfall S9 numeric translation via Object.frozen `STREAM_RULE_TYPE_TO_NUMERIC` map (shipped Plan 03-02). Pitfall S10 empty-string defaults for variant-irrelevant fields (always_match emits both value="" + field=""; present + match_input emit field=""). D-13 `__SERVER_ASSIGNED__` sentinel for postApplyEstimate.id. toIdBody idFields:["streamrule_id", "id"] per Pitfall S7.
  - **`update_stream_rule` (STREAM-09) shipped with STRICT_NO_ECHO + Pitfall S8 type-echo exception**: U1 smoke RESULT `UNREACHABLE_STRICT_NO_ECHO` locked as CONSTANT in docstring (per Plan 02 precedent). Wire body emits ONLY fields in args.changes — with the ONE structural exception of Pitfall S8: `CreateStreamRuleRequest.type` is non-nullable Java `int`, so the wire body ALWAYS includes `type: current.type` echoed from the rule pre-flight GET unconditionally. Schema-layer `UpdateStreamRuleChangesShape` does NOT declare `type` as a key — zod's default `strip` mode silently removes agent-supplied `changes:{type:...}` at parse time (Test 17). Defense-in-depth at both schema + build layers. D-09 parent-mutable pre-flight (FIRST — saves a rule-GET round-trip).
  - **`delete_stream_rule` (STREAM-10) shipped as LEAF DELETE per Discretion-04**: build() returns descriptor WITHOUT a `cascades` key AND WITHOUT a `_confirmationToken` key. handler.js's preview emitter (Plan 02-01 amendment) spreads cascades only when truthy and emits confirmationToken only when _confirmationToken is set — so absence at the descriptor layer maps to absence at the dry-run-JSON layer. Test 13 + Test 14 pin BOTH structural absence (`Object.prototype.hasOwnProperty.call(payload, "cascades") === false`) AND textual absence (`assert.doesNotMatch(res.content[0].text, /"cascades"/)`) — catches the regression where a future refactor would set `cascades: undefined` explicitly. Only D-09 parent-mutable pre-flight as the safety gate (Pitfall S12 — leaf-delete still owes the parent-mutable check).
  - **`test_stream_match` (STREAM-11) shipped as D-07 server-side wrapper**: synchronous build() (NO pre-flight — D-07 explicitly forbids JS re-implementation of rule semantics; Graylog is authoritative for regex flags, type coercion, the `inverted` flag, and the global stream matching_type AND|OR). Literal outer key `{ message: args.message }` per StreamResource.java:561-564 — case-sensitive. D-08 streamId required at schema layer (z.string().min(1)). Response forwarded verbatim under `result.body` (default toIdBody fallback maps raw → { id: raw?.id, body: raw }). Routes through defineMutatingHandler even though there's no Graylog state change — preserves the cross-cutting "dryRun:true shows the would-be POST body" invariant.
  - **D-09 mutable pre-flight pattern NOT extracted at 7 callsites**: chose to keep inline duplication because the per-tool error message text varies meaningfully ("refusing rule creation" vs "refusing rule update" vs "refusing rule deletion" vs "refusing start" vs "refusing pause" vs "refusing delete" vs no-verb on update_stream) AND the structured error's method+path context differs per tool. Extraction would still require verb + path parameters; the savings (6-8 lines per callsite) are not yet worth the new abstraction barrier. Recommend revisiting at Phase 4 if pipelines reuses the pattern.
  - **Schema-parity helper required custom code path for z.intersection**: `CreateStreamRuleSchema._def.left.shape` exposes the outer mutatingBase-extended shape (z.intersection doesn't expose `.shape` directly). One special-case in test/schema-parity.test.js's `schema-parity: create_stream_rule`; the other 3 new Plan-04 tools use the standard helper.
  - **Tool count growth: 50 → 54** (delta +4; plan acceptance criteria expected 51→55 inheriting the +1 accounting drift from Plan 01 frontmatter → Plan 02 → Plan 03; documented in 03-04-SUMMARY.md so the inherited drift is acknowledged but baseline carries forward unambiguously).
  - **Test growth**: 417 → 447 (+30 net-new — 26 streams handler/schema tests + 4 schema-parity assertions). Full `npm test` 447/447 pass; zero regressions. No new npm dependencies (zod was already in package.json; Plan 04 uses `z.intersection`/`.and()` from the existing 3.25.76 install).
  - **Commits (TDD RED → GREEN per task)**: `4e29f73` (Task 1 RED — 15 streams tests + 2 schema-parity for create + delete), `84d7e0b` (Task 1 GREEN — schemas.js extended with all 4 Plan-04 schemas; create-stream-rule.js + delete-stream-rule.js shipped; index.js 8→10 registers; tools.js 2 new entries), `7e00ad2` (Task 2 RED — 11 streams tests + 2 schema-parity for update + test_match), `2cd33bc` (Task 2 GREEN — update-stream-rule.js + test-stream-match.js shipped; index.js 10→12 registers COMPLETING the 12-tool surface; tools.js 2 new entries).
  - **4 Rule-1 deviations**: (1) Task 1 RED initially over-included Task 2 handler imports + 2 Task-2 schema-parity tests — fixed at Task 1 GREEN by pulling them back to Task 2 RED's scope; (2-3) plan acceptance `grep "cascades"/"_confirmationToken" returns 0 lines` were overly strict for delete-stream-rule.js (5 + 3 occurrences respectively, ALL in comments documenting WHY no cascades; same pattern as Phase 1 delete-extractor.js with 7 cascade mentions — Tests 13+14 are the real structural assertion at the JSON-output layer); (4) inherited tool-count baseline off-by-one from Plan 01. All zero production-code drift from the plan.
  - **ROADMAP SC3 provably met**: "test_stream_match accepts a stream config + a sample message and returns per-rule match outcomes." Verified via Test 23 (literal outer-key body shape) + Test 25 (apply path forwards `{ matches, rules }` verbatim from Graylog response).
  - **Plan 03-05 hand-off**: ~11 snapshot fixtures pending — (a) create_stream_rule dry-run for each of 8 D-11 variants pinning numeric wire type + empty-string defaults; (b) update_stream_rule dry-run for each subset (value-only, field-only, inverted-flip, description-clear) showing STRICT_NO_ECHO body + Pitfall S8 type-echo from current; (c) delete_stream_rule dry-run preview JSON pinning absence of `cascades` and `confirmationToken`; (d) test_stream_match dry-run preview pinning the literal outer key + apply-path response forwarded verbatim. All fixtures will be deterministic across CI runs.

- **Plan 04-01 (pipeline-dsl foundation — PIPE-11 + shared infrastructure for Plans 04-02..05)**:
  - **5-module `src/pipeline-dsl/` infrastructure landed**: builtins.js (frozen 133-entry source-verified static catalogue covering 16 categories: root, arrays, conversion, dates, dates/periods, debug, encoding, hashing, ips, json, lookup, maps, messages, strings, syslog, urls); escape.js (escapeString covering RuleLang.g4:363-381 char set — 8 short-form escapes + \uNNNN fallback for U+0000..U+001F; escapeValue type-aware wrapper rejecting objects/arrays/undefined at the boundary); emit.js (structured-intent RuleSpec → DSL emitter covering all 7 Condition variants comparison/and/or/not/function_call/has_field/field_ref+literal AND all 6 Action variants set_field/remove_field/rename_field/lookup_value/function_call_statement/let_assignment; byte-stable output; single-chokepoint escape routing per T-04-01-05); function-catalogue.js (per-connection process-lifetime cache mirroring src/tools/inputs/type-catalogue.js shape — `Map<connectionName, {fetchedAt, merged}>`; 1 GET per connectionName per process; live overlay with live-wins-on-collision semantics; static fills description gaps; _clearFunctionCatalogueForTests seam); validate.js (consumes MERGED Map NOT raw staticBuiltins per Pitfall 5; flags unknown_function + paren_imbalance; live-only names accepted).
  - **computeRuleCascadeHash thin wrapper landed in _shared/cascade-hash.js**: forwards into computeCascadeHash with parameter renaming (ruleId→streamId slot, pipelineIds→pipelineConnIds slot, empty ruleIds + eventDefIds). Test 'computeRuleCascadeHash is BYTE-IDENTICAL to the equivalent computeCascadeHash call (forwarding semantics)' pins so a future regression in computeCascadeHash breaks Phase 4 hashes too — single source of truth, no parallel canonical-form drift. Available for Plan 04-04 delete_pipeline_rule consumption.
  - **D-16 UNREACHABLE_STRICT_NO_ECHO locked**: 04-U1-SMOKE.md records the partial-update decision per Phase 3 precedent (no API token in executor environment; safe-default branch). Applies to BOTH update_pipeline (Plan 02) AND update_pipeline_rule (Plan 03). Pipelines carry no encrypted fields (verified against PipelineSource.java; no `is_encrypted` annotation). Rules carry simulator_message as Nullable String which CAN be intentionally cleared — STRICT_NO_ECHO disambiguates the two semantics structurally (omit → no-op; explicit null → clear). MERGE_FROM_CURRENT would conflate them; STRICT_NO_ECHO preserves the intent precisely.
  - **1 Rule-1 deviation: plan inventory off-by-3**: plan claimed 130 builtins; source-of-truth `grep "public static final String NAME" source-code/.../pipelineprocessor/functions/**/*.java | grep -oP 'NAME\\s*=\\s*"\\K[^"]+' | sort -u | wc -l` returns 133 unique NAME values across 135 *.java files (2 base-class shells without NAME). My transcription diffs against the live source NAMEs return ZERO mismatches in either direction. Adjusted test gate from 130→133 with inline source-verification methodology comment. Zero functionality lost; downstream plans see 3 MORE valid function names than the planner anticipated (additive).
  - **Test growth**: 459 → 526 (+67 net-new — 53 pipeline-dsl tests covering A/B/C/D/E behavior categories + 6 cascade-hash wrapper tests + 8 existing cascade-hash tests preserved). Full `npm test` 526/526 pass; zero regressions. No new tool registrations (Plan 04-01 is shared-infrastructure-only); tool count stays at 54. No new npm dependencies; src/graylog/errors.js NOT modified (only imported via GraylogValidationError).
  - **Commits (TDD RED → GREEN)**: `9f0b519` (Task 1 RED — 59 net-new tests across test/pipeline-dsl.test.js + test/cascade-hash.test.js); `877bec1` (Task 1 GREEN — 5 src/pipeline-dsl/ modules + _shared/cascade-hash.js append + 04-U1-SMOKE.md + builtins.js inventory aligned 130→133).
  - **Plan 04-02 hand-off**: Plan 02 consumes the src/pipeline-dsl/ infrastructure: emit.js for structured-intent if D-10 is extended to pipeline source (currently raw-DSL-only per D-11 scope); function-catalogue.js getMergedCatalogue for any function-reference validation in pipeline source; STRICT_NO_ECHO for update_pipeline per 04-U1-SMOKE.md; D-06 server-authoritative parse pre-flight via POST /api/system/pipelines/pipeline/parse before any apply. delete_pipeline is leaf-delete per D-15 (pipelines carry no `mutable: boolean` flag; no cascade enumeration needed — per-call dryRun:true + writable-flag gate is the sole safety contract).

- **Plan 04-03 (pipeline-rule CRUD — PIPE-06..PIPE-09; C4 acceptance gate centerpiece)**:
  - **4 net-new tools registered**: list_pipeline_rules (PIPE-06) + get_pipeline_rule (PIPE-07) + create_pipeline_rule (PIPE-08) + update_pipeline_rule (PIPE-09). Tool count 59 → 63. assertAllToolsRegistered passes.
  - **D-11 full DSL coverage shipped at the schema layer**: ConditionSchema as `z.lazy(() => z.discriminatedUnion("type", [...]))` covering 8 variants (comparison/and/or/not/function_call/has_field/field_ref/literal); ActionSchema as a 6-variant discriminatedUnion covering set_field/remove_field/rename_field/lookup_value/function_call_statement/let_assignment; ExpressionSchema as a z.lazy union allowing recursion; RuleSpecSchema = `{name, when: ConditionSchema, then: ActionSchema[].min(1)}`. Tests pin 4-level deep AND/OR/NOT trees + all 6 Action variant types accepted.
  - **D-10 mutual exclusion enforced via zod .refine on CreatePipelineRuleSchema**: `Boolean(args.structured) !== Boolean(args.ruleSource)` — Boolean-XOR ensures exactly-one-of. Same refine applies to `UpdatePipelineRuleSchema.changes` envelope (rejects when BOTH structured AND ruleSource present; either both absent or one set is allowed). Tests pin all 4 combinations (both/neither/structured-only/ruleSource-only).
  - **D-05 server-authoritative rule-parse pre-flight (POST /api/system/pipelines/rule/parse)**: Mirrors Plan 04-02's preflightParsePipeline shape but for the rule surface. `title:"preflight"` supplied because the parse endpoint expects a RuleSource wrapper. On 400 + Set<ParseError> body, wrapper throws GraylogValidationError(reason:"rule_parse_failed") with parseResult.error attached (Pitfall 6: positionInLine camelCase IN → position_in_line snake_case OUT, 4 wire-read + 4 snake-emit references in create-pipeline-rule.js). preflightParseRule is EXPORTED from create-pipeline-rule.js; update-pipeline-rule.js imports it — single source of truth for reason name + Pitfall 6 translation. C4 acceptance gate proven across 3 distinct fail paths via tests (create-structured server fail, create-raw server fail, update server fail).
  - **D-04 client-side lint BEFORE network round-trip**: validateRuleSource consumes the MERGED catalogue (Pitfall 5 — live-only function names accepted; the synthetic __phase4_test_function__ acceptance is proven). On lint failure, throws GraylogValidationError(reason:"rule_validation_failed") BEFORE the parse pre-flight even fires. Test `create_pipeline_rule C4 CLIENT-SIDE LINT FAIL (toUpperCase)` confirms zero parse-endpoint calls when the client-side lint refuses.
  - **D-17 __SERVER_ASSIGNED__ sentinel** on create_pipeline_rule.postApplyEstimate.id.
  - **D-16 STRICT_NO_ECHO for update_pipeline_rule per 04-U1-SMOKE.md**: wire body emits ONLY the fields the agent set in `changes`. Conditional spread preserves omit-vs-explicit-null intent for description AND simulator_message (Nullable String). 3-state semantics survive: undefined → omit; null → wire-emit explicit null (clear-intent); string → set value. Tests pin all 3 cases for simulator_message + the description:null clear case. Parse pre-flight + lint + function-catalogue fetch ALL skip when only description/simulator_message change (cosmetic edits = single PUT request).
  - **D-15 generalisation: NO is_editable mutable check** (rules have no such flag — verified RuleSource.java). One JSDoc reference in update-pipeline-rule.js documents the structural absence (Plan 04-02 precedent — "future-reader discoverability over grep-purity").
  - **M5 conflict pre-check supports BOTH modes**: structured.name is the authoritative title source; for raw DSL, a regex (`^\\s*rule\\s+"([^"]+)"`) extracts the title for the matchFn comparison against the bare-array list. When extraction fails, existingMatches stays empty (informational, not blocking). 3 tests pin all 3 paths (structured/raw-with-title/raw-no-title).
  - **Test growth**: 570 → 618 (+48 net-new — 44 in test/pipelines.test.js covering schema-layer recursion + D-10 mutual exclusion + list/get + create with structured+raw paths + C4 fail paths + Pitfall 5 + Pitfall 6 + M5 cross-mode + idempotency + writable + update with STRICT_NO_ECHO + conditional pre-flight + 3-state simulator_message + D-15 no-mutable; 4 schema-parity tests for the new tools). Full `npm test` 618/618 pass; zero regressions. No new npm dependencies; src/graylog/errors.js NOT modified.
  - **Zero deviations**: Plan executed verbatim. The plan's <action> sketches were lifted nearly verbatim with light editorial adjustments to honor the omit-vs-explicit-null contract for simulator_message in create (the plan's sketch always emitted `simulator_message: null` when undefined, which would have conflated omit with explicit-clear; my implementation skips the key when undefined). The export-from-create approach for preflightParseRule was chosen per the plan's <output> hand-off line that recommended single-source-of-truth.
  - **Commits (2 TDD tasks × RED + GREEN gates)**: `453d19f` (Task 1 RED — 21 net-new schema/list/get tests); `27698e2` (Task 1 GREEN — schemas.js extended + list-pipeline-rules.js + get-pipeline-rule.js + barrel + 2 tools.js entries; tool count 59 → 61); `1814ba3` (Task 2 RED — 27 net-new create/update tests; tool count test bumped to 63); `457f6a9` (Task 2 GREEN — create-pipeline-rule.js + update-pipeline-rule.js + barrel update + 2 tools.js entries; tool count 61 → 63).
  - **Plan 04-04 hand-off**: delete_pipeline_rule consumes Plan 04-01's computeRuleCascadeHash; simulate_pipeline_rule can import preflightParseRule from create-pipeline-rule.js for its parse-then-simulate sequence (the helper is already exported); list_pipeline_functions composes against function-catalogue.js getMergedCatalogue. Plan 04-05 (connect/disconnect) can compose emit.js + escape.escapeString for any literal in simulate payloads.

- **Plan 04-05 (pipeline↔stream connections — PIPE-13/14; Pitfall 2 mitigation centerpiece)**:
  - **2 net-new tools registered**: connect_pipelines_to_stream (PIPE-13) + disconnect_pipelines_from_stream (PIPE-14). Tool count 63 → 65. assertAllToolsRegistered passes. Plan executed in wave 2 ahead of Plan 04-04 (wave 3 — depends on 04-03; 04-05 only depends on 04-01); the 3 wave-3 tools (delete_pipeline_rule + simulate_pipeline_rule + list_pipeline_functions) will land independently → 68 final at phase close.
  - **Pitfall 2 acceptance gate proven for BOTH handlers**: Graylog's `POST /api/system/pipelines/connections/to_stream` is REPLACE-the-full-set semantics (verified PipelineConnectionsResource.java:81-100 — connectionsService.save is full replacement, NOT merge). The wrappers do client-side set arithmetic: connect = GET-merge-POST (union); disconnect = GET-subtract-POST (difference). Tests pin the load-bearing invariant: for current=[a,b] and args=[new], body.pipeline_ids===["a","b","new"] (NOT just ["new"] which would silently disconnect a and b); for current=[a,b,c] and args=[b,c], body.pipeline_ids===["a"] (NOT [] which would silently disconnect a). 2 dedicated tests `connect_pipelines_to_stream PITFALL 2 ACCEPTANCE GATE` + `disconnect_pipelines_from_stream PITFALL 2 mirror` are the gate.
  - **404-as-empty-set duck-typed**: GET /api/system/pipelines/connections/{streamId} returns 404 until first POST creates the record (Assumption A7 verified PipelineConnectionsResource.java:160-178). Wrapper catches via `err?.isGraylogError && err.status === 404` and treats `current = { pipeline_ids: [] }`. NO new typed-error import — src/graylog/errors.js NOT modified (honors success_criteria).
  - **404 + POST consistency on disconnect**: even when GET returns 404 (no record yet), the POST still fires with `pipeline_ids: []`. Agents see the same wire shape regardless of whether the connection record existed. Tested explicitly.
  - **D-07 writable gate short-circuits BEFORE the GET pre-flight fires for both handlers** (verified by tests asserting captured-request count === 0 on read-only connection).
  - **Idempotency surfaces via existingMatches**: connect adds {id, similarity_reason:"already_connected"} for ids already in currentSet; disconnect adds {id, similarity_reason:"not_currently_connected"} for ids not in currentSet. CRITICAL — the snapshot order is BEFORE the mutation: `if (currentSet.has(id)) existingMatches.push(...); currentSet.add(id);` (for connect; .delete() for disconnect). Reversing the order would break idempotency reporting (every arg would appear "already_connected" after .add()).
  - **Alphabetical wire sort** for deterministic snapshot fixtures (Plan 06): `[...mergedSet].sort()` / `[...reducedSet].sort()` on both `body.pipeline_ids` and `postApplyEstimate.pipeline_ids` so the dry-run preview ≡ apply body invariant survives.
  - **Schema-layer enforcement**: `pipelineIds: z.array(z.string().min(1)).min(1)` for BOTH handlers — empty arrays rejected before build() runs; ensures the agent commits to at least one pipeline ID per call.
  - **Tool descriptions cite Pitfall 2 explicitly**: agents reading the tools.js entry see "the wrapper preserves previously-connected pipelines (Pitfall 2 — Graylog's endpoint POST /api/system/pipelines/connections/to_stream is REPLACE-the-full-set; this wrapper does GET-merge-POST client-side)" as the discrimination sentence.
  - **Test growth**: 618 → 642 (+24 net-new — 21 in test/pipelines.test.js covering HAPPY union/subtract + Pitfall 2 gates + 404 cases + idempotency + partial-idempotency + deterministic sort + 5xx + writable + paths + apply round-trips for both handlers + 2 schema-layer tests; 2 schema-parity tests; +1 from retargeted count test). Full `npm test` 642/642 pass; zero regressions.
  - **1 Rule 3 deviation: brittle count assertion retargeted** — Plan 04-03 had left `assertAllToolsRegistered ... (count = 63)` as a hard literal. Plan 04-05 bumps to 65; the test was renamed + assertion bumped + comment updated to document the wave structure (04-04 wave-3 will bump again to 68 final). Mechanical fix; no functionality affected.
  - **Commits (1 TDD task × RED + GREEN gates)**: `d7bc858` (RED — 23 net-new tests + 2 schema-parity tests; pipelines.test.js fails to load due to ERR_MODULE_NOT_FOUND on missing handler files); `f22b553` (GREEN — connect-pipelines-to-stream.js + disconnect-pipelines-from-stream.js + schemas extended + barrel +2 register lines + tools.js +2 entries + count-test retarget; 642/642 green).
  - **Plan 04-04 hand-off**: still independent; Plan 04-04 ships delete_pipeline_rule + simulate_pipeline_rule + list_pipeline_functions in wave 3. No coupling with Plan 04-05's tools; the connection handlers leave no orphan state for 04-04 to clean up.
  - **Plan 04-06 hand-off**: 4 new snapshot fixtures needed alongside the ~10 from Plans 02-04: (a) connect dry-run merged set; (b) connect dry-run already_connected idempotency; (c) disconnect dry-run reduced set; (d) disconnect dry-run not_currently_connected idempotency. Optional 5th fixture pinning the Pitfall 2 wire-body proof at snapshot level for future code-review visibility.

- **Plan 05-01 (events-notifications foundation — schemas + migrator + cascade-hash wrapper + S5 displacement + U1 smoke)**:
  - **Zero net-new tools registered (foundation-only plan)**: ships 11 zod schemas + 6-variant discriminator + EventDefinitionDtoSchema common shape + closed-set 8-name v6→v7 migrator + computeNotificationCascadeHash thin wrapper + conflict.js `elements` envelope amendment + empty events/index.js barrel + S5 displacement of v2.3 list_event_definitions/list_event_notifications. Plans 05-02/03/04 are pure composition against these primitives.
  - **conflict.js `elements` envelope amendment**: single additive line in the envelope-unwrap fallback chain (position 5, BEFORE `items`). Phase 5's `/paginated` PageListResponse shape (`{elements, pagination, attributes, defaults, total}`) now resolves end-to-end. Without this, `findExistingMatches({listPath: "/api/events/definitions/paginated"})` would return [] for any non-empty cluster (Pitfall 1 of 05-RESEARCH.md). 2 dedicated conflict tests pin (a) the envelope normalization, (b) the chain position guard (elements MUST win over items).
  - **computeNotificationCascadeHash thin wrapper**: byte-identical to the underlying computeCascadeHash forward (D-09 / Discretion-03). Two frozen 64-hex literals locked in test/cascade-hash.test.js as Plan 05-04 snapshot drift detection anchors: empty-cascade = `e2ba7147288dc6e9dbc0086881b87975c60b3925d8835bba2ffaf5db439f269c`; 2-cascade `{notificationId: 66e8aaaa..., eventDefIds: [66e8bbbb..., 66e8cccc...]}` = `d986f30b3afe6d6ab54b8fdb667716837dbfcac395720390639d7198287b02dd`. T-05-01-07 mitigation in production: any future drift in computeCascadeHash canonicalization breaks Phase 5 hashes too — single source of truth, no parallel canonical-form drift.
  - **NotificationConfigSchema 6-variant discriminator (D-05 corrected)**: closed-set z.discriminatedUnion("type", [email-notification-v1, http-notification-v1, http-notification-v2, slack-notification-v1, pagerduty-notification-v2, teams-notification-v2]). Rejects `script-notification-v1` (does not exist on 7.2-source), `pagerduty-notification-v1` (correct is `-v2`), `teams-notification-v1` (D-05 picks v2 only), and any unknown type at z.parse BEFORE any HTTP call (T-05-01-01 mitigation). Per-variant fields source-walked from EmailEventNotificationConfig.java / HTTPEventNotificationConfigV2.java / SlackEventNotificationConfig.java / PagerDutyNotificationConfig.java / TeamsEventNotificationConfigV2.java (~28/11/14/7/4 fields respectively).
  - **Zod v3 discriminator vs ZodEffects deviation (Rule 1 - Bug)**: First Task 2 GREEN implementation placed cross-field refinements via `.superRefine()` on each variant — exactly as the plan instructed. This wrapped each variant in ZodEffects, and `z.discriminatedUnion` calls `type.shape[discriminator]` directly, which threw `TypeError: Cannot read properties of undefined (reading 'type')`. Refactored to keep all variants as raw ZodObjects and moved cross-field refinements to an outer `.superRefine()` on the union itself, dispatching on `value.type`. Discriminator inference and closed-set rejection preserved; threat model T-05-01-01 intact. Fixed inside Task 2 GREEN commit (no separate commit). The refinement constraints kept: email recipient/body XOR; slack notify_channel/here XOR + include_title=false→custom_message-non-empty.
  - **migrateV6ToV7AggregationConditions closed-set migrator**: V6_FUNCTION_NAMES = Object.freeze(new Set([count, sum, avg, min, max, stddev, percentile, card])) — 8 entries, no more. T-05-01-02 mitigation: open-string match would mis-rewrite agent inputs like `counter` / `summary` / `averaged`. Detects {type:"function", function:<one of 8>, parameter:<string>} nodes; emits v7 number-ref with underscored key (count_source, etc.); surfaces visible warnings via {migrated_from_v6_shape:true, original, emitted}; recursive Expr-tree visit on left/right/child operands (covers Expr.And/Or/Not/Comparison); no-op safety on v7-shape input returns input verbatim. Bare-function emission when parameter is empty string.
  - **EventDefinitionDtoSchema common shape**: derived from EventDefinitionDto.java:65-170. Excludes the `scheduler` READ_ONLY field (Pitfall 5 — agent can't echo it back on POST/PUT). Treats `id` as optional (Pitfall 8 — server-assigned on POST; required on PUT but populated from URL path by wrapper, never round-tripped). EventProcessorConfig kept as permissive z.record(z.unknown()) for forward-compat: new variants land server-side without a wrapper schema change; the v6→v7 migrator narrows only config.conditions.expression. key_spec ⊆ field_spec superRefine implements RESEARCH Open Question 3 — gives the agent a clear pre-flight error rather than a generic server-side 400.
  - **EventDefinitionDtoChangesSchema partial envelope**: STRICT_NO_ECHO partial-update for update_event_definition (D-10) — every field optional via .optional(); excludes `id` (URL path overrides) and `scheduler` (READ_ONLY).
  - **05-U1-SMOKE.md UNREACHABLE branch → body:undefined locked**: No ~/.graylog-mcp/config.json available at any of three search paths; per Plan 02-01 / 03-01 / 04-01 SMOKE precedent, default to `body: undefined` for enable_event_definition + disable_event_definition wire shape. HTTP-layer hygiene (no spurious Content-Length: 0 header), aligned with Phase 0 makeClient default, wire-additively reversible to `body: ""` if a proxy returns 411/415.
  - **_register.js S5 displacement**: Removed `getEventDefinitionsHandler` + `getEventNotificationsHandler` imports from ../handlers.js (handlers stay exported there for HARD-03 audit reference in Phase 7 but no longer wired into dispatch). Removed `register("list_event_definitions", ...)` and `register("list_event_notifications", ...)` lines. Added `import "./events/index.js";` between the Phase 4 and existing register sections. Comment block citing Phase 3 list_streams precedent appended in the import section. Also removed the v2.3 tool-definition entries from src/tools.js — count drops to 66 (Plan 05-02 re-adds list_event_definitions → 67; Plan 05-04 re-adds list_event_notifications + 3 more → 71; phase-end count 77 after 11 net-new across Plans 05-02/03/04).
  - **Test growth**: 701 → 745 (+44 net-new — 35 events tests covering all 11 schemas + 6-variant discriminator acceptance/rejection + variant refinements (slack XOR + color hex + pagerduty 32-char routing_key + teams adaptive_card JSON.parse) + migrator behaviors (RED migration + visible warning + v7 no-op + closed-set rejection of out-of-set names + recursive Expr-tree + bare-name emission on empty parameter) + EventDefinitionDtoSchema key_spec ⊆ field_spec superRefine + empty barrel smoke; 7 cascade-hash tests for computeNotificationCascadeHash byte-identity + sort-stability + notificationId-distinctness + malformed-input rejection + 2 frozen literal anchors; 2 conflict tests for elements envelope + chain position guard). Full `npm test` 745/745 pass; zero regressions. No new npm dependencies (zod 3.25.76 already present); src/graylog/errors.js NOT modified.
  - **1 Rule-1 deviation**: zod-discriminator-vs-ZodEffects limitation forced cross-field refinement relocation from per-variant to outer-union (see "Zod v3 discriminator vs ZodEffects deviation" bullet above). No scope creep; all plan-specified refinements preserved; T-05-01-01 mitigation intact.
  - **Commits (2 TDD tasks × RED + GREEN gates + 1 plumbing)**: `0c06163` (Task 1 RED — 9 new tests for elements envelope + computeNotificationCascadeHash byte-identity); `f05fd66` (Task 1 GREEN — conflict.js elements amendment + cascade-hash.js computeNotificationCascadeHash wrapper; 710/710 pass); `ed23a40` (Task 2 RED — 35 events tests covering schemas + migrator + barrel; module import fails because schemas.js does not exist); `3af06cf` (Task 2 GREEN — schemas.js + v6-to-v7-migration.js + index.js barrel; 745/745 pass); `9ba9ba8` (Task 3 — S5 displacement of v2.3 list_event_*; 05-U1-SMOKE.md decision artifact; tool-count assertion 68→66).
  - **Plan 05-02 hand-off**: Plan 02 composes against `src/tools/events/schemas.js` (ListEventDefinitionsSchema, CreateEventDefinitionSchema, UpdateEventDefinitionSchema, GetEventDefinitionSchema) + `src/tools/events/v6-to-v7-migration.js` (migrateV6ToV7AggregationConditions for D-03/D-04/C5 dry-run output) + `src/tools/_shared/conflict.js` elements envelope (for create_event_definition existingMatches probe against /api/events/definitions/paginated). Append handlers + register() lines to events/index.js — no further _register.js edits needed. Tool count: 66 → 70 (+4: list/get/create/update event-definition).
  - **Plan 05-03 hand-off**: Plan 03 composes against `src/tools/events/schemas.js` (DeleteEventDefinitionSchema D-08 informational; EnableEventDefinitionSchema + DisableEventDefinitionSchema D-07 minimal shapes). Wire body shape locked at `body: undefined` per 05-U1-SMOKE.md. Tool count: 70 → 73 (+3).
  - **Plan 05-04 hand-off**: Plan 04 composes against `src/tools/events/schemas.js` (ListEventNotificationsSchema, CreateEventNotificationSchema with NotificationConfigSchema discriminator, UpdateEventNotificationSchema, DeleteEventNotificationSchema D-09 cascade-hash gate) + `src/tools/_shared/cascade-hash.js` (computeNotificationCascadeHash for D-09 drift refusal; snapshot fixtures may reference the two frozen 64-hex literals above) + `src/tools/_shared/conflict.js` elements envelope (for create_event_notification existingMatches probe against /api/events/notifications/paginated). Tool count: 73 → 77 (+4: list/create/update/delete event-notification).

- **Plan 05-02 (event-definition CRUD — list/get/create/update; M1 + C5 acceptance gates landed)**:
  - **M1 ACCEPTANCE GATE landed (4 distinct proofs)**: (1) `create_event_definition` wire path UNCONDITIONALLY `/api/events/definitions?schedule=false` (D-01 path-level structural enforcement; verified by test "M1 wire-path proof"); (2) dry-run `postApplyEstimate.wouldStartScheduling: false` + `state: "DISABLED"` + `id: __SERVER_ASSIGNED__` (test "M1 summary proof"); (3) agent-injected `schedule:true` stripped by zod's default `strip` mode BEFORE build() sees it — wire path STILL `?schedule=false` (test "M1 STRUCTURAL" + schema-level test "D-04 defense-in-depth"); (4) `update_event_definition` mirror — wire path UNCONDITIONALLY `/api/events/definitions/{id}?schedule=false` (test "D-02 wire-path proof"). Partial updates can NEVER silently re-enable scheduling. The schedule field is intentionally ABSENT from CreateEventDefinitionSchema + UpdateEventDefinitionSchema; structural enforcement, not runtime check — there is no `if (args.schedule)` branch anywhere in the wrapper.
  - **C5 ACCEPTANCE GATE landed (3 proofs)**: (1) v6 input `{type:"function", function:"count", parameter:"source"}` produces visible `migration:{migrated:true, warnings:[{migrated_from_v6_shape:true, original, emitted:"count_source"}]}` in dry-run JSON; wire body's `entity.config.conditions.expression` is the v7 number-ref `{type:"number-ref", ref:"count_source"}` (test "C5 GATE: v6 aggregation shape surfaces migration"); (2) v7 input passes through with NO `migration` key in dry-run JSON — omitted when migrated:false to keep preview JSON lean on the v7 happy path (test "C5 NO-OP: v7 input passes through"); (3) `update_event_definition` conditional migration — fires ONLY when `args.changes.config` is touched (synthetic-DTO wrapping `{config: args.changes.config}` lets the create-path migrator visit-tree work without modification); cosmetic edits (title-only) emit NO migration key.
  - **handler.js req.migration cross-cutting slot**: req.migration joins req.cascades / req.parseResult / req._confirmationToken conditional-spread chain in the dry-run preview emitter. Each handler's build() decides whether to include it; handler.js spreads conditionally. C5 visibility surface is now reusable — Plan 05-04 `update_event_notification` inherits without further handler.js changes.
  - **STRICT_NO_ECHO partial-update extended to event-definitions (3rd landing in 3 phases — Phase 1 update_input C3, Phase 3 update_stream D-14, Phase 5 update_event_definition D-10)**: NO pre-flight GET; wire body built from args.changes alone via conditional-spread pattern over 13 allowed change keys (title, description, priority, alert, config, field_spec, key_spec, notification_settings, notifications, storage, remediation_steps, event_procedure, event_summary_template). Pitfall 5 (scheduler READ_ONLY contamination) structurally impossible because nothing from current state ever flows to the wire. body.id is the ONLY non-changes wire field (Pitfall 8 — URL/body match). Verified by test "Pitfall 5: scheduler NEVER on the wire" with both Object.prototype.hasOwnProperty check AND assert.doesNotMatch on the full JSON string. Cosmetic-edit path stays at 1 wire request (the PUT itself; no GET).
  - **Plan 05-01 `elements` envelope amendment proven end-to-end**: both `list_event_definitions` paginated unwrap AND `create_event_definition`'s `findExistingMatches({listPath: "/api/events/definitions/paginated"})` exact-title probe consume `response.elements` correctly without modification at the handler site. Validates Plan 05-01's position-5 amendment to conflict.js.
  - **Exact-title-only existingMatches** (no fuzzy buckets like create_stream / create_input): `matchFn: (def) => def.title === args.definition.title` + `similarityReason: "exact"`. Phase 5 does not adopt the 3-bucket exact/case_insensitive/prefix classifier. Plan 05-04 will follow the same exact-only policy for notifications.
  - **Test growth**: 745 → 778 (+33 net-new — 28 events tests + 4 schema-parity + 1 tool-count assertion bump). 9 tests at each task (Task 1: list HAPPY + URL bare + URL with query/sort/order + fields:'all' + get HAPPY + get URL + get 404 + get empty-id reject + dispatch; Task 2: M1 wire-path + M1 summary + M1 STRUCTURAL + D-04 schema strip + C5 v6 migration + C5 v7 no-op + Pitfall 3 entity wrap + Pitfall 8 id strip + FOUND-11 existingMatches happy + existingMatches empty + dispatch; Task 3: D-02 wire-path + STRICT_NO_ECHO title-only + STRICT_NO_ECHO multi-field + C5 migration fires + C5 migration omitted + Pitfall 5 scheduler omitted + D-02 STRUCTURAL + Pitfall 8 body.id matches URL + dispatch). All Phase 0/1/2/3/4 + Plan 05-01 contracts preserved. assertAllToolsRegistered passes against 70 tools. `src/graylog/errors.js` NOT modified.
  - **2 deviations auto-fixed**: (1) [Rule 3 - Blocking] handler.js lacked req.migration spread for D-03/C5 visibility — added parallel to req.cascades / req.parseResult / req._confirmationToken; (2) [Rule 1 - Bug] Plan 05-01's "empty barrel" test premise no longer held — updated to reflect Plan 05-02 reality (registers handlers; _clearForTests removed per streams test 10 ES-module-cache pattern). Both fixes shipped in their respective task GREEN commits; no separate commits. No scope creep.
  - **Commits (3 TDD tasks × RED + GREEN gates)**: `a6c2a64` (Task 1 RED — 9 failing tests for list + get); `7cf9dc7` (Task 1 GREEN — list-event-definitions.js + get-event-definition.js + events/index.js register + tools.js +2 entries + tool-count bump 66→68 + 2 schema-parity; 756 pass); `5ae0ac2` (Task 2 RED — 10 failing tests for M1 + C5 acceptance gates); `e3748ec` (Task 2 GREEN — create-event-definition.js + handler.js req.migration spread + tools.js +1 entry + tool-count bump 68→69 + 1 schema-parity; 768 pass); `d751b71` (Task 3 RED — 9 failing tests for D-02 + STRICT_NO_ECHO + C5); `39aa566` (Task 3 GREEN — update-event-definition.js + tools.js +1 entry + tool-count bump 69→70 + 1 schema-parity; 778 pass).
  - **Plan 05-03 hand-off**: still independent. Plan 05-03 reads `get_event_definition`'s response shape (notifications[] for D-08 informational cascade preview), uses `list_event_definitions` URL pattern for id-resolution probes if needed, and ships delete + enable + disable_event_definition. The 4 byte-stable response shapes are now locked for Plan 05-05 snapshot pinning: list `{tool, connection, count, limit, fields, items}`, get `{tool, connection, definition}`, create/update dry-run preview JSON (with conditional migration + existingMatches + postApplyEstimate slots).
  - **Plan 05-04 hand-off**: still independent. `findExistingMatches({listPath: "/api/events/notifications/paginated"})` probe will work without modification thanks to Plan 05-01 envelope amendment proven by Plan 05-02 Task 2. handler.js `req.migration` slot is ready (Plan 05-04 update_event_notification can adopt for any v6→v7 notification migrations that surface in research).

- **Plan 05-03 (event-definition lifecycle — enable + disable + delete_event_definition; EVENT-05 + EVENT-06)**:
  - **EVENT-06 (Pitfall 4 WILDCARD empty body)**: `enable_event_definition` PUT `/api/events/definitions/{id}/schedule` + `disable_event_definition` PUT `/api/events/definitions/{id}/unschedule` both send `body: undefined` via module-level `ENABLE_DISABLE_EMPTY_BODY` constant locked per Plan 05-01's 05-U1-SMOKE.md UNREACHABLE branch. Graylog's `@Consumes(MediaType.WILDCARD)` accepts but ignores the body — the agent does NOT construct a fake body. Both compose through `defineMutatingHandler` (lifecycle-as-mutation contract; mirror of Phase 1 INPUT-07 start_input / stop_input D-08 precedent) so dryRun:true default + writable-flag gate + idempotency-key dedupe + zod validation + structured error envelopes inherit uniformly. postApplyEstimate.state ENABLED/DISABLED surfaces the eventually-consistent target. Apply envelope: 200 + full EventDefinitionDto; normalize via toIdBody extracts id from raw.id (Pitfall 2 / M2).
  - **EVENT-05 (D-08 INFORMATIONAL cascade)**: `delete_event_definition` DELETE `/api/events/definitions/{id}` pre-flights GET to project current.notifications[] into cascades.notifications in the dry-run preview JSON (mirror Phase 1 delete_input D-05 pattern — same try/catch shape line-for-line, only the endpoint path + field projection differ). CRITICAL: NO confirmationToken issued, NO cascade-hash computed, NO drift refusal at apply. Notifications survive the delete as independent resources owned by EVENT-07..EVENT-09; only the def→notification wiring vanishes. CONTRAST Plan 05-04 `delete_event_notification` which IS load-bearing (notifications are referenced across multiple event definitions + event_procedure surfaces — D-09 issues a 64-hex cascade hash via `computeNotificationCascadeHash` thin wrapper + REFUSES apply if cascade drifts between preview and apply).
  - **Static-grep invariant lock**: a dedicated test reads `src/tools/events/delete-event-definition.js` from disk and asserts ZERO matches for the JavaScript identifiers `computeCascadeHash` / `_confirmationToken` / `computeNotificationCascadeHash`. Any future refactor attempting to upgrade the D-08 informational cascade to a refusal gate will fail this test loudly. The handler's D-08-vs-D-09 contrast narrative uses generic English ("64-hex cascade hash via the Plan 05-01 thin wrapper" / "apply-time confirmation token") rather than the JS identifiers.
  - **Writable-gate-before-build proof**: the Task 2 writable-gate test uses `_setCaptureRequest_p2(() => { throw new Error("Pre-flight GET fired despite writable=false; gate is broken"); })` — a poisoned capture function that throws on ANY HTTP call. Proves the writable gate fires in handler.js step 3 BEFORE the pre-flight GET in build(), not just before the DELETE in apply. Stronger assertion than the inputs.test.js writable test (which only asserts the error envelope shape).
  - **Apply-ordering proof**: the Task 2 apply HAPPY test asserts `captured.deepEqual([{method:"GET",path:"..."}, {method:"DELETE",path:"..."}])` (rather than just `captured.length === 2`). Catches accidental refactors that swap the order or move the pre-flight GET from `build()` to `apply()`. The order matters: the cascade preview MUST be available before the dry-run preview emits.
  - **Best-effort pre-flight**: GET 404 / 403 / any-failure during the pre-flight falls through to an empty `cascades.notifications` array; the DELETE itself surfaces the real error via wrapGraylogError on apply. Mirrors Phase 1 delete_input lines 30-47.
  - **Sibling-file constant duplication**: ENABLE_DISABLE_EMPTY_BODY = undefined is duplicated in enable-event-definition.js + disable-event-definition.js. The plan explicitly authorized this and the refactor-if-third-consumer trigger; Plan 05-04 confirms no third consumer materializes (delete_event_notification uses JSON bodies, not WILDCARD empty body). Refactor cost exceeds duplication cost (one line × two files) until a third consumer appears.
  - **Test growth**: 778 → 802 (+24 net-new — 21 events tests + 3 schema-parity). Task 1: 11 tests (enable wire path + disable wire path + enable empty-body proof + disable empty-body proof + enable postApplyEstimate.state ENABLED + disable postApplyEstimate.state DISABLED + enable writable=false short-circuit + disable writable=false short-circuit + enable idempotencyKey 32-hex pin + enable dryRun:true default + dispatch resolves both). Task 2: 10 tests (DELETE wire path + D-08 informational cascade content byte-stable + NO confirmationToken invariant + 404 best-effort pre-flight + 403 best-effort pre-flight + static-grep guard + postApplyEstimate.deleted + writable-gate-before-build via poisoned capture + apply HAPPY ordering via captured.deepEqual + dispatch). All Phase 0/1/2/3/4 + Plans 05-01/05-02 contracts preserved. assertAllToolsRegistered passes against 73 tools. `src/graylog/errors.js` NOT modified. No new npm deps.
  - **1 deviation auto-fixed**: [Rule 1 - Bug] Static-grep guard test asserted ZERO matches for forbidden JS identifiers but the handler's D-08-vs-D-09 contrast narrative referenced computeNotificationCascadeHash + _confirmationToken by their literal JavaScript identifiers. Fix reworded both comment occurrences to use generic English; semantic content preserved verbatim. Shipped in Task 2 GREEN commit (no separate commit). No scope creep.
  - **Commits (2 TDD tasks × RED + GREEN gates)**: `db6f0a2` (Task 1 RED — 11 failing tests for enable + disable); `e623339` (Task 1 GREEN — enable-event-definition.js + disable-event-definition.js + events/index.js +2 register lines + tools.js +2 entries + tool-count bump 70→72 + 2 schema-parity; 791 pass); `8957d1b` (Task 2 RED — 10 failing tests for delete D-08); `539cd67` (Task 2 GREEN — delete-event-definition.js + events/index.js +1 register line + tools.js +1 entry + tool-count bump 72→73 + 1 schema-parity + comment rewrite for static-grep guard; 802 pass).
  - **Plan 05-04 hand-off**: notifications ARE load-bearing; D-09 cascade-hash refusal gate flips the D-08 informational dial. `computeNotificationCascadeHash` thin wrapper (Plan 05-01) is the canonical hashing surface; two frozen 64-hex literals (e2ba7147...269c + d986f30b...02dd) are the snapshot anchors. handler.js's `_confirmationToken` conditional spread (Plan 02-01) handles the apply-time gate. No further handler.js amendments needed. Tool count 73 → 77 (phase-end).
  - **Plan 05-05 hand-off**: byte-stable D-08 fixture #6 shape locked (see 05-03-SUMMARY.md §"D-08 Informational-Cascade Test Fixture") — `confirmationToken` key MUST be absent; `cascades.notifications` preserves pre-flight GET ordering; `notification_parameters: null` (not undefined/missing); `existingMatches: []` default. Plan 05-05 freezes this + all M1/C5/WILDCARD acceptance gates as snapshots.

- **Plan 05-04 (event-notification CRUD — list/create/update/delete; EVENT-07 + EVENT-08 + EVENT-09)**:
  - **EVENT-07 (S5 displacement)**: `list_event_notifications` reclaims the v2.3 dispatch name with a defineListHandler-backed reader over `/api/events/notifications/paginated`; narrow projection `[id, title, description, config]` (defineListHandler.projectItem is dot-notation-unaware — config carries verbatim, items[i].config.type reaches the agent without a helper-layer change); URL byte-stable (page=1 + per_page=<limit>, query/sort/order appended only when set); unwraps `response.elements` per Plan 05-01's conflict.js envelope amendment.
  - **EVENT-08 (D-05/D-06 + Pitfall 3 + C3 create)**: `create_event_notification` routes through the 6-variant `NotificationConfigSchema` discriminator from Plan 05-01 — `email-notification-v1` / `http-notification-v1` / `http-notification-v2` / `slack-notification-v1` / `pagerduty-notification-v2` / `teams-notification-v2` accept; `script-notification-v1` (does not exist on 7.2-source) + `pagerduty-notification-v1` (correct: -v2) + `teams-notification-v1` (deprecated; only -v2 supported) reject at zod.parse BEFORE any HTTP call. Wire body wraps in CreateEntityRequest envelope `{entity:{title, description, config}, share_request:null}` (Pitfall 3 — flat body returns 400 "missing entity"). FOUND-11 existingMatches probe fires against /api/events/notifications/paginated (Plan 05-01's elements envelope amendment makes the list response unwrap correctly). For http-notification-v2, basic_auth + api_secret wrap as `{set_value:<plaintext>}` on the wire and surface as `<redacted>` in the dry-run preview via the `_applyBody` sibling pattern (mirror create_input D-04 — handler.js stays body-agnostic; the sibling is internal to create_event_notification).
  - **EVENT-09 part A (D-10 STRICT_NO_ECHO + C3 ACCEPTANCE GATE on http-v2)**: `update_event_notification` PUT body emits ONLY agent-touched outer fields (title/description/config). Pre-flight GET sources current.config.type for the encrypted-field lookup; encrypted basic_auth + api_secret are NEVER on the wire when the agent did not pass them — THE LOAD-BEARING C3 PROTECTION (mirror Phase 1 update_input D-12; the previous-iteration bug copied the masked `<value hidden>` placeholder from current state into the wire body, causing Graylog to re-encrypt the literal placeholder string and WIPE the secret). Encrypted fields the agent DID pass wrap as `{set_value:<new>}` on the wire and surface as `<redacted>` in the dry-run preview. Variant change (changes.config.type ≠ current.config.type) replaces the variant entirely; old fields ABSENT from the new variant's wire config. body.id always matches the URL segment (Pitfall 8). NOTE: zod's `.default()`-marked scalars on http-v2 (method:"POST", time_zone:"UTC", skip_tls_verification:false, api_key_as_header:false) inflate during parse and DO appear on the wire — harmless because none are encrypted; the load-bearing C3 protection is structurally about encrypted-field absence, not "minimal wire shape".
  - **EVENT-09 part B (D-09 cascade-hash + apply-time drift refusal load-bearing)**: `delete_event_notification` is the load-bearing analog of D-08 — notifications are referenced by event_defs via notification_id; deleting one breaks any alert that fires that def. Single-endpoint cascade pre-flight: paginates `/api/events/definitions/paginated?page=N&per_page=50` and client-side filters on `def.notifications[].notification_id` (no server-side notification_id filter exists on Graylog 7.2 — mirror Phase 3 Pitfall S6 from delete_stream). Freezes referencing event_defs into a 64-hex sha-256 confirmationToken via `computeNotificationCascadeHash` (Plan 05-01 thin wrapper; canonical JSON is byte-identical to `computeCascadeHash` with streamId=notificationId and the other 3 buckets empty — T-05-04-06 cross-tool replay disarmed). D-04 cascade_preflight_failed hard-blocks the dry-run on any per-page GET failure (NO confirmationToken issued). D-03 drift refusal: apply() re-fetches the cascade + recomputes the hash + returns `isError reason:cascade_changed_since_preview` when the hash drifted between dry-run and apply; handler.js passes the envelope through verbatim (Plan 02-01 amendment). requireConfirm gate refuses apply with `confirmation_mismatch` when args.confirm is absent or stale; DELETE NEVER fires. Safety cap 1000 pages × 50/page = 50000 defs max (T-05-04-07; same cap as Phase 3 delete_stream paginated walk). Apply envelope is SYNC `{deleted:true, notificationId}` — no system-job spawn (notification delete is in-process per EventNotificationConfigEntityResource source).
  - **Single source of truth — encrypted-fields.js**: `src/tools/events/encrypted-fields.js` exports `ENCRYPTED_FIELDS_BY_TYPE` (Object.freeze map; only http-notification-v2 has encrypted fields on 7.2: basic_auth + api_secret) + `getEncryptedFieldsForType(t)` accessor returning an empty Set for unknown types so callers do not need a null-guard. T-05-04-04 (silent attack surface): a future Graylog version adding encrypted fields to other variants requires updating ONLY this file; update_event_notification's STRICT_NO_ECHO logic is variant-agnostic.
  - **Frozen empty-cascade literal for Plan 05-05 snapshot drift detection**: `computeNotificationCascadeHash({notificationId:"target-notif", eventDefIds:[]}) = de6f0611eedf13b44f3267dbc02b07edbd822ef803d6af4702facf1bc88e538c`. Pinned at test/events.test.js Task 3 test #3. Companion literal for non-empty single-cascade case is `284c6ce566377b844244f28f5447e97aa59be9f6416d018fca2ba87a5f4bab0b` (eventDefIds:["def-1"]), exercised structurally by Task 3 test #2 (byte-identity assertion).
  - **Drift-refusal test pattern (mirror Phase 3 delete_stream test 11)**: the agent's args.confirm must match the token build() computes at apply-time (build runs again on apply). The actual cascade_changed_since_preview path fires when apply()'s re-fetch sees ADDITIONAL drift beyond what build() saw. Test mock returns call #1 (build at apply-time) with the dry-run cascade → token X matches; call #2 (apply's re-fetch) with the drifted cascade → token Y differs → isError. This is the structural pattern; "drift between dry-run and apply" is actually "drift between apply-time build() and apply-time re-fetch" — the dry-run's token is a stand-in for the agent's frozen view of the system at decision time.
  - **Test growth**: 802 → 843 (+41 net-new — 32 events + 4 schema-parity + 5 incidental dispatch/cascade-hash coverage tests). Task 1: 13 tests (list URL byte-stable, narrow projection, fields:'all' bypass, HAPPY projection; create wire-body envelope, 6 variant accepts, 3 zod-parse rejections, FOUND-11 existingMatches, C3 wire/preview asymmetry; dispatch). Task 2: 9 tests (STRICT_NO_ECHO title-only + config-block-verbatim + variant change + Pitfall 8 + apply HAPPY + C3 ACCEPTANCE GATE + C3 wire {set_value} + C3 preview <redacted> + schema rejection + dispatch). Task 3: 11 tests (D-09 cascade pre-flight + byte-identity + empty-cascade frozen literal + multi-page walk + safety cap + confirmation_mismatch + drift refusal ACCEPTANCE GATE + happy DELETE + cascade_preflight_failed + writable-flag defense-in-depth + dispatch). All Phase 0/1/2/3/4 + Plans 05-01/05-02/05-03 contracts preserved. assertAllToolsRegistered passes against 77 tools. `src/graylog/errors.js` NOT modified. No new npm deps.
  - **3 deviations auto-fixed (all Rule 1 — test-spec / schema mismatch)**: (1) STRICT_NO_ECHO slack-config test reframed — Plan test 2 specified `{type, color}` would produce wire keys `[type, color]` ONLY but Plan 05-01's slack-v1 schema requires webhook_url + channel + color at zod.parse; reframed to assert load-bearing invariant (current.config.user_name NEVER echoed onto the wire). (2) http-v2 wire-keys assertion adjusted — Plan test 3 expected wire keys EXACTLY `[type, url]` but zod's `.default()`-marked scalars inflate the wire shape; reframed to assert the load-bearing C3 invariant (basic_auth + api_secret ABSENT regardless of non-encrypted defaulted scalars). (3) Dispatch test capture seam loosened — eventsMultiCapture's strict-equality path matcher does not accept the list handler's query-string-suffixed URL; dispatch test uses an inline `.startsWith("/api/events/notifications/paginated")` matcher instead. All three are test-only adjustments; handler implementations match the plan specifications verbatim.
  - **Commits (3 tasks, 1 commit each — no separate RED/GREEN gates this plan)**: `c3baf9c` (Task 1 — list-event-notifications.js + create-event-notification.js + encrypted-fields.js + events/index.js +2 register lines + tools.js +2 entries + tool-count bump 73→75 + 2 schema-parity + 13 tests; 820 pass); `350e638` (Task 2 — update-event-notification.js + events/index.js +1 register line + tools.js +1 entry + tool-count bump 75→76 + 1 schema-parity + 9 tests; 831 pass); `f4286fd` (Task 3 — delete-event-notification.js + events/index.js +1 register line + tools.js +1 entry + tool-count bump 76→77 + 1 schema-parity + 11 tests; 843 pass).
  - **Plan 05-05 hand-off**: snapshot fixture matrix is structurally complete — all 11 EVENT-XX wire tools have at least one ad-hoc test pinning their wire shape (list URL byte-stable; create wire-body shape pinned; update STRICT_NO_ECHO + C3 envelope pinned; delete cascade-hash + drift-refusal envelope pinned). 4 schema-parity assertions shipped this plan; 7 from prior plans bring the Phase 5 schema-parity coverage to 11 of 11. Plan 05-05's job is to freeze byte-stable snapshots + flip VALIDATION.md from `pending` to `complete`. The frozen empty-cascade literal `de6f0611eedf13b44f3267dbc02b07edbd822ef803d6af4702facf1bc88e538c` is the canonical drift-detection anchor. Tool count 77 (Phase 5 final; Phase 5 ships 11 net-new wire tools — list_event_definitions + get + create + update + enable + disable + delete + list_event_notifications + create + update + delete).

- **Plan 06-01 (Phase 6 foundation — services layer + blueprint helpers + widget-templates skeleton + U1-smoke)**:
  - **Services layer (D-09)** — `src/services/{streams,pipelines,inputs,index-sets,events,dashboards}.js` — 6 thin `(client, args) → Promise<response>` wrappers. NO MCP coupling (`grep "defineMutatingHandler" src/services/` empty; `grep "from \"../tools/" src/services/*.js` empty). Defense-in-depth via `client.request` writable-flag gate (T-06-01-06 mitigation). `src/services/dashboards.js` ships `buildSearchDTO` + `buildViewDTO` assemblers used by BOTH Plan 06-02 `create_dashboard` AND Plan 06-05 BLUE-01/BLUE-03 — single source of truth for the ViewDTO wire shape with `titles` + `display_mode_settings` emitted explicitly per 06-U1-SMOKE Q2 default. `createSearch` ships BARE SearchDTO body (Pitfall 3 explicit exception — `/views/search` does NOT use CreateEntityRequest envelope); `createDashboard` ships envelope-wrapped (standard Pitfall 3). `createEventDefinition` carries forward Phase 5 D-03 M1 default `?schedule=false` so blueprint chains can stage UNSCHEDULED definitions.
  - **executeChain + substitutePlaceholders (D-07/D-08)** — `src/tools/_shared/blueprint-chain.js` ships the chain-transcript walker. `substitutePlaceholders` does EXACT-MATCH first (preserves replacement type — numeric replacement stays numeric) then falls back to SUBSTRING-replacement-per-placeholder (handles path interpolation like `/api/streams/__SERVER_ASSIGNED__step1/connect`). Initial implementation was exact-match-only; path-substitution test failed; fixed inline as Rule 1 bug (committed atomically in Task 2 `0a3b943`). Supports single-object OR array-shape `dependsOn` (BLUE-01 step 4 has TWO prior-step dependencies). Partial-failure short-circuit emits `blueprint_chain_partial_failure` envelope with `transcript` + `failed_at_step` + `succeeded_steps`. Unresolved-dependency rejection emits `blueprint_chain_unresolved_dependency` envelope. No rollback (Pitfall 9 explicit acceptance) — idempotency-key auto-derivation makes retries convergent.
  - **D-03 widget-position integrity validator** — `src/tools/_shared/widget-position-integrity.js` exports `validateWidgetPositionIntegrity(widgets, widgetPositions)` — bidirectional strict-equality check tightening Graylog's server-side superset-only check (`ViewsResource.java:371-378`). Throws with `reason: "widget_position_integrity_violation"` + `isClientSide: true`. Refuses BEFORE HTTP — saves a round-trip AND surfaces a clearer error than the server-side "missing position" 400.
  - **conflict.js views envelope amendment (Phase 6 Pitfall 1)** — one-line additive change: `?? response?.views` positioned between `?? response?.elements` (Phase 5) and `?? response?.items` (generic fallback). Phase 5's elements precedence preserved (regression-guarded by existing test 10). +1 test for the views unwrap (test 11). Unlocks `list_dashboards` (DASH-01) and `create_dashboard` FOUND-11 duplicate-title pre-check in Plan 06-02.
  - **widget-templates skeleton (D-04)** — `src/widget-templates/index.js` exports `Object.freeze`d `TEMPLATE_NAMES` (8 closed-set names: error_rate_over_time, top_sources_by_volume, level_distribution, top_error_clusters, request_rate_over_time, field_value_distribution, recent_events_table, stream_activity_overview) + `Object.freeze({})` for `WIDGET_TEMPLATES`. Plan 06-03 fills builders; the closed-set zod-enum surface is production-ready for Plans 06-02 / 06-04 / 06-05 right now without circular-import concerns.
  - **06-U1-SMOKE.md (UNREACHABLE branch — researcher AFK defaults locked)**: no API token discoverable (`getConnections() === {}`; no `~/.graylog-mcp/config.json`; no `$GRAYLOG_CONFIG_PATH`; no project-local config). Precedent set by 02/04/05-U1-SMOKE. Decisions: **Q1 `WRAPPER_SIDE_TYPE_FILTER`** — `list_dashboards` ALWAYS post-filters `response.views.filter(v => v.type === "DASHBOARD")` regardless of upstream filter behavior; **Q2 `EMIT_BOTH_EXPLICIT`** — `buildViewDTO` always emits `titles: {titles: {}}` + `display_mode_settings: {positions_inferred: false, show_summary: false, show_message_row: false}` per Pitfall 6; **Q3 `TEXT_WIDGET_PLACEHOLDER`** — `top_error_clusters` ships as text widget with `searchType: null` per user AFK directive interpreted as standing authorization for researcher recommendation; **Q4 `TAGGED_UNION_INFINITY`** — full-width Position emits `{type: "infinity"}` per Jackson sealed-class convention (Phase 6 default 8 templates use IntegerPosition only, so this is documentation-only for Phase 6).
  - **`buildViewDTO` widget_mapping for text-widget placeholders**: emits `{txt1: []}` (empty array) when `searchType: null` (Q3 path) so the text widget renders without a data binding. Pinned by bonus test in `test/services.test.js` (one of the 13 service tests — plan target 12 + 1 scope-adjacent test for Q3 verification per Rule 2 missing correctness coverage).
  - **Test growth**: 856 → 882 (+26 net-new — 1 conflict views envelope + 8 blueprint-chain + 4 widget-position + 13 services). Plan-target was 23 net-new (1+6+4+12); the 3 extras are 2 `substitutePlaceholders` pure-function sanity tests (preserves type on primitives; recurses into nested objects/arrays) + 1 `buildViewDTO` Q3 text-widget placeholder widget_mapping test. All Phase 0/1/2/3/4/5 + Plan 06-01 contracts preserved. `src/graylog/errors.js` NOT modified. No new npm deps. Foundation-only: no tool surface added; tool count unchanged at 77.
  - **2 deviations auto-fixed (1 Rule 1 inline bug + 1 Rule 2 missing correctness coverage)**: (1) `substitutePlaceholders` initial exact-match-only implementation failed path-interpolation test 3 — fixed by adding substring-replacement fallback while preserving exact-match-first for type fidelity; (2) added `buildViewDTO emits empty widget_mapping for text-widget placeholder (Q3 searchType: null)` test to pin the Q3 path's widget_mapping shape (without it, Plan 06-03 could regress the empty-array branch silently).
  - **Commits (4 tasks, 1 commit each)**: `1d263bf` (Task 1 — U1-smoke + conflict.js views envelope + 1 test; 857 pass); `0a3b943` (Task 2 — widget-position-integrity.js + blueprint-chain.js + 12 tests; 869 pass); `eb7923f` (Task 3 — 6 services modules + 13 tests; 882 pass); `ab49e0f` (Task 4 — widget-templates skeleton; 882 pass — no new tests this task).
  - **Plans 06-02..06-06 hand-off**: every downstream plan composes against the new modules. Plan 06-02 (`create_dashboard`) uses `src/services/dashboards.js::buildSearchDTO/buildViewDTO/createSearch/createDashboard` + `validateWidgetPositionIntegrity` + `conflict.js` views envelope. Plan 06-03 (widget-templates) fills `WIDGET_TEMPLATES` builders; closed-set names live in `TEMPLATE_NAMES` already. Plans 06-04/06-05 (blueprints) compose `executeChain` + the 6 service modules — no service module dispatches through `defineMutatingHandler`, so the chain transcript is byte-stable and reusable across all 6 BLUE-XX blueprints. Plan 06-06 (snapshot freeze) pins the 18 snapshot fixtures named in 06-RESEARCH §"Snapshot Fixture Design".

- **Plan 06-03 (widget-template library + add_widget_from_template — DASH-08 finalized + DASH-06 + M7 ACCEPTANCE GATE)**:
  - **DASH-08 finalized — 8 widget-template builders shipped**: `src/widget-templates/{error-rate-over-time,top-sources-by-volume,level-distribution,top-error-clusters,request-rate-over-time,field-value-distribution,recent-events-table,stream-activity-overview}.js` each export `build<Name>(options) → Object.freeze({widget, position, searchType})`. Plan 06-01 shipped the closed-set `TEMPLATE_NAMES` tuple + empty frozen registry skeleton; this plan populates `WIDGET_TEMPLATES` (8 entries, frozen map; T-06-03-08 prototype-pollution mitigation). Per-widget `timerange` ABSENT by default via conditional spread `...(options.timerangeOverride ? {timerange} : {})` so D-05 inherit-from-dashboard pins via `Object.prototype.hasOwnProperty` semantics. `streams` always emitted as JSON array per Pitfall 7. UUID widget+searchType IDs default to `randomUUID()` but accept agent overrides (BLUE-01 will pin IDs for byte-stable chain transcripts).
  - **Q3 TEXT_WIDGET_PLACEHOLDER landed for top_error_clusters**: builder returns `Object.freeze({widget:{type:"text", config:{text:"Top N error clusters (placeholder — populate via cluster_log_messages tool)."}, ...}, position, searchType: null})` — NO Graylog SearchType invented (Drain3 clustering is wrapper-side, not server-side). Agent populates body via cluster_log_messages + Graylog UI. `add_widget_from_template` detects `searchType:null` and emits 1-step chain (View PUT only) with empty `widget_mapping[newWidgetId] = []` entry. Honors DASH-08's 8-template commitment without inventing a non-existent SearchType.
  - **M7 ACCEPTANCE GATE landed (4 proofs)**: (1) `AddWidgetFromTemplateSchema` uses `z.enum(TEMPLATE_NAMES)` so agent-supplied invalid templateName ("bogus_template") rejects at zod.parse — `isError:true` envelope; NO HTTP fires (Test 1 pins refusal-before-HTTP via capture-flag assertion). (2) Schema-parity test pins `AddWidgetFromTemplateSchema.parse({templateName:"not_a_template"})` throws ZodError at parse layer. (3) Defense in depth: every name in `TEMPLATE_NAMES` accepted at schema parse (Test 2). (4) `Object.freeze(WIDGET_TEMPLATES)` at the registry layer (T-06-03-08) prevents map mutation even if a future codepath bypassed the schema. Mirrors D-02 .strict() pattern (CreateDashboardSchema/UpdateDashboardSchema) but for a different attack vector — template enumeration / typo amplification.
  - **DASH-06 add_widget_from_template — symmetric INVERSE of remove_widget**: 2-step PUT chain for SearchType-bearing templates `[{PUT /api/views/search/{searchId}, append searchType}, {PUT /api/views/{id}, append widget+position+widget_mapping}]`; 1-step chain for the text-widget placeholder. Pre-flight GETs View THEN Search (order load-bearing — View first because we don't know searchId until we GET the view). Search update fires FIRST in the chain so the View never references a not-yet-existing SearchType (load-bearing inverse of remove_widget which strips Search first so the View never points at an already-stripped SearchType). D-03 widget-position integrity validator runs on the prospective post-add sets BEFORE wire emission with `_setWidgetPositionValidatorForTests` seam (third landing of the pattern after create-dashboard.js and remove-widget.js). `dashboard_missing_search_binding` + `dashboard_empty_state` `isClientSide` refusals fire when view.search_id absent or view.state map empty — no PUT fires beyond the pre-flight GETs.
  - **Per-template option validation owned by builders**: `AddWidgetFromTemplateSchema.options = z.object({}).passthrough().default({})` — open shape because option contracts vary across 8 templates (streamIds for all, queryString for some, field for field_value_distribution, limit/intervalUnit for time-bucket templates, clusterCount for top_error_clusters). `buildFieldValueDistribution` throws `Error("field option required for field_value_distribution")` when options.field absent; error propagates through `defineMutatingHandler.build()` → catch → `wrapGraylogError` so the agent sees a structured isError envelope without a stack trace.
  - **D-04 top-level freezing only (not deep-freeze)**: `Object.freeze({widget, position, searchType})` prevents reassignment of the three keys at the wrapper triplet. Wrapper consumes the triplet via spread-merge into a new ViewDTO so inner-object mutations have no observable effect on the wire emission. Avoids `deepFreeze` cost + `WeakMap.set(widget, ...)` bypass attacks. Test 14 pins `Object.isFrozen(result) === true` for all 8 builders via the `callAllBuilders` helper that iterates across the full registry.
  - **Tool count 83 → 84**: add_widget_from_template registered via `src/tools/dashboards/index.js` barrel (Plan 06-02 extended); tool-count assertion bumped 83 → 84 in BOTH test/pipelines.test.js (where the Plan 06-02 baseline pin lives) AND test/dashboards.test.js (where the Plan 06-03 delta pin lives). Single source of truth would be cleaner but moving the assertion would have rippled across plan-boundary comments tied to Plan 06-02 SUMMARY.
  - **Test growth**: 924 → 956 (+32 net-new — 18 in test/widget-templates.test.js + 14 in test/dashboards.test.js). All Phase 0/1/2/3/4/5 + Plan 06-01/02 contracts preserved. `src/graylog/errors.js` NOT modified in any Plan 06-03 commit. No new npm deps. `assertAllToolsRegistered` passes against 84 tools. DASH-06 + DASH-08 closed; only DASH-09 (share-permissions; deferred per 06-CONTEXT.md) remains outside Phase 6 scope. C7 + M7 + D-03 + D-04 + D-05 + Pitfall 7 + Q3 TEXT_WIDGET_PLACEHOLDER acceptance gates all proven via named tests.
  - **0 deviations**: plan executed exactly as written. Two implementation details worth documenting (NOT deviations — both anticipated): (1) `AddWidgetFromTemplateSchema = mutatingBase.extend({...})` matches the plan's Task 2 Step A literally; (2) handler uses `Array.isArray(search?.queries) ? search.queries : []` defensive guard mirroring remove-widget.js's existing pattern.
  - **Commits (2 TDD tasks × RED + GREEN gates)**: `51cb893` (Task 1 RED — 18 failing widget-template tests); `a58e1cf` (Task 1 GREEN — 8 builders + populated frozen registry; 924 → 942); `4afc457` (Task 2 RED — 14 failing DASH-06 tests + pipelines tool-count bump 83→84); `e3f8bf7` (Task 2 GREEN — add-widget-from-template.js + AddWidgetFromTemplateSchema + dashboards/index.js register + tools.js +1 entry; 942 → 956).
  - **Plan 06-04 hand-off**: blueprint composition layer (BLUE-01..03) consumes `WIDGET_TEMPLATES` registry verbatim. Plan 06-04 BLUE-01 (set up app monitoring) will compose `WIDGET_TEMPLATES.error_rate_over_time`, `WIDGET_TEMPLATES.top_sources_by_volume`, etc. with agent-supplied serviceName as the queryString filter; D-04 frozen-registry contract + M7 closed-set rejection give the blueprint composer a stable structurally-safe surface. The DASH-06 handler is independent — blueprints will build their own SearchDTO + ViewDTO via the service-layer assemblers (Plan 06-01 `buildSearchDTO` + `buildViewDTO`) and call `create_dashboard` apply path directly (NOT `add_widget_from_template`, which is single-widget by design). Tool count 84 → 87 (Plan 06-04 ships 3 blueprint tools).

- **Plan 07-01 (HARD-01 tool-description audit + wholesale-fix pass)**:
  - **scripts/audit-tool-descriptions.js** — pure-static walker over `toolDefinitions` from `src/tools.js`. Exports `audit(toolDefinitions) → Array<{name, rule:"empty"|"length"|"discrimination", ...ctx}>` as the test seam. CLI exits 0 (clean), 1 (violations), 2 (load failure). `DESCRIPTION_BUDGET = 200` hard-coded; `COMPARATIVE_PHRASES` regex is a flat case-insensitive alternation with word-boundary anchors (no catastrophic backtracking risk) over `vs.|rather than|instead of|use this when|use this vs|use this rather|contrast with|unlike|as opposed to|prefer this when|distinct from`; `SENTENCE_SPLIT = /[.!?](\s+|$)/g` provides the multi-sentence alternative path so single-sentence descriptions WITH a comparative keyword pass. SRC_TOOLS_PATH env var lets the CLI test against tmpdir fixtures without touching `src/tools.js`.
  - **test/tool-description-audit.test.js** — 10 node:test cases. (1-7) `audit()` classifies good/bad fixtures correctly (compliant tool, length violation, single-sentence discrimination violation, multi-sentence pass, 4 comparative-phrase passes, empty description, missing description field); (8) real `src/tools.js` returns ZERO violations — this is the regression gate that fails any future PR adding a non-compliant description; (9-10) CLI smoke tests via `spawnSync` (tampered fixture → exit 1 with "violation" in stderr; compliant fixture → exit 0).
  - **Wholesale rewrite of 79 descriptions in `src/tools.js`**: every description now ≤200 chars with a discrimination sentence (either multi-sentence or comparative keyword). Safety-critical primitives preserved per the rubric: dry-run default on all mutators; `confirm` token requirement on `delete_stream`, `delete_event_notification`, `delete_pipeline_rule` (load-bearing cascade-hash deletes); `deleteIndices:false` inversion + confirm token on `delete_index_set`; `schedule:false` inversion + `enable_event_definition` reference on `create_event_definition` and `update_event_definition`; encrypted-field protection language on `update_input` and `update_event_notification`. Stripped: internal mitigation IDs (C1/C3/D-04/T-XX/M5/Pitfall N/ND2 etc.), HTTP-level wire minutiae, test-IDs (T-XX-YY-ZZ), plan numbers. The pure-discrimination flags (single sentence + no comparative keyword) on `list_connections`, `list_saved_searches`, `delete_saved_search`, and the log-template family (`list/delete/update/export/import_log_templates`) got short comparative-keyword additions. Spot-check across 7 critical tools confirmed all safety primitives still readable in the trimmed text.
  - **package.json** — added `"audit:tool-descriptions": "node scripts/audit-tool-descriptions.js"` for one-liner PR checks (`npm run audit:tool-descriptions` from repo root).
  - **.gitignore narrowed** (Rule 3 blocker — Task 1): `scripts/` was a blanket ignore for ad-hoc local scripts. Narrowed to `scripts/* + !scripts/audit-tool-descriptions.js` so the new audit script is committable while ad-hoc local scratch under `scripts/` stays ignored. Single-line fix; documented in Task 1 commit.
  - **Test growth**: 1045 → 1055 (+10 net-new — all in test/tool-description-audit.test.js). No regressions in the prior 1045 tests; descriptions are documentation strings, not parsed by any code path. Tool count unchanged at 90.
  - **0 production-code deviations**: 1 .gitignore blocker auto-fixed in Task 1 (Rule 3); 24 descriptions required a second trimming pass after the first wholesale rewrite slightly over-shot the budget (200-260 chars range). Total time cost ~5 min of iterative `node scripts/audit-tool-descriptions.js → trim → re-run` loops; the audit script's per-violation report drove the loop exactly as the plan describes.
  - **Commits (2 atomic tasks)**: `f7ac88c` (Task 1 — audit script + 10 tests + npm script + .gitignore narrowing; 8 of 10 tests green, the "real src/tools.js" + spawnSync-tampered tests are the planned RED gate for Task 2); `4bb0323` (Task 2 — 79 description rewrites; all 10 audit tests green; full suite 1055/1055; HARD-01 closed).
  - **HARD-02 hand-off**: Plan 07-02 `list_admin_tools` meta-tool composes against the now-trimmed `toolDefinitions` — every entry is ≤200 chars so the meta-tool's output budget is predictable (~18 KB worst case for all 90 tools). Future tool additions are caught by `npm test` if they exceed the budget or lack a discrimination sentence — no PR can regress HARD-01 silently.

### Foundation Primitives To Be Built In Phase 0

These are the cross-cutting concerns every later phase depends on. They live in `FOUND-01` through `FOUND-13`:

- `src/dispatch.js` — `Map<toolName, handler>` replacing the `if (name === ...)` chain
- `src/graylog/client.js` — single axios HTTP client with auth, `X-Requested-By`, typed error mapping
- `defineMutatingHandler` factory in `src/tools/_shared/handler.js`
- `runOrPreview` helper with `__SERVER_ASSIGNED__` ID sentinels
- Per-domain zod `schemas.js` co-location pattern
- `engines.node >= 20.6.0` (preferred `>= 22.3.0`) + working `npm test` against `node:test`
- Snapshot test harness with 5–10 fixture tests proven
- Response normalizer returning `{ id, body }` regardless of Graylog's inconsistent create-response shapes
- Per-call `connectionName` arg with singleton fallback on every mutating tool
- Idempotency-key mechanism (auto-generated from `hash(connection, tool, args)`)
- Create-conflict `existingMatches` pre-check pattern
- List-projection helper (default narrow `id, title, description`; default `limit: 25`)
- Tool-naming convention `<verb>_<domain>_<noun>`

### Reusable Primitives Introduced In Later Phases

- **`await_system_job`** (INDEX-08, Phase 2) — polls `/system/jobs/{id}`; reused by any async-completing operation in later phases.
- **Partial-update pattern** (INPUT-05, Phase 1) — wrapper fetches current config and merges; reused for index sets and event definitions.
- **Cascade-preview pattern** (STREAM-05, Phase 3) — pre-delete `cascades: {…}` output; reused for pipeline-rule deletes (PIPE-10) and pattern reference for event-def deletes.
- **Parse pre-flight pattern** (PIPE-08, Phase 4) — server-authoritative validation before apply; reused by `validate_event_definition` (Phase 5).

### Critical Pitfalls Per Phase

Sourced from `research/PITFALLS.md`. Each phase plan MUST address these:

- **Phase 0**: M2 (response shape inconsistency), M4 (idempotency on retries), M5 (skipped list-before-create), M6 (list-response context bloat), C6 (server-assigned ID sentinels)
- **Phase 1**: C3 (encrypted-field zero-out on update)
- **Phase 2**: C1 (`delete_indices=true` server default — INVERT IT), m2 (`regular: true` for default), m3 (deflector cycle is destructive), m5 (async system-job)
- **Phase 3**: C2 (silent cascade on stream delete), m1 (built-in stream protection), m6 (clone creates independent copy)
- **Phase 4**: C4 (agent-invented function names — parse pre-flight required), M3 (DSL escaping/coercion/then-block semantics)
- **Phase 5**: C5 (v6→v7 aggregation syntax migration), M1 (`schedule: true` server default — INVERT IT), m4 (WILDCARD empty-body endpoints)
- **Phase 6**: C7 (dashboard Search+View two-step; widget-position integrity)
- **Phase 7**: M7 (tool-discovery degradation at ~91 tools), backward-compat audit against v7.2

### Todos

(Populated as work progresses)

### Blockers

None.

## Session Continuity

**Last action**: Completed `07-01-PLAN.md` — Phase 7 Plan 1 (HARD-01 tool-description audit + wholesale-fix pass). `scripts/audit-tool-descriptions.js` shipped as the static auditor over `src/tools.js` (exports `audit()` pure function for tests; CLI exits 0/1/2; SRC_TOOLS_PATH env var supports tmpdir-fixture testing). `test/tool-description-audit.test.js` shipped with 10 node:test cases pinning audit() classification + the "real src/tools.js returns zero violations" regression gate. `package.json` wired `audit:tool-descriptions` npm script. 79 descriptions rewritten in `src/tools.js` — every description now ≤200 chars with a discrimination sentence (multi-sentence or comparative keyword); safety-critical primitives preserved (dry-run defaults, confirm tokens, encrypted-field protection, `schedule:false` and `deleteIndices:false` inversions); internal mitigation IDs (C1/C3/D-04/T-XX/M5/Pitfall N) stripped — agent-context-only material. 1 Rule-3 blocker auto-fixed inside Task 1 (`.gitignore` narrowing from blanket `scripts/` to `scripts/* + !scripts/audit-tool-descriptions.js`). Tool count unchanged at 90. Test growth 1045 → 1055 (+10 audit tests; no regression in prior 1045). Commits: `f7ac88c` (Task 1 — audit script + 10 tests + npm wiring + .gitignore fix; 8 of 10 tests green, the 2 "real src/tools.js" + tampered-fixture tests are the planned RED gate for Task 2); `4bb0323` (Task 2 — 79 description rewrites; all 10 audit tests green; 1055/1055 full suite). HARD-01 closed; `npm run audit:tool-descriptions` is the one-liner PR gate.

**Previous action**: Completed `06-06-PLAN.md` — Phase 6 snapshot freeze + VALIDATION.md flip + AFK auto-approval. 24 byte-stable snapshot fixtures shipped across 3 files (test/snapshots/dashboards.test.js — 9 fixtures covering DASH-01..07 dry-run shapes + 2 acceptance-gate refusal envelopes D-03 + M7; test/snapshots/widget-templates.test.js — 8 fixtures covering every WIDGET_TEMPLATES builder including top_error_clusters Q3 TEXT_WIDGET_PLACEHOLDER; test/snapshots/blueprints.test.js — 7 fixtures covering all 6 BLUE-XX blueprints + BLUE-01 partial-failure transcript). 15 schema-parity assertions added (13 net-new tool parity + 2 D-02 structural pinning CreateDashboardSchema.shape.searchId === undefined AND UpdateDashboardSchema.changes.shape.searchId === undefined). Auth-redaction lint passes cleanly against all 24 new fixtures with 0 violations and 0 allowlist amendments — Phase 6 introduces no encrypted-field surfaces (delete_dashboard is a leaf delete with informational cascade only). Determinism contract met: two consecutive npm test runs produce byte-identical md5sums of all 3 snapshot files. VALIDATION.md flipped to status:complete + nyquist_compliant:true + wave_0_complete:true with all 9 Wave 0 checkboxes ticked and AFK auto-approval recorded per user directive "keep going to end of milestone without me". Full test suite 1045 tests green (was 1006 after Plan 06-05). 2 deviations auto-fixed inside Task 1 commit: (Rule 3 - Blocking) RuleSpecSchema shape mismatch on F21 (set_field is a discriminated action with flat {type, field, value}, not function_call_statement with args.named); (Rule 3 - Blocking) t.mock.method(crypto, "randomUUID") fails on node:crypto namespace exports — replaced with normalizeUUIDs() pre-snapshot transform preserving structural drift detection. Phase 6 wire-tool surface COMPLETE; ALL 5 ACCEPTANCE GATES PINNED at snapshot layer (C7 internal Search+View chain + D-02 schema-level reject + D-03 widget-position integrity refusal + M7 z.enum closed-set rejection + BLUE-01 6-step chain with partial-failure transcript). Commits: `6d87dff` (Task 1 — 24 snapshot fixtures), `8f39544` (Task 2 — 15 schema-parity assertions), `e4bee14` (Task 3 — VALIDATION.md flip). Phase 6 closed; ready for /gsd-verify-work.

**Previous action**: Completed `06-05-PLAN.md` — Phase 6 Blueprints B (BLUE-01/02/03). BLUE-01 setup_app_monitoring_stack ships the HEADLINE 6-step multi-domain chain composing streams + pipelines + dashboards + events from a single agent intent. Composite step 5 carries an inner Search+View 2-step chain (C7 mitigation preserved inside the blueprint); custom apply walker handles step 1's `stream_id` response key (Phase 3 wire shape, NOT `id`) via resolveDottedPath against the transcript and walks step 5's nested body.chain inline (Search FIRST → substitute searchId → View). Step 4 carries ARRAY-shape dependsOn for stream_id + pipeline id; partial-failure transcript surfaces succeeded_steps + failed_at_step (test 10 pins [1,2,3]+failed_at_step:4). T-06-05-01 mitigation: app_name regex-clamped to alphanumeric + underscore/hyphen at zod parse. BLUE-02 setup_error_alerting wraps events.createEventDefinition with agent-supplied notification_id (foreign-key validated server-side; no pre-flight GET — T-06-05-07 accepted) and aggregation-v1 query 'level:>=4'; schedule=false carry-forward (Phase 5 M1). BLUE-03 create_app_health_dashboard is a thin convenience wrapper over create_dashboard's internal Search+View chain with 4 default widgets (error_rate_over_time, top_sources_by_volume, level_distribution, recent_events_table) pre-wired to args.streamId; documented in file header as trade-off (flexibility vs ergonomics). D-09 services-layer compose contract grep-pinned across all 6 BLUE-XX. 26 net-new tests in test/blueprints.test.js (12 BLUE-01 + 5 BLUE-02 + 5 BLUE-03 + 1 D-09 + 3 schema-parity); suite 980 → 1006. Tool count 87 → 90 via 3 atomic per-task commits (1c24d26 BLUE-01, d8fab4b BLUE-02, c70f6f2 BLUE-03) — each landed a fully-green suite with assertions ratcheting at boundaries. No new npm deps; src/graylog/errors.js NOT modified. 3 small deviations (all defensive/additive — custom apply walker decision documented in SUMMARY; BLUE-01 description exceeded 200-char guideline for headline discoverability; TEMPLATE_NAME_TUPLE JSDoc cast for zod enum type inference). Phase 6 wire-tool surface COMPLETE — Plan 06-06 is the final phase plan (snapshot freeze + checkpoint).

**Previous action**: Completed `06-04-PLAN.md` — Phase 6 Blueprints A (BLUE-04/05/06). 3 of the 6 BLUE-XX blueprints shipped (the simpler chains): BLUE-04 setup_pipeline_for_stream (variable-length N+2-step chain reusing Phase 4's emitRule + RuleSpecSchema as single source of truth across phases); BLUE-05 setup_long_term_archival_index (1-step chain wrapping createIndexSet with bundled SizeBasedRotationStrategyConfig 1 GiB/index + DeletionRetentionStrategyConfig max_number_of_indices ≈ retentionDays); BLUE-06 setup_debug_log_dropping (3-step chain with syslog level inversion `level > minLevel` documented in tool description + pinned by test). BLUE-04/06 route pipeline title + rule title literals through Phase 4 escapeString (T-06-04-03 mitigation; defense-in-depth deviation). Architectural-boundary `void serviceFn` markers preserve service imports as the D-09 audit signal even when executeChain handles actual invocation. 24 net-new tests bring suite to 980; tool count 84 → 87 via 3 atomic commits. src/graylog/errors.js NOT modified; no new npm deps. Commits: `be251f0` (BLUE-05 Task 1), `85e7812` (BLUE-06 Task 2), `7d7d80f` (BLUE-04 Task 3).

**Earlier action**: Completed `06-03-PLAN.md` — Phase 6 widget-template library + DASH-06 add_widget_from_template. 8 widget-template builders shipped under src/widget-templates/ (error-rate-over-time, top-sources-by-volume, level-distribution, top-error-clusters TEXT_WIDGET_PLACEHOLDER per Q3, request-rate-over-time, field-value-distribution, recent-events-table, stream-activity-overview); each returns Object.freeze({widget, position, searchType}). WIDGET_TEMPLATES frozen-map registry populated (T-06-03-08 prototype-pollution mitigation). Add_widget_from_template tool (DASH-06) shipped as symmetric INVERSE of remove_widget — 2-step PUT chain for SearchType-bearing templates (Search appends FIRST, View appends SECOND); 1-step for text-widget placeholder. M7 ACCEPTANCE GATE landed via z.enum(TEMPLATE_NAMES) closed-set rejection at zod.parse — invalid templateName "bogus_template" → isError envelope with NO HTTP fired (defense in depth: schema + frozen registry). D-03 widget-position integrity validator runs on prospective post-add sets BEFORE wire emission with _setWidgetPositionValidatorForTests seam. dashboard_missing_search_binding + dashboard_empty_state isClientSide refusals fire when view.search_id absent or view.state map empty. Per-template option validation owned by builders (buildFieldValueDistribution throws when options.field absent; error propagates through wrapGraylogError as structured isError envelope). DASH-08 finalized (Plan 06-01 shipped the closed-set skeleton; this plan populates 8 builders). Tool count 83 → 84. Test growth 924 → 956 (+32 net-new — 18 widget-templates + 14 dashboards). 0 deviations — plan executed exactly as written. Commits: `51cb893` (Task 1 RED — 18 failing widget-template tests), `a58e1cf` (Task 1 GREEN — 8 builders + populated registry; 942 pass), `4afc457` (Task 2 RED — 14 failing DASH-06 tests + pipelines tool-count bump), `e3f8bf7` (Task 2 GREEN — add-widget-from-template.js + schema + barrel; 956 pass).

**Previous action**: Completed `06-02-PLAN.md` — Phase 6 Dashboard CRUD (DASH-01..05 + DASH-07). 6 wire tools shipped under src/tools/dashboards/: list_dashboards (DASH-01 narrow projection over /api/views?query=type:DASHBOARD with Q1 WRAPPER_SIDE_TYPE_FILTER drop-saved-searches + response.views envelope unwrap per Pitfall 1), get_dashboard (DASH-02 full ViewDTO via plain async handler; 404 via wrapGraylogError; load-bearing for remove_widget pre-flight + update_dashboard GET-current overlay), create_dashboard (DASH-03 THE C7 ACCEPTANCE GATE — internal Search+View 2-step chain via executeChain with __SERVER_ASSIGNED__step1 placeholder substitution; agent NEVER sees the intermediate Search ID; D-01 chain transcript surfaced via handler.js req.chain spread amendment; D-02 .strict() schema rejects agent-supplied searchId at zod parse with Unrecognized key — agent CANNOT inject a malicious search binding; D-03 validateWidgetPositionIntegrity bidirectional strict-equality runs BEFORE the FOUND-11 existingMatches probe — refuses orphan widget/position with reason:widget_position_integrity_violation + isClientSide:true and NO HTTP fires; wrapper-generated UUID widget IDs via node:crypto's randomUUID() + leading-underscore _setUUIDGeneratorForTests + _setWidgetPositionValidatorForTests test seams for byte-stable snapshot fixtures + refusal-before-HTTP contract pinning; FOUND-11 existingMatches probe filters on view.type==="DASHBOARD" so saved-search title collisions don't count as duplicates), update_dashboard (DASH-04 STRICT_NO_ECHO partial-update with GET-current pre-flight + overlay of title/description/summary only; UpdateDashboardSchema.changes is .strict() so searchId IS REJECTED at parse — search-ID immutable post-creation by design; Pitfall 8 body.id matches URL segment defensively; CreateEntityRequest envelope mirrors createDashboard; refines refuses empty changes object with "must contain at least one field"), delete_dashboard (DASH-05 LEAF DELETE Phase 5 D-08 informational-cascade pattern — NO cascade-hash, NO confirmationToken, NO requireConfirm gate; cascades.widgets.count surfaces blast radius across all queryStates via best-effort GET pre-flight; 404/403/any-failure pre-flight falls through to widgetCount:0 and DELETE itself surfaces the real error on apply via wrapGraylogError), remove_widget (DASH-07 symmetric Search+View atomic two-step PUT chain via executeChain — PUT /api/views/search/{searchId} FIRST stripping widget's search_types from queries[stateKey].search_types so View never references a search_type we're about to remove, then PUT /api/views/{id} stripping widget+position+widget_mapping entry from ViewDTO state[stateKey]; D-03 validator runs on prospective post-remove sets BEFORE wire emission — refuses orphan-position corruption from prior buggy edits; widget_not_found refusal trips when widgetId absent from current ViewDTO; dashboard_missing_search_binding refusal when ViewDTO has no search_id; Pitfall 9 explicit acceptance — no transactional rollback; transcript surfaces failed_at_step for agent retry; test seam _setWidgetPositionValidatorForTests mirrors create-dashboard.js with as-aliased import). handler.js gains TWO cross-cutting amendments in Plan 06-02: (A) req.chain spread onto dry-run preview JSON between the existing migration spread and applyHint — surfaces multi-step chain transcripts to the agent; reused by remove_widget AND every BLUE-XX blueprint in Plans 06-04/05; (B) _testConnection stripped from rawArgs via `const { _testConnection, ...rawArgsForParse } = rawArgs` BEFORE schema.parse so .strict() schemas (CreateDashboardSchema, UpdateDashboardSchema.changes) don't reject the test seam at parse with Unrecognized key — Rule 3 unblocking fix landed inside Task 2 GREEN atomically with the initial create_dashboard implementation; without this strip every future .strict() schema in the codebase would break the project-standard test seam. errors.js gains a plain-Error reason surface in wrapGraylogError (Rule 2 cross-cutting fix mirroring the existing GraylogError reason suffix path) — surfaces `[reason: <tag>]` suffix in the envelope text AND copies err.reason onto the response object for client-side plain Errors with structured reasons; brings widget_not_found, widget_position_integrity_violation, dashboard_missing_search_binding (plus any future structured-reason client-side errors) to agent-programmatic identification parity with GraylogError instances. test/dashboards.test.js created with 42 net-new tests across 5 sections: schema parity (3 tests: ListDashboardsSchema extends listBase, GetDashboardSchema rejects empty dashboardId, RemoveWidgetSchema requires both ids); DASH-01/02 read tools (8 tests pinning narrow projection, Q1 wrapper-side filter, ?query URL encoding, fields:"all" full-DTO, response.views envelope unwrap, full ViewDTO with state/widget_mapping/positions, 404 via wrapGraylogError, zod-reject missing dashboardId); DASH-03 create_dashboard C7 ACCEPTANCE GATE (12 tests pinning 2-step chain shape, dependsOn:{from:"step1.response.id", as:"searchId"}, step 1 BARE SearchDTO body Pitfall 3, step 2 CreateEntityRequest envelope + __SERVER_ASSIGNED__step1 placeholder, primary preview mirrors step 2, D-02 .strict() reject at handler level, D-02 schema-parity at parse level, D-03 refusal before HTTP via validator-stub seam, D-03 validator-is-called defense-in-depth, wrapper-generated UUID matches /^[0-9a-f]{8}-.../ regex, FOUND-11 title-collision surfaces, FOUND-11 DASHBOARD-only matchFn drops saved-searches, apply walks chain in sequence, apply substitutes step1.response.id into step2 body.entity.search_id); DASH-04/05 (10 tests pinning STRICT_NO_ECHO overlay merges changes onto GET-current full ViewDTO, D-02 reject at handler + schema, empty-changes refusal "at least one field", PUT body shape, apply path, leaf delete with cascades.widgets.count across multiple states, NO confirmationToken, DELETE apply path returns deleted:true body, best-effort 404 pre-flight falls through to count:0, schema-parity rejects missing dashboardId); DASH-07 remove_widget (7 tests pinning GET-view-then-GET-search call order, 2-step chain {PUT /api/views/search, PUT /api/views}, search_types stripping drawn from widget_mapping, widget+position+widget_mapping entry stripping, D-03 refusal-before-PUT via validator-stub seam, widget_not_found refusal before any PUT, apply walks chain PUT-search-then-PUT-view in order). test/pipelines.test.js assertAllToolsRegistered count bumped 77 → 79 (Task 1) → 80 (Task 2) → 82 (Task 3) → 83 (Task 4 / Plan 06-02 end) with count-trail comment block updated to document the running total. 4 deviations all auto-fixed inside their task GREEN commits: (Rule 3 unblocker — Task 2) .strict() schemas required handler.js _testConnection strip-before-parse; (Rule 1 test design — Task 2) test 6 reframed from unreachable-by-construction "craft invalid input" to validator-stub seam pattern since the wrapper's build() synthesizes widgetPositions from widget IDs making a real bidirectional mismatch unreachable via the agent surface; (Rule 2 cross-cutting — Task 4) wrapGraylogError plain-Error reason surface for client-side errors with structured reasons; (scope-adjacent module isolation — Task 4) two _setWidgetPositionValidatorForTests seams (one per handler module) with as-aliased import on the test side rather than a shared global state. No scope creep. Test growth 882 → 924 (+42 net-new dashboards.test.js). Tool count 77 → 83. C7 + D-02 + D-03 + STRICT_NO_ECHO + LEAF DELETE + symmetric two-step PUT chain acceptance gates landed end-to-end. assertAllToolsRegistered passes against 83 tools. src/graylog/errors.js NOT modified in any Plan 06-02 commit. No new npm dependencies. Commits: `44bb1e8` (Task 1 — list_dashboards + get_dashboard DASH-01/02; +10 tests; 882→892), `18b8253` (Task 2 — create_dashboard DASH-03 C7 ACCEPTANCE GATE; +13 tests; 892→905), `e8630ee` (Task 3 — update_dashboard + delete_dashboard DASH-04/05; +12 tests; 905→916), `426ff2d` (Task 4 — remove_widget DASH-07 + wrapGraylogError reason surface; +7 tests; 916→924).

**Earlier action**: Completed `05-03-PLAN.md` — Phase 5 event-definition lifecycle (EVENT-05 + EVENT-06). 3 wire tools shipped under src/tools/events/: enable_event_definition (EVENT-06 part A — PUT /api/events/definitions/{id}/schedule with module-level `ENABLE_DISABLE_EMPTY_BODY = undefined` constant locked per 05-U1-SMOKE.md UNREACHABLE; composes through defineMutatingHandler so dryRun:true + writable-flag gate + idempotency-key dedupe + zod validation inherit uniformly per lifecycle-as-mutation contract mirror of Phase 1 INPUT-07 start_input; postApplyEstimate.state ENABLED surfaces the eventually-consistent target; normalize via toIdBody for M2 200+full-DTO response shape); disable_event_definition (EVENT-06 part B — symmetric mirror PUT /api/events/definitions/{id}/unschedule; only deltas are path segment and postApplyEstimate.state DISABLED; sibling-file constant kept in sync — refactor to shared module only if a third WILDCARD consumer appears, Plan 05-04 confirms no third consumer materializes); delete_event_definition (EVENT-05 — DELETE /api/events/definitions/{id} with D-08 INFORMATIONAL cascade: best-effort pre-flight GET reads current.notifications[] and projects {notification_id, notification_parameters} into cascades.notifications in the dry-run preview; mirrors src/tools/inputs/delete-input.js:30-47 try/catch shape line-for-line; NO confirmationToken issued, NO cascade-hash computed, NO drift refusal at apply — notifications survive the delete as independent resources owned by EVENT-07..EVENT-09; only the def→notification wiring vanishes; static-grep test reads source file from disk and asserts ZERO matches for computeCascadeHash / _confirmationToken / computeNotificationCascadeHash so any future refactor attempting to upgrade D-08 to a refusal gate fails loudly; 404/403/any-failure pre-flight falls through to empty cascades.notifications array — the DELETE itself surfaces the real error via wrapGraylogError on apply). events/index.js extended with 3 register lines (Task 1: enable + disable; Task 2: delete). tools.js extended with 3 new tool entries (count 70 → 73). test/schema-parity.test.js extended with 3 new schema-parity tests under a new Plan 05-03 section header (all extend mutatingBase with {definitionId} → 4 keys). test/events.test.js extended with 21 net-new tests in 2 sections (Task 1: 11 tests covering both wire paths + empty-body proofs accepting either body:undefined-omitted OR body:"" per chosen_default + postApplyEstimate.state ENABLED/DISABLED + writable-gate inheritance for both + 32-hex idempotencyKey auto-derive pin + dryRun:true default + dispatch resolution; Task 2: 10 tests covering DELETE wire path + informational cascade content byte-stable + NO-confirmationToken invariant + 404 best-effort pre-flight + 403 best-effort pre-flight + static-grep guard + postApplyEstimate.deleted + writable-gate-before-build precedence via poisoned capture function + apply HAPPY ordering via captured.deepEqual + dispatch resolution); imports widened to include _setConnectionsForTests + _clearConnectionsForTests for writable-gate tests. test/pipelines.test.js tool-count assertion bumped 70 → 72 (Task 1 GREEN) → 73 (Task 2 GREEN) with count-trail comment block updated. 1 deviation auto-fixed inside Task 2 GREEN commit: [Rule 1 - Bug] Static-grep guard test asserted ZERO matches for forbidden JS identifiers but the handler's D-08-vs-D-09 contrast narrative mentioned computeNotificationCascadeHash + _confirmationToken by their literal JavaScript identifiers; fix reworded both comment occurrences to use generic English ("64-hex cascade hash via the Plan 05-01 thin wrapper" / "apply-time confirmation token") preserving semantic content verbatim. No scope creep. Test growth 778 → 802 (+24 net-new: 21 events + 3 schema-parity). Tool count 70 → 73. EVENT-05 (D-08 informational cascade) + EVENT-06 (Pitfall 4 WILDCARD empty body) acceptance gates landed end-to-end. assertAllToolsRegistered passes against 73 tools. src/graylog/errors.js NOT modified. No new npm dependencies. Commits: `db6f0a2` (Task 1 RED — 11 failing tests); `e623339` (Task 1 GREEN — enable + disable + schema-parity for both + tool-count bump 70→72; 791 pass); `8957d1b` (Task 2 RED — 10 failing tests); `539cd67` (Task 2 GREEN — delete + comment rewrite for static-grep guard + schema-parity + tool-count bump 72→73; 802 pass).

**Previous action**: Completed `05-02-PLAN.md` — Phase 5 event-definition CRUD (EVENT-01..04). 4 tools shipped under src/tools/events/: list_event_definitions, get_event_definition, create_event_definition (M1+C5 centerpiece — D-01 path-level `?schedule=false` UNCONDITIONALLY + v6→v7 migration via Plan 05-01 closed-set 8-name detector + Pitfall 3 CreateEntityRequest envelope + Pitfall 8 id strip + FOUND-11 existingMatches), update_event_definition (D-02 mirror + STRICT_NO_ECHO partial-update + C5 conditional migration via synthetic-DTO wrapping). handler.js gains req.migration conditional spread parallel to req.cascades / req.parseResult / req._confirmationToken. Tool count 66 → 70. Tests 745 → 778. Commits: `a6c2a64` (T1 RED), `7cf9dc7` (T1 GREEN), `5ae0ac2` (T2 RED), `e3748ec` (T2 GREEN), `d751b71` (T3 RED), `39aa566` (T3 GREEN).

**Previous action**: Completed `05-01-PLAN.md` — Phase 5 foundation (schemas + migrator + cascade-hash wrapper + S5 displacement + U1 smoke). 3 tasks shipped: (Task 1 TDD RED→GREEN) conflict.js `elements` envelope amendment at position 5 (BEFORE `items`) of the fallback chain — Pitfall 1 of 05-RESEARCH.md so /paginated PageListResponse list-before-create probes return non-empty; computeNotificationCascadeHash thin wrapper appended to _shared/cascade-hash.js (D-09 / Discretion-03; byte-identical canonical JSON to computeCascadeHash forward; two frozen 64-hex literals e2ba7147...269c and d986f30b...02dd locked in test/cascade-hash.test.js as Plan 05-04 drift detection anchors per T-05-01-07). (Task 2 TDD RED→GREEN) src/tools/events/schemas.js created with 11 per-tool zod schemas + 6-variant NotificationConfigSchema z.discriminatedUnion (D-05 corrected) + EventDefinitionDtoSchema + EventDefinitionDtoChangesSchema; src/tools/events/v6-to-v7-migration.js created with closed-set 8-name detector V6_FUNCTION_NAMES (T-05-01-02); src/tools/events/index.js created as empty side-effect barrel. (Task 3 plumbing) S5 displacement of v2.3 list_event_definitions + list_event_notifications. Test growth 701 → 745 (+44). Commits: `0c06163`, `f05fd66`, `ed23a40`, `3af06cf`, `9ba9ba8`.

**Previous action**: Completed `04-03-PLAN.md` — Phase 4 pipeline-rule CRUD (PIPE-06..PIPE-09). 4 tools shipped under src/tools/pipelines/: list_pipeline_rules (PIPE-06, defineListHandler with narrow projection [id, title, description, created_at, modified_at] over bare-array GET /api/system/pipelines/rule), get_pipeline_rule (PIPE-07, plain async handler returning full RuleSource DTO incl rule_builder + simulator_message), create_pipeline_rule (PIPE-08, ROADMAP SC1 C4 mitigation centerpiece — accepts EITHER structured intent (D-11 RuleSpec → emit.js compile) OR raw DSL via D-10 mutual exclusion at zod refine layer; client-side lint via validateRuleSource over MERGED catalogue (Pitfall 5 — live-only function names accepted) followed by D-05 server-authoritative parse pre-flight at POST /api/system/pipelines/rule/parse; on lint failure reason=rule_validation_failed BEFORE network round-trip; on parse failure reason=rule_parse_failed with parseResult.error[{line, position_in_line, type, message}] (Pitfall 6 camelCase→snake_case); M5 conflict pre-check supports BOTH structured.name AND best-effort regex extraction of `rule "..."` from raw DSL; D-17 __SERVER_ASSIGNED__ sentinel; EXPORTS preflightParseRule helper), update_pipeline_rule (PIPE-09, STRICT_NO_ECHO conditional-spread per 04-U1-SMOKE.md D-16 + conditional parse pre-flight firing ONLY when changes.structured OR changes.ruleSource is touched + D-15 generalisation no-mutable-check + simulator_message Nullable String 3-state semantics preserved omit/null/string + imports preflightParseRule from create-pipeline-rule.js single source of truth). schemas.js extended with D-11 full DSL grammar (8-variant ConditionSchema via z.lazy discriminatedUnion + 6-variant ActionSchema + RuleSpecSchema) + 4 PIPE-06..09 schemas with D-10 .refine. Barrel grown 7 → 9 register lines. src/tools.js gained 4 new tool definitions (59 → 63) with full inputSchema + discrimination sentences. test/pipelines.test.js shipped with 48 net-new tests covering schema-layer (D-10 mutual exclusion + 4-level recursion + all 6 Action variants) + list/get + create with happy structured/raw + 3 C4 fail paths + Pitfall 5 + Pitfall 6 + M5 cross-mode + update with STRICT_NO_ECHO + conditional pre-flight + 3-state simulator_message + D-15 generalisation; test/schema-parity.test.js extended with 4 parity tests. Full `npm test` 618/618 pass (was 570; +48 net-new); zero regressions; assertAllToolsRegistered passes against 63 tools. No new npm dependencies; src/graylog/errors.js NOT modified. C4 acceptance gate proven across 4 distinct fail paths: client-lint fail (toUpperCase), structured server-parse fail, raw DSL server-parse fail, update path server-parse fail. Zero deviations. Commits: `453d19f` (Task 1 RED — 21 tests), `27698e2` (Task 1 GREEN — schemas + 2 tools 59→61), `1814ba3` (Task 2 RED — 27 tests), `457f6a9` (Task 2 GREEN — 2 tools 61→63).

**Previous action**: Completed `04-02-PLAN.md` — Phase 4 pipeline CRUD (PIPE-01..PIPE-05). 5 tools shipped under src/tools/pipelines/: list_pipelines, get_pipeline, create_pipeline (D-06 parse pre-flight + EXPORTS preflightParsePipeline), update_pipeline (STRICT_NO_ECHO + conditional pre-flight), delete_pipeline (LEAF DELETE per D-15). Tool count 54 → 59. Tests 526 → 570 (+44). Commits: `3a484ec` (RED), `74215b5` (GREEN).

**Previous action**: Completed `04-01-PLAN.md` — Phase 4 pipeline-dsl foundation. Shipped src/pipeline-dsl/ infrastructure (5 modules: builtins.js w/ frozen 133-entry source-verified catalogue across 16 categories, escape.js w/ escapeString+escapeValue per RuleLang.g4 char set, emit.js w/ structured-intent RuleSpec→DSL emitter covering all 7 Condition + 6 Action variants byte-stable, function-catalogue.js w/ per-connection live-overlay cache mirroring inputs/type-catalogue.js shape, validate.js consuming MERGED Map per Pitfall 5 fix). _shared/cascade-hash.js gained computeRuleCascadeHash thin wrapper byte-identical to computeCascadeHash. 04-U1-SMOKE.md locks D-16 UNREACHABLE_STRICT_NO_ECHO for update_pipeline + update_pipeline_rule. 1 Rule-1 deviation: plan inventory off-by-3 (claimed 130 builtins; source-of-truth grep against source-code/.../pipelineprocessor/functions/**/*.java returns 133 unique NAME values; test gate aligned 130→133; zero functionality lost). Tests 459 → 526 (+67 net-new); zero regressions; tool count stays at 54 (shared-infrastructure-only plan). Commits: `9f0b519` (RED), `877bec1` (GREEN).

**Previous action**: Completed `03-02-PLAN.md` — Phase 3 streams CRUD + lifecycle (4 mutating tools). Shipped `src/tools/streams/create-stream.js` (STREAM-03 M5 mitigation with CreateEntityRequest envelope wrap `{ entity:..., share_request:null }` per StreamResource.java:229-230 + 3-bucket strictest-wins existingMatches via classifySimilarity helper + numeric rule translation through Object.frozen STREAM_RULE_TYPE_TO_NUMERIC + D-13 __SERVER_ASSIGNED__ sentinel + toIdBody idFields:["stream_id","id"]), `src/tools/streams/update-stream.js` (STREAM-04 STRICT_NO_ECHO per 03-U1-SMOKE.md UNREACHABLE_STRICT_NO_ECHO + D-09 mutable pre-flight reading canonical wire is_editable, refusing with err.reason="stream_immutable" BEFORE PUT), `src/tools/streams/start-stream.js` (STREAM-06a POST /api/streams/{id}/resume with body undefined + D-09), `src/tools/streams/pause-stream.js` (STREAM-06b POST /pause mirror + D-09). All 4 compose through defineMutatingHandler per D-12 (lifecycle-as-mutation; no special-cased runtime path). Extended `src/tools/streams/schemas.js` with 7 new exports: 8-variant StreamRuleSchema discriminated union (exact|regex|greater|less|present|contains|always_match|match_input), Object.frozen STREAM_RULE_TYPE_TO_NUMERIC wire map (1..8), closed-set SimilarityReasonEnum (exact|case_insensitive|prefix), CreateStreamSchema (D-10 index_set_id required no default), UpdateStreamSchema, StartStreamSchema, PauseStreamSchema. `src/tools/streams/index.js` extended to 7 register lines (3 Plan 01 + 4 Plan 02). `src/tools.js` extended with 4 new tool entries. `test/schema-parity.test.js` extended with 4 new assertSchemaParityForTool calls. `test/streams.test.js` extended with 40 net-new tests (21 Task 1 schema + 12 Task 2 handler + 7 Task 3 handler) + streamsMultiCapture helper. 2 Rule-1 deviations (both accounting-doc bugs inherited from Plan 01 frontmatter math; test count off-by-one + tool count baseline off-by-one — zero production-code drift). Tool count 45 → 49 (+4); test growth 358 → 402 (+44); full `npm test` 402/402 pass; zero regressions; assertAllToolsRegistered(toolDefinitions) passes against 49 tools. No new npm dependencies; src/graylog/errors.js NOT modified (only imported via GraylogValidationError). ROADMAP SC4 ("create_stream dry-run output includes existingMatches: [{ id, title, similarity_reason }] across 3 buckets") provably met (3 dedicated tests, one per bucket). Commits: `3358488` (Task 1 RED — 21 schema tests), `37b1af5` (Task 1 GREEN — schemas.js extended), `79ce0b3` (Task 2 RED — 12 handler tests + 2 schema-parity), `84e3433` (Task 2 GREEN — create_stream + update_stream shipped), `9d30fbb` (Task 3 RED — 7 handler tests + 2 schema-parity), `1795e5d` (Task 3 GREEN — start_stream + pause_stream shipped).

**Previous action**: Completed `03-04-PLAN.md` — Phase 3 stream-rule CRUD + test_stream_match. 4 mutating tools shipped (create_stream_rule, update_stream_rule, delete_stream_rule, test_stream_match) completing the Phase 3 12-tool surface. Tool count 50 → 54; tests 417 → 447. ROADMAP SC3 provably met. All Phase 3 ROADMAP SCs met (SC1+SC2+SC3+SC4).

**Stopped at**: Completed 07-01-PLAN.md — HARD-01 tool-description audit + wholesale-fix pass. scripts/audit-tool-descriptions.js + test/tool-description-audit.test.js + npm run audit:tool-descriptions wired; all 90 tool descriptions ≤200 chars with discrimination sentence; 1055/1055 tests green. Ready for Plan 07-02 (HARD-02 list_admin_tools meta-tool).

**Previous stop**: Completed 06-06-PLAN.md — Phase 6 snapshot freeze + VALIDATION.md flip + AFK auto-approval. Phase 6 wire-tool surface COMPLETE (14 net-new tools shipped across Plans 06-01..06-06; 1045 tests green; 90 total tools). Ready for /gsd-verify-work.

**Previous stop**: Completed 05-04-PLAN.md — Phase 5 event-notification CRUD (EVENT-07 + EVENT-08 + EVENT-09; 4 wire tools list + create + update + delete_event_notification). EVENT-07 (S5 displacement): list_event_notifications reclaims v2.3 dispatch name with narrow projection `[id, title, description, config]` over /api/events/notifications/paginated unwrapping response.elements. EVENT-08 (D-05/D-06 + Pitfall 3 + C3 create): 6-variant discriminator closed-set rejection at zod.parse (3 invalid types reject BEFORE any HTTP); CreateEntityRequest envelope wrap; FOUND-11 existingMatches probe; http-notification-v2 encrypted basic_auth + api_secret wrap as `{set_value}` on wire + `<redacted>` in preview via _applyBody sibling pattern. EVENT-09 part A (D-10 STRICT_NO_ECHO + C3 ACCEPTANCE GATE on http-v2): PUT body emits ONLY agent-touched outer fields; encrypted fields NEVER on wire when agent did not pass them (LOAD-BEARING C3 protection mirror Phase 1 update_input D-12); variant change replaces variant cleanly; body.id matches URL (Pitfall 8). EVENT-09 part B (D-09 cascade-hash + apply-time drift refusal load-bearing): paginated walk + client-side filter on def.notifications[].notification_id; confirmationToken via computeNotificationCascadeHash; apply re-fetches + recomputes + refuses with isError reason:cascade_changed_since_preview; requireConfirm gate refuses apply with confirmation_mismatch; cascade_preflight_failed hard-blocks; safety cap 1000 pages × 50/page; SYNC envelope `{deleted:true, notificationId}`. Single source of truth: src/tools/events/encrypted-fields.js (Object.freeze map; only http-v2 has encrypted fields on 7.2). Frozen empty-cascade literal de6f0611eedf13b44f3267dbc02b07edbd822ef803d6af4702facf1bc88e538c pinned for Plan 05-05 snapshot drift detection. Tool count 73 → 77 (Phase 5 wire surface complete). Tests 802 → 843 (+41: 13 Task 1 + 9 Task 2 + 11 Task 3 + 4 schema-parity + 5 incidental dispatch/cascade-hash coverage). 3 deviations auto-fixed inside Task 1/2 GREEN commits (all Rule 1: test-spec vs schema mismatch — STRICT_NO_ECHO slack-config test reframed to load-bearing invariant when full per-variant required-field set blocks partial config; http-v2 wire-keys assertion adjusted because zod's `.default()`-marked scalars inflate the wire shape; dispatch test capture seam loosened because eventsMultiCapture's strict-equality matcher does not accept query-string-suffixed list URLs — all handler implementations match plan specs verbatim). Commits: `c3baf9c` (Task 1 — list + create + encrypted-fields.js + 13 tests; 820 pass), `350e638` (Task 2 — update + 9 tests; 831 pass), `f4286fd` (Task 3 — delete + 11 tests; 843 pass).

**Next action**: Execute `07-02-PLAN.md` — HARD-02 list_admin_tools meta-tool. The audit gate is now in place so the new meta-tool's output is guaranteed ≤200 chars per entry; the meta-tool surfaces `[{name, description, domain}]` from `toolDefinitions` grouped by name-prefix or per-tool `domain` field (D-04/D-05 per 07-CONTEXT.md). Tool count 90 → 91 (Phase 7 ships exactly 1 new tool).

**Previous next**: Run `/gsd-verify-work` (verifier on Phase 6 SUMMARY) to gate phase closure; then begin Phase 7 if applicable per ROADMAP, or run /gsd-milestone-complete if Phase 6 was the final phase.

**Previous next**: Execute `05-05-PLAN.md` (final Phase 5 plan — byte-stable snapshot fixtures for all M1/C5/WILDCARD/D-08/D-09 acceptance gates + VALIDATION.md flip from `pending` to `complete`). Plan 05-05 composes against the 4 net-new schema-parity assertions from Plan 05-04 (list/create/update/delete event-notification) + the frozen empty-cascade literal `de6f0611eedf13b44f3267dbc02b07edbd822ef803d6af4702facf1bc88e538c` (Task 3 test #3) + the byte-stable wire shapes pinned across the 11 EVENT-XX wire tools. Tool count stays at 77 (Phase 5 final; no net-new tools in Plan 05-05). Phase 5 complete after Plan 05-05; Phase 6 (dashboards) is the next milestone scope, Phases 6/7/8 out of scope per ROADMAP.

---
*State initialized: 2026-05-13*
